# ×N Speedup Playbook — find-x-speedups workflow result

Generated by a 6-finder + 1-synthesizer fan-out workflow (afh agents).
Run ID: wf_19138b0f-d39

## Synthesized Top-10

# Top 10 Ã—N speedups â€” implementation playbook

## #1: Parallelize per-table writes inside commit critical section (subsystem: Commit) â€” multiplicative
File:line: crates/shamir-engine/src/tx/materialize.rs:67, :147
Why this is the top: Every multi-table commit currently serializes N storage round-trips inside the global `commit_lock`, blocking every other committer behind the slowest single tx's full table walk. Tables are independent stores â€” there is no correctness reason for sequencing. A `join_all` here shrinks the critical section from O(sum-of-tables) to O(max-table) and lifts throughput of *every* concurrent committer, not just the one being optimized. No wire/format change, no new data structures, pure structural fix.
Implementation:
  - Convert the per-table loops in Phase 5a (`apply_data_batch`) and Phase 5c (`apply_index_batch`) to build `Vec<Future>` and drive with `futures::future::join_all`.
  - Aggregate `ok` via `&&` fold; preserve first-error semantics with `try_join_all` if any apply returns Err.
  - Cache `repo.table_by_token(token)` lookup so Phase 5b counter increments reuse the resolved handle (eliminates Target 8 from Commit report as a free side-effect).
Blocks / depends on: enables #2 (per-table commit sharding) cleanly.
Risk: medium â€” must verify per-table idempotency holds and that `ok=false` aggregation matches today's all-or-nothing semantics.

## #2: Replace global commit_mutex with per-table writer locks (subsystem: Commit) â€” multiplicative
File:line: crates/shamir-tx/src/repo_tx_gate.rs:69
Why this is the top headline: today every committer waits on one `tokio::sync::Mutex<()>` for the entire pre_commit â†’ materialize span. After #1, the only thing still requiring serialization is `assign_next_version + record_commit_writes + publish_committed` â€” a microsecond-scale critical section. Disjoint-table commits should run fully parallel; this is the literal Ã—N (cores) win for write-heavy mixed workloads.
Implementation:
  - Keep `commit_mutex` around only the version-assign + footprint-record + publish triple.
  - Move materialize outside, gated by existing `uwl_guards` per touched table (`scc::HashMap<u64, tokio::sync::Mutex<()>>`).
  - Carefully order SSI predicate-conflict check vs. version assignment (still under sequencer).
Blocks / depends on: #1 must land first (parallel materialize is the structural prereq); interacts with #5 (predicate_conflicts speed) since the sequencer window grows shorter.
Risk: high â€” SSI footprint ordering is subtle; needs careful testing under abort/retry races.

## #3: Zero-copy fan-out for subscription delivery (subsystem: Subscription) â€” multiplicative
File:line: crates/shamir-server/src/subscriptions/{bridge.rs:310,330; push.rs:48-60}
Why: the cache exists specifically to share a serialized payload across N subscribers, then `(*cached).clone()` deep-copies it per subscriber and `try_push_event` re-msgpacks the entire `PushEnvelope` wrapping bytes that are already encoded. Pure Ã—N waste in subscribers. Fixing both at once turns one event into one alloc + N refcount bumps + N 8-byte seq patches.
Implementation:
  - Change `PushSink::try_push_event` to accept `Bytes` / `Arc<[u8]>`.
  - In `deliver_cache`, precompute the full `PushEnvelope` msgpack with a fixed-offset placeholder for `seq`; cache as `Bytes`.
  - Per subscriber: `bytes.clone()` (refcount), patch 8 bytes of seq in-place via a writable slice or two-buffer write (header_pre_seq | seq_bytes | header_post_seq | payload).
Blocks / depends on: enables Target 7 (snapshot path unification).
Risk: medium â€” needs careful msgpack frame layout with stable seq offset.

## #4: Eliminate bytes-prefilter double-decode (subsystem: Read) â€” multiplicative
File:line: crates/shamir-engine/src/query/filter/eval_bytes.rs:163; table_manager_streaming.rs:82
Why: every row currently pays a full `rmpv::decode::read_value` (allocating an Rv tree) just to evaluate one scalar compare, then on hit the row is re-decoded into `InnerValue`. Two full msgpack walks per row over wide records â€” the dominant CPU cost on shallow filters. Fixing this is contained to one module.
Implementation:
  - Write a zero-alloc msgpack cursor that seeks an interned key path and decodes only the target scalar.
  - On match, hand the already-positioned record bytes forward and decode into `InnerValue` exactly once.
  - Combine with Target 2 from Read (precompute KeyBuf per filter atom; hoist `interned_key_bytes` out of the row loop).
Blocks / depends on: enables #7 (top-K heap â€” once decode is cheap, the heap's "project only winners" optimization compounds).
Risk: medium â€” new cursor code needs fuzz testing against rmpv parity.

## #5: Push pagination into index2 path (subsystem: Read) â€” multiplicative (1000Ã—-class case)
File:line: crates/shamir-engine/src/table/read_exec.rs:166-196
Why: a `LIMIT 10` over an FTS match of 10K rows currently decodes all 10K. Trivial slice-before-get_many fix; massive win on ranked search workloads; zero risk to correctness given `count_total` can still use `rids_vec.len()`.
Implementation:
  - Before `get_many(&rids_vec)`, slice `[skip..skip+take]` when `(skip,take)` are known and no post-filter requires the full set.
  - Preserve `count_total = rids_vec.len()` for pagination metadata.
  - Gate identically to `try_project_page_only`.
Blocks / depends on: none.
Risk: low.

## #6: Fuse Serializable read-tx double-scan (subsystem: Read) â€” exactly 2Ã—
File:line: crates/shamir-engine/src/table/table_manager_streaming.rs:467, 477, 524-548
Why: `record_query_reads` performs a full scan+decode+filter to populate the read-set, then `read()` repeats the exact same scan to emit rows. A clean 2Ã— win on every SSI read.
Implementation:
  - Plumb `tx` into `read()` / `read_collecting` / `read_streaming` / `read_counting`.
  - Use `list_stream_tx` / `filter_stream_tx` as the single backing scan; record into read-set as side-effect during emission.
  - Delete the standalone `record_query_reads` call site after coverage in tests.
Blocks / depends on: none.
Risk: medium â€” SSI correctness; needs the read-set to be populated even for rows discarded post-projection.

## #7: Top-K heap for ORDER BY + LIMIT (subsystem: Read) â€” multiplicative
File:line: crates/shamir-engine/src/table/read_exec.rs:503-508
Why: today `ORDER BY x LIMIT 10` over 1M matches sorts all 1M then slices. Bounded `BinaryHeap<Reverse<â€¦>>` of size skip+take makes it O(N log K) and defers projection to only winners â€” eliminates 99.999% of projection work in the worst case.
Implementation:
  - Detect (ORDER BY without backing index) âˆ§ (finite LIMIT).
  - Maintain `BinaryHeap<Reverse<(sort_key, rid)>>` bounded by skip+take.
  - Project only winners after winnowing; preserve stable order with secondary tiebreaker.
Blocks / depends on: benefits compound with #4.
Risk: low.

## #8: Per-tx StagingStore: replace scc::HashMap with IndexMap+Fx (subsystem: Insert) â€” large
File:line: crates/shamir-tx/src/staging_store.rs:85
Why: `scc::HashMap` is single-writer per-tx by documented invariant â€” sharding+atomics pay pure overhead with zero concurrency benefit. Switch to `IndexMap<RecordKey, StagedOp, THasher>` for O(1) plain access, deterministic drain order, and dramatically lower per-row insert cost. Caller-side bonus: enables Target 6 from WAL (drop bytesâ†’RecordId reparse) once StagingStore is typed.
Implementation:
  - Change field type; rewrite `staged_op`, `upsert`, `scan`, `drain` to IndexMap APIs.
  - Verify no caller relies on `&` concurrent access (none should â€” comment at line 170 documents the invariant).
Blocks / depends on: enables WAL Target 6 (zero-parse `wal_ops_from_tx`).
Risk: low.

## #9: Eliminate per-row counter_deltas / staged_vectors / write_set RandomState (subsystem: Insert/Commit) â€” medium-multiplicative
File:line: crates/shamir-tx/src/tx_context.rs:105,118,131; commit-phase HashMaps in repo_tx_gate.rs:49,429
Why: u64 token keys hashed with SipHash on the hottest per-row paths in a workspace whose ideology mandates Fx. 2â€“5Ã— per lookup. Pure mechanical change.
Implementation:
  - Alias `type FxMap<K,V> = HashMap<K, V, THasher>`; replace field types in `TxContext`, `repo_tx_gate`, `materialize::CommitWriteRecord.per_table`, and the by_token grouping site.
  - Run gate, fix any inference fallout.
Blocks / depends on: none.
Risk: low.

## #10: O(1) target-match index by interned table id (subsystem: Subscription) â€” multiplicative when T large
File:line: crates/shamir-server/src/subscriptions/{bridge.rs:114,258,291; target_match.rs:18-52}
Why: every change runs two linear scans over the `targets` Vec doing String == String compares; tables already have an interned id upstream. One-time `scc::HashMap<(repo_idx, table_id), SmallVec<TargetSpec>>` built at subscribe collapses to O(1) per change and fuses the two walks.
Implementation:
  - Build the (repo_idx, table_id) â†’ targets index in `subscribe_handler`.
  - Replace `any_target_interested` + `matches_any` with a single function returning surviving target indices in a stack SmallVec.
  - StreamMap key by `usize` repo_idx (drops the per-event String hash probe).
Blocks / depends on: composes with #3 (zero-copy fan-out).
Risk: low.

# Prerequisite chains
- Commit: #1 (parallelize materialize) â†’ #2 (per-table sharded commit) â€” must land in this order; #2 is unsafe without #1's parallel apply already proven.
- Insert: #8 (typed StagingStore) â†’ WAL Target 6 (zero-parse wal_ops_from_tx).
- Read: #4 (single-decode) â†’ #7 (top-K heap) â€” top-K's projection-deferral win compounds when single-row decode is already cheap.
- Subscription: #3 (zero-copy fan-out) â†’ Subscription Target 7 (snapshot path unification via shared EventData encoder).
- Commit: #9 (FxHash everywhere) is a soft prereq for #2 (per-table sharding adds new map lookups that should be Fx from day one).

# Format-breaking changes (pre-release only)
- WAL Target 1/10 (zero-copy framed codec, retire bincode V1/V2) â€” bumps WAL on-disk format; requires dual-read for one release.
- Server Target 9 (positional msgpack response, drop `to_vec_named`) â€” wire-protocol change; gated on `query_version` bump.
- Server Target 10 (typed `sub_id` field on `QueryResult`) â€” additive wire field but still a wire change.
- Subscription Target 1/2 (PushEnvelope frame with stable seq offset, hand-rolled msgpack) â€” wire change for push frames.
- Commit Target 10 (zero-copy interner overlay serialization) â€” touches `WalOpV2` enum, semver-internal but disk-visible.

---

## Raw finder reports

### 1. Insert hot path

I have enough. Producing the report.

```
[Insert hot path]

## Target 1: counter_deltas / staged_vectors / write_set use std::HashMap with RandomState
File:line: crates/shamir-tx/src/tx_context.rs:105, 118, 131, 153
Current shape: `HashMap<u64, â€¦>` with the default `RandomState` siphash for hot per-table maps touched on every staged write.
What's wasteful: SipHash is 3â€“5Ã— slower than Fx for 8-byte u64 keys; `ensure_table_staging` + `bump_counter` + `stage_vector` each hash on every row.
Ã—N proposal: Switch to `HashMap<u64, â€¦, THasher>` (Fx) per CLAUDE.md pillar 4 â€” workspace mandate is being violated in the very crate the insert path lives in.
Estimated win: medium per call, multiplicative across N rows Ã— per-row hits to these maps.
Risk: low.
Attack hint: alias `type FxMap<K,V> = HashMap<K, V, THasher>` and rename three fields.

## Target 2: per-row `validate_unique_for_create` in `insert_tx_many` is O(NÃ—U) with async-fn-per-call
File:line: crates/shamir-engine/src/table/table_manager_tx_ops.rs:316-318 and crates/shamir-index/src/legacy/index_manager_unique.rs:33-54
Current shape: For each row, `validate_unique_for_create` allocates `Vec<IndexDefinition>` via `indexes_unique.iter().collect()` and awaits one `info_store.get` PER unique index PER row.
What's wasteful: O(NÃ—U) async storage gets serialized; the def-vec is rebuilt N times; no batching against the storage backend.
Ã—N proposal: Hoist `defs` once outside the row loop, then batch all (def, values) lookups into a single `multi_get`/`scan`-style call on the unique info_store, settled in one async hop.
Estimated win: multiplicative on tables with unique indexes (â‰ˆUÃ— fewer awaits and â‰ˆNÃ— fewer Vec allocs).
Risk: medium (touches storage interface).
Attack hint: add `IndexManager::validate_unique_for_create_batch(values: &[InnerValue]) -> DbResult<()>` that builds all keys then issues one batched `info_store.get_many`.

## Target 3: `unique_keys_for` rebuilt per row even when unique_indexes is empty list of defs computed N times
File:line: crates/shamir-engine/src/table/table_manager_tx_ops.rs:360-368
Current shape: For every row, iterates `indexes_unique` (allocates internally) and calls `build_index_key(...).to_bytes()` row-by-row to record a guard.
What's wasteful: def list snapshot + path slice re-resolution per row; no shared "compiled extractor".
Ã—N proposal: Compile a one-shot `UniqueExtractor` (Vec of (name_interned, paths, key_prefix)) ONCE before the row loop, then row work becomes `extract+encode` only.
Estimated win: medium-multiplicative for batches with unique indexes.
Risk: low.
Attack hint: precompute `Vec<&IndexDefinition>` once and inline the encode.

## Target 4: per-row `acquire_pessimistic_write_lock` loop awaits N times even for Snapshot/Serializable txs
File:line: crates/shamir-engine/src/table/table_manager_tx_ops.rs:350-352
Current shape: `for key in &id_bytes { self.acquire_pessimistic_write_lock(...).await }` â€” even though the function returns Ok(()) immediately when `isolation != Pessimistic`.
What's wasteful: N async-fn state machines + N `Bytes::clone()` + N `tx.record_locked_key` invocations for the 99% Snapshot path.
Ã—N proposal: Gate the entire loop on `if tx.isolation == Pessimistic` and add a `lock_keys_batch` MvccStore method that takes the slice and acquires under one internal lock pass.
Estimated win: large for Snapshot (skip N awaits entirely); multiplicative for Pessimistic via batched lock acquire.
Risk: low.
Attack hint: early-return Snapshot first; for Pessimistic, batch into MvccStore.

## Target 5: `index2_registry.all_backends().await` returns Vec â€” and per-row backend iteration calls async `staged_vector` even for stateless backends
File:line: crates/shamir-engine/src/table/table_manager_tx_ops.rs:377-390 and crates/shamir-index/src/registry.rs:85-89
Current shape: backends are cached once, but the inner row loop awaits `backend.plan_insert_tx` AND `backend.staged_vector` PER (row, backend) pair â€” most backends return `None`/empty synchronously.
What's wasteful: NÃ—B async-fn invocations, B mostly returning trivially. For tables with zero vector indexes, every `staged_vector` call is wasted.
Ã—N proposal: Partition backends ONCE into (stateful-vector, stateless-posting) at registration time; iterate vector-backends only when a vector field is set on the value; expose a sync `plan_insert_sync` for stateless backends.
Estimated win: multiplicative for tables with many backends; Ã—B reduction in await count.
Risk: medium (touches IndexBackend trait).
Attack hint: split `IndexBackend` into `SyncBackend` + `VectorBackend` subtraits, pre-bucketed in registry.

## Target 6: ACL/computed scan + `serde_json::Value::from(v.clone())` round-trip in `resolve_computed_record` runs per row even when no `$fn`
File:line: crates/shamir-engine/src/table/write_helpers.rs:82-120
Current shape: For every record, even when no field is computed, code allocates `obj.values().any(is_computed_field)`-scan; when present it builds a `serde_json::Map` of all literal fields via `Value::from(v.clone())` per row.
What's wasteful: clone-to-json round-trip per row; `serde_json::from_value` for every `$fn` arg; then `inner_to_json_value` back. Pure conversion churn on the hot insert.
Ã—N proposal: Skip the json round-trip â€” evaluate `$fn` against the existing `QueryValue`/`InnerValue` literal map directly via a typed evaluator; cache the "no `$fn`" predicate as a single fast scan returning a bool, computed once for the batch when a homogeneous schema is known.
Estimated win: multiplicative for inserts that use computed fields; otherwise small (still removes the `any()` allocation chain).
Risk: medium.
Attack hint: add `eval_write_value_qv(fv, &TMap<String,QueryValue>, â€¦)` taking QueryValue refs directly.

## Target 7: `insert_tx_many` extends `index_write_set` from `Vec` â€” quadratic memmove risk + per-op tuple build
File:line: crates/shamir-engine/src/table/table_manager_tx_ops.rs:417-418 and crates/shamir-tx/src/tx_context.rs:110
Current shape: `tx.index_write_set: Vec<(u64, IndexWriteOp)>` is extended row-by-row across many tables; each op pays for the `(token, op)` tuple wrap.
What's wasteful: Token is identical for every op in a batch â€” storing it per-op duplicates 8 bytes Ã— M ops; growing one Vec across batches reallocates O(log) times; cross-table batches lose cache locality.
Ã—N proposal: Replace with `HashMap<u64, Vec<IndexWriteOp>>` (token â†’ ops), pre-reserved per table. Single token entry; commit-path scan still O(M) but with better cache behavior + no per-op token allocation.
Estimated win: medium-multiplicative for large batches (memory + allocator pressure).
Risk: low (commit consumer can iterate the map).
Attack hint: change collection type, update `stage_mutation` + commit pipeline consumer.

## Target 8: StagingStore uses `scc::HashMap` per tx â€” overkill for single-writer per-tx buffer
File:line: crates/shamir-tx/src/staging_store.rs:85
Current shape: `writes: scc::HashMap<RecordKey, StagedOp>` â€” sharded, lock-free CAS map.
What's wasteful: A `StagingStore` is private to one task (per-tx); `scc::HashMap` pays sharding + atomics overhead with zero concurrency benefit. `upsert`/`scan` walk shards. Comment at line 170 explicitly says "the StagingStore is per-tx and only the owning task writes to it" â€” admits the violation.
Ã—N proposal: Replace with `IndexMap<RecordKey, StagedOp, THasher>` (or plain `FxHashMap`) â€” preserves insertion order for deterministic drain, no sync overhead. Keeps `staged_op` as O(1) plain lookup.
Estimated win: large-multiplicative on insert_many â€” every `set_many_live` row pays scc shard arithmetic + atomic CAS today.
Risk: low (single-owner invariant already documented).
Attack hint: switch field type; `drain()` becomes `into_iter().map(...)`.

## Target 9: `set_returning_version` / `delete_returning_version` called per-row in `execute_update` / `execute_delete` (non-tx path)
File:line: crates/shamir-engine/src/table/write_exec.rs:320, 558
Current shape: After collecting `matched`, loops awaiting one `set_returning_version`/`delete_returning_version` per record â€” N async storage roundtrips.
What's wasteful: Each call hits MvccStore + WAL + index hooks individually; no batching, even though `insert_many_returning_version` already exists upstream.
Ã—N proposal: Add `set_many_returning_versions(&[(RecordId, InnerValue)])` and `delete_many_returning_versions(&[RecordId])` that fan out to one Store::transact + one batched index plan call (mirroring the existing `plan_records_*_batch` family).
Estimated win: large-multiplicative (single fsync, single index-plan walk).
Risk: medium (need to preserve per-row WAL semantics; the marker already spans the whole batch).
Attack hint: build a non-tx `update_many` symmetric to `insert_many`.

## Target 10: `merge_inner_maps` clones the entire `orig_map` per matched row, then inserts overwrites
File:line: crates/shamir-engine/src/table/write_exec.rs:932-946
Current shape: `let mut merged = orig_map.clone(); for (k,v) in set_map { merged.insert(k.clone(), v.clone()); }`
What's wasteful: Full IndexMap clone (O(orig)) + per-key clone of `set_map` value (which is identical for every matched row); `set_map`'s entries are cloned N times even though they're the same constant overlay.
Ã—N proposal: Take the old_record by value out of `matched` (`mem::take`), mutate in place (`extend`); for InnerValue values reuse a shared `Arc<InnerValue>` for set_map values so per-row clones are refcount bumps not deep copies.
Estimated win: medium-multiplicative for wide records or set_map with strings/maps.
Risk: low.
Attack hint: change `matched: Vec<(RecordId, InnerValue)>` consumption to `into_iter`; introduce `Arc<InnerValue>` in `set_map` lanes.
```

---

### 2. Read hot path

Reminder not applicable â€” this is a read-only analysis. I have enough context to produce the candidate list.

[Read hot path]

## Target 1: Eliminate double-decode in bytes-prefilter path
File:line: crates/shamir-engine/src/query/filter/eval_bytes.rs:163; table_manager_streaming.rs:82
Current shape: `matches_msgpack_bytes` does `rmpv::decode::read_value` (full Rv tree allocation) to evaluate one or two scalar compares, then on `Some(true)` the row is *re-decoded* into `InnerValue` for the authoritative filter.
What's wasteful: Two full msgpack walks per row (Rv tree alloc + InnerValue decode); the Rv tree alloc per row dominates for shallow filters on wide records.
Ã—N proposal: Replace `rmpv::decode::read_value` with a zero-alloc cursor scanner that seeks the field by interned-key bytes and decodes only that scalar; on `Some(true)`, reuse the already-decoded InnerValue by promoting bytesâ†’InnerValue once in the seam (decode once, filter against the value).
Estimated win: multiplicative (2â€“5Ã— on wide records, shallow filters; eliminates O(record_size) per-row Rv alloc).
Risk: medium
Attack hint: streaming cursor + skip-scalar table for msgpack; or fully drop bytes-prefilter and rely on cheap InnerValue eval after a single decode.

## Target 2: Per-row find_field_bytes is O(map_size) linear scan
File:line: crates/shamir-engine/src/query/filter/eval_bytes.rs:65-84
Current shape: For each nested map segment, iterates `entries.iter().find_map(...)` comparing key bytes â€” O(NÃ—depth) per record.
What's wasteful: msgpack maps in Rv are `Vec<(Rv,Rv)>`; on every row we linearly scan to find one key. With M filter atoms over the same path this multiplies.
Ã—N proposal: Single-pass cursor that records byte offsets of all needed paths in one walk; or sort/index map entries once at decode time. Also: hoist `interned_key_bytes(id)` out of the per-row loop (currently recomputed per row per node).
Estimated win: medium-large for wide records (10+ fields).
Risk: low
Attack hint: precompute `KeyBuf` array at filter-compile time; fuse multi-field lookup into one pass.

## Target 3: list_stream_filtered uses async_stream macro per batch â€” synchronous CPU work behind async overhead
File:line: crates/shamir-engine/src/table/table_manager_streaming.rs:62-93, 25-54
Current shape: `decode_raw_batch_filtered` runs synchronously inside an `async_stream::stream!{...}` that yields each decoded sub-batch one at a time through generator state machine.
What's wasteful: Generator resumes (poll overhead, self-ref state machine, heap-pinned future) for pure-CPU decode work on every batch. The whole filter+decode loop is CPU-bound but wrapped in async machinery.
Ã—N proposal: Drive the read pipeline with a sync iterator over decoded batches and only `await` the storage I/O; or move decode+filter to `spawn_blocking` and pipeline N batches in parallel (CPU parallelism â€” currently fully serial).
Estimated win: multiplicative on multi-core (parallel decode); medium otherwise.
Risk: medium
Attack hint: rayon over decoded batches; or sync-iter + tokio mpsc.

## Target 4: project_value re-builds output map and clones output key per row
File:line: crates/shamir-engine/src/query/read/select_projection.rs:109-135; read_exec.rs:471, 583, 590, 680
Current shape: For each matched row, `new_map_wc(n)` allocates a TMap, then `obj.insert(key.clone(), val)` clones the String key per field per row.
What's wasteful: N rows Ã— F fields key-string clones + per-row map alloc; output keys are immutable across rows.
Ã—N proposal: Output a columnar buffer (Vec<QueryValue> per column with a single shared schema Vec<Arc<str>>), then materialize to row-form only at the wire boundary; or use `Arc<str>` for keys so per-row clone is a refcount bump.
Estimated win: multiplicative for high-cardinality results with many projected fields.
Risk: medium-high (touches QueryRecord shape)
Attack hint: columnar staging buffer; QueryRecord::Columnar variant.

## Target 5: get_many on index2 path materializes ALL matches before any pagination
File:line: crates/shamir-engine/src/table/read_exec.rs:166-196
Current shape: `get_many(&rids_vec)` decodes every matched record into InnerValue, then `inner_value_to_query_value` converts each â€” *before* any LIMIT/pagination logic. The path has no LIMIT push-down.
What's wasteful: A `WHERE fts_match(...) LIMIT 10` over 10K matches decodes 10K records.
Ã—N proposal: Push pagination into the index2 path â€” slice `rids_vec` to `[skip..skip+take]` *before* `get_many`. Same gating as `try_project_page_only`.
Estimated win: multiplicative (1000Ã—-class for huge ranked sets + small LIMIT).
Risk: low
Attack hint: add page-slice before get_many; preserve count_total via rids_vec.len().

## Target 6: record_scan_reads / record_query_reads drains streams twice
File:line: crates/shamir-engine/src/table/table_manager_streaming.rs:467, 477, 524-548
Current shape: `read_tx` runs `record_query_reads` (a full scan stream that does decode+filter+record), THEN runs `self.read(...)` which does the **same** scan+decode+filter again.
What's wasteful: Serializable txs pay 2Ã— full-scan cost â€” once to populate read-set, once to actually return rows.
Ã—N proposal: Fuse the two: run a single scan that both records into read-set and produces output. The current architecture already has `list_stream_tx` doing recording as a side-effect â€” just plumb it through `read()` itself.
Estimated win: exactly 2Ã— for Serializable read-tx, multiplicative across SSI workloads.
Risk: medium
Attack hint: pass `tx` into `read()`/`read_collecting/_streaming/_counting` and use `list_stream_tx`/`filter_stream_tx` instead of the non-tx variants.

## Target 7: resolve_filter_value clones String/Bin literals on every row evaluation
File:line: crates/shamir-engine/src/query/filter/resolve.rs:102-117
Current shape: `FilterValue::String(s) => Some(InnerValue::Str(s.clone()))` for every row, even in pure-literal compares; same for `Binary`. Called from projection scalar fns and from `FilterNode::Compare` resolve paths.
What's wasteful: Heap clone of the literal per row, for what is by definition a compile-time constant.
Ã—N proposal: Borrow path: `resolve_filter_value_ref<'a>(... ) -> Option<Cow<'a, InnerValue>>` or pre-build `InnerValue` literals at compile time and store them inline in FilterNode (already partially done via `pre_resolved` for Compare â€” extend to FnCall args and projection funcs).
Estimated win: medium (per-row alloc removal).
Risk: low
Attack hint: extend the `pre_resolved` pattern; return Cow from resolve.

## Target 8: read_collecting collects then re-iterates with `.into_iter().map(json::Value::from).collect()` 
File:line: crates/shamir-engine/src/table/read_exec.rs:497-498, 512-515
Current shape: Builds `Vec<QueryRecord>`, then `result.into_iter().map(serde_json::Value::from).collect()` (full JSON tree alloc per record), then post-processing, then maps back to `QueryRecord::Json`.
What's wasteful: For ORDER BY / DISTINCT paths, every projected row is converted to serde_json::Value (deep tree alloc per row) just to drive `apply_order_by` / `apply_distinct` / `apply_pagination`.
Ã—N proposal: Make `apply_order_by`/`apply_distinct`/`apply_pagination` operate on `QueryValue` directly (it's already the native type); eliminate the QueryRecordâ†”json::Valueâ†”QueryRecord round-trip.
Estimated win: multiplicative for ORDER BY/DISTINCT (currently 2Ã— allocation of the entire result set).
Risk: medium (touches order/distinct internals)
Attack hint: port `Hashable`/`Ord` helpers from `HashableJson` to `QueryValue`.

## Target 9: ORDER BY sort path is O(N log N) materialize+sort instead of top-K heap
File:line: crates/shamir-engine/src/table/read_exec.rs:503-508
Current shape: For `ORDER BY x LIMIT K` without a sorted-index match, the full `json_result` vec is sorted, then paginated.
What's wasteful: Sorts all N matches when only top K are needed. Bytes-prefilter + collect = N rows projected + N log N sort vs. O(N log K) with a bounded heap.
Ã—N proposal: Top-K min-heap of size K+skip when both ORDER BY and a finite LIMIT exist (no index). Project only the K winners (not all N).
Estimated win: multiplicative (large N, small K â€” e.g. N=1M, K=10 â†’ 60000Ã— sort work avoided + 99.999% projection avoided).
Risk: low
Attack hint: BinaryHeap<Reverse<...>> bounded by skip+take; defer projection until after winnowing.

## Target 10: SelectProjection holds empty_refs TMap allocated per query, not per-server
File:line: crates/shamir-engine/src/query/read/select_projection.rs:30, 70, 92, 126
Current shape: Every `SelectProjection::new` allocates a fresh `empty_refs: TMap` even when there are zero scalar-fn projections; `FilterContext::new(interner, &self.empty_refs)` is built once per projection but every projection allocs.
What's wasteful: Allocation per query for a structurally-empty map; FilterContext construction even when `funcs` is empty.
Ã—N proposal: `static EMPTY_REFS: OnceLock<TMap<String, QueryResult>>` shared; skip FilterContext build entirely when `funcs.is_empty()` (already true in the loop but ctx still built).
Estimated win: small per query but eliminates an alloc on every read â€” adds up under high QPS.
Risk: low
Attack hint: static singleton; lazy ctx construction.

Files referenced: D:\dev\rust\shamir-db\crates\shamir-engine\src\table\read_exec.rs, D:\dev\rust\shamir-db\crates\shamir-engine\src\table\table_manager_streaming.rs, D:\dev\rust\shamir-db\crates\shamir-engine\src\query\filter\eval_bytes.rs, D:\dev\rust\shamir-db\crates\shamir-engine\src\query\filter\resolve.rs, D:\dev\rust\shamir-db\crates\shamir-engine\src\query\read\select_projection.rs, D:\dev\rust\shamir-db\crates\shamir-engine\src\query\read\exec.rs.

---

### 3. Commit pipeline

I have enough context. Crucial findings: `tx.write_set`, `counter_deltas`, `staged_vectors` are `std::collections::HashMap` (default RandomState â€” ideology violation), `CommitWriteRecord.per_table` is `std::HashMap` with RandomState, and the commit-lock holds Phase 5a/5c serially. Producing report.

[Commit pipeline]

## Target 1: Phase 5a/5c per-table writes serialized inside commit_lock
File:line: `crates/shamir-engine/src/tx/materialize.rs:67` and `:147`
Current shape: per-table loop awaits `apply_data_batch` / `apply_index_batch` sequentially under `commit_lock`.
What's wasteful: a tx touching N tables pays N sequential storage round-trips inside the global gate â€” every other committer is blocked for that whole walk.
Ã—N proposal: fan out per-table batches with `futures::future::join_all` (or `FuturesUnordered`); each MvccStore/info_store is independent â†’ N-way parallel I/O, critical-section shrinks from O(N rows) to O(max single-table).
Estimated win: multiplicative (â‰ˆN on wide multi-table txs; combined with commit_lock, raises throughput of *every* concurrent committer).
Risk: medium â€” must preserve per-table idempotency and the `ok` aggregation.
Attack hint: `join_all` over `(table_id, base, ops)` futures; collect Err into `ok=false`.

## Target 2: `std::HashMap<_, _>` with RandomState on commit hot paths (ideology violation)
File:line: `crates/shamir-tx/src/tx_context.rs:105,118,131`; `crates/shamir-tx/src/repo_tx_gate.rs:49,429`; `crates/shamir-engine/src/tx/commit_phases.rs:136`; `materialize.rs:142`.
Current shape: `HashMap<u64, â€¦>` (default RandomState) used per commit for write_set / counter_deltas / staged_vectors / CommitWriteRecord.per_table / by_token grouping.
What's wasteful: SipHash on u64 token â€” 2â€“5Ã— slower than FxHash; CLAUDE.md mandates `THasher`.
Ã—N proposal: replace with `HashMap<u64, V, THasher>` (the one in `commit_phases.rs:136` already uses THasher â€” propagate everywhere).
Estimated win: medium-to-large per-token lookup heavy paths (group-by-token, footprint build).
Risk: low.
Attack hint: workspace-wide `THasher` default; affects struct field types â€” semver-touching only inside crate.

## Target 3: `tokio::sync::Mutex<()>` commit_mutex on every committer (the global serializer)
File:line: `crates/shamir-tx/src/repo_tx_gate.rs:69`
Current shape: a single per-repo async mutex serializes the entire pre_commitâ†’materialize section.
What's wasteful: even with Phase 6.5/7 out, the lock still spans Phase 1 interner merge + Phase 5a + 5c I/O â€” every committer waits.
Ã—N proposal: split into (a) version-assign-only CAS (already atomic), (b) per-table writer pipeline where assign_next_version + WAL begin + publish are a thin critical section, and data/index materialization runs concurrently keyed by table_token using `scc::HashMap<u64, tokio::sync::Mutex<()>>` per-table writer locks (you already hold uwl_guards per touched table). Then commits to *disjoint* tables run fully parallel.
Estimated win: multiplicative for disjoint-table workloads â€” the headline Ã—N win.
Risk: high â€” interacts with SSI footprint ordering (must record under a sequencer); essentially "group commit lite" but legal because per-table sharding sidesteps the cross-follower SSI block.
Attack hint: keep `commit_mutex` only around `assign_next_version + record_commit_writes + publish_committed`; materialize per-table outside, gated by per-table write lock.

## Target 4: O(commits Ã— postings) `predicate_conflicts` linear scan
File:line: `crates/shamir-tx/src/repo_tx_gate.rs:366` and `:410`
Current shape: `for (_v, rec) in tree.range(start..=last)` then nested `inserted_index_keys.iter().any(|k| key_in_interval(k, â€¦))`.
What's wasteful: O(W Ã— K) per Serializable commit, where W = commits in window, K = postings per commit; `inserted_index_keys` is already sorted but unused.
Ã—N proposal: use the already-sorted vector with `partition_point` for `IndexRange` (binary search by prefix) â†’ O(W Â· log K). Better: keep a per-table `scc::TreeIndex<IndexKey, commit_version>` so probe is O(log total). Add a per-record Bloom filter over keys for instant negatives.
Estimated win: multiplicative when window or fanout is large.
Risk: lowâ€“medium.
Attack hint: `partition_point` + slice scan; Bloom filter optional.

## Target 5: Interner overlay merged + persisted per-table sequentially under lock
File:line: `crates/shamir-engine/src/tx/pre_commit.rs:53-74`
Current shape: for each touched table: `get_interner â†’ commit_overlay â†’ rewrite staging â†’ persist`. Sequential awaits, inside `commit_lock`.
What's wasteful: N tables Ã— (I/O persist + remap walk) serialized; remap is per-table even though overlay is shared.
Ã—N proposal: (a) compute remap concurrently across tables (`join_all`); (b) batch `persist().await` into a single grouped flush; (c) defer `persist` to post-publish (the overlay merge result is already in the WAL via `InternerOverlayMerge`, recovery can re-apply).
Estimated win: large for multi-table writes touching new field names.
Risk: medium â€” must ensure recovery replays InternerOverlayMerge before reading stamped ids (it does).
Attack hint: parallel remap + lazy persist.

## Target 6: `wal_ops_from_tx` does a full snapshot + RecordId re-parse from bytes
File:line: `crates/shamir-engine/src/tx/commit.rs:178-202`
Current shape: walks every StagingStore op and `RecordId::try_from_bytes(&k)` for each Put/Delete.
What's wasteful: per-row bytesâ†’RecordId parse on the commit critical path (right before WAL fsync); ops were originally typed at insert time, so this is a round-trip back from raw bytes.
Ã—N proposal: have `StagingStore` store typed `KvOp<RecordId>` (or carry the RecordId alongside) so wal_ops_from_tx is a `Vec::extend` zero-parse fast path. Also pre-size `ops` exactly (the `with_capacity` adds counts but each staging may contribute many ops â€” currently undercount).
Estimated win: medium-to-large on row-heavy batches.
Risk: medium â€” touches StagingStore API.
Attack hint: type-preserving staging, drop `try_from_bytes` on commit hot path.

## Target 7: `release_pessimistic_locks` groups via std::HashMap<u64, Vec> per call
File:line: `crates/shamir-engine/src/tx/commit.rs:535-548`
Current shape: scan scc map â†’ build std HashMap<u64, Vec<Bytes>, THasher> â†’ sequential per-table `release_locks().await`.
What's wasteful: per-commit allocation of grouping map+vecs; sequential release.
Ã—N proposal: parallelize releases via `join_all`; reuse a thread-local `TMap`/`IndexMap` for grouping (or stream directly into per-table channels).
Estimated win: smallâ€“medium (only Pessimistic tx); but it runs on every abort + commit so amortizes.
Risk: low.
Attack hint: `join_all` over per-table release futures.

## Target 8: Phase 5b counter loop awaits table lookup per token
File:line: `crates/shamir-engine/src/tx/materialize.rs:107-125`; `commit_phases.rs:98-117`
Current shape: sequential `repo.table_by_token(*table_id).await` then `.counter().increment(*delta).await` per table.
What's wasteful: N sequential awaits in lock; counter increment is in-memory `AtomicI64`-ish â€” easily parallel.
Ã—N proposal: `join_all` over counter increments; cache `table_by_token` resolution (already done once in Phase 5a â€” pass results forward).
Estimated win: medium.
Risk: low.
Attack hint: hoist table resolution to Phase 5a, reuse for 5b/5c.

## Target 9: `build_footprint_from_tx` allocates fresh HashMap + sort per commit
File:line: `crates/shamir-tx/src/repo_tx_gate.rs:426-465`
Current shape: builds `HashMap<u64, TableWriteFootprint>` from scratch, pushes keys, then `sort_unstable` each table's `inserted_index_keys`.
What's wasteful: invoked under `commit_lock`. Cloning Bytes is cheap, but sort is O(K log K) where K can be large; `index_write_set` is a Vec preserving insertion order â€” if it were already a sorted structure (e.g. `scc::TreeIndex` or BTreeMap keyed by key bytes), the footprint sort is free.
Ã—N proposal: switch `tx.index_write_set` to a per-table sorted structure (or sort once at stage-time amortized), drop sort on commit path; pre-size per_table with `with_capacity(touched_tables_count)`.
Estimated win: medium on wide-index commits.
Risk: low.
Attack hint: stage-time ordering.

## Target 10: `wal_ops_from_tx` `interner_overlay.scan_async` allocation churn + clone of Strings
File:line: `crates/shamir-engine/src/tx/commit.rs:145-151`
Current shape: `scan_async` collects `(u64, String)` cloning every overlay key into `entries`.
What's wasteful: String clones on the WAL hot path under `commit_lock`; if overlay is large this is per-byte cost.
Ã—N proposal: emit `Bytes`/`Arc<str>` instead of `String`; or stream entries directly into the WAL encoder without intermediate Vec (serialize directly via a visitor).
Estimated win: smallâ€“medium per-tx; bigger when overlay grows.
Risk: lowâ€“medium â€” touches WalOpV2 enum (semver inside crate).
Attack hint: zero-copy overlay serialization.

---

### 4. WAL emit + recovery

I have enough. Let me finalize the report.

[WAL emit + recovery]

## Target 1: serde_bytes_bytes round-trips Bytes via owned Vec on every WalOpV2 decode
File:line: D:/dev/rust/shamir-db/crates/shamir-wal/src/wal_entry_v2.rs:116-119
Current shape: `deserialize` collects into `ByteBuf` then `Bytes::from(bb.into_vec())` â€” heap alloc + copy per Put/IndexPut body during recovery, and the encode side `serde_bytes::Bytes::new(b.as_ref()).serialize` re-copies through bincode's byte path.
What's wasteful: Body bytes (often KB-sized record payloads, MessagePack) are copied at least once per encode and once per decode; bincode's default `serialize` then materialises whole buffer in `encode()` too. For N-op batches with R-byte records this is O(NÂ·R) memcpy that could be zero-copy.
Ã—N proposal: Replace bincode + serde_bytes with a hand-rolled framed codec (tag byte + varint len + raw slice) writing directly into the caller's `BytesMut`; on decode hand out `Bytes::slice` ranges over the original storage value (zero-copy). Drops one full copy of every payload and the entire serde machinery.
Estimated win: multiplicative on large-body batches (commit emit dominated by body copy)
Risk: medium (wire format change â€” needs version bump V2â†’V3 with dual-read)
Attack hint: zero-copy `Bytes::slice` framing, eliminate bincode for V2

## Target 2: begin_many allocates+encodes serially in a Vec loop
File:line: D:/dev/rust/shamir-db/crates/shamir-tx/src/repo_wal_manager.rs:88-100
Current shape: `for entry in entries { encode()?; items.push(...) }` â€” sequential CPU encode of N independent entries before any I/O.
What's wasteful: Each `encode()` is pure CPU (bincode) yet runs serially on the commit task's executor thread. With group-commit batching dozens-hundreds of txs, encode latency adds linearly to commit critical-path; also each entry allocates a fresh `Vec<u8>` (capacity 256).
Ã—N proposal: (a) Pre-compute total capacity from `ops.len()` heuristics and encode all entries into one contiguous arena `BytesMut`, slicing per-entry views â€” single allocation. (b) For batches above a threshold, `rayon::join` / `par_iter` the encode (CPU-bound, embarrassingly parallel) before the single `set_many`.
Estimated win: largeâ†’multiplicative on big group-commit fan-in
Risk: low
Attack hint: arena encode + parallel encode for N>=K

## Target 3: encode/begin are `async fn` despite being pure CPU until the final `set`
File:line: D:/dev/rust/shamir-db/crates/shamir-wal/src/wal_entry_v2.rs:187 and repo_wal_manager.rs:72,88
Current shape: `WalEntryV2::encode` is sync (OK), but it's called from inside `async fn begin/begin_many` which means the entire encode lives in the async state machine of the commit task.
What's wasteful: Holding the tokio task across a multi-ms bincode encode of a large batch blocks the reactor thread; no `spawn_blocking` boundary.
Ã—N proposal: For `begin_many` with large aggregate ops count, move encode into `spawn_blocking`; or simpler â€” run encode in a `rayon` scope and `.await` a oneshot. Frees the runtime worker for other commits / reads.
Estimated win: medium-large under concurrent load (improves p99 commit, reduces tail under fan-in)
Risk: low
Attack hint: spawn_blocking boundary at the encode step

## Target 4: list_inflight pushes into unsized Vec and re-allocates the decoded body
File:line: D:/dev/rust/shamir-db/crates/shamir-wal/src/wal_manager.rs:123-146 and shamir-tx/src/repo_wal_manager.rs:137-150
Current shape: `let mut out = Vec::new(); ... out.push(...)` â€” recovery scan grows unbounded `Vec`; both managers scan the *same* prefix independently in series.
What's wasteful: Recovery iterates the whole `__wal_active_` prefix twice (once per manager), doubling I/O. Each entry is fully decoded via bincode even when only `txn_id` / `commit_version` is needed for the seed-floor / sort step.
Ã—N proposal: Single shared scan that dispatches V1/V2 once by magic, returning a borrowed view; lazy-decode bodies only for the entries selected for replay. Pre-allocate `Vec::with_capacity` after a count probe, or build directly into a typed map (`scc::TreeIndex<u64,â€¦>` keyed by `commit_version`) to skip the later sort step.
Estimated win: medium structurally (recovery rare but currently 2Ã— I/O + O(N log N) sort done downstream)
Risk: medium
Attack hint: fuse two scans, lazy decode, build sorted container inline

## Target 5: Per-entry WalActiveKey allocates a fresh BytesMut(21) in a tight loop
File:line: D:/dev/rust/shamir-db/crates/shamir-wal/src/active_key.rs:30-35 (called from repo_wal_manager.rs:95)
Current shape: `BytesMut::with_capacity(21); extend_from_slice(PREFIX); put_u64; freeze` â€” heap alloc per key.
What's wasteful: For begin_many with N entries we allocate N independent 21-byte buffers. Heap traffic dominates for small-payload index-only txs.
Ã—N proposal: Pack all N keys into one contiguous `Vec<u8>` (N*21 bytes) and hand out `Bytes::slice` views referencing one Arc â€” single allocation. Or use inline `[u8; 21]` arrays since keys are fixed-length and `Store::set_many` accepts owned `Bytes` (could be `Bytes::from_static`-style cheap slice).
Estimated win: medium on small-tx group-commit
Risk: low
Attack hint: arena-allocated key block, shared Arc backing

## Target 6: V1/V2 dispatch via magic-sniff + double-decode path on every recovered entry
File:line: D:/dev/rust/shamir-db/crates/shamir-wal/src/wal_manager.rs:135-142
Current shape: `looks_like_v2` then either `WalEntryV2::decode` (re-validates magic again) or bincode V1.
What's wasteful: Magic bytes checked twice for every V2 entry (`looks_like_v2` then `decode`); branch is also unpredictable when V1+V2 mixed.
Ã—N proposal: Single fused dispatch that consumes the 5-byte header in one parse and dispatches into a single decoder, returning `WalEntryAny`. Combined with Target 1's framed codec this becomes a tag-byte switch.
Estimated win: small (recovery only)
Risk: low
Attack hint: one-shot header parse

## Target 7: commit() is awaited synchronously despite commit_async existing unused
File:line: D:/dev/rust/shamir-db/crates/shamir-tx/src/repo_wal_manager.rs:107-112 (comment at 117-120 confirms commit path uses sync)
Current shape: Commit path awaits `info_store.remove` inline; on fsync backends this is 5-10 ms NTFS.
What's wasteful: Critical-path ACK blocked on marker delete though recovery is idempotent (forward-fix re-applies). Comment at line 117-120 documents `commit_async` exists for exactly this but isn't wired.
Ã—N proposal: Switch `commit_tx` to fire-and-forget `commit_async`, batch removals into a delete-queue periodically flushed via `remove_many` (mirror of begin_many group-commit). Removes marker-delete from commit latency entirely.
Estimated win: multiplicative for per-commit-fsync backends (persy / nebari / canopy on NTFS â€” 5-10 ms saved per tx)
Risk: medium (recovery must remain idempotent under racing replays)
Attack hint: group-commit on remove side too â€” add `WalManager::commit_many` and a coalescing queue

## Target 8: started_at_ns SystemTime::now() syscall per WalEntryV2::new
File:line: D:/dev/rust/shamir-db/crates/shamir-wal/src/wal_entry_v2.rs:160-163 (and wal_entry.rs:34-38)
Current shape: Every entry calls `SystemTime::now().duration_since(UNIX_EPOCH)` â€” a VDSO syscall.
What's wasteful: In begin_many with N entries, N syscalls for what could be one timestamp + N identical (or `+i`) values. The field is informational (recovery sorts by `commit_version`, not `started_at_ns`).
Ã—N proposal: Sample `SystemTime::now()` once per batch in `begin_many` and stamp all entries; or, since `commit_version` is the canonical ordering key, drop `started_at_ns` from the encoded entry entirely (saves 8 bytes + the syscall per entry).
Estimated win: small-medium on high-N batches
Risk: low
Attack hint: hoist timestamp out of the loop / drop field

## Target 9: WalOp counter ops for insert_many allocate per-row Vec via map+collect
File:line: D:/dev/rust/shamir-db/crates/shamir-wal/src/wal_manager.rs:150-169
Current shape: `record_ids.iter().map(...).collect()` produces a fresh `Vec<WalOp>` of N enum-tagged 24-byte items just to be re-serialized.
What's wasteful: An intermediate Vec of `WalOp` is built solely to feed bincode that then walks it linearly. For N=10k inserts that's ~240 KB of throwaway memory and an extra pass.
Ã—N proposal: Stream-serialize directly: bypass `Vec<WalOp>` and write the bincode header + repeat-loop over `record_ids` straight into the output buffer (custom emitter). Eliminates the intermediate allocation entirely.
Estimated win: medium on large insert_many
Risk: low
Attack hint: direct-emit serializer, skip intermediate Vec<WalOp>

## Target 10: WalEntry (V1) still uses bincode with no envelope versioning â€” locks future format
File:line: D:/dev/rust/shamir-db/crates/shamir-wal/src/wal_manager.rs:73-74,139-141
Current shape: V1 encode/decode is raw bincode; magic-prefix dispatch only protects V2.
What's wasteful: Any future V1 schema change requires the doctor's full-rebuild fallback. More importantly, the V1 path keeps bincode dependency hot and prevents unifying both onto Target 1's zero-copy framed codec.
Ã—N proposal: Migrate V1 entries to the same framed envelope as V2 (single magic-byte tag), unify both decoders to one branchless path, and retire bincode from the WAL hot path entirely.
Estimated win: large structurally (enables Targets 1, 6 to land cleanly)
Risk: medium (back-compat read of old V1 entries required for one release)
Attack hint: unify wire envelope, retire bincode in WAL

---

### 5. Subscription delivery

I have enough. Composing the report.

```
[Subscription delivery]

## Target 1: Eliminate Arc<Vec<u8>>â†’Vec<u8> clone in deliver-cache hot path
File:line: bridge.rs:310, 330
Current shape: deliver_cache returns Arc<Vec<u8>> but `(*cached).clone()` deep-copies the entire serialized frame for every subscriber.
What's wasteful: defeats the cache's whole purpose â€” N subscribers still do N full payload memcpy + N PushEnvelope reserialisations wrapping it.
Ã—N proposal: change try_push_event/PushSink to accept Arc<[u8]> or Bytes; wrap the cached payload + envelope header once (or precompute the encoded PushEnvelope sans seq and patch seq bytes in-place).
Estimated win: multiplicative (Ã—N in number of subscribers per event)
Risk: medium
Attack hint: zero-copy fan-out via Arc<[u8]>/Bytes through PushSink; precompute envelope skeleton in deliver_cache.

## Target 2: try_push_event re-serialises the entire PushEnvelope per subscriber per event
File:line: push.rs:48-60
Current shape: rmp_serde::to_vec_named runs for every Event delivery, re-encoding cached `data` bytes inside the envelope.
What's wasteful: msgpack-encoding a Vec<u8> that's already encoded â€” pure CPU + alloc per push, identical across all N bridges for the same change.
Ã—N proposal: cache the encoded PushEnvelope prefix (with placeholder seq) per (change, mode) in deliver_cache; only patch the 8-byte seq field per subscriber.
Estimated win: multiplicative
Risk: medium
Attack hint: hand-craft a minimal msgpack frame with fixed-offset seq slot, or cache `Arc<Bytes>` of the envelope and append-mutate seq.

## Target 3: repo_hash recomputed every cache_get / cache_insert
File:line: decode_cache.rs:38-41, 50, 63; deliver_cache.rs:38-41, 52, 73
Current shape: THasher::default().hash_one(repo) called twice per change per bridge.
What's wasteful: `repos` is fixed at subscribe time; hashing the same String repeatedly per event is O(|repo|) waste â€” and a fresh BuildHasher allocation each call.
Ã—N proposal: precompute `repo_hashes: Vec<u64>` alongside `repo_idx` once in bridge_task; pass the u64 hash (or directly use repo_idx) as the key prefix.
Estimated win: medium-large (per change Ã— per subscriber)
Risk: low
Attack hint: hoist hash into bridge state; or key cache by `(repo_ptr_or_idx, cv, idx)` using a per-repo Arc<str> identity.

## Target 4: Replace DashMap with scc::HashMap keyed (cv, change_idx) under a per-repo shard
File:line: decode_cache.rs:25-26; deliver_cache.rs:25-26
Current shape: global DashMap with a composite key including repo_hash and db_id; eviction is `retain` scanning whole map.
What's wasteful: `retain` is O(N) over the entire global cache on every event; lock-shard contention across unrelated repos; key tuple includes redundant discriminants.
Ã—N proposal: per-repo `scc::TreeIndex<u64 cv, scc::HashMap<usize, Arc<â€¦>>>` â€” eviction becomes O(log N) range-drop of versions â‰¤ up_to; no full scan; no cross-repo contention.
Estimated win: multiplicative on throughput at scale (eviction currently is O(N) per event)
Risk: medium
Attack hint: TreeIndex.remove_range(..=up_to) replaces retain walk; per-repo state lives in registry, not global Lazy.

## Target 5: matches_any / any_target_interested are O(targets) per change â€” should be O(1) by table
File:line: target_match.rs:18-23, 32-52; bridge.rs:114, 258, 291
Current shape: linear scan of `targets: Vec<(String,String,â€¦)>` for every change, doing String == String.
What's wasteful: For each change we scan ALL targets across ALL repos+tables; on busy bridges with many sources this is O(C Ã— T) per event with full string compares.
Ã—N proposal: build a `scc::HashMap<(repo_idx, table_intern_id), SmallVec<TargetSpec>>` once at subscribe; lookup once per change using the change's already-known repo and table interned id; skip the StringEq.
Estimated win: multiplicative when T large
Risk: low
Attack hint: index by interned table-id (change.table is interned upstream) â†’ O(1) lookup; collapse any_target_interested + matches_any into one indexed pass.

## Target 6: Fuse any_target_interested + matches_any into single walk
File:line: bridge.rs:114-129, 258-291
Current shape: two separate iter().any() passes over `targets` per change, with identical repo+table+mask checks.
What's wasteful: duplicated O(T) walk; cache-misses on the targets Vec twice; second pass only adds filter eval.
Ã—N proposal: single function returning (interested, needs_value, matching_targets) â€” defer filter eval after decode but reuse the survivor list.
Estimated win: medium (constant-factor 2Ã—, but compounds with Target 5)
Risk: low
Attack hint: collect surviving target indices in a stack SmallVec on first pass.

## Target 7: Initial snapshot uses serde_json::json!{} per record and serde_json::to_vec
File:line: bridge.rs:189-196
Current shape: builds a serde_json::Value object per record then serialises.
What's wasteful: huge alloc churn per row; also payload format here is *JSON*, but live deliveries go through make_event_data which is also JSON over msgpack envelope â€” duplicate code path that bypasses deliver_cache entirely.
Ã—N proposal: call make_event_data with a synthesised RecordChange (or share EventData<'_> serializer); also batches: build snapshot output as a single Vec via streaming serializer.
Estimated win: large (per row in snapshot, often the largest single burst)
Risk: low
Attack hint: unify with payload::EventData; use serde_json::Serializer on a reused Vec<u8> buffer.

## Target 8: bridge.rs targets cloning Strings per source on startup AND filter.clone() every Query rebuild
File:line: bridge.rs:48-58, 174
Current shape: `(repo.clone(), table.clone(), mask.clone(), filter.clone())` â€” and filter cloned again for each snapshot Query.
What's wasteful: not per-event but Filter can be deep (nested expression tree); cloned per source even when shared.
Ã—N proposal: store `Arc<str>` for repo/table and `Arc<Filter>` for filters; structural sharing across snapshot Query and live match.
Estimated win: small-medium (mostly subscribe latency)
Risk: low
Attack hint: Arc<str> + Arc<Filter>; teach Query/Filter to accept borrowed.

## Target 9: hex_encode allocates a String per Delete/non-msgpack key
File:line: payload.rs:4-11, 32, 57
Current shape: fmt::write per byte â€” branching formatter, slow.
What's wasteful: hot only on weird keys, but make_event_data also rmp_serde::from_slice(&change.key) on EVERY change even though the same key is decoded for every subscriber.
Ã—N proposal: cache decoded key per change (key decode result joins decode_cache value), and replace hex_encode with a SIMD-friendly lookup table (faster_hex crate or manual 256-entry LUT).
Estimated win: medium (key decode dedup is multiplicative across subscribers; hex LUT is incremental)
Risk: low
Attack hint: extend decode_cache value to hold `(Arc<Option<Value>>, Arc<Value> /*key*/)`.

## Target 10: Watermark check uses HashMap[&repo] lookup per change inside event loop
File:line: bridge.rs:248, 158
Current shape: `repo_idx[&repo]` is a HashMap probe per event (string key).
What's wasteful: StreamMap yields `String` repo; we hash + strcmp every event when there's typically 1â€“few repos.
Ã—N proposal: StreamMap key by usize (repo idx); skip the HashMap entirely.
Estimated win: small but free
Risk: low
Attack hint: insert into StreamMap with `usize` keys instead of repo Strings; keep repo->idx only for setup.
```

Key files: `D:/dev/rust/shamir-db/crates/shamir-server/src/subscriptions/{bridge,push,decode_cache,deliver_cache,target_match,payload}.rs`.

---

### 6. Server connection / wire

I have plenty. Producing the final report.

```
[Server connection / wire]

## Target 1: Drop msgpack decode â†’ BatchRequest in handler; route by tag, not full deserialize
File:line: crates/shamir-server/src/db_handler/handler.rs:220-221
Current shape: Every request decodes full `DbRequest` via `rmp_serde::from_slice` â€” including the entire `BatchRequest` tree (queries map, every filter/value/projection) â€” before even checking the op tag. Then `ShamirDb::execute` decodes again into engine ASTs.
What's wasteful: Two full passes over the msgpack tree (allocations per filter, per value, per alias key) for every Execute. Ping decodes the same envelope shape. Errors (bad version, no superuser, HMAC) still pay full decode cost.
Ã—N proposal: Peek the `op` tag and the `query_version` first via a lightweight `rmp` reader; for `Ping` / version-reject / HMAC-miss paths short-circuit before touching `batch`. For Execute, pass the inner `batch` msgpack slice straight to the engine and decode once inside the planner (or with `rmp_serde::from_read_ref` against an arena). Eliminates one full clone of the request.
Estimated win: multiplicative on small-batch / high-RPS workloads
Risk: medium
Attack hint: split DbRequest into header (op,version,db) + opaque `&[u8]` tail; lazy-decode batch

## Target 2: `BatchRequest.queries: TMap<String, QueryEntry>` â€” String alias allocation per query
File:line: crates/shamir-query-types/src/batch/batch_request.rs:85
Current shape: Alias keys are `String`. Every request allocates one heap `String` per alias on decode; the handler then `alias.clone()`s into error paths.
What's wasteful: For a 50-query batch that's 50 mallocs just for keys â€” purely to round-trip a wire identifier. Alias is short (<32 bytes typical) and lives only for the request.
Ã—N proposal: Use `SmartString`/`CompactString` or arena-allocated `&str` keys (`TMap<Cow<'req,str>, QueryEntry>`) backed by the request buffer. Or interned `u32` alias ids assigned at decode time â€” every cross-alias `$query` reference becomes an integer compare.
Estimated win: medium-to-large (allocation Ã— N queries)
Risk: medium (touches QueryEntry consumers â€” limited blast radius vs the rejected DTO Cow refactor since aliases are local-only)
Attack hint: CompactString drop-in for hot short-string keys

## Target 3: `distinct_repos` allocates `HashSet<String>` per TxExecute
File:line: crates/shamir-query-types/src/batch/query_entry.rs:73-78 and crates/shamir-server/src/db_handler/tx_handlers.rs:146
Current shape: Returns `std::collections::HashSet<String>` (RandomState SipHash) built by cloning each repo name from `TableRef`. Caller only iterates once and compares each to `it.repo()`.
What's wasteful: Full HashSet build (RandomState DoS hash on every byte) + N string clones â€” purely to do a linear "all equal to X?" check. Violates Fx-hash pillar and is O(N) clones for an O(N) compare.
Ã—N proposal: Replace with `pub fn all_repos_match(queries: &TMap<...>, expected: &str) -> Option<&str>` returning the first mismatch â€” zero allocations, zero hashing, early exit.
Estimated win: medium (per-tx-call constant overhead â†’ ~0)
Risk: low
Attack hint: fused single-pass predicate, no Set materialization

## Target 4: Two sequential O(N) walks over `batch.queries` for admin-gate + HMAC-gate
File:line: crates/shamir-server/src/db_handler/handler.rs:306-328 (and tx_handlers.rs:102-120)
Current shape: First `for (alias, entry) in &batch.queries { is_admin() }`, then `check_destructive_hmacs` does another full walk matching the same enum.
What's wasteful: Two cache passes over the same `TMap`; for non-superuser non-destructive workloads the second walk recomputes `match &entry.op` for every op only to `continue`.
Ã—N proposal: Fuse into one walk that returns `(needs_superuser_violation, first_destructive_hmac_check)`. Also: most batches contain zero admin/destructive ops â€” add a single-bit "has_admin_or_destructive" precomputed at decode/builder time so the walks are skipped entirely on the common path.
Estimated win: medium for tiny batches, multiplicative for many small batches/sec
Risk: low
Attack hint: bitset on BatchRequest, set during decode; skip both gates if zero

## Target 5: `check_destructive_hmacs` allocates `Vec<u8>` per destructive op via `join_null`
File:line: crates/shamir-query-types/src/hmac.rs:53-63 (called from db_handler/admin.rs:155-197)
Current shape: `canonical_*` builds a fresh `Vec<u8>` with capacity calc; HMAC reads it once and drops it. `verify_tag_hex` separately allocates `Vec<u8>` for the decoded candidate.
What's wasteful: 1 heap alloc per destructive op for the canonical input, plus 1 for hex_decode, plus a `String` cap clone of alias on error. Hot when batched DDL drops fly.
Ã—N proposal: Make HMAC streaming â€” pass `&mut Hmac` and feed segments with explicit `0u8` separators (`mac.update(prefix); mac.update(&[0]); mac.update(db.as_bytes()); ...`). Decode hex tag into a stack `[u8; 32]` (length is fixed at SHA-256 output). Zero allocations on the destructive-op fast path.
Estimated win: medium
Risk: low
Attack hint: stream-update HMAC, stack-buffer hex decode

## Target 6: `cap_lock: parking_lot::Mutex<()>` serialises every per-user-cap insert across the entire SessionStore
File:line: crates/shamir-connect/src/server/session.rs:263, 308-345
Current shape: `insert_with_per_user_cap` takes a process-global mutex and then does `for entry in self.by_sid.iter()` â€” full O(N) scan of all sessions to find ones owned by the inserting user.
What's wasteful: Auth-burst (many users logging in at once) serialises through one mutex; each insert also scans the entire DashMap (could be 10k sessions) to find the at-most-16 owned by this user. O(N) where O(1) is possible. parking_lot::Mutex on a hot auth path violates the ideology pillar.
Ã—N proposal: Add a secondary `scc::HashMap<[u8;16], scc::TreeIndex<u64, [u8;32]>>` indexing user_id â†’ (last_activity_ns â†’ sid). Insert is O(log k) (kâ‰¤16); LRU eviction is `pop_min()`. Drop the global cap_lock â€” per-user TreeIndex is the natural serialization boundary and is lock-free.
Estimated win: large under auth burst; multiplicative as session count grows
Risk: medium
Attack hint: scc::TreeIndex per user for ordered LRU; no global mutex

## Target 7: `gc_expired` and `snapshot_by_user`/`count_for_user` are full-store scans
File:line: crates/shamir-connect/src/server/session.rs:388-418
Current shape: GC walks every session and collects victims into a `Vec`; per-user queries linearly filter the entire DashMap.
What's wasteful: O(N_all_sessions) per GC tick and per admin kick. With the secondary user-index above and an `expiry_ts â†’ sid` TreeIndex, GC becomes "drain everything â‰¤ now" â€” O(victims), not O(total).
Ã—N proposal: Maintain `scc::TreeIndex<u64 /*expiry_at_ns*/, [u8;32]>` updated on `touch_at`; GC = `range(..now)`. user_id index already gives O(1) admin scoping.
Estimated win: large at scale (10k+ sessions); multiplicative as N grows
Risk: medium (extra index to keep in sync with touch_at)
Attack hint: indexed eviction, not scan

## Target 8: `Box<dyn Future>` per request (HandlerFuture) â€” heap alloc + virtual call on every dispatch
File:line: crates/shamir-connect/src/server/dispatch.rs:29-30, 39-44, used handler.rs:218-219
Current shape: `RequestHandler::handle` returns `Pin<Box<dyn Future + Send + 'a>>`. Every request boxes the handler future.
What's wasteful: One heap allocation + dynamic dispatch per request, just for trait-object erasure that the codebase doesn't actually need (single concrete impl `ShamirDbHandler`).
Ã—N proposal: Make dispatch generic over `H: RequestHandler` with an associated `type Future<'a>: Future`; let the compiler stamp out a concrete future type and inline `.await`s. The trait already has the single impl. (Workspace TAIT or capture-trait pattern.) Boxed future stays as an opt-in for plugin paths.
Estimated win: small-to-medium per request, but multiplicative at sustained RPS (allocator pressure + indirect branches)
Risk: low
Attack hint: GAT-typed future or `impl Future` via TAIT; remove Box on hot path

## Target 9: `rmp_serde::to_vec_named` for response â€” names-as-strings encoding doubles wire size & alloc
File:line: crates/shamir-server/src/db_handler/handler.rs:263
Current shape: Response encoded with field names embedded (`to_vec_named`), one fresh `Vec<u8>` allocated per response.
What's wasteful: Field-named msgpack is ~2Ã— the size of array-based encoding; the bytes are then handed to the transport which msgpack-frames it again. Per-response Vec alloc with no capacity hint.
Ã—N proposal: Switch to positional encoding (`rmp_serde::to_vec`) on the wire â€” clients pin to the schema by version; reuse a per-connection `BytesMut`/`SmallVec` scratch buffer (`thread_local!` or in `ConnectionServices`) so the response shares a single growing buffer across requests on the same conn.
Estimated win: large (bytes on wire + alloc count). For Ping/Pong / TxOpened / error responses the field-name overhead dominates payload.
Risk: medium (wire compatibility â€” gated on query_version bump)
Attack hint: positional msgpack + buffer reuse via ConnectionServices

## Target 10: subscribe_handler mutates response JSON via `serde_json::Value::Object` map.insert
File:line: crates/shamir-server/src/db_handler/subscribe_handler.rs:56-60
Current shape: After spawning the bridge, the handler reaches into the already-built response and mutates a `serde_json::Map` â€” RandomState hasher, dyn-typed Value, per-key heap String.
What's wasteful: Every subscribe pays a `serde_json::Value` mutation tax. Also keeps `serde_json::Value` on the hot path of the new wire (`QueryResult.value` is JSON Value rather than typed msgpack). Field name "sub" is constant â€” recomputed each call.
Ã—N proposal: Add a typed `sub_id: Option<u64>` to `QueryResult` and drop the JSON Object mutation step; the bridge writes the typed field directly. Removes serde_json from the subscribe hot path entirely.
Estimated win: medium per subscription activation; structurally cleans a JSON-Value leak in the response path
Risk: medium (wire field add, but additive)
Attack hint: typed field on QueryResult, no Value::Object editing
```

---

