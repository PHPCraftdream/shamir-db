בְּשֵׁם יהוה הָרַחֲמָן וְהַחַנּוּן

# Checkpoint — 2026-06-24 21:30 [phase-d-bug-won-uncommitted]

## Session summary

Продолжение Phase D кампании после `/resume 2026-06-24-1955`. Цель сессии
(`/goal "давай победим оставшийся баг"`) — **достигнута**: побеждён баг #236
(D.2/D.3 CASCADE/SET NULL/drop-guard не срабатывали через сервер).

**Корень бага (вскрыт, не предполагался):** НЕ в `plan_cascade` (как гласила
прошлая гипотеза), а в **писателе каталога** — `admin_schema.rs`
`insert_constraint_fields` сериализовал у FK только `ref_table`/`ref_field` и
**терял `on_delete`** → при чтении дефолт `NoAction` → вся reverse-FK discovery
(RESTRICT/CASCADE/SET NULL) молча отключалась. Вторичный баг: drop-guard читал
in-memory validator-binding кэш, **некогерентный** между admin-`DbInstance` и
engine-инстансом execute-пути → пустой; перевёл на чтение персистентного
каталога (`system_store.load_table_record` + `SCHEMA_FIELD`).

**Метод находки (быстрый):** написал in-process регрессионный тест через
**реальный `db.execute`** (тот же путь, что сервер) —
`crates/shamir-db/tests/declarative_schema_fk_ondelete_e2e.rs`, ~2 c/прогон.
Контрольный RESTRICT-тест воспроизвёл провал → инструментировал
`discover_restrict_refs` (eprintln, потом убрал) → увидел `on_delete=NoAction`
хотя задавал `Restrict` → дошёл до писателя. Также добавил
`FieldBuilder::foreign_key_on_delete()` (Rust-билдер хардкодил `Restrict`).

**Верификация (всё зелёное):** Rust in-process **5/5** (CASCADE tx+autocommit,
SET NULL, RESTRICT delete-guard, drop-guard) · TS e2e через **свежесобранный
сервер** (release build 19m, в `D:\dev\rust\.cargo-target\release` — профиль
ставит `CARGO_TARGET_DIR`) **5/5** · гейт `fmt --check` + `clippy --all-targets
-D warnings` + lib **1810/1810**. Снял `describe.skip` с 3 TS-describe (D.2
CASCADE, D.2 SET NULL, D.3 drop-guard).

**Доки:** создал `docs/research/DONE.md` (журнал выполненного, маппинг на
ACTION-ITEMS + SHA). Затем по просьбе пользователя **выпилил выполненное** из
плановых доков: ACTION-ITEMS.md (убраны A1/B1/B3/D1/E6, A3 урезан до остатка,
карта приоритетов переписана), completeness-oql (§2.7+H3 keyset), completeness-
ddl (G3/G7/fail-open/вывод), coverage-rust-query-builder (#26/#27/#36/#37 +
summary + recommendations), REVIEW.md (статус-баннер). META-REVIEW уже имел
ретракцию A1. TS-coverage доки гапов по нашей работе не имели.

**ВАЖНО — НИЧЕГО НЕ ЗАКОММИЧЕНО.** Вся работа сессии в рабочем дереве (9 M +
2 untracked: новый тест + DONE.md). Жду явной просьбы на коммит. babysit-крон
самоудалён (TaskList пуст). `/goal` активен (условие выполнено фактически).

## Active goal
`давай победим оставшийся баг` — фактически достигнут (баг #236 побеждён +
верифицирован end-to-end). Hook ещё может считаться активным.

## TaskList
Пусто (все задачи completed; #236 закрыт в этой сессии). babysit-крон удалён.
### recently completed
- #236 Phase D.2/D.3: CASCADE+SET NULL+drop-guard+e2e — КОРЕНЬ найден и
  исправлен, 5/5 in-process + 5/5 TS e2e зелёные.
- #229–235, #237–240 — Phase D.0/D.1, keyset, B1/B3, блок1a/b (ранее).

## Decisions
- Корень #236 — писатель каталога терял `on_delete` (НЕ plan_cascade, как
  гласила прошлая гипотеза). Прошлый чекпоинт был оптимистичен про «D.1 RESTRICT
  e2e green» — на деле RESTRICT тоже был сломан этим же багом.
- Drop-guard чинён через персистентный каталог (а не через попытку сделать
  in-memory binding-кэш когерентным — это глубже storage-архитектуры, рискованно).
- Диагностика через in-process тест (`db.execute`) вместо 13-мин пересборки
  сервера — окупилось (2 c/прогон).
- DONE.md как «обратная сторона» ACTION-ITEMS; из плановых доков выполненное
  убрано, ревью-доки (REVIEW/META-REVIEW) помечены баннером, не переписаны.

## Open questions
- **Коммит #236** — жду явной просьбы. Предложенное разбиение:
  (1) `fix(engine,db): Phase D — persist FK on_delete + catalogue-based drop-guard (#236)` + новый тест;
  (2) `feat(query-builder): foreign_key_on_delete для Rust-паритета`;
  (3) `test(client-ts): un-skip Phase D.2/D.3 e2e (#236 fixed)`;
  (4) `docs(research): DONE.md + prune выполненного из плановых доков`.
- `.recovery-backup/` (untracked, мусор с прошлой аварии) — прибрать?
- Остаток Phase D: `ON UPDATE` actions, `DropFunction`-guard. Следующий P0 — A2
  (открытые access-дефолты `0o777`).

## Repo state
```
 M crates/shamir-client-ts/src/__tests__/e2e-fk-ondelete.test.ts
 M crates/shamir-db/src/shamir_db/execute/admin_schema.rs
 M crates/shamir-db/src/shamir_db/execute/admin_table_index.rs
 M crates/shamir-query-builder/src/ddl/schema.rs
 M docs/research/ACTION-ITEMS.md
 M docs/research/REVIEW.md
 M docs/research/completeness-ddl.md
 M docs/research/completeness-oql.md
 M docs/research/coverage-rust-query-builder.md
?? .recovery-backup/
?? crates/shamir-db/tests/declarative_schema_fk_ondelete_e2e.rs
?? docs/checkpoints/2026-06-24-1955.md
?? docs/research/DONE.md
```
```
909b650 test(client-ts): Phase D e2e — D.1 RESTRICT green; D.2/D.3 skipped (e2e gap)
dc4b3a3 feat(engine,db): Phase D.2/D.3 — CASCADE, SET NULL, drop-guard
7b065e2 fix(client-ts): correct createIndex args in e2e-fk-ondelete
9cf10e2 docs(prompts): Phase D.2/D.3 CASCADE/SET NULL/drop-guard brief (git-ban)
cf11378 feat(engine): Phase D.1 — FK ON DELETE RESTRICT gate
```
