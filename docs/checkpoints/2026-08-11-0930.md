# Checkpoint — 2026-08-11 09:30

## Session summary

Governed by a standing session-scoped `/babygoal` Stop-hook (Russian text: "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу") that re-fires after nearly every turn until its condition is verifiably met via TaskList/git state. This session picked up from a prior compaction already deep into a P0/P1 release-blocker sweep sourced from an independent code review (`docs/dev-artifacts/research/2026-08-09-new-wave-review-claude.md`).

Completed this segment, each via the established pattern (write a grounded brief citing exact file:line evidence → commit the brief → dispatch to a delegate → independently zero-trust-verify the diff by reading it directly, re-running fmt/clippy/the full test gate myself, and — for concurrency/crash-safety fixes — doing a mandatory revert-and-check to confirm the new test actually goes red without the fix): **#1064** (P0 SECURITY, GetDdlOpStatus ACL), **#1065** (P0 DDL status crash-safe write order — took 4 rounds, two of which self-reported false success that direct verification caught: round 1 claimed a write-order fix that wasn't real, round 2 left a compile break across 9+2 call sites plus 3 empty test stubs), **#1066** (P0 index2 RENAME non-atomicity, durable tombstone + recovery), **#1067** (P1 DDL op-log family misclassification for sorted/index2, plus a NEW gap the delegate's own tests caught: index2 DROP's inline path never wrote a terminal status at all), **#1068** (P1 DDL op-log unbounded growth — real fix hinges on the discovery that `RecordId`'s raw bytes are already big-endian-timestamp-prefixed, so the existing key scheme is already chronologically sortable via `scan_prefix_stream` with no schema change needed), **#1069** (P1 OQL SELECT * silently dropping columns + colliding output keys — done single-context; Defect 3, the null-collapse semantics, was explicitly scoped OUT as a much larger cross-cutting evaluator change and documented as a deliberate alpha-scope decision rather than attempted), **#1070** (HNSW recall floor/doc mismatch — split the conflated fidelity+quality test, then with EXPLICIT user approval pushed all 103 previously-unpushed local commits to `origin/master` and dispatched a real GitHub Actions workflow-dispatch matrix across ubuntu/macos/windows to collect actual cross-platform recall data before recalibrating floors and docs, rather than reacting to another single CI observation), **#1072** (CRITICAL doctor blindness to wire-created tables — the delegate's investigation found the ORIGINAL root-cause theory in the task description was stale: `ShamirDb::init` already restores tables from the system store's persisted catalogue, so the "doctor sees zero tables" scenario doesn't reproduce on current code; the TablesRegistry replay was kept anyway as defense-in-depth, and the GENUINELY real defects — filter-no-match returning Ok, `process::exit(1)` skipping destructors, tautological tests — were fixed and verified), and **#1073** (MEDIUM CursorRegistry shard-lock race — fixed directly single-context; a pre-existing flaky stress test already in the repo, previously observed flaky under full-workspace load per an earlier round's report, was confirmed via revert-and-check to be the exact discriminator: 2/15 runs failed on reverted code at the "stuck above zero" assertion, 10/10 clean with the fix restored).

**Deleted #1071** (manual perf-gate benchmark run + release evidence report) per explicit user instruction ("удали все задачи с прогонами бенчей, решай другие") after surfacing that it was a multi-hour QA artifact fundamentally different in kind from the code-fix tasks, tied to an actual release-tag SHA.

**Currently in flight: #1074** (MEDIUM — intra-tx unique-guard false `UniqueViolation` when a unique key is released via UPDATE/DELETE and reclaimed by a different record within the SAME transaction; a real regression against #1039, already completed earlier this session). Brief committed at `docs/dev-artifacts/prompts/release-blockers/77-p1074-intra-tx-unique-guard-false-violation.md`, dispatched via `crush run --session p1074-intra-tx-unique-guard` (background task id `b3ucmghr5`), still running as of this checkpoint. The brief lays out two candidate fixes (rebuild `seen` from `tx.index_write_set`'s ordered `SetPosting`/`RemovePosting` — recommended as more principled — vs. threading release markers alongside `tx.unique_guards`) and leaves the final choice to the delegate with a requirement to justify it.

**Delegation channel history this segment**: started via `@sh` (Agent tool), which mid-run failed once with an org-policy error ("Your organization has disabled Claude subscription access") — the user clarified this was transient ("@sh всегда доступен") and NOT to be treated as a standing blocker. Per explicit user instruction ("почле завершения агента переходим на /crush"), switched back to `/crush` after that agent's task (#1068) completed, and `/crush` has been the delegation channel for #1072 onward. `/crush` itself hit its own real blocker earlier in the session (weekly quota exhausted, reset 2026-08-13) which is why `@sh` was used for a stretch in the middle — both channels are now understood to have occasional but non-fatal failure modes; the working pattern is to route around a failure with the OTHER channel rather than stall.

**Commit-message hygiene note for future rounds**: two commits this segment (`#1072`, `#1073`) had their `-m` messages mangled by bash interpreting backtick-quoted code identifiers (e.g. `` `slot` ``) as command substitution when passed via double-quoted `-m "..."`. Both were caught immediately (checked `git log -1 --format="%B"` after committing) and fixed via `git commit --amend -q -F <tmpfile>` before anything was pushed. Going forward: write commit messages containing backticks to a temp file first (`git commit -F <file>`), never pass them through `-m "..."` with double quotes.

Zero uncommitted risky state: the working tree currently has `#1074`'s in-progress changes (`crates/shamir-engine/src/tx/pre_commit.rs`, `crates/shamir-engine/src/tx/tests/base_index_tx_tests.rs`) plus some `/crush` run-log droppings (`clippy.log`, `run_intra.log`, `run_rev1.log`, `run_rev2.log`, `run_revert.log` — untracked, likely safe to `rm` once #1074 is verified and committed, but NOT touched now per the standing rule against unrequested cleanup) and 4 pre-existing untracked checkpoint/research files from before this segment.

## Active goal

Standing `/babygoal` Stop-hook (verbatim): "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу"

## TaskList

### in_progress
- #1074 MEDIUM: #1039 unique-guard даёт ложный UniqueViolation при освобождении ключа внутри той же tx (crush session `p1074-intra-tx-unique-guard`, background task `b3ucmghr5`, still running)

### pending
- #982 crush-fallback state (persistent sentinel — do not complete)
- #1049 P1-6 follow-up: identify actual stalling operation using new diagnostic logging, then fix (non-blocking, waits on a real recurrence)
- #1075 MEDIUM: typed CreateIndex builder silently drops options (.unique(), .include(), .vector_dim() etc.)
- #1076 MEDIUM: CLAUDE.md's "exactly one sanctioned exception" for std::sync::Mutex no longer matches the code
- #1077 LOW: PerIpLimiter::try_acquire holds shard write-lock for the whole CAS cycle contrary to its own doc (blockedBy: #1073 — now unblocked, #1073 completed)
- #1078 LOW: DropIndexOp::unique is dead for resolution but still changes the HMAC signature
- #1079 PERF: sorted range-scan BTreeSet per-element + lease_by_field_and_kind O(N) + audit-checkpoint without spawn_blocking (3 independent code-fix points; bench-measurement requirement to be dropped per user's "no bench runs" instruction, code fixes kept)
- #1080 nit: QueryResult { ..Default::default() } x133 sites disables compiler new-field checking
- #1081 ReaderDrainGate: perf-hardening (3xSeqCst, unbounded spin, gate on whole family) + incorrect livelock-freedom proof
- #1082 CachedStore::flush — unbounded busy-poll in two places, background-write error silently lost
- #1083 Batch::try_build does a full msgpack round-trip per op + 4 expect() in a public non-test path
- #1085 Watchdog at the request/batch boundary + cancellation-safety audit before a real timeout
- #1086 Docs: sizing guide numbers for cursor rescans, FTS/functional rebuild time, vector quality status (item 3 now directly answerable from #1070's real matrix data; items 1-2 permit extrapolation from already-known numbers, no new bench runs needed per user's instruction)

### recently completed (this segment)
- #1073 MEDIUM: CursorRegistry shard-lock race (cap overrun / permanent lock-out)
- #1072 CRITICAL: doctor wire-table blindness, exit-0 fail-open, tautological tests
- #1070 HNSW recall floor/doc mismatch — real cross-platform matrix + recalibration
- #1069 P1 OQL SELECT * data loss + colliding output keys
- #1068 P1 DDL op-log unbounded growth — real FIFO retention
- #1067 P1 DDL op-log family misclassification + index2 DROP terminal-status gap
- #1066 P0 index2 RENAME non-atomicity
- #1065 P0 DDL status crash-safe write order (4 rounds)
- #1064 P0 SECURITY GetDdlOpStatus ACL

### deleted
- #1071 (release-evidence perf gate — explicit user instruction, multi-hour bench-run task deleted rather than deferred)

## Decisions

- **#1070's CI-matrix push**: explicitly asked and got user approval to push all 103 then-unpushed local commits to `origin/master` (not just a throwaway branch) specifically to enable a real `workflow_dispatch` GitHub Actions run — a genuinely shared-state-affecting action, confirmed rather than assumed.
- **#1071 deleted, not deferred**: user's explicit instruction was to delete bench-running tasks outright and continue with the rest, not to reorder them to the end.
- **#1079 scoped down**: its 3 code-fix points are being kept, but the task's own "measure before/after via bench_scale_tool::Harness" requirement is being dropped per the same "no bench runs" instruction — code correctness/perf fixes proceed, verified via the normal test gate, not fresh benchmark numbers.
- **#1074's fix direction left to the delegate**: rather than prescribing a single design (which would have required deeper investigation of `tx.index_write_set`'s exact construction than time allowed mid-session), the brief presents two candidate approaches with an explicit recommendation and requires the delegate to justify whichever they pick.
- **Delegation channel is adaptive, not fixed**: both `/crush` (quota) and `@sh` (org policy, once) have shown transient failures this session; the working rule is to route around a failed channel with the other one when the user permits, not to treat either failure as a permanent block.

## Open questions

None outstanding — the immediate next step is unambiguous: wait for `/crush` background task `b3ucmghr5` (#1074) to complete, then apply the same zero-trust verification (read the diff, re-run fmt/clippy/the full `shamir-engine`+`shamir-tx` gate, confirm the revert-and-check outcome) before committing, then continue down the TaskList in id order (#1075 next).

## Repo state

```
 M crates/shamir-engine/src/tx/pre_commit.rs
 M crates/shamir-engine/src/tx/tests/base_index_tx_tests.rs
?? clippy.log
?? devrust.cargo-target-check/
?? docs/checkpoints/2026-08-09-0945.md
?? docs/checkpoints/2026-08-09-1911.md
?? docs/checkpoints/2026-08-10-0842.md
?? docs/dev-artifacts/research/2026-08-09-new-wave-review-claude.md
?? run_intra.log
?? run_rev1.log
?? run_rev2.log
?? run_revert.log
```

```
b1485f6a docs(prompts): brief for #1074 -- intra-tx unique-guard false UniqueViolation on release-then-reclaim within one transaction
d423b278 fix(server): #1073 -- CursorRegistry::register held the shard write-lock only through the entry() call, not the CAS loop, racing free_session_slot
d707153e fix(server): #1072 -- doctor filter-no-match and unhealthy-table now exit non-zero; process::exit removed; tests actually discriminate
a2b323fb docs(prompts): brief for #1072 -- doctor CRITICAL wire-table blindness, exit-0 fail-open, tautological tests
c4ea8d22 test(index),docs(search): #1070 -- recalibrate HNSW recall floors from real cross-platform data, sync docs to match
```
