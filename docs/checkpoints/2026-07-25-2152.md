# Checkpoint — 2026-07-25 21:52

## Session summary

This session picked up from an earlier `@fh` release-readiness review of shamir-db at HEAD `4d5436a0` (the tip after the prior session's Wave D/E campaign). The review found the new wave's direction sound but flagged several release blockers and lesser correctness issues for `0.1.0-alpha.1`: cursor keyset mixed-type/NaN row loss, inconsistent Int↔F64 numeric comparison across fast paths, bootstrap credential rotation fail-open, non-atomic schema DDL, an unsafe online-migration API, a `count_total` bug, a `with_version` silent-loss bug, a `u64` serialization wrapping bug, a cursor-reaper race, and silently-dropped corrupt records. I decomposed this into a 15-task "Wave F" (`#791`–`#805`) with explicit `blockedBy` dependencies (F-2 blocks F-1; F-1–F-12 block F-14 CHANGELOG; F-14 blocks F-15 final gate), armed a 15-minute `/babysit` cron, and worked through them via the standing prompt-first discipline: write a fully-investigated brief to `docs/dev-artifacts/prompts/post-alpha/`, commit it, delegate implementation, then personally verify (read full diff, re-run `fmt`/`clippy`/`./scripts/test.sh`) before committing the code+tests.

Delegation channel shifted mid-session per explicit user direction: started on `@sh` (Agent tool), the user then said to switch to `/crush` and not use other agents, which held through F-3 (bootstrap rotation) → F-8 (u64 serializer) — including navigating a `crush` provider (`zai`) peak-hours refusal (08:00–12:00) that I correctly did NOT bypass unilaterally, instead waiting it out per explicit user confirmation once the window cleared, and a separate `crush --timeout` expiry on F-4 that was resolved by relaunching into the same `--session` with a larger timeout (per the crush skill's own transient-failure guidance). A `/goal` Stop-hook (text: "реализуй задачи с помощью @sh, между задачами делай коммиты, покрой их тестами") was active for much of this session and repeatedly re-fired demanding `@sh` even while the user's more recent explicit instruction was "use `/crush` only" — I held to the user's live instruction over the stale automated condition each time, explaining the conflict rather than silently picking a side. The user then explicitly said to switch back to `@sh` "after the current task finishes," which I honored (F-9, F-10 ran via `@sh`).

F-1 through F-9 are fully complete: committed, verified, tests green. **F-10 (#800, cursor #800) is IN PROGRESS, NOT YET COMMITTED** — its `@sh` agent hit Claude's weekly usage quota mid-task (270 tool calls in, resets 4:00 Europe/Berlin) and returned only a quota-error message, no final report. I independently verified the state it left behind is NOT a half-finished mess: `cargo check` across every affected crate (`shamir-engine`, `shamir-query-types`, `shamir-db`, `shamir-query-builder`, `shamir-server`) compiles clean, `cargo fmt --check` is clean, and `cargo clippy` on the two primary-scope crates (`shamir-engine`, `shamir-query-types`) is clean with zero warnings. I read the full `read_exec.rs` diff (all ~14 `Err(_) => continue` sites) and the new `QueryResult::corrupt_records`/`CorruptRecordRef` type in `shamir-query-types` — the work looks complete and high-quality, including a subtle correctness detail (resetting the `corrupt` accumulator on each ANN-search retry-loop iteration to avoid double-reporting) and an honest, explicitly-documented exception (`try_project_page_only_bytes`, a free function with no `QueryResult` to attach to, left out of scope with a code comment explaining why — its two callers, `read_index_scan.rs`/`read_temporal.rs`, were correctly identified as out-of-scope per the brief). A new test file `crates/shamir-engine/src/table/tests/corrupt_record_tests.rs` (245 lines, untracked) has 5 tests covering `read_streaming`, `read_collecting` (plain + aggregate/GROUP BY branches), the no-corruption regression guard (including a real wire msgpack round-trip proving the field is omitted when empty), and a filtered-scan case.

**What's NOT yet done for F-10**: I have NOT yet run `./scripts/test.sh` (the actual test suite — only `cargo check`/`clippy`/`fmt` so far), have NOT committed the code, and have NOT updated the TaskList to `completed`. A broader `cargo clippy -p shamir-db -p shamir-query-builder -p shamir-server` call (checking the sibling crates that only got mechanical `corrupt_records: Vec::new()` literal additions from the new struct field) was interrupted/rejected by the user mid-tool-call — unclear if this was a deliberate "don't bother, scope is fine" signal or an accidental interrupt; this should be either resumed or explicitly asked about, not silently skipped, before considering F-10 done. The very next steps are: (1) decide/ask about the interrupted broader clippy check, (2) run the full test suite for the brief's stated scope (`shamir-engine`, `shamir-query-types`) via `./scripts/test.sh`, (3) commit the verified diff with a proper message, (4) mark `#800` completed and move to `#801` (F-11, memory budgets in deploy profiles) via `@sh` per the user's last explicit routing instruction.

The `babysit` cron (`1c88fc6a`, every `7,22,37,52 * * * *`) is still armed and has been ticking throughout, correctly reporting "still running #800" on stalls with no new commit signal (it does not yet know to distinguish "agent hit quota, needs a decision" from "still working" — this is a known blind spot of the tick script, not something to fix here). Two other crush sessions from earlier in this campaign (`f3-bootstrap-rotation` through `f8-serialize-u64-big`) have already been fully consumed and are no longer relevant.

## Active goal

A `/goal` Stop-hook was active for much of this session with condition text: **"реализуй задачи с помощью @sh, между задачами делай коммиты, покрой их тестами"** (implement tasks using @sh, commit between tasks, cover them with tests). This conflicted repeatedly with the user's live routing instructions (switch to crush; don't use other agents; switch back to @sh later) — I prioritized the live instructions each time. Current status of this hook at checkpoint time is unclear (not explicitly cleared by the user, may still be armed) — if it re-fires demanding `@sh` while the plan calls for something else, defer to whatever the user's most recent explicit instruction in the NEW session is, not this stale text.

## TaskList

### in_progress
- #800 F-10: corrupt record silently dropped in read paths instead of explicit error (blockedBy: none) — code written, compiles clean, clippy clean on primary scope, NOT yet test-verified via `./scripts/test.sh`, NOT committed.

### pending
- #801 F-11: конечные memory budgets в deploy profiles (blockedBy: none)
- #802 F-12: backup/restore — fsync перед rename + crash-тест (blockedBy: none)
- #803 F-13: query-builder parity (Rust/TS) — having без group_by, page=0, build() Result (blockedBy: none)
- #804 F-14: CHANGELOG — реальная секция [0.1.0-alpha.1] (blockedBy: #800, #801, #802 — NOT #803, which is lower-priority/non-blocking per the original design)
- #805 F-15: финальный release gate — fmt/clippy/test/e2e/bench regression (blockedBy: #804)

### recently completed
- #799 F-9: cursor reaper skips cursors with an in-flight fetch (commit `28d70e9b`)
- #798 F-8: QueryValueSerializer::serialize_u64 promotes to Big above i64::MAX (commit `ce42157a`)
- #797 F-7: with_version rejects collapse paths, threads RecordId through plain ORDER BY (commit `594da439`)
- #796 F-6: count_total=true excludes the top-K LIMIT fast path (commit `0b81913d`)
- #795 F-5: online storage migration API gated as experimental (commit `f2ffa1b1`)
- #794 F-4: schema DDL validate/precompile before persist, rollback on activation failure (commit `76b487b8`)
- #793 F-3: bootstrap credential rotation fail-open ordering (commit `29f80622`)
- #792 F-1: keyset cursor schema-typed-scalar gate (commit `220c8b10`)
- #791 F-2: unified Int↔F64 numeric comparator across all fast paths (commit `5a7b65b9`)

## Decisions

- **Held to the user's live delegation-channel instructions over the stale `/goal` Stop-hook condition every time they conflicted** — explained the conflict in plain text rather than silently obeying either side; never launched `@sh` while the user's most recent explicit word was "crush only," and switched back to `@sh` only once the user said to.
- **Did not bypass `crush`'s peak-hours refusal** (`zai`, 08:00–12:00) — waited for the user's explicit "peak hours just ended, use crush now" signal rather than adding `--allow-peak-hours` unilaterally.
- **On a `crush --timeout` expiry (F-4)**, relaunched into the SAME `--session` id with a larger timeout rather than falling back to a different agent — matches the crush skill's own guidance that a self-inflicted timeout is a transient/recoverable failure, not a hard quota block.
- **On F-10's `@sh` agent hitting its weekly quota mid-task**, did NOT discard the large partial diff it left behind — independently verified via `cargo check`/`fmt`/`clippy` that the state is coherent and complete-looking before deciding whether to keep, continue, or redo it. Chose to keep verifying/committing it rather than re-running the whole task from scratch, since the compile-clean, clippy-clean state strongly suggests the agent actually finished before the quota message cut off its final report.
- **F-10 scoped to `read_exec.rs` only** (per its own brief, written earlier this session) — `table_manager_index_mgmt.rs` and `table_manager_streaming.rs` share the same silent-skip pattern but were deliberately deferred to a documented follow-up rather than expanding this task's blast radius.

## Open questions

- **Was the interrupted `cargo clippy -p shamir-db -p shamir-query-builder -p shamir-server --all-targets -- -D warnings` call a deliberate "skip it, out of scope" signal from the user, or an accidental interrupt?** Needs to be resolved (ask, or just re-run it since it's a cheap, safe, read-only check) before treating F-10 as fully gated — the brief's own verification section only names `shamir-engine`/`shamir-query-types`, so this broader check may simply have been the orchestrator's own extra caution, not something the brief requires; leaning toward "safe to skip per the brief's stated scope, but worth a quick confirming run since `cargo check` already proved these crates compile."
- **Is the `/goal` Stop-hook from earlier in the session still armed?** Not explicitly cleared. If it fires again in a new session/after resume, treat the user's fresh, in-session instructions as authoritative, not this checkpoint's summary of the old conflict.

## Repo state

```
 M crates/shamir-db/src/shamir_db/execute/function_invoker.rs
 M crates/shamir-db/src/shamir_db/execute/helpers.rs
 M crates/shamir-db/src/shamir_db/execute/tests/filter_value_tests.rs
 M crates/shamir-engine/benches/filter_eval.rs
 M crates/shamir-engine/src/query/batch/query_runner.rs
 M crates/shamir-engine/src/query/batch/tests/executor_tests/query_runner_tests.rs
 M crates/shamir-engine/src/query/batch/tests/query_value_serializer_tests.rs
 M crates/shamir-engine/src/query/filter/tests/eval_tests/collection_tests.rs
 M crates/shamir-engine/src/query/filter/tests/eval_tests/cond_gap_tests.rs
 M crates/shamir-engine/src/query/filter/tests/eval_tests/in_query_ref_column_tests.rs
 M crates/shamir-engine/src/query/filter/tests/eval_tests/query_ref_tests.rs
 M crates/shamir-engine/src/query/filter/tests/query_ref_cache_tests.rs
 M crates/shamir-engine/src/query/read/mod.rs
 M crates/shamir-engine/src/table/read_exec.rs
 M crates/shamir-engine/src/table/read_index_scan.rs
 M crates/shamir-engine/src/table/read_temporal.rs
 M crates/shamir-engine/src/table/tests/filter_stream_tests.rs
 M crates/shamir-engine/src/table/tests/mod.rs
 M crates/shamir-engine/src/table/tests/recordview_cutover_parity_tests.rs
 M crates/shamir-engine/src/table/tests/s3_bytes_path_parity_tests.rs
 M crates/shamir-query-builder/src/response/tests/response_tests.rs
 M crates/shamir-query-types/src/read/mod.rs
 M crates/shamir-query-types/src/read/query_result.rs
 M crates/shamir-query-types/src/wire/tests/db_message_tests.rs
 M crates/shamir-server/src/db_handler/tests/subscribe_handler_tests.rs
 M docs/guide-docs/KNOWN_LIMITATIONS.md
?? crates/shamir-engine/src/table/tests/corrupt_record_tests.rs
```

```
7c862f29 docs(prompts): brief for #800 -- F-10 corrupt record reporting (read_exec.rs scope)
28d70e9b fix(server): F-9 -- cursor reaper skips cursors with an in-flight fetch (#799)
13062199 docs(prompts): brief for #799 -- F-9 cursor reaper in-flight guard
ce42157a fix(engine): F-8 -- QueryValueSerializer::serialize_u64 promotes to Big above i64::MAX (#798)
63df5dbe docs(prompts): brief for #798 -- F-8 serialize_u64 Big promotion fix
```
