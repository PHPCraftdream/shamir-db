# Checkpoint — 2026-08-12 00:10

## Session summary

Continuing the standing `/babygoal` Stop-hook session: "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу". This checkpoint follows `2026-08-11-2130.md`, written mid-flight while `#1096`'s `/crush` dispatch had produced file changes not yet reviewed.

**`#1096` is now fully complete, verified, and committed.** The `/crush` run (session `p1096-tx-aware-unique-check`) eventually timed out/failed after producing a partially-correct diff — it correctly implemented the core design from the brief (`released_unique_keys_in_tx`, `validate_unique_for_create_with_released`, the `insert_tx`/`insert_tx_many` call sites) but left several mechanical defects: a `#[cfg(test)]` on a function called from non-test code (a hard compile error outside test builds), a `pub use` re-export of a `pub(crate)` item (E0364), two calls to non-existent `Value` API methods (`map_ref`/`str_ref`) in the new test file, and a blind "silence the unused-variable warning" pass that renamed `email_id` to `_email_id` in two functions where the variable WAS still used later (breaking compilation). All were found via direct `cargo check`/`cargo check --tests` runs, not by trusting the run's self-report, and fixed by hand.

**A more serious, genuinely new finding surfaced only once the fixed code actually ran**: `./scripts/test.sh -p shamir-engine -- p1096` showed the two core reproduction scenarios (DELETE-then-reclaim, UPDATE-off-then-reclaim) now staged successfully but failed at `commit_tx` with `UniqueViolation` — the brief's own design (informed by my own investigation) had only addressed the STAGE-TIME durable-only blind spot in `insert_tx`, but `pre_commit.rs`'s Step 2 cross-tx durable check has the IDENTICAL durable-only assumption at COMMIT time: `info_store.get(index_key)` still returns the released record's posting, because Phase 5c's materialize write (which would apply the `RemovePosting`) hasn't run yet at validation time. Fixed by tracking `ever_released` (every key that had at least one `RemovePosting` during Step 1's walk, a superset of the final-state `released`) and tolerating a durable mismatch for keys in that set in Step 2's check — safe because the key bytes deterministically encode the indexed value, so a `RemovePosting` for that exact key in this tx can only have been planned against whoever currently holds it durably. A second test bug was also found and fixed: `tx_genuine_double_claim_still_rejects` incorrectly expected a stage-time rejection for an intra-tx double-claim with no release between — the actual (and correct) architecture defers that rejection to `pre_commit.rs`'s Step 1 walk at commit time; rewrote the test to assert `commit_tx` returns `CommitError::UniqueViolation`, matching the pre-existing #1039 coverage pattern in `base_index_tx_tests.rs`.

**Full gate re-verified personally after the fix**: `cargo fmt --all -- --check` clean, `cargo clippy --workspace --all-targets -- -D warnings` clean (3m20s, zero warnings), `shamir-index --full` 629/629, `shamir-engine --full` 1993/1993, `shamir-tx --full` 385/385, `shamir-db --full` 719/720 (the 1 failure was a `wasm_call_shares_batch_context` crates.io network timeout, confirmed unrelated by an isolated rerun that passed 1/1). Committed in two commits: `1600ea0f` (fix + tests) and `d65c2780` (CHANGELOG). `#1096` marked `completed` in the TaskList.

**This closes out the session's second extended wave** (`#1090`/`#1091`/`#1093` plus three `@oh`-found fixes, then `#1096`). Remaining pending tasks (`#982` sentinel, `#1049` non-blocking, `#1092`/`#1094`/`#1095` each needing dedicated new-infrastructure design) are unchanged from the prior checkpoint's assessment — each requires a fresh investigation-then-implementation cycle, not a continuation of current context. Next step per the standing hook's closing sequence: commit this checkpoint plus the still-uncommitted `2026-08-11-2130.md`, then launch an `@oh` review pass specifically over `#1096`'s final committed diff (the `/crush`-plus-manual-fix combination has not yet had adversarial review, unlike the prior wave's three items).

## Active goal

Standing `/babygoal` Stop-hook (verbatim, unchanged across the whole session): "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу"

## TaskList

### pending
- #982 crush-fallback state (persistent sentinel — do not complete, structurally unsatisfiable)
- #1049 P1-6 follow-up (non-blocking, waits on a real recurrence)
- #1092 PERF: ReaderDrainGate architectural hardening — needs a loom model test before touching proven-correct ordering
- #1094 Watchdog for stuck batch ops — needs new OS-thread-based infrastructure design
- #1095 PERF: SegmentSet::inner off std::sync::Mutex — needs bench-level contention-isolation instrumentation first

### recently completed
- #1096 tx-aware unique check for release-then-reclaim of a durable value — fully implemented (via /crush + manual fixes), tested, gate-verified, committed
- #1093 Batch::try_build typed visitor — fully implemented, tested, verified
- #1091 lease_by_field_and_kind investigation — documented blocker, no code change
- #1090 post_auth_bucket lock-free migration (partial: SegmentSet half deferred to #1095)

## Decisions

- **Took over `#1096`'s implementation by hand after `/crush`'s dispatch timed out with a partially-correct diff**, rather than re-dispatching or reverting — the core design was already correct and verified against the brief; the remaining defects were mechanical (visibility, wrong API calls, a bad global rename) and fixable faster directly than through another delegation round.
- **Fixed `pre_commit.rs`'s Step 2, beyond the brief's original scope**, once test failures proved the brief's stage-time-only fix was incomplete — the commit-time durable check had the identical blind spot. This was NOT something the brief anticipated; found empirically by refusing to trust "tests pass" without actually running them.
- **Rewrote `tx_genuine_double_claim_still_rejects` to assert commit-time rejection instead of stage-time** — the original test (written by `/crush` per the brief's requirement #2) encoded an incorrect assumption about when intra-tx double-claims are caught; verified the correct behavior against the pre-existing `#1039` test in `base_index_tx_tests.rs` before rewriting.

## Open questions

None blocking. Next step is unambiguous: commit this checkpoint + `2026-08-11-2130.md`, then run an `@oh` adversarial review over `#1096`'s final diff (commits `1600ea0f`/`d65c2780`) before considering the standing hook's closing sequence complete.

## Repo state

```
?? devrust.cargo-target-check/
?? docs/checkpoints/2026-08-11-2130.md
```

```
d65c2780 docs: CHANGELOG entry for #1096's fix
1600ea0f fix(engine): #1096 -- tx-aware unique check for release-then-reclaim of a durable value
dbe1f197 docs(prompts): brief for #1096 -- tx-aware stage-time unique check for release-then-reclaim of durable values
93b1df71 docs: checkpoint after second extended wave (#1090/#1091/#1093 + 3 @oh-found fixes)
b556913f docs: CHANGELOG entry for #1093
a4cc080e perf(query-builder): #1093 -- Batch::try_build's $query-ref walk skips the msgpack round-trip for the 5 hot-path op types
```
