# Checkpoint — 2026-08-05 19:55

## Session summary

Continuation of the `/babygoal`-driven campaign that closed out #986 (typed `IndexSpec` enum), then decomposed and executed #1001's three nits (#1002/#1003/#1004) found by an `@oh` adversarial review, then hit `crush`'s genuine weekly/monthly hard quota limit mid-#1002 (resets 2026-08-06 17:01) and switched all delegated work to `Agent(subagent_type: "sh")` per the already-armed `/crush-fallback` sentinel (#982, now `STATUS: active, TRIGGER: hard-limit`). All four tasks (#986, #1002, #1003, #1004) were completed, independently gate-verified, and committed — covered in the prior checkpoint (`2026-08-05-1900.md`).

After that wave closed, a **follow-up `@oh` review of the whole #986/#1000/#1002-1004 batch** was launched per the standing campaign instruction. It found `#1003`'s fix (the #984 degraded-index gauge false-positive fix) had a genuine regression of its own: `degraded_index_count()` subtracted a scalar `Arc<AtomicU64>` in-flight-create COUNT from the raw non-`Ready` tally, but the unique and index2 families both register their definition only ONCE, post-backfill — so an in-flight create in either family contributed 0 to the raw tally while still subtracting 1, silently masking an UNRELATED genuinely-stuck index for the whole duration of an unrelated healthy create. A false negative on a health gauge, worse than the false positive #984/#1003 existed to fix. The same review also found #1004's "reject cases cover all variants" test (both Rust and TS) only ever compared case COUNTS, never actually cross-referencing which variants the cases produced — #1004's `==12`→`>=12` relaxation removed the last coincidental tripwire that had been catching this.

**Independently re-verified both findings by reading the actual code before fixing anything** (not just trusting the review): confirmed `create_unique_index_from_records` registers post-backfill only (`crates/shamir-index/src/base_index/index_manager_unique.rs`); confirmed `create_index_v2`'s index2 path registers only right before flipping `Ready`; confirmed the Rust "cover all variants" test's `expected_variants`/`reject_names` comparison was purely a length check.

**Fixed as task #1005** (two commits, `b7cc8464` + `df0bab54`, plus a CHANGELOG commit `6140d9cf`): replaced the scalar counter (`InFlightCreateCounter`) with a per-identity set (`InFlightCreateSet`, keyed by interned index-name id, refcounted `BTreeMap<u64,u32>` for the rare concurrent-same-name race) — `degraded_index_count()` now skips a non-`Ready` definition only if that SPECIFIC index's own name is in flight, never a scalar subtraction that can bleed across unrelated indexes. All four call sites moved their guard-entry to right after the name is actually interned. Added a new pause hook (`index2_registered_before_ready_hook`, fires after registry insert, before the `Ready` flip) to fix a previously-vacuous index2 test. Rewrote the Rust variant-coverage test to actually run every reject case through `try_build()` and check the real resulting error variants (exhaustive `variant_tag()` match, no wildcard arm — a future 13th `CreateIndexBuildError` variant is a compile error here until updated); the TS mirror checks coverage by canonical fixture case name instead (TS has no typed error enum to match against; explicitly rejected a `reason_contains`-substring-based approach since several distinct variants share the substring "sorted").

**A second, lighter `@oh` review pass** (scoped to just the #1005 fix commits, closing the loop on the fix-of-a-fix) found the production code was fully correct on all four families, but the NEW regression test itself (`degraded_index_count_unrelated_in_flight_create_does_not_mask_stuck_index`) didn't actually discriminate: it drove the "unrelated" create through the REGULAR family (`create_index`), which registers at `Building` BEFORE its own backfill pause point — so it contributed 1 to the raw tally at the park point, and the OLD reverted scalar model would compute `2.saturating_sub(1) == 1`, matching the test's own assertion. **Fixed** (commit `c5d60ec8`) by driving the unrelated create through `create_index_v2`'s functional path instead, parked at the EXISTING `create_index2_backfill_hook` (fires strictly before registry insert, contributing 0 to the raw tally) — now the OLD scalar model would compute `1.saturating_sub(1) == 0`, genuinely contradicting the test's assertion of 1. Also added a one-paragraph doc clarification about a pre-existing (not newly introduced) cross-family index-name-collision gap the review flagged as a nit.

**A significant tooling detour** occurred mid-#986 in the prior segment (already covered in `2026-08-05-1900.md`): a persistently-failing `.git/index.lock` traced not to another process but to the orchestrating shell's own `cd`-drift (an earlier `npx vitest` invocation from `crates/shamir-client-ts` was never `cd`-ed back from), confirmed via an absolute-path `cmd //c dir` cross-check and safely resolved per this session's established stale-lock precedent.

The user explicitly asked, mid-session, to do a `git push` once the current work was done — this is the next immediate action after this checkpoint, following the standing `/babygoal` condition's remaining steps (checkpoint ✓, this file; CHANGELOG ✓, already updated across this and the prior segment; commit all `.md` — in progress, this checkpoint is the last piece; `@oh` review — done, twice, both rounds of findings fixed and re-verified).

## Active goal

None — no `/goal` Stop-hook condition is in force; the session-scoped Stop hook currently active is the `/babygoal` condition text itself: "реализуй задачи с помощью @sh, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу" — believed satisfied as of this checkpoint (pending the user's requested push).

## TaskList

### pending (sentinel, never completes)
- #982 crush-fallback state (persistent — do not complete): sh — ACTIVE (hard-limit), resets ~2026-08-06 17:01 (no auto-revert cron armed, per `/crush-fallback`'s Trigger-2 rule)

### recently completed (this segment)
- #1005 fix #1003's scalar-counter over-subtraction masking bug (found by @oh review) — including its own follow-up fix for a non-discriminating regression test, found by a second review pass
- #1004, #1003, #1002, #986, #1000 — completed in the prior segment (see `2026-08-05-1900.md`)
- (#957-999 and earlier — the full original release-blocker campaign — all completed; deleted from the live TaskList earlier this session for hygiene, see `git log` / prior checkpoints for the full trail)

## Decisions

- Split the #1005 fix into TWO commits (the engine-side over-subtraction fix, and the separate query-builder/TS test-integrity fix) since they address two independently-found, unrelated defects in two different crates, even though both were found by the same review pass.
- On the second `@oh` review finding the new regression test itself didn't discriminate, fixed it by choosing a DIFFERENT family (index2, parked pre-registry-insert) rather than trying to patch the regular-family scenario, since the regular family structurally cannot produce the "contributes 0 to the raw tally" property the test needs.
- Declined to fix two lower-value nits from the second review (deriving `expected_variants` from a single source shared with `variant_tag`; switching `InFlightCreateSet`'s `std::sync::Mutex` to `scc::HashMap` for strict pillar-1/5 compliance) — both are real but minor, and the existing `dropping_regular`/`dropping_unique` precedent already uses the same `std::sync::Mutex` shape for identical low-frequency DDL guard sets.
- Relied on direct analytical reasoning (tracing the exact hook-firing order and raw-tally arithmetic) rather than a full manual revert-and-run cycle to confirm the fixed regression test discriminates correctly — a deliberate effort-budget tradeoff, unlike #1000 earlier this session where the timing subtlety genuinely warranted a live revert/restore cycle.

## Open questions

- None blocking. `/crush`'s hard limit resets ~2026-08-06 17:01 — until then (or a manual `/crush-fallback clear`), delegated work continues to route through `sh`.
- Immediate next action (already implicit in the conversation, not yet executed as of this checkpoint): `git push`, per the user's explicit mid-session request.

## Repo state

```
(clean — nothing uncommitted at checkpoint time)
```
```
c5d60ec8 fix(engine): #1005 follow-up -- regression test didn't discriminate, doc nit
6140d9cf docs(changelog): note #1005's #1003/#1004 corrections
df0bab54 test(query-builder,client-ts): #1005 -- #1004's variant-coverage check was a count, not real coverage
b7cc8464 fix(engine): #1005 -- #1003's in-flight counter could mask an unrelated stuck index
237feda4 docs: CHANGELOG entry for #986/#1000/#1002-1004 + session checkpoint
6dd1d1ae test(query-builder,client-ts): #1004 -- vector_dim boundary fixture rows
230ef178 docs(prompts): brief for #1004 -- vector_dim boundary fixture rows
72756201 fix(engine): #1003 -- fix #984 degraded-gauge false-positive on in-progress CREATE INDEX
215ab3ba docs(prompts): brief for #1003 -- fix #984 degraded-gauge false-positive
4eb8e8d9 docs(engine,client): #1002 -- fix stale comments from #983's round-1 misdiagnosis
```
