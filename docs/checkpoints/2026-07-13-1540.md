# Checkpoint — 2026-07-13 15:40

## Session summary

This window closed out the entire audit-remediation campaign (`/babygoal`-driven, TaskList as source of truth, no `/goal` Stop hook). At session start, `#519` (napi-rs 2.x→3.x upgrade + typed `.code`/`.retryable` client-node errors) and `#563` (unlocked group-record TOCTOU race) were both stuck on the crush provider `zai`'s peak-hours window (08:00–12:00 local). The user asked to pause all work until peak hours ended; a one-shot cron (`472e4aba`, armed in the prior session) fired at 12:03 with a wakeup prompt, and work resumed from there.

**#519** — relaunched into the same crush session with a continuation prompt after a mid-turn 60m timeout (stale lock cleared via `crush sessions kill`). The design uncertainty flagged in the brief was resolved via real compilation: napi-rs 3.x's async `#[napi]` fns hard-pin the error type to `Error<Status>` (confirmed, not assumed), so the documented JS-side-wrapper fallback was used — domain DB errors now travel as a `Buffer`-encoded `DbResponse::Error{kind:"error",code,message}` marker that a new `wrapper.js` decodes and throws as `ShamirDbError{.code,.retryable}`. Pinned `napi = "=3.10.5"` (exact — a caret range resolves to 3.8.6, which lacks a fn the derive macro needs). Verified independently (forced clean rebuild, rebuilt the `.node` binary myself, re-ran the proof script). Committed `d8be19e3`.

**#563** — added a `group_member_locks: DashMap<u64, Arc<Mutex<()>>>` (mirroring the established `admin_user_locks` pattern) across all 5 group-mutation call sites in `access_control.rs` (chown, rename, add/remove-member, and drop — a 5th site found during investigation). A 48-way concurrent-stress regression test proved the race and the fix. Adversarial review found one real gap: `rename_group_as`'s name-uniqueness check isn't race-free across DIFFERENT group_ids — split off as `#570`. Committed `3e403b11`.

Then worked sequentially through the deferred follow-up backlog:
- **#565** (done directly, small): fixed a redundant `dir.user_id()` lookup in `PrincipalResolver` (now threads `user_id` through `UserDirectoryState` from the already-decoded record), 2 stale `CreateRole`/`DropRole` doc-comment references, a `clippy::type_complexity` on a test mock, plus 4 new tests for error-classification (`not_found` vs `query` code mapping) and the no-resolver `drop_user` degrade path. Committed `c1031010`.
- **#566** (done directly): added `assertServerBinaryFresh()` to the TS e2e harness — walks `crates/` for the newest `.rs`/`Cargo.toml` mtime and refuses to spawn a `shamir-server` binary older than the source tree (closes the exact gap that let a week-stale binary silently mask real bugs for the #552–560 chain). Committed `250ba15f`.
- **#567** (done directly, then split): confirmed via a fresh-binary e2e run that `access_denied` is the correct, current, unified denial code (the real Root/User/Group DAC model), fixed 2 stale `permission_denied` assertions + doc comments. Discovered mid-investigation that 2 of the original 4 failures were NOT test bugs but a real client bug — split to `#569`. Committed `2cbf3372`.
- **#569** (delegated to crush, verified, committed `aec22e75`): `chown`/`addGroupMember`/`removeGroupMember`'s `string` username overload used a stale client-side FxHash (`principalId()`) that stopped matching the server's real principal64 after task #548's identity redesign. Removed the string overload entirely (sync HMAC-signing builders can't correctly support an inherently-async username→principal64 resolution), added `ShamirClient.resolvePrincipal(username): Promise<bigint>` backed by `ListOp::Users`, deleted the dead `principal-id.ts`, rewrote the affected e2e tests. Verified against a genuinely fresh binary: 275/275.
- **#570** (delegated to crush, verified, committed `a083c889`): `create_group_as` had NO name-uniqueness check at all (not a race — a complete absence); `rename_group_as`'s check wasn't race-free across group_ids. Reused the existing global `group_id_lock` as the single serialization point for the whole name namespace. 3 new tests (sequential duplicate, 16-way concurrent same-name storm, cross-path create-vs-rename race).
- **#568** (done directly): added 3 tests for the boot-audit's untested WARN branches (phantom Store B user, role divergence, phantom superuser) using a `seed_store_b_user()` helper built on `query_value_to_inner()` (a public codec fn that turned out to make this much simpler than #565's original scoping feared). Committed `071eb67c`.

**#529 (FINAL-GATE)** — the campaign's last task, run only once everything else was closed: `cargo fmt --all` found 40 files of accumulated per-task-scoped drift, reformatted and committed alone as a `style:` commit (`f52211d3`) + registered in `.git-blame-ignore-revs`. `cargo clippy --workspace --all-targets -- -D warnings` found one real blocker: Rust 1.93's new `clippy::octal_escapes` lint on 3 byte-string literals in `hmac_tests.rs` (`\0448`/`\07`/`\03` look like unsupported octal escapes) — disambiguated to `\x00...`, no behavior change (`67550558`). `./scripts/test.sh --full` (5389 tests) found 3 deterministic failures in `crash_recovery.rs`'s truncation crash-seam tests — delegated to crush as `#571`, root-caused via real instrumentation to an unrelated earlier commit (`32e63e17`, "A5" interner-checkpoint work) that moved interner persistence from every-commit to every-64-commits, silently breaking a 40-record test's assumption; fixed by bumping `TRUNC_RECORDS` to 96 (`6b099940`). Adversarial review of that fix surfaced one more real thing: `drainer.rs`'s module doc comment promised a "forced interner persist" step that was never actually implemented — investigated whether this was a real production bug or stale docs; concluded (backed by the default 8 MiB segment size ≫ the 64-commit checkpoint interval) it's a genuine but practically-unreachable bounded/self-healing gap, and corrected the doc comment rather than adding unneeded complexity (`#572`, `03132272`).

The one known pre-existing flake (`vr5_cofilter_sees_staged_and_filters_residual`, already addressed once by task #492) TIMEOUT'd under the full 5389-test parallel run but passed cleanly (91s) in isolation both times it was checked — confirmed contention, not a regression, consistent with an identical false-alarm pattern seen earlier this session on an unrelated test (`rename_table_durability`) during heavy concurrent crush+cargo-build load.

TaskList is now completely empty (`#481` through `#572`, all completed) — the babysit cron (`5bc3e6be`) self-deleted per its own tick logic once it observed 0 pending/in_progress. No `/goal` Stop hook was ever set this session. No active crush sessions remain (all confirmed `done` via `crush sessions why`).

## Active goal

None. No `/goal` Stop hook was set. The babysit cron that drove this session has self-deleted (TaskList empty).

## TaskList

Empty. All 92 tasks created since the campaign began (`#481`–`#572`) are `completed`. No `pending`, no `in_progress`, no `blocked`.

## Decisions

- **napi-rs 3.x error-typing resolved by real compilation, not doc research** — async `#[napi]` fns cannot carry a custom error type; used the brief's documented JS-side-wrapper fallback (`#519`).
- **`chown`/`addGroupMember`/`removeGroupMember`'s string-username overload was removed, not patched** — resolving a username to its real principal64 is inherently async, and there's no correct way to keep a synchronous string path post-#548; added `ShamirClient.resolvePrincipal()` as the explicit resolve step instead (`#569`).
- **Group-name uniqueness reuses the existing global `group_id_lock`** rather than inventing a new lock — one lock now serializes the whole name namespace across both `create_group_as` and `rename_group_as` (`#570`).
- **`drainer.rs`'s stale "forced interner persist" doc claim was corrected as documentation, not implemented as new behavior** — a genuine design review (backed by the 8 MiB default segment size vs. the 64-commit checkpoint interval) concluded the real-world gap is bounded and self-healing, not worth the added background I/O complexity (`#572`).
- **The full-workspace fmt sweep and the octal-escape clippy fix were committed separately** from the crash_recovery test fix and the drainer doc fix, per the repo's own style-sweep-gets-its-own-commit discipline.

## Open questions

None outstanding — this checkpoint reflects a fully clean, fully closed state.

## Repo state

```
 M CLAUDE.md
?? .build3.log
?? .tmp_napi_cargo.toml
?? "D\357\200\272devrustshamir-db_audit_diff.patch"
?? _check3.pdb / _check_escape.pdb
?? (a large number of stray root-level *.log files from earlier bench/test sessions — pre-existing clutter, not from this window's work)
?? docs/audits/2026-07-11-security-permission-triage.md
?? docs/checkpoints/... (prior-session checkpoint files)
?? docs/design/*.md (identity/privilege decision docs from earlier sessions)
?? docs/prompts/audit/crush-launch-command.md
```

The `M CLAUDE.md` entry predates this session (carried over, not mine). The untracked clutter is likewise pre-existing across many prior sessions — none of it was touched or is attributable to this window's work.

```
03132272 docs(engine): correct drainer.rs's stale forced-interner-persist claim (#572)
6b099940 test(engine): fix crash_recovery truncation crash-seam never firing (#571)
f677b66b docs(prompts): brief for #571 crash_recovery truncation-seam fix
67550558 fix(query-types): disambiguate octal-looking null-byte escapes in hmac_tests (#529)
0ab4f075 chore: register fmt-sweep commit in .git-blame-ignore-revs (#529)
```
