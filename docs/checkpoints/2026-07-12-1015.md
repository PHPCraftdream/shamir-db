# Checkpoint — 2026-07-12 10:15

## Session summary

Continuing the standing audit-remediation campaign (`/babygoal`, goal condition: "Используй агентов @sx (agent). Реализуй все задачи" — implement all tasks using `sx` sub-agents). This window's work was entirely on the DECISION-track tail of the campaign (#548, #549, #550) — the last few implementable tasks (#538–#547, #551) were already completed and committed in the prior window (see `git log`).

For **#550** (Root/User/Group always-open meta + wire-admin DAC posture + CreateFunctionOp wire-field gap), I investigated all three sub-claims, wrote `docs/design/root-user-group-dac-posture-550-decision.md` with my own first-pass proposals, and put them to the user via `AskUserQuestion`. The user REJECTED all three of my first-pass proposals and asked for independent expert consultation instead: P.1 ("Root/User/Group need real, separate permissions — this should be designed by @fl"), P.2 and P.3 ("посоветуйся с @fm"). I ran the `fl` and two `fm` consultations; all three came back with materially different, better-reasoned designs than my originals — notably, `fm`'s P.2 review caught that my proposed fix (exempt any `is_admin()` op where `is_write()==false`) would have reopened a real ACL bypass via nested `BatchOp::Batch` (same bug class as #510), plus silently exempted 8 unreviewed ops including two real data leaks. I rewrote `docs/design/root-user-group-dac-posture-550-decision.md` in full to replace my rejected proposals with the vetted versions, per the user's explicit instruction "как получишь все советы - запиши их в файл, заведи таски и остановись" (once you have all the advice, write it to the file, create tasks, and stop). I then created three follow-up implementation tasks (#552, #553, #554), each `blockedBy` #550, and stopped as instructed.

A Stop-hook fired immediately after, insisting all tasks be implemented via `sx`. Rather than resuming work unilaterally (every remaining task genuinely requires the user's own sign-off — #552-554 need #550 approved, #548/#549 need the user's answer on 6 explicit sign-off points in `docs/design/identity-privilege-unification-548-549-decision.md`, #519 needs an explicit version-bump go-ahead), I put a consolidated 3-question `AskUserQuestion` to the user covering exactly these blockers. **The user answered all three, this session's most consequential open item**: (1) approved all three revised #550 designs (§1/§2/§3) — clears #552/#553/#554 to be implemented; (2) approved the full combined #548+#549 plan from `fxx`'s design doc as-is — clears that redesign to begin (7-step plan already outlined: impl-briefs `docs/prompts/audit/70-76`); (3) explicitly DECLINED the napi-rs 3.x bump for #519 ("оставить как есть" — leave as-is), so #519 stays pending/parked, unblocked-but-untouched.

Immediately after those three answers arrived, a `# babysit tick` cron prompt fired (routine 15-minute heartbeat, not a new user request) — before I could act on it, the user interrupted with `/checkpoint` (no name arg), which is what this file is answering. **No implementation work has started yet on #550's three sub-tasks or on #548/#549** — the sign-offs just arrived this same turn and no `sx`/`fxx`/`fl`/`fm` agent has been launched for implementation, no brief has been written yet for #552/#553/#554 or for the #548/#549 combined redesign.

Also noted at checkpoint time: an untracked file `D\357\200\272devrustshamir-db_audit_diff.patch` (odd escaped-colon path) is still sitting in the working tree, unexplained since the prior checkpoint — still not investigated. `docs/design/*-decision.md` and this session's checkpoint files are themselves still untracked/uncommitted (git status shows `?? docs/design/...`) — per the prompt-first discipline, decision docs should probably get committed at some point, though the user has never explicitly asked for that commit and no instruction says checkpoints/design docs must be committed automatically.

## Active goal

Session-scoped Stop hook, condition verbatim: `Используй агентов @"sx (agent)". Реализуй все задачи` (use `sx` agents; implement all tasks). Has fired at least twice this window; both times the orchestrator judged that a more specific, more recent user instruction (explicit "stop after report", then explicit "write file/create tasks/stop") took precedence over resuming unprompted, and instead surfaced the genuine blockers back to the user via `AskUserQuestion` rather than guessing. That escalation just resolved: the user has now signed off on #550 and #548/#549 in full.

## TaskList

### in_progress
(none)

### pending
- #519 CLIENT: node-binding typed error .code/.retryable (needs napi-rs 3.x bump — user explicitly DECLINED the bump this session; parked, not blocking anything else)
- #529 FINAL-GATE: full fmt + clippy + test --full across workspace (blocked by: #507, #515, #516 [stale/already-absorbed, cosmetic], #548, #549)
- #548 DECISION: unbind security-Actor from fxhash(username), bind to stable user_id — **user just approved `fxx`'s combined design in full; ready to move to implementation briefs**
- #549 DECISION: unify parallel superuser/role privilege axis + reconcile desynced user/role stores — **folded into #548's combined design, same approval**
- #550 DECISION: Root/User/Group always-open meta + wire-admin DAC posture + wire Definer/secret_grants gap — **user just approved all three revised (fl/fm-vetted) designs; ready to unblock #552/#553/#554**
- #552 IMPLEMENT: Root/User/Group real permission model (revised per fl consultation, #550 P.1) — blockedBy #550 (approval just given, blocker should be cleared next turn)
- #553 IMPLEMENT: wire-admin DAC gate explicit per-op allowlist (revised per fm consultation, #550 P.2) — blockedBy #550 (approval just given)
- #554 IMPLEMENT: CreateFunctionOp per-field wire gating for security/secret_grants/visibility (revised per fm consultation, #550 P.3) — blockedBy #550 (approval just given)

### recently completed (most recent 10)
- #551 SECURITY: extend destructive-op HMAC confirmation to group-mutating ops (residual from #542)
- #547 FIX: wire changePassword flow into the live server request dispatcher
- #546 HARDENING: unified authz-mapping registry + doc-guards + create-TOCTOU + coverage matrix
- #545 SECURITY: WASM module sanitizer/verifier before instantiation (CRITICAL residual)
- #544 FIX: WASM per-function egress capability + gate global_set into env.* namespace
- #543 FIX: validate chown/chgrp/addGroupMember target ids resolve to real principals
- #542 SECURITY: extend destructive-op HMAC confirmation to privilege/create/chmod/retention ops
- #541 SECURITY: effective_fn_actor missing-owner escalates definer function to System
- #540 SECURITY: resource_meta fail-open on storage error → fail-closed
- #539 FIX: MemBuffer dirty_nonempty transient reader-visible window + get() tombstone-poisoning

## Decisions

- **#550 §1 (Root/User/Group)**: rejected the orchestrator's original "leave `ResourceMeta::open()`, just document it" in favor of `fl`'s per-kind design — Root gets full persisted settings-backed meta (mirrors `FunctionNamespace`, default `0o755`/System); User gets a fixed computed 3-tier rule (System/self/nobody), never persisted; Group gets a new persisted `owner` field enabling self-management via `Manage(Root) OR Manage(Group{...})`.
- **#550 §2 (wire-admin gate)**: rejected the orchestrator's original `is_write()`-derived blanket relaxation after `fm`'s consultation found it reopens a nested-`Batch` authorization bypass (same class as #510) and silently exempts 8 unreviewed ops incl. 2 real data leaks — replaced with an explicit 4-op allowlist (`List`/`AccessTree`/`DescribeTable`/`GetTableSchema`), never derived from a classifier.
- **#550 §3 (CreateFunctionOp fields)**: rejected the orchestrator's original uniform "gate all 3 fields like chmod" in favor of `fm`'s per-field scheme — `visibility` ungated, `security: Definer` via HMAC (parity with the existing setuid gate, not an extra Manage check), `secret_grants` via `Manage(Root)` + HMAC (no secrets-ACL concept exists yet).
- **#548/#549**: user approved the full combined `fxx` redesign as one package rather than adjudicating the 6 points individually — no scope was trimmed.
- **#519**: user explicitly declined the napi-rs 3.x bump this session — task stays parked, not reattempted without a future explicit ask.

## Open questions

- None outstanding for #550 or #548/#549 — both just received full sign-off this turn.
- The stray `D\357\200\272devrustshamir-db_audit_diff.patch` untracked file — origin still not investigated.
- Whether `docs/design/*-decision.md` docs should be committed (not yet done, not yet explicitly requested).

## Repo state

```
 M CLAUDE.md
?? "D\357\200\272devrustshamir-db_audit_diff.patch"
?? docs/audits/2026-07-11-security-permission-triage.md
?? docs/checkpoints/2026-07-08-audit-remediation-a10-inflight.md
?? docs/checkpoints/2026-07-08-audit-remediation-a4-inflight.md
?? docs/checkpoints/2026-07-08-audit-remediation-babygoal.md
?? docs/checkpoints/2026-07-09-audit-remediation-perf-radical-2.md
?? docs/checkpoints/2026-07-09-audit-remediation-perf-radical.md
?? docs/checkpoints/2026-07-10-audit-remediation-500-inflight.md
?? docs/checkpoints/2026-07-10-audit-remediation-520-inflight.md
?? docs/checkpoints/2026-07-10-audit-remediation-532-inflight.md
?? docs/checkpoints/2026-07-11-2326.md
?? docs/checkpoints/2026-07-12-0840.md
?? docs/design/identity-privilege-unification-548-549-decision.md
?? docs/design/principal-id-identity-548-decision.md
?? docs/design/root-user-group-dac-posture-550-decision.md
?? docs/prompts/audit/crush-launch-command.md
?? (a large number of stray root-level *.log/*.pdb files from earlier bench/test sessions — pre-existing clutter, not from this window's work; not cleaned up)
```

```
ebca25a0 security(hmac): extend destructive-op HMAC gate to group-mutating ops (#551)
555b7f8d docs(prompts): brief for #551 HMAC coverage for group-mutating ops
11ce5a3d fix(server): wire changePassword flow into the live request dispatcher (#547)
2ff522d6 docs(prompts): brief for #547 wire changePassword into server dispatcher
37fd5703 hardening(authz): unify per-op mapping + close gate-coverage gaps (#546)
```
