# Checkpoint — 2026-07-28 [p0-wave2]

## Session summary

This session continued from checkpoint `2026-07-28-0100.md`. Since that resume, the session: (1) finished F-40b (RI barrier explicit-Snapshot fix) Steps 1-2, fully closing that campaign — Step 1 spike settled the design (flat `TFxSet<u64>` token shape, reuse `PhantomConflict`/`"tx_conflict"` error code), Step 2 extended the mechanism to all four FK actions and all commit-pipeline guard sites; (2) the user asked for a fresh readonly review of the whole wave, which I studied and independently verified against actual source (not just trusted) — found all 5 named P0s genuinely reproduced in code, including one (P0-1, the RI barrier's own one-sided commit-lock condition) that directly critiqued work I had just landed; (3) filed 10 new tasks (F-46 through F-55, #857-#866) covering the review's 5 P0s, 3 P1s, and 2 P2s, with dependency edges where real (F-50 blocks F-53's FK-perf portion; F-48 blocks F-50).

**All P0s closed except F-50 (in progress):**
- **F-46** (`57382bab`) — RI barrier's `commit_tx_lockfree` lock condition was missing `footprint_tokens`, letting a concurrent FK-child Snapshot writer publish DURING the parent's own commit-lock window (a genuinely different, worse bug than the backward-looking race the existing tests covered). Added a new later pause seam (`TEST_POST_VALIDATE_PRE_PUBLISH_HOOK`) proving the forward-race, fixed by adding the missing clause. Bench showed no measurable regression. Also found and fixed a real test-harness deadlock (pause hook firing for every committer, not just the intended one) along the way.
- **F-47** (`408ffc97`) — the reverse-FK cache's own doc already named a residual: `generation: AtomicU64` and `state: ArcSwap<Option<CacheState>>` were two separate atomics, so a concurrent `invalidate` could still over-publish a stale scan between the post-scan generation check and the store. Merged into one `ArcSwap<VersionedState>`, publish is now a single `compare_and_swap` (arc-swap 1.7.1's actual API, verified from source not memory).
- **F-48** (`c2cb6e13`) — built a reusable `WriterDrainBarrier` (active-writer counter, chosen over seqlock/full-RwLock) and wired it into the 4 non-tx `TableManager` writer methods + the schema-activation DDL guard, closing the check-then-act race the review's P0-3 flagged. **My own zero-trust review caught a real scoping gap**: this only covered the non-tx write path — the tx-commit prelock (`pre_commit.rs` Phase 2.5), which is how essentially ALL production writes actually happen, was untouched. Filed and immediately executed a follow-up:
- **F-48b** (`26124d30`) — wired the same `WriterDrainBarrier` into `pre_commit_prelock`'s Phase 2.5, threading kept-alive drain guards (parallel `Vec<WriterDrainGuard>`, chosen over a combined-type refactor to avoid touching `group_commit.rs`'s dead code) through both live commit paths into `materialize`/`materialize_async_tail`. This is the fix that actually closes the schema-activation race for real traffic. Incidentally also closes the same class of gap for `index2_create_barrier`'s tx-commit path (Phase 2.5's flag-check is barrier-agnostic), pending F-50 wiring the DDL side.
- **F-49** (`45214c0a`) — `MirroredStore::transact` mutated `primary` before the atomic `mirror.transact` for the durable subset (opposite order from F-41's already-fixed `set`/`remove`). Reordered to mirror-first; updated two pre-existing F-39 tests whose assertions literally encoded the bug being fixed (a legitimate update, not a weakening).

**F-50 (online index build lifecycle) is genuinely large** — user explicitly chose the full lifecycle redesign over a temporary offline-only restriction. Decomposed into Step 1 (spike, #869, in flight) and Step 2 (implement, #870, blocked), mirroring the F-40b/F-48→F-48b precedent. Before writing the spike brief I did real investigative groundwork myself: confirmed `table_manager_tx_ops.rs`'s `plan_insert_ops`/etc. snapshot `all_backends()` once at STAGE time, and — critically — confirmed `commit.rs`'s `wal_ops_from_tx` serializes `tx.index_write_set` DIRECTLY into the WAL entry, meaning any fix must re-derive the ops-plan BEFORE Phase 4 (WAL begin), not after (re-deriving after would create a crash-safety hole since recovery replays the WAL-logged stale ops). This finding is baked into the spike brief so the agent doesn't have to rediscover it.

A separate, low-priority task (#868) was filed per explicit user request to investigate the `filtered_ann_tests::vr5_cofilter_sees_staged_and_filters_residual` test that's shown up as "pre-existing SLOW" in nearly every full-suite run this session (30-120s, inconsistent) — scoped to run LAST, after the P0/P1/P2 wave, per CLAUDE.md's own "hunt and fix hangs, never tolerate" mandate.

**Delegation channel switched mid-session per explicit user instruction**: started this window on `@sh` (Agent tool, subagent_type "sh") for F-46, then the user re-invoked `/babygoal` specifying `/crush` — every task since F-47 uses `/crush`, not the Agent tool. Zero-trust verification discipline held constant regardless of channel: every diff read in full, `fmt`/`clippy`/tests personally re-run, and for EVERY task this window I personally reproduced the red→green proof myself (temporarily reverting the fix in the working tree, confirming the new test genuinely fails, restoring the fix, confirming it passes again) rather than trusting the agent's own claimed red-test output — this caught nothing wrong in any case, but once (F-46, F-48, F-49 all needed a forced rebuild after a `touch` because of file-lock contention with concurrent background processes producing a stale-binary false negative) required care to avoid a false conclusion.

**Standing babysit cron `5ce533bb`** (every 6/21/36/51 min) has been ticking throughout and is still active. No `/goal` Stop hook is currently armed (the earlier `/goal` invocation this session was superseded by re-running `/babygoal`, which doesn't use `/goal`).

## Active goal

None currently armed via `/goal`. The operative directive is `/babygoal`'s own TaskList-driven contract: continue solving tasks via `/crush` agents, commit between each, verified by `CronList`/`TaskList` checks at the start of this response.

## TaskList

### in_progress
- #869 F-50 Step 1 (P0, spike): settle design for online index build lifecycle — close the tx.index_write_set stale-ops-plan "guaranteed miss". `/crush` session `f50-step1-index-lifecycle-spike` confirmed alive moments before this checkpoint.

### pending
- #870 F-50 Step 2 (P0, implement): land the full lifecycle per Step 1's memo (blockedBy: #869)
- #861 F-50 (P0, umbrella): blockedBy #870
- #862 F-51 (P1): docs/config/builder truthfulness sweep — Argon2 ceiling, CreateRepo.path builder, KNOWN_LIMITATIONS overclaim
- #863 F-52 (P1): release packaging/version/action pinning
- #864 F-53 (P1/P2): streaming top-K + cursor index-seek + FK scan-heavy performance wave (blockedBy: #857, already resolved — ready)
- #865 F-54 (P2): group_commit.rs's run_leader dead-code decision
- #866 F-55 (P2): public-repo hygiene sweep — requires explicit user sign-off on approach before starting (touches docs/dev-artifacts/, protected by a standing user rule against deletion/archival)
- #868 (P-low): investigate/fix the recurring SLOW ANN cofilter test — run LAST

### recently completed (this window, in order)
- #857 F-46, #858 F-47, #859 F-48, #867 F-48b, #860 F-49 — see Session summary for detail.
- (prior window) #855/#856 F-40b Step 1/Step 2; #843-#853 F-35 through F-45.

## Decisions

- **User explicitly chose F-50's full lifecycle redesign over the cheaper temporary offline-only restriction** (asked directly via AskUserQuestion) — this is a materially larger scope commitment than the alternative, expect it to need its own Step 1→Step 2 (possibly →Step 3) arc like F-40b did.
- **F-48's scoping gap (non-tx path only) was caught by my own zero-trust review, not the delegated agent** — filed and closed F-48b in the same turn rather than deferring, since the primary production DML path being unprotected is not an acceptable state to leave mid-wave.
- **Guard-threading in F-48b used a parallel `Vec<WriterDrainGuard>` over a combined enum type**, specifically to avoid touching `group_commit.rs`'s confirmed-dead code with a type change — matches this session's `timebox toward the simpler option` principle stated in multiple briefs.
- **F-49's two updated pre-existing tests were a legitimate correctness update, not a weakening** — their old assertions directly encoded the bug being fixed ("primary DOES hold the ops after mirror failure" was the bug).
- **F-50's spike brief bakes in a confirmed architectural finding** (WAL entry construction depends on stage-time `index_write_set`, so re-derivation must happen pre-Phase-4) rather than leaving the agent to rediscover it from scratch — found by personally reading `commit.rs`'s `wal_ops_from_tx` before writing the brief.

## Open questions

- **F-50 Step 1's spike outcome is unknown as of this checkpoint** — it was confirmed alive via `crush sessions locks` moments before this file was written, but its actual design decisions, prototype results, and Step 2/Step 3 scope split are not yet known. Resolve via the completion notification, then zero-trust-verify (read the diff, check the produced memo at `docs/dev-artifacts/research/f50-index-lifecycle-spike.md`, re-run fmt/clippy/tests) before committing, exactly as done for every other task this window.
- No other open questions — the review's findings are otherwise fully triaged (all filed, most closed, remainder explicitly sequenced).

## Repo state

```
?? docs/checkpoints/2026-07-28-0100.md
```
```
a6a14de0 docs(prompts): brief for F-50 Step 1 -- online index build lifecycle design spike (#869)
45214c0a fix(storage): F-49 -- MirroredStore::transact commits to mirror before mutating primary for the durable subset (#860)
e42f621e docs(prompts): brief for F-49 -- MirroredStore::transact mirror-first ordering (#860)
26124d30 fix(engine): F-48b -- wire the writer-drain barrier into the tx-commit prelock, closing the schema-activation race for the primary production DML path (#867)
fb27b133 docs(prompts): brief for F-48b -- wire writer-drain into the tx-commit prelock (#867)
c2cb6e13 fix(engine,db): F-48 -- reusable writer-drain barrier closes the schema-activation check-then-act race for the non-tx write path (#859)
b1df5821 docs(prompts): brief for F-48 -- schema/index DDL writer-drain protocol (#859)
408ffc97 fix(engine): F-47 -- atomic versioned publish closes the reverse-FK cache's residual invalidate race (#858)
```

Note: the working tree is otherwise clean (every task this window has been committed immediately after zero-trust verification) except for the prior checkpoint file, which remains untracked by design (checkpoint files are not auto-committed).
