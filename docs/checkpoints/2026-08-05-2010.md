# Checkpoint — 2026-08-05 20:10

## Session summary

Continuation of a long `/babygoal`-driven release-blocker campaign. This segment: closed out #986 (typed `IndexSpec` enum for `CreateIndexOp`), then #1001's three decomposed nits (#1002 stale comments, #1003 degraded-gauge false-positive fix, #1004 vector_dim boundary fixtures) — all found earlier by an `@oh` adversarial review and executed via `Agent(subagent_type: "sh")` after `crush` hit a genuine weekly/monthly hard quota limit mid-#1002 (the already-armed `/crush-fallback` sentinel, task #982, was flipped to `STATUS: active, TRIGGER: hard-limit`; resets ~2026-08-06 17:01).

After that wave, a **follow-up `@oh` review of the whole #986/#1000/#1002-1004 batch** found a real regression in #1003's own fix: `degraded_index_count()` subtracted a scalar in-flight-create COUNT from the raw non-`Ready` tally, but the unique and index2 index families both register their definition only once, post-backfill — so an in-flight create in either family could still subtract 1 while contributing 0, silently masking an UNRELATED genuinely-stuck index. Fixed as task **#1005**: replaced the scalar `Arc<AtomicU64>` counter with a per-identity set (`InFlightCreateSet`, keyed by interned index-name id, refcounted `BTreeMap<u64,u32>`) so the gauge only ever excludes the SPECIFIC index whose create is in flight, never bleeding into an unrelated index's count. The same review also found #1004's "one reject case per error variant" completeness test (Rust + TS) only ever compared case COUNTS, never actually cross-referencing variant coverage — fixed by running every reject case through `try_build()` and checking the real resulting error-variant set (Rust) / canonical case names (TS, which has no typed error enum to match against).

**A second, lighter `@oh` review** (scoped just to the #1005 fix, closing the loop on the fix-of-a-fix) found the production code was correct on all four index families, but the NEW regression test itself didn't discriminate — it drove the "unrelated in-flight create" through the regular-hash family, which (unlike unique/index2) DOES contribute to the raw tally at its own pause point, so the test would have passed against the reverted buggy code too. Fixed by switching the unrelated create to the index2/functional family, parked at the pre-existing `create_index2_backfill_hook` (fires strictly before registry insertion, contributing 0 to the raw tally) — now genuinely discriminating. Also added a doc clarification about a pre-existing (not newly introduced) cross-family index-name-collision gap the review flagged as a minor nit.

All of #986/#1000/#1002/#1003/#1004/#1005 are committed, independently gate-verified (fmt/clippy/full test suites, not just accepted from delegate reports), and the CHANGELOG + two checkpoints (`2026-08-05-1900.md`, `2026-08-05-1955.md`) reflect the whole wave. The user then asked for a `git push`, which was executed (`f1821aa9..37cc59a3`, 102 commits).

**Currently open, mid-investigation:** the pushed commit's CI run (`31032501528`) shows one failing job — `cargo test integration (windows-latest)` — where 4 unrelated batch-execution e2e tests (`batch_sequencing_e2e::batch_mixed_after_and_query_edges_report_expected_provenance`, three `batch_for_each_e2e::for_each_*` tests) all failed identically on BOTH the initial run and nextest's automatic retry, each with the SAME error (`Db { code: "limits", message: "batch execution exceeded its 30s time budget" }`) and near-identical (~140s) wall-clock duration both times. Every other job (fmt, clippy on 3 platforms, lib tests on 3 platforms, integration on macOS/ubuntu, loom, TS unit) is green. Working hypothesis (not yet confirmed): this looks like Windows-CI-runner slowness/contention rather than a real regression from this session's changes — none of #986/#1000/#1002-1005 touch the batch-execution/timing hot path these tests exercise (they're either a pure internal query-builder refactor, engine-side index-DDL/recovery/gauge logic, doc comments, or test fixtures). The repo's own CI history shows other unrelated commits (#946, #949, #950, #951) also failing intermittently, supporting the "known flakiness pattern" reading, but this has NOT been conclusively proven — the user was asked whether to re-run the failed job (`gh run rerun 31032501528 --failed`) or investigate the Windows batch-execution timing more deeply first, and has not yet answered (this `/checkpoint` invocation interrupted before a reply).

`/babysit`'s cron (`54e91ce1`) was explicitly deleted by the orchestrating session once all tracked TaskList work was done (only the permanent #982 sentinel remains, which by design is never "worked").

## Active goal

None — no `/goal` Stop-hook condition is in force. (Earlier in this segment, a session-scoped Stop hook was active carrying the `/babygoal` condition text — "реализуй задачи с помощью @sh, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу" — which is believed satisfied as of the prior checkpoint + push; whether it has since auto-cleared is not something this session can directly observe.)

## TaskList

### pending (sentinel, never completes)
- #982 crush-fallback state (persistent — do not complete): sh — ACTIVE (hard-limit), resets ~2026-08-06 17:01 (no auto-revert cron armed, per `/crush-fallback`'s Trigger-2 rule)

### recently completed
- #1005 fix #1003's scalar-counter over-subtraction masking bug (found by @oh review) — including its own follow-up fix for a non-discriminating regression test, found by a second review pass
- #1004 restore lost vector_dim boundary test coverage from #998's deleted file
- #1003 fix #984 degraded-index gauge false-positive on healthy in-progress CREATE INDEX
- #1002 fix stale comments left over from #983's round-1 misdiagnosis
- #986 typed IndexSpec Rust enum + wire-compatible DTO for CREATE INDEX
- #1000 fix #997 recovery multi-entry tombstone data-loss found by @oh review
- (#957-999 and earlier — the full original release-blocker campaign — all completed and deleted from the live TaskList earlier this session for hygiene; see `git log` / prior checkpoints for the full trail)

## Decisions

- Routed all delegated work through `Agent(subagent_type: "sh")` instead of `crush run` once `crush` hit a genuine hard weekly/monthly quota limit (distinguished from a transient failure by the explicit "Weekly/Monthly Limit Exhausted... resets at <timestamp>" message, per `/crush-fallback`'s Trigger-2 criteria) — updated the already-armed #982 sentinel rather than leaving it stale.
- On the first `@oh` review's #1003 finding, replaced the scalar in-flight counter with a per-IDENTITY set rather than trying to patch the scalar model (e.g. per-family sub-counters) — a per-identity check structurally cannot bleed across unrelated indexes, which a per-family counter still could in principle.
- On the second `@oh` review finding the new regression test didn't discriminate, fixed the TEST (switched families) rather than declaring the underlying production fix unproven — traced the exact hook-firing order and raw-tally arithmetic to confirm the corrected test genuinely fails against the reverted scalar model before committing.
- Declined two lower-value nits from the second review (deriving `expected_variants` from a single shared source with `variant_tag`; migrating `InFlightCreateSet`'s `std::sync::Mutex` to `scc::HashMap` for strict pillar-1/5 compliance) as out of scope for this pass — both real but minor, and the `std::sync::Mutex` shape already has a direct precedent (`dropping_regular`/`dropping_unique`) for this exact low-frequency DDL-guard use case.
- Pushed to `origin/master` on the user's explicit request, without first confirming CI was green — this is now the open thread being investigated.

## Open questions

- **Directly asked to the user, unanswered as of this checkpoint:** should the failing `cargo test integration (windows-latest)` CI job be re-run (`gh run rerun 31032501528 --failed`), or should the Windows-runner batch-execution timing be investigated more deeply first, before assuming it's environmental flakiness rather than a real (if hard-to-connect) regression?
- `/crush`'s hard limit resets ~2026-08-06 17:01 — until then (or a manual `/crush-fallback clear`), delegated work should continue to route through `sh`.

## Repo state

```
(clean — nothing uncommitted at checkpoint time)
```
```
37cc59a3 docs(checkpoints): 2026-08-05-1955 -- #1005 fix + follow-up review wave
c5d60ec8 fix(engine): #1005 follow-up -- regression test didn't discriminate, doc nit
6140d9cf docs(changelog): note #1005's #1003/#1004 corrections
df0bab54 test(query-builder,client-ts): #1005 -- #1004's variant-coverage check was a count, not real coverage
b7cc8464 fix(engine): #1005 -- #1003's in-flight counter could mask an unrelated stuck index
```
