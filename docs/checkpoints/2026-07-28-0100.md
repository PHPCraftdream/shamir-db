# Checkpoint — 2026-07-28 01:00

## Session summary

This session continued from checkpoint `2026-07-27-0140.md`. Since that resume, the session: (1) finished F-33 (hybrid repo backend) Steps 2-6, fully closing that 6-step campaign (`c32c4e3d`, `af4053da`, `606a8b48`, `ca95939d`, `952ba2ed`) — including a real bug the delegated Step 2 agent found and fixed mid-run (`interner.persist()` never called after `create_index`/`create_unique_index`, closed in Step 5's commit); (2) closed F-32 (doc/repo hygiene sweep) via three pieces — F-32a (README role wording + AGENTS.md crate count, `02a5fbf8`), F-32b (KNOWN_LIMITATIONS.md audit, `640bc056`), plus committing two loose checkpoint files; (3) the user then asked me to study a NEW independent readonly review (`docs/dev-artifacts/research/2026-07-27-new-wave-readonly-review.md`, HEAD `640bc056`) that found the whole "Wave F" campaign substantially improved the codebase but was NOT yet alpha-tag-ready: 5 P0 release blockers plus several P1/P2 findings. The user said "заведи таски под исправление" (file tasks for the fixes) — I decomposed the review into tasks #843-853 (F-35 through F-45, mirroring the F-number sequence) plus a follow-up #854 (F-40b) that itself needed decomposing into two more steps (#855 spike, #856 implement) once F-40's own investigation concluded its second concern needed a dedicated design spike rather than a rushed fix.

**All 5 P0s are now closed**: F-35 (`44c4317e`, FK reverse cache tracks on_update separately from on_delete — the cache previously only stored on_delete, silently breaking the Serializable upgrade for on_update-only FKs), F-36 (`d3d06c82`, FK cache generation-safe invalidate — closes a cache-aside invalidate-vs-build race that could publish a stale snapshot), F-37 (`83a621ee`, keyset_safe write barrier — mirrors the existing unique-index DDL write-barrier pattern via a new `schema_activation_barrier` sibling flag on `TableManager`), F-38 (`56f41c74`, bootstrap TTL fails closed on a metadata read error — a security-critical fix, verified with a genuine corrupted-blob fault injection, not a mock), F-39 (`c27839fc`, `MirroredStore::transact` atomically commits its durable subset via the mirror's real fjall `OwnedWriteBatch`, correcting a wrong assumption in the original review that `InMemoryStore` had a native atomic transact — it doesn't, confirmed by reading the actual source).

**All P1s are now closed too**: F-40 (`5679edfa`, FK footprint/isolation discovery fails closed on error — plus a rigorous investigation of the separate explicit-Snapshot gap that REJECTED an isolation-mid-flight-upgrade fix as unsound, with a precise technical argument about SSI predicate/read-set recording being gated on isolation at read-time; produced a scoping memo instead of rushed code), F-41 (`bb764ddd`, MirroredStore mirror-first writes + hydration classifier re-filter), F-42 (`db23a1fd`, index-create persists interner ids BEFORE publishing the index — also found and fixed the identical bug pattern in `create_sorted_index_with_include`, not explicitly named in the original review), F-43 (`ab3647ce`, CreateRepo.path rejected instead of silently ignored + a new additive `RepoEngine` typed enum for the query builder), F-44 (`97cb9417`, base deploy profile made conservative to match code defaults, high-capacity numbers moved to a new `server.large.example.ktav`). F-45 (`34ec0292`, P2) re-synced `KNOWN_LIMITATIONS.md`'s FK closure claims against the now-landed F-35/F-36/F-40 fixes, correctly keeping the explicit-Snapshot gap stated as OPEN (not overclaiming closure) while noting it's now scoped via a memo + follow-up task.

**Currently in flight**: F-40b's own two-step follow-up for the explicit-Snapshot RI-barrier gap F-40's investigation scoped (not fixed). Step 1 (#855, a design spike mirroring F-28 Step 3's precedent) is running via a background `@sh`-via-`/crush` agent, session id `f40b-step1-spike` (crush session, NOT an Agent-tool id), launched with a 90-minute timeout. Its brief (committed at `docs/dev-artifacts/prompts/post-alpha/89-f40b-step1-ri-barrier-spike.md`) asks it to settle two open design questions from `docs/dev-artifacts/research/f40-explicit-snapshot-ri-gap-memo.md` (token shape: flat token set vs richer `PredicateDep`; retry/error-code contract for explicit-tx clients), prototype ONE recording site + ONE guard-widening site, prove the mechanism with a deterministic race test + a ~50-trial quiescent-abort check, and produce a decision memo at `docs/dev-artifacts/research/f40b-ri-barrier-spike.md` scoping the real implementation for Step 2 (#856, blocked on #855). At checkpoint time, `git status --short` shows the spike agent has already started editing 4 files (`fk_restrict.rs`, `commit.rs`, `pre_commit.rs`, `tx_context.rs`) — **this diff is completely unreviewed/unverified**; the crush session was confirmed alive moments before this checkpoint (`crush sessions locks f40b-step1-spike` → `alive`).

**Standing operational context, unchanged from the prior checkpoint and still in force**: every delegated task this whole session (both the F-17-through-F-34 wave AND this new F-35-through-F-45+F-40b wave) followed brief-first discipline (a committed `docs/dev-artifacts/prompts/post-alpha/NN-*.md` file before launching each agent) and zero-trust verification (full diff read + my own `fmt`/`clippy`/`./scripts/test.sh` reruns before every commit). Mid-session the user gave an explicit new instruction to switch delegation from the `Agent` tool (`subagent_type: "sh"`) to `/crush` (`crush run --role smart/fast --session <slug> --timeout <N>m`) for the remainder of the session — every task since (F-35 onward) used `/crush`, not `Agent`. This zero-trust discipline caught real, substantive issues multiple times this session: the delegated F-39 agent corrected the original review's own wrong claim about `InMemoryStore` having native atomic transact; the F-40 agent's investigation independently discovered and rejected an unsound "fix" for the explicit-Snapshot gap with a genuinely deep technical argument; several agents found and fixed bugs beyond their literal brief scope when the SAME bug pattern appeared in an adjacent, un-cited function (F-42's `create_sorted_index_with_include`).

## Active goal

A session-scoped Stop hook is active with condition: "продолжай решать задачи с помощью агентов /crush, меджу задачами делай коммиты." (continue solving tasks via /crush agents, commit between tasks). Re-armed via `/babygoal` twice this session (once for the original F-17+ wave, once for this new F-35+ wave after the readonly review was decomposed into tasks). Standing directive: continuous execution through the TaskList via `/crush`, commit after each verified task, do not stop to ask unless genuinely blocked on a decision only the user can make.

## TaskList

### in_progress
- #855 F-40b Step 1 (P2, spike): design RI barrier for explicit-Snapshot FK parent mutations — a `/crush` agent (session `f40b-step1-spike`) is running; confirmed alive at checkpoint time. Diff in progress touches `fk_restrict.rs`/`commit.rs`/`pre_commit.rs`/`tx_context.rs` — UNVERIFIED. Check this first on resume: has the crush session finished? (`crush sessions locks f40b-step1-spike` or wait for the background command's completion notification — do NOT poll with sleep.)

### pending (blocked, in campaign order)
- #856 F-40b Step 2 (P2, implement): land the RI barrier for explicit-Snapshot FK parent mutations — blocked by #855's spike settling the design questions.
- #854 F-40b (P2, umbrella): blocked by #856; close once Step 2 lands.

### recently completed (this session's tail, in order)
- #853 F-45: KNOWN_LIMITATIONS.md FK re-sync (`34ec0292`)
- #852 F-44: conservative base deploy profile (`97cb9417`)
- #851 F-43: CreateRepo.path rejection + typed RepoEngine (`ab3647ce`)
- #850 F-42: index-create persist-before-publish ordering (`db23a1fd`)
- #849 F-41: MirroredStore mirror-first writes + hydration re-filter (`bb764ddd`)
- #848 F-40: FK discovery fail-closed + explicit-Snapshot memo (`5679edfa`)
- #847 F-39: MirroredStore::transact atomicity (`c27839fc`)
- #846 F-38: bootstrap TTL fail-closed (`56f41c74`)
- #845 F-37: keyset_safe write barrier (`83a621ee`)
- #844 F-36: FK cache generation-safe invalidate (`d3d06c82`)
- #843 F-35: FK cache on_update role flag (`44c4317e`)
- (earlier this session, all closed) #826 F-33 umbrella + Steps 2-6; #825 F-32 (all 3 pieces)

## Decisions

- **Delegation channel switched from `Agent`(sh) to `/crush`** partway through the prior session and held for this entire session — every task F-35 onward used `crush run --role smart/fast --session <slug> --timeout <N>m`, never the `Agent` tool.
- **F-39's atomicity fix delegates the durable subset to the mirror's REAL atomic transact (fjall's `OwnedWriteBatch`)**, rejecting the original review's own suggestion to use "`InMemoryStore`'s atomic transact" — verified `InMemoryStore` has NO such thing; both it and `MirroredStore`'s prior default relied on the same non-atomic trait loop.
- **F-40's explicit-Snapshot gap is scoped via a memo + follow-up task (F-40b), NOT rushed into F-40 itself** — the investigation found the "auto-upgrade explicit tx's isolation mid-flight" option is unsound (SSI read-set/predicate recording is gated on isolation AT READ TIME; escalating mid-flight produces a false Serializable with a blind spot for pre-upgrade reads) and the alternative ("RI barrier") is a 4-site commit-pipeline change deserving its own spike-then-implement split, mirroring F-28 Step 3 → Step 5.
- **F-40b decomposed into Step 1 (spike, #855) and Step 2 (implement, #856)** rather than one task, directly mirroring the F-28 precedent — Step 1 is a timeboxed investigation + minimal prototype, not the full implementation.
- **F-42 found the identical register-then-persist bug in `create_sorted_index_with_include`** (not explicitly named in the original review) and fixed it too, using the pre-existing `SortedIndexDefinition::with_included_interned` constructor rather than inventing a new API.
- **F-44 kept the base `server.example.ktav` matched EXACTLY to code-level defaults** (not the medium profile's values) and moved the old high-capacity numbers to a brand-new `server.large.example.ktav`, rather than just editing medium/small.

## Open questions

- **Is the F-40b Step 1 spike (`f40b-step1-spike` crush session) still running, finished, or dead?** This must be resolved before touching `fk_restrict.rs`/`commit.rs`/`pre_commit.rs`/`tx_context.rs` on resume. If a completion notification already arrived, read the diff and zero-trust-verify per the established discipline (read the diff, rerun fmt/clippy/tests, check the produced memo at `docs/dev-artifacts/research/f40b-ri-barrier-spike.md` for the settled design decisions) before committing. If the session appears dead with no notification, investigate via `crush sessions why f40b-step1-spike` / `crush sessions last f40b-step1-spike` before deciding whether to resume it or re-launch.
- No other open questions — the review's own findings are otherwise fully triaged and either closed or explicitly scoped into tracked follow-up work.

## Repo state

```
 M crates/shamir-engine/src/query/batch/fk_restrict.rs
 M crates/shamir-engine/src/tx/commit.rs
 M crates/shamir-engine/src/tx/pre_commit.rs
 M crates/shamir-tx/src/tx_context.rs
```
```
b9aec971 docs(prompts): brief for F-40b Step 1 -- RI barrier design spike (#855)
34ec0292 docs: F-45 -- re-sync KNOWN_LIMITATIONS.md's FK closure claims after F-35/F-36/F-40 (#853)
6328ec4b docs(prompts): brief for F-45 -- KNOWN_LIMITATIONS FK re-sync after F-35/F-36/F-40 (#853)
97cb9417 fix(server): F-44 -- base deploy profile made conservative, high-capacity numbers moved to server.large.example.ktav (#852)
07d0ba18 docs(prompts): brief for F-44 -- conservative base deploy profile + large profile (#852)
```

Note: the modified 4 files are the F-40b Step 1 spike agent's IN-FLIGHT, UNVERIFIED work — see "Open questions" above. A recurring session-only babysit cron is active (confirmed present at last tick — re-confirm via `CronList` on resume if uncertain).
