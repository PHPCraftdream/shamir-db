# Checkpoint — 2026-07-12 08:40

## Session summary

Continuing the standing audit-remediation campaign under `/babygoal` (goal condition: "Используй агентов @sx (agent). Реализуй все задачи" — implement all tasks, using `sx` sub-agents). `/babysit` cron `5bc3e6be` (every 15m, `7,22,37,52 * * * *`) has been ticking throughout and nudging resumption between turns. This window closed a long run of security/hardening tasks from the 2026-07-10 permission-system audit triage (`docs/audits/2026-07-11-security-permission-triage.md`), all via the established pipeline: investigate → write self-contained brief to `docs/prompts/audit/<NN>-*.md` → commit brief alone → launch `sx` implementing agent → orchestrator's OWN independent verification (never trust self-report) → `sx` adversarial review (sometimes two passes) → commit.

Tasks #538 through #547 were all completed and committed this session (see TaskList below and `git log`). Two of them surfaced real problems the orchestrator caught independently, not the implementing/reviewing agents:
- **#542** (destructive-op HMAC coverage extension): implementer's own diff moved `futures` into the wrong Cargo.toml section (fixed); adversarial review found 5 of 9 new ops shipped with only `hmac_required`/`accepted` tests, missing `hmac_mismatch` (fixed by orchestrator directly).
- **#543** (chown/chgrp/addGroupMember id validation): the orchestrator's own mandatory full `--full` test run (not the implementer's, not the reviewer's) surfaced 14+ pre-existing test failures across `shamir-db`/`shamir-server` — the "ids must resolve to a real principal" design conflicted pervasively with this codebase's established convention of treating numeric owner/group ids as free-standing. Scoped down by hand to keep only the two genuinely non-conflicting checks (chown-to-OWNER_SYSTEM-forbidden-for-non-System, and the `add_group_member` `GroupRef::Id` phantom-group closure), rewrote the test file honestly, re-verified clean, sent through a second adversarial review, then committed.
- **#546** (authz gate-coverage hardening cluster): after a clean adversarial review, the orchestrator's own combined 3-crate `--full` run showed 6 failures/5 timeouts; investigated before committing (per the standing "hangs/timeouts are bugs, never dismissed" rule) — traced 3 of them to an already-documented pre-existing flake in unrelated `shamir-engine` crash-injection tests (the test file's own comments describe the exact symptom), and the rest to resource contention from running three heavy crates' test suites concurrently (confirmed non-deterministic across repeated runs, and clean/fast when each crate's suite ran in isolation: `shamir-db` 538/538, `shamir-query-types` 289/289). Committed only after this was honestly reasoned through, not assumed.

Task **#545** (WASM sanitizer, the one CRITICAL-severity finding, gating FINAL-GATE) is also done: the implementing agent's own mandatory pre-implementation investigation established that `wasmtime`'s own `Linker::instantiate_pre` already rejects unsanctioned imports, so the new `wasmparser`-based structural sanitizer's real value is fail-fast cost savings + an explicit, auditable, drift-detected-by-test allowlist — not closing a hole the linker leaves open. The implementer's own review then caught a genuine gap (WASM *component*-encoding payloads silently bypassing the import scan) and fixed it; a second, high-effort orchestrator-launched review traced `wasmparser`'s actual parser state machine to confirm the fix's correctness and found nothing else.

Task **#551** (HMAC coverage for the 5 group-mutating ops left unwired by #542) is **currently IN FLIGHT, NOT YET COMMITTED** — a `sx` implementing agent (agentId `afd1a38f8705a10cf`, launched via background `Agent` call) is actively modifying `crates/shamir-client-ts/src/core/hmac.ts`, `crates/shamir-server/tests/hmac_gate.rs`, `crates/shamir-query-types/src/admin/access.rs`, `crates/shamir-query-types/src/hmac.rs`, `crates/shamir-server/src/db_handler/admin.rs`, `crates/shamir-query-builder/src/ddl/access_control.rs`, and related test files at the moment this checkpoint was written — no completion notification has arrived yet. **The orchestrator has NOT yet independently verified or reviewed this diff** — when resuming, the next step is: wait for the agent's completion, then run the full orchestrator verification pipeline (read diff, run `cargo check --workspace --all-targets`, run `./scripts/test.sh -p shamir-server -p shamir-query-types -p shamir-query-builder` plus the TS suite, launch an adversarial review, and only then commit) exactly as done for every other task this session — do NOT skip straight to committing #551's work.

An untracked file `D\357\200\272devrustshamir-db_audit_diff.patch` appeared in `git status` at checkpoint time (odd escaped-colon path, likely a stray artifact from some tool) — not something this session created deliberately; investigate before touching it, don't delete blindly.

**Remaining TaskList work after #551 lands:**
- **#548, #549, #550** — all three are DECISION tasks (unbinding Actor identity from `fxhash(username)`; unifying the parallel superuser/role privilege axis + reconciling desynced user/role stores; Root/User/Group always-open-meta + DAC posture). Per this campaign's established discipline (precedent: #512, #533), these require investigation + a written decision document + the user's EXPLICIT sign-off before any code lands — they must NOT be implemented directly by an agent without that sign-off step, even though the standing goal says "implement all tasks."
- **#529 FINAL-GATE** — blocked by `#507`, `#515`, `#516` (confirmed via `TaskGet` to be stale references to tasks that no longer exist under those ids, already absorbed into completed `#530`/`#531` earlier in the campaign — cosmetic blockedBy hygiene issue, not real blocking work) plus the two real blockers `#548`/`#549`.
- **#519** — CLIENT node-binding typed error, needs a napi-rs 3.x version bump; blocked on an explicit user decision not yet asked this session (does not block FINAL-GATE).

## Active goal

Session-scoped Stop hook (set via `/babygoal` earlier this session, still in force): condition text verbatim — `Используй агентов @"sx (agent)". Реализуй все задачи` (use `sx` agents; implement all tasks). The hook has already fired once this session (after task #545 was reported done and the orchestrator asked the user how to proceed) with feedback insisting on continuing since pending tasks remained — that feedback is why work continued through #546/#547 and into #551 without pausing again.

## TaskList

### in_progress
- #551 SECURITY: extend destructive-op HMAC confirmation to group-mutating ops (residual from #542) — implementing agent running, diff uncommitted, NOT yet reviewed/verified by the orchestrator

### pending
- #519 CLIENT: node-binding typed error .code/.retryable (needs napi-rs 3.x bump — user decision required)
- #529 FINAL-GATE: full fmt + clippy + test --full across workspace (blocked by: #507, #515, #516 [stale/already-absorbed], #548, #549)
- #548 DECISION: unbind security-Actor from fxhash(username), bind to stable user_id
- #549 DECISION: unify parallel superuser/role privilege axis + reconcile desynced user/role stores
- #550 DECISION: Root/User/Group always-open meta + wire-admin DAC posture + wire Definer/secret_grants gap

### recently completed
- #547 FIX: wire changePassword flow into the live server request dispatcher
- #546 HARDENING: unified authz-mapping registry + doc-guards + create-TOCTOU + coverage matrix
- #545 SECURITY: WASM module sanitizer/verifier before instantiation (CRITICAL residual)
- #544 FIX: WASM per-function egress capability + gate global_set into env.* namespace
- #543 FIX: validate chown/chgrp/addGroupMember target ids resolve to real principals
- #542 SECURITY: extend destructive-op HMAC confirmation to privilege/create/chmod/retention ops
- #541 SECURITY: effective_fn_actor missing-owner escalates definer function to System
- #540 SECURITY: resource_meta fail-open on storage error → fail-closed
- #539 FIX: MemBuffer dirty_nonempty transient reader-visible window + get() tombstone-poisoning
- #538 SECURITY/CORRECTNESS: close index2 lost-write race on the tx-commit path

## Decisions

- **#543 scope-down**: reverted the "owner/group/member id must resolve to a real persisted principal" checks entirely (chown, chgrp, add_group_member's user side) after the orchestrator's own full-suite test run proved they conflict with a pervasive, established codebase convention (numeric ids are free-standing, not required to be pre-registered). Kept only the two non-conflicting narrow closures: chown-to-`OWNER_SYSTEM` forbidden for non-System actors, and `add_group_member`'s `GroupRef::Id` phantom-group-record closure.
- **#544 empty-`net_grants` semantics**: chose "empty = full DB-wide allowlist" (backward-compatible) over "empty = no egress" (which would match `secret_grants`'s own precedent but silently break every pre-existing function that never sets `net_grants`) — deliberately documented as inconsistent with `secret_grants`'s precedent, with the tradeoff spelled out in both the code and commit message.
- **#545 sanitizer scope**: after confirming `wasmtime`'s own linker already rejects unsanctioned imports, kept the sanitizer scoped to (1) fail-fast cost savings and (2) an explicit, test-enforced-in-sync allowlist — did NOT add checks for exports/memory-minimum/start-function abuse after investigating and finding none of them load-bearing gaps beyond what the linker/Store already close.
- **#546 dispatcher wiring (item d)**: chose "redundant inline `authorize_access` check inside each group-CRUD method" over a typestate/proof-of-authorization design, since no existing typestate idiom was found in this codebase and a full redesign was judged out of scope for a LOW-severity hardening task.
- **#547 KDF fix**: implementer's own review caught `admin.kdf` (server default) being passed where `record.kdf_params` (user's own current KDF) was needed for both the persist path and the client-facing challenge echo — fixed before the orchestrator's own review pass, independently re-verified by a fresh review.

## Open questions

- **#551's diff is uncommitted and unreviewed** — see session summary above; this is the immediate next step on resume, not a question for the user, but flagged here so it isn't silently skipped.
- **#519** needs the user's explicit go-ahead for a napi-rs 3.x version bump before any code — not yet asked this session (does not block anything else).
- **#548/#549/#550** will each need a decision document + an explicit `AskUserQuestion` (or equivalent) sign-off before implementation — none of that investigation has started yet this session.
- The stray `D\357\200\272devrustshamir-db_audit_diff.patch` file in the working tree's untracked list — origin unclear, not investigated yet.

## Repo state

```
 M CLAUDE.md
 M crates/shamir-client-ts/src/__tests__/e2e-permissions.test.ts
 M crates/shamir-client-ts/src/__tests__/e2e-principal.test.ts
 M crates/shamir-client-ts/src/core/builders/__tests__/admin.test.ts
 M crates/shamir-client-ts/src/core/builders/admin.ts
 M crates/shamir-client-ts/src/core/hmac.ts
 M crates/shamir-client-ts/src/core/types/admin.ts
 M crates/shamir-query-builder/src/ddl/access_control.rs
 M crates/shamir-query-builder/src/ddl/tests/access_ddl_tests.rs
 M crates/shamir-query-types/src/admin/access.rs
 M crates/shamir-query-types/src/hmac.rs
 M crates/shamir-query-types/src/tests/hmac_tests.rs
 M crates/shamir-server/src/db_handler/admin.rs
 M crates/shamir-server/tests/hmac_gate.rs
 M crates/shamir-server/tests/permission_e2e.rs
?? "D\357\200\272devrustshamir-db_audit_diff.patch"
?? (a large number of stray root-level *.log files from earlier bench/test sessions — pre-existing clutter, not from this window's work; not cleaned up)
```
(the modified files above are task #551's in-flight, uncommitted diff from the still-running implementing agent)

```
555b7f8d docs(prompts): brief for #551 HMAC coverage for group-mutating ops
11ce5a3d fix(server): wire changePassword flow into the live request dispatcher (#547)
2ff522d6 docs(prompts): brief for #547 wire changePassword into server dispatcher
37fd5703 hardening(authz): unify per-op mapping + close gate-coverage gaps (#546)
4adac0c9 docs(prompts): brief for #546 authz gate-coverage hardening cluster
```
