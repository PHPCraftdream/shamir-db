# Checkpoint — 2026-08-06 16:15

## Session summary

Continuation of a very long session that started from the R0 correctness-freeze wave and its adversarial review (F-1..F-8). Since the last checkpoint (`2026-08-06-0345.md`), the following landed, each independently gate-verified (fmt/clippy/full test suites), never trusting a delegate's own "done" report at face value:

1. **F-3 + F-5..F-8 (#1030, #1031)** — closed: `DROP TABLE CASCADE` / `doctor::repair()` now hold `ddl_admission` for their whole index-mutation block; four LOW doc/hygiene nits fixed. Commit `67f5121d`, pushed, CI confirmed green.
2. **#1032 (CI-BLOCKING hang)** — root-caused via real CI reproduction + the session's own earlier fine-grained instrumentation: `rename_table_stores`'s forced `drain_to_history` and the background `Drainer`'s periodic `write_committed_batch_to_history` both call `self.history.transact(...)` on the SAME table's history store with zero coordination. Fixed with a new `MvccStore::drain_exclusive: tokio::sync::Mutex<()>` (forced drain holds it; Drainer's path only `try_lock`s and defers on contention). Commit `f7ff0f63`, pushed, **CI confirmed green (`success`) on SHA `f7ff0f63`** — the hang has not recurred and the fix is real, not a diagnosis.
3. **#1011 (P0-3a Slice 1)** — user explicitly chose to IMPLEMENT (not just document) the DROP-vs-in-flight-readers known gap for the regular index family, after a research pass (`Explore` agent) + a design pass (`@oh`, full plan saved to `docs/dev-artifacts/research/2026-08-06-p0-3a-reader-drain-gate-plan.md`, committed `e8c4d7a8`). Per the user's `/babygoal` instruction ("реализуй задачи с помощью /crush, между тасками делай коммиты..."), the remaining implementation was delegated to `/crush` (session id `p1011-slice1`) — the primitive itself (`ReaderDrainGate`, commit `0abf36f7`) was written directly by me first as the template; the wiring into `lookup_by_index`/`drop_index` + ~15 engine call sites was delegated to crush, which needed a resume (`--timeout 60m` fired mid-build-check; resumed same session with `90m`).
   - **Zero-trust verification caught THREE real bugs the crush delegate's own "all gates green" report missed entirely**: (a) a genuine deadlock in one of the new regression tests (`p1011_reader_blocks_drop_until_released`) confirmed via `nextest`'s own 180s TIMEOUT (reproduced twice) — a pre-drop sanity read warmed the posting cache for the same key a later "parked" read used, so the parked read hit the cache and never reached its pause hook, and the test awaited a notification that could never fire; (b) two sibling tests passed the wrong interned id (`key_id(&tbl, "status")`, the FIELD's id, where `lookup_by_index` needs the INDEX's own interned name `"status_idx"`) — one test's own assertion on the actual returned count caught it (0 vs 2 expected), a sibling test happened not to check the value and passed vacuously; (c) a SEPARATE, unrelated self-deadlock in `doctor.rs` — wrapping `verify()`'s entry-count block in `begin_write_barrier` (mirroring `repair()`) deadlocked against two PRE-EXISTING tests that intentionally park `create_index` mid-backfill (holding the same barrier) and call `verify()` concurrently to inspect the `Building` state — exactly the diagnostic scenario `verify()` exists for. Reverted that one piece.
   - All three fixed, full gate re-run clean multiple times (`shamir-index`+`shamir-engine --full`: 2523/2523, twice; `@oracle`: 2273/2273; workspace clippy clean). One interleaved run hit an unrelated, load-induced HNSW vector test TIMEOUT that passed cleanly in isolation and on a clean re-run — confirmed transient, not a regression.
   - Committed as `130087de` (the wiring + bug fixes) on top of `0abf36f7` (the primitive). `#1037`/`#1038` (sorted family / index2 slices) are now unblocked but NOT started.
4. **#1034, #1035, #1036, #1039** — found and triaged during this session's own CI/e2e investigation work (not pre-existing tasks): #1034 is a NEW vector-recall flake on ubuntu-latest below an already-calibrated floor (documented, NOT touched — needs its own empirical recalibration pass, explicitly deferred). #1035/#1036 (ts-e2e-nightly test fixes) — both root-caused as stale tests, not product bugs, fixed directly (commits `c014c2ef`, `b2117aec`). #1039 is a NEW, real, separately-tracked engine correctness gap found while investigating #1035: unique-index validation never cross-validates against another not-yet-committed claim staged earlier in the SAME open transaction — any transactional batch with two ops claiming the same unique key can silently corrupt the index instead of aborting. NOT fixed (test-side workaround only for #1035); this needs its own dedicated implementation pass.
5. CHANGELOG updated for #1011 and #1035/#1036 (commit `b939b3d5`).

**Governing instruction (still active, from the most recent `/babygoal`):** "реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу." This checkpoint step is in progress; CHANGELOG is done; next steps are commit all `.md` files and launch `@oh` for a review pass over everything landed this session.

**Not pushed yet**: commits `0abf36f7` through `b939b3d5` (everything since `f7ff0f63`) are LOCAL ONLY — not pushed to `origin/master`. Per the session's standing critical rule (never push without fresh explicit permission each time), pushing needs to be asked for separately before the `@oh` review or after, per user preference.

## Active goal

No `/goal` Stop-hook is currently armed (the earlier `/babygoal` "давай наладим все тесты, ci" governed the PRIOR portion of this session and was satisfied — all tests were green and #1032/#1030/#1031/#1033 were closed before this segment's new `/babygoal` was issued). The CURRENT governing instruction is the `/babygoal` quoted above (crush-delegate the remaining tasks, commit between them, then checkpoint+changelog+md-commit+`@oh` review) — tracked via the TaskList + this checkpoint, not a Stop-hook condition.

## TaskList

### pending
- #982 crush-fallback state (persistent sentinel, never completes) — sh ACTIVE (hard-limit)
- #1014 P1-1: doctor/repair в операторский интерфейс
- #1015 P1-2: DDL result contract (operation id + typed status)
- #1016 P1-3: убрать panic-by-default из Batch builders
- #1017 P1-4: typed public CreateIndex API
- #1018 P1-5 (решение оператора, не код): CREATE INDEX write-stall policy
- #1019 P1-6: Windows integration CI investigation
- #1020 REL-1: performance gate runner + baseline
- #1021 REL-2 (решение оператора, не код): version/CHANGELOG conflict
- #1022 REL-3 (решение оператора, не код): product composition + release drill
- #1024 OQL: SelectItem::Expression dead surface
- #1025 DDL: unified DROP INDEX by name
- #1026 PERF backlog (post-alpha, umbrella — needs its own decomposition before delegating)
- #1034 Flake: HNSW recall test below calibrated floor on ubuntu — needs empirical recalibration, explicitly deferred
- #1037 P0-3a Slice 2: ReaderDrainGate for sorted family (blockedBy #1011 — now unblocked, #1011 completed)
- #1038 P0-3a Slice 3: index2 lease-based drain guard (blockedBy #1011 — now unblocked)
- #1039 Engine gap: unique-index validation doesn't cross-validate against same-tx uncommitted claims — real correctness gap, needs its own implementation pass

### recently completed
- #1006-#1010, #1012, #1013, #1023 (R0 wave)
- #1027-#1031 (F-1..F-8)
- #1032, #1033 (hang fix, CI confirmed green)
- #1011 (P0-3a Slice 1)
- #1035, #1036 (ts-e2e-nightly test fixes)

## Decisions

- User explicitly chose to IMPLEMENT #1011 now (reader-epoch mechanism) rather than just document the known gap as an alpha limitation — the originally-recommended lower-risk option.
- Placed the `ReaderDrainGate` guard INSIDE the read chokepoint (`lookup_by_index`), never at the earlier planner "resolve" step — the only placement that keeps it the innermost lock in the `ddl_admission → bit → drain_writers → unique_write_lock → reader_gate` hierarchy, avoiding a constructible ABBA deadlock.
- Reverted `doctor::verify()`'s `begin_write_barrier` wrap (found via its own deadlock) rather than trying to make it non-blocking somehow — `verify()`'s diagnostic purpose is fundamentally incompatible with requiring DDL admission.
- Did NOT attempt to fix #1039 (the newly-found unique-validation gap) inline — scoped it as its own separate task, since it's a real, independent correctness issue deserving its own careful design pass, not a quick patch bolted onto the #1035 test fix.
- Continued to personally re-verify every delegated (crush) result against the zero-trust discipline even after the delegate self-reported "all gates green" — this caught 3 real bugs (2 test deadlocks/bugs, 1 production self-deadlock) that would have shipped broken/flaky otherwise.

## Open questions

- Should #1037/#1038 (sorted/index2 drain-gate slices) be started now via more `/crush` delegation, or is #1011's slice 1 sufficient for this session's scope? Not yet decided — next step per the governing instruction is checkpoint (this file) → commit .md → `@oh` review, not further slices.
- Push timing: the 5 unpushed local commits need explicit push permission before (or as part of) wrapping up — not yet asked.
- #1026 (PERF backlog) is an umbrella task that needs its own decomposition before any of it could be crush-delegated — not addressed this session.

## Repo state

```
?? docs/checkpoints/2026-08-06-0345.md
?? docs/checkpoints/2026-08-06-1615.md
```
(only the two checkpoint files are untracked; everything else this session produced is already committed locally, per the "commit between tasks" instruction)

```
b939b3d5 docs(changelog): entries for #1011 (P0-3a slice 1) and #1035/#1036 test fixes
130087de feat(index,engine): #1011 -- wire ReaderDrainGate into the regular index family (P0-3a slice 1, part 2/2)
b2117aec test(client-ts): #1035 -- for_each mid-loop-abort test used a unique-index trigger that can't detect intra-tx duplicates
c014c2ef test(e2e): #1036 -- composite sorted-index rejection test matched a stale server-only error string
0abf36f7 feat(index): #1011 -- ReaderDrainGate primitive (P0-3a slice 1, part 1/2)
e8c4d7a8 docs(research): #1011 -- reader-drain-gate implementation plan (@oh design pass)
f7ff0f63 fix(tx): #1032 -- mutual exclusion between forced drain_to_history and the Drainer's per-tick history write
67f5121d fix(engine,db,index,tx): F-3 + F-5..F-8 -- close remaining R0 adversarial-review findings (#1030, #1031)
```
