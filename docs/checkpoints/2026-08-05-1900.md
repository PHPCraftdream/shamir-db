# Checkpoint — 2026-08-05 19:00

## Session summary

This session continued the `/babygoal`-driven release-blocker campaign, picking up right after task-list revision, the #983 binary-value bug fix, #997's crash-recovery, #984's degraded-index gauge, #998's shared fixture matrix, and #985's DDL result-contract RFC (all covered in the prior checkpoint, `2026-08-05-1447.md`). All of that earlier work was closed out, then the user asked to review it: an `@oh` adversarial review of the whole #983/#984/#985/#997/#998 batch was launched and found ONE real blocker plus several minor nits.

**The real blocker (#1000):** `recover_hash_renames` cleared each recovered RENAME-INDEX tombstone entry individually via `IndexManager::clear_from_renaming`, which derives its persisted snapshot from an in-memory map that is ALWAYS EMPTY at open time (`load_renaming_list` never rehydrates it). So the first per-entry clear during recovery silently discarded any not-yet-processed sibling tombstone if recovery was then interrupted — reachable because a *failed* (not just crashed) rename legitimately leaves its tombstone behind. Independently re-verified the root cause by reading the code directly (not just trusting the review), then fixed with a new `IndexManager::clear_all_renaming` (clear once per family after the whole loop, mirroring #959's proven-safe pattern) plus a genuine regression test using a new between-entries pause hook. Verified via a manual fail/pass cycle: temporarily reintroduced the bug, confirmed the new test failed, restored the fix, confirmed it passed.

**Task-list hygiene:** at the user's request, all completed tasks were deleted from the TaskList (keeping it to only actionable items), and #963 (the operator-only bench-runner registration, undelegable) was deleted outright per the user's explicit call. The user then asked to (a) implement #986 (a previously-deferred typed `IndexSpec` enum for `CreateIndexOp`) now instead of deferring it further, and (b) decompose and implement #1001 (the three minor `@oh`-review nits), which was split into three sequential leaf tasks: #1002 (stale #983 comments), #1003 (the #984 gauge false-positive), #1004 (restoring #998's lost `vector_dim` boundary test coverage).

**#986** (typed `IndexSpec`, done via `crush --session task986-index-spec`): introduced an internal `IndexSpec` enum (`Hash`/`Sorted`/`Fts`/`Functional`/`Vector`) making the mutually-exclusive index combinations `try_build()` used to reject by hand unrepresentable at the type level (`Sorted` has a single field not a list, `include` only exists on `Sorted`, `Vector::dim` is `NonZeroU32`). `CreateIndexOp`'s wire shape stayed completely unchanged, proven by #998's fixture matrix staying green byte-identical and by none of 20 flat-literal test files needing changes. Verified independently (fmt/clippy/634 query-builder tests/23 TS tests, all re-run personally) before committing.

**Mid-#1002, `crush` hit a genuine hard quota limit** ("Weekly/Monthly Limit Exhausted", resets 2026-08-06 17:01), not a transient error — the already-armed `/crush-fallback` sentinel (task #982) was flipped to `STATUS: active, TRIGGER: hard-limit, AGENT: sh`, and all subsequent delegated work for this session routed through `Agent(subagent_type: "sh")` instead of `crush run`. (An earlier resume attempt into the same crush session also failed once on a simple invocation mistake — a missing `--role smart` flag — before the real hard-limit error surfaced on the correctly-formed retry.)

**#1002** (stale comments) had, by the time of the hard-limit hit, already been fully and correctly rewritten by the crush session before it was cut off mid-gate — verified the diff directly, re-ran both affected test scopes personally (eval_bytes 23/23, full shamir-client suite 63/63 including all 4 batch_for_each_e2e tests — the initial narrow filter attempt without `--full` misleadingly showed 0/0 for an integration-test file, resolved by re-running with `--full`), then committed.

**#1003** (fix #984's gauge false-positive on a healthy in-progress `CREATE INDEX`) was delegated to a fresh `sh` sub-agent, which reported finding the fix ALREADY IMPLEMENTED, uncommitted, in the working tree — later determined this matches the exact file set shown as modified/untracked in this session's very first `git status` snapshot, meaning it was leftover uncommitted work from before this session even began, never accidentally committed since every commit in this campaign used explicit file lists rather than `git add -A`. The design (a process-local `InFlightCreateCounter` + RAII `InFlightCreateGuard`, subtracted from the raw non-Ready tally) was sound and matched the brief's recommended approach precisely, but a genuine convention violation was found and fixed during independent review: an inline `#[cfg(test)] mod tests { ... }` inside `in_flight_create_guard.rs`, against this repo's explicit "tests live in a sibling `tests/` directory" rule — relocated to a new `in_flight_create_guard_tests.rs`, wired via the existing manifest. Full gate re-run personally (7/7 + 3/3 narrow scopes, 588/588 full shamir-server) before committing.

**#1004** (restore lost `vector_dim` boundary fixture rows) was delegated to `sh`, which correctly added the two fixture rows plus a documented, justified relaxation of two "exactly 12 reject cases" assertions (Rust + TS) to "at least 12", since the new second `VectorDimRequired` case legitimately makes a variant have >1 fixture row — a narrow, correct fix, not a validation-logic change. Verified independently (634/634 Rust, 25/25 TS) before committing.

**A significant tooling detour** occurred mid-#986: `git add` began failing with `.git/index.lock: File exists` on every attempt, even with no `git.exe` process running system-wide. Extensive investigation (checking crush sessions, worktrees, concurrent `claude.exe`/`node.exe` processes, even asking the user directly whether another session was active — user confirmed no) eventually traced the root cause to a self-inflicted bug: the orchestrating shell's cwd had silently drifted into `crates/shamir-client-ts` (from an earlier `npx vitest` invocation) and never returned to the repo root, so every `ls .git/...`/`find .git` check was looking in the wrong place while `git` itself (which walks up to find `.git`) correctly found a genuinely stale, 0-byte lock file at the true repo root — confirmed via an absolute-path `cmd //c dir`. Fixed by `cd`-ing back to the repo root and removing the confirmed-stale (0 bytes, no live process) lock, per the exact precedent already established earlier this session for an identical incident.

After #1004 closed, the CHANGELOG was updated with a new `[Unreleased]` entry covering #986/#1000/#1002-1004, and this checkpoint is being written as the next step, followed by committing all `.md` changes and launching a follow-up `@oh` review of this second wave of work, per the standing campaign instruction.

## Active goal

None — no `/goal` Stop-hook condition is in force; the session-scoped Stop hook currently active is the `/babygoal` condition text itself: "реализуй задачи с помощью @sh, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу" — this checkpoint is itself part of satisfying that condition.

## TaskList

### pending (sentinel, never completes)
- #982 crush-fallback state (persistent — do not complete): sh — ACTIVE (hard-limit), resets ~2026-08-06 17:01 (no auto-revert cron armed, per `/crush-fallback`'s Trigger-2 rule — manual `/crush-fallback clear` or a fresh `/crush` attempt after reset time)

### recently completed (this segment)
- #1004 restore lost vector_dim boundary test coverage from #998's deleted file
- #1003 fix #984 degraded-index gauge false-positive on healthy in-progress CREATE INDEX
- #1002 fix stale comments left over from #983's round-1 misdiagnosis
- #986 typed IndexSpec Rust enum + wire-compatible DTO for CREATE INDEX
- #1000 fix #997 recovery multi-entry tombstone data-loss found by @oh review
- (#957-999 and earlier — the full original release-blocker campaign chain plus the #983/#984/#985/#997/#998 wave — all completed; user had these deleted from the live TaskList this segment for hygiene, see `git log` / prior checkpoints for the full trail)

## Decisions

- Deleted #963 (operator-only bench-runner registration) outright at the user's explicit request, rather than leaving it parked — it was never actionable by any agent.
- Decomposed #1001 (the three `@oh`-review nits) into three sequential leaf tasks (#1002→#1003→#1004) with explicit `blockedBy` chaining, rather than one umbrella task, per `/babygoal`'s anti-umbrella-task rule.
- On hitting `crush`'s genuine hard quota limit mid-#1002, correctly distinguished it from a transient failure (per the `/crush-fallback` skill's Trigger-2 criteria — "Weekly/Monthly Limit Exhausted" with an explicit reset timestamp, no retry-after) and switched routing to `Agent(subagent_type: "sh")` for the rest of the session, updating the already-armed #982 sentinel rather than leaving it stale.
- When #1003's delegate reported finding the fix already implemented uncommitted in the working tree, chose to fully independently re-verify the diff (not just accept the report) precisely because it was NOT written by that delegate in this turn — this caught a real test-organization convention violation (inline `#[cfg(test)] mod tests`) that the delegate's own summary hadn't flagged.
- On the `.git/index.lock` mystery, investigated thoroughly (multiple process-enumeration angles, explicitly asking the user whether another session was active) before concluding it was a self-inflicted cwd-drift bug, rather than assuming/asserting a cause without verification.

## Open questions

- None blocking. `/crush`'s hard limit resets ~2026-08-06 17:01 — until then (or a manual `/crush-fallback clear`), all delegated work continues to route through `sh`.
- Pending: commit this checkpoint + the CHANGELOG update as the ".md" commit step, then launch a follow-up `@oh` review of the #986/#1000/#1002-1004 wave, per the standing campaign instruction.

## Repo state

```
M CHANGELOG.md
```
```
6dd1d1ae test(query-builder,client-ts): #1004 -- vector_dim boundary fixture rows
230ef178 docs(prompts): brief for #1004 -- vector_dim boundary fixture rows
72756201 fix(engine): #1003 -- fix #984 degraded-gauge false-positive on in-progress CREATE INDEX
215ab3ba docs(prompts): brief for #1003 -- fix #984 degraded-gauge false-positive
4eb8e8d9 docs(engine,client): #1002 -- fix stale comments from #983's round-1 misdiagnosis
39527291 docs(prompts): brief for #1002 -- fix stale #983 round-1-misdiagnosis comments
e93b870c refactor(query-builder): #986 -- typed IndexSpec IR for CreateIndex::try_build
8ff4a785 docs(prompts): brief for #986 -- typed IndexSpec enum (internal, builder-only)
4e9563fc docs(changelog): note #1000's #997 recovery data-loss fix
27f95817 fix(engine,index): #1000 -- recover_hash_renames could discard a sibling tombstone
```
