# Checkpoint — 2026-08-04 21:12

## Session summary

Continuation of a large `/babygoal`-driven campaign (originally: *"реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh"*), working through a 15-task release-blocker chain derived from `docs/dev-artifacts/research/2026-08-03-new-wave-readonly-review.md` (a NO-GO verdict for `v0.1.0-alpha.1`), plus 8 e2e-coverage-gap clusters (A-H) discovered by a dedicated inventory task (#964), plus a module rename (`legacy`→`base_index`, #973).

**Fully completed this session** (all committed, all independently re-verified zero-trust before commit — never trusting a sub-agent's own claimed diff/test-run):
- All 7 P0 correctness blockers (#957-962, #972): write-barrier multi-owner race, tx/index lifecycle staleness (regular/unique/sorted/index2), DROP INDEX crash-safety (all 4 families), corrupt-unique fail-open fix, index2 + sorted RENAME INDEX persistence/resumability.
- Repo-wide rename #973 (`crates/shamir-index/src/legacy/` → `.../base_index/`, 44 files).
- The full 8-cluster e2e coverage sweep (#974-981): replication lifecycle, HAVING/EXPLAIN/PlanType, functional/FTS breadth, covering/composite indexes + DROP if_exists, DDL singletons (rename_db/describe_table/changePassword — a real SDK gap closed with new `buildAuthMessageCp`/`ShamirClient.changePassword()`), keyset after_id/NULLS/PaginationInfo, and two low-priority OQL/DDL grab-bags. Net: JS e2e suite grew 130→147 tests (19 files), TS vitest suite grew 1028→1055 tests (57 files).
- 4 of 7 P1 tasks: #965 (typed `TryIntoBatchOp`/`Batch::try_*` methods added alongside the existing panicking API — additive, no signature changes), #966 (doctor::verify() now reports a stuck-`Building` regular/unique/sorted index as unhealthy with a diagnostic message, mirroring the existing index2 pattern — scoped down from a full "self-heal on open" per the review's own sanctioned fallback), #967 (every CREATE/DROP/RENAME INDEX site that can fail after an earlier phase's durable persist now returns an enriched, honest error explaining what was persisted and how to check/resolve — NOT a wire-protocol redesign), #968 (fixed `Store::transact`'s misleadingly-framed doc comment + completed its stale caller list — doc-only, zero logic touched, scoped down from the review's suggested full API-method split).

**In flight right now**: #969 (P1-4, CREATE INDEX write-stall) is running via a backgrounded `crush run --session p1-4-create-index-stall` (task id `bwjg5gbfd`), scoped to the review's own "alpha-minimum" checklist (KNOWN_LIMITATIONS.md doc entry, progress logging during backfill, client-timeout documentation, cross-reference to #966's health visibility, and extending the ALREADY-EXISTING `f78_writer_latency.rs` benchmark to 100k/1M rows — NOT the "post-alpha online build" redesign, which the review itself explicitly defers separately). `git status` shows it has already touched `ddl.ts`, `f78_writer_latency.rs`, `table_manager_index_mgmt.rs`, `table_manager_sorted_index.rs`, `index_manager.rs` — uncommitted, not yet verified. **Next action once it completes: read its report, review the diff (especially the new benchmark numbers at 100k/1M rows), independently re-run `cargo fmt -p shamir-engine --check` / `cargo clippy -p shamir-engine --all-targets -- -D warnings` / `./scripts/test.sh -p shamir-engine` (+ `npx tsc --noEmit` in shamir-client-ts since ddl.ts was touched), then commit.**

**Still pending after #969**: #970 (P1-6, typed `IndexSpec` instead of stringly-typed index builder options — blocked by #969), #971 (fix stale comments/contract mismatches found by the review — intentionally last in the P1 chain, blocked by #970), #963 (P0-6 release perf-gate — left `pending`, NOT delegatable, pure human/hardware/GitHub-admin action per `docs/guide-docs/CI_PERF_GATE_RUNBOOK.md` which already fully documents the manual steps).

**New follow-up tasks filed this session** (discovered during verification, deliberately scoped OUT of their parent task and tracked separately rather than silently dropped or scope-crept): #983 (real bug: a `Uint8Array` field value written via `write.insert()` round-trips corrupted into a plain `{"0":0,"1":1,...}` object instead of binary — root cause not yet located, several conversion sites already ruled out, documented for whoever picks it up), #984 (wire degraded base-index health into `shamir-server`'s `/readyz`/`/metrics` — deferred from #966 since `/readyz`'s own doc says it must stay cheap/subsystem-independent), #985 (design a unified DDL "operation ID + status" wire contract — deferred from #967, a large cross-cutting wire-protocol change deserving its own RFC-style task).

**Standing directive** (reaffirmed by the user mid-campaign with "Продолжать всю цепочку" after I paused to check in once scope grew large): keep working the whole chain autonomously via `/crush` as the primary transport (with `sh` armed as fallback per `/crush-fallback @sh`, only for peak-hours refusal or hard quota limits — used once, then reverted once the window closed per the user's explicit "переключаемся на агентов /crush"). Between-task commits are mandatory; every commit is preceded by a committed brief in `docs/dev-artifacts/prompts/release-blockers/` and followed by MY OWN independent re-run of the full gate (never trusting the sub-agent's self-reported numbers) before committing. `/babysit` (15m, job id confirmed via `CronList`, currently `909f6b8b`) is armed and has been ticking throughout, correctly reporting "still running #N" on live sessions and once correctly resuming a task whose crush session had died (a "Process restarted" transient failure, self-healing per policy — required a manual `crush sessions kill` + relaunch when the user explicitly said "перезапусти агента" for a genuinely hung session at least twice this session).

**End-state still owed once the P1 chain (#969-971) finishes**: update `CHANGELOG.md`, commit all `.md` files, and launch an `@oh` review agent to check the entire campaign's work — per the original `/babygoal` instruction. None of these three final steps have been done yet.

## Active goal

None — this session uses `/babygoal` + `/babysit` (TaskList-driven), not a `/goal` Stop-hook condition.

## TaskList

### in_progress
- #969 P1-4: долгий CREATE INDEX блокирует все writes на время полного scan (blockedBy: none — #968 resolved)

### pending
- #963 P0-6: release perf-gate неработоспособен — нет bench-baseline.json и не зарегистрирован runner (no blockers; intentionally left for a human operator, not delegatable)
- #970 P1-6: индексный builder stringly typed — ввести типизированный IndexSpec (blockedBy: #969)
- #971 Исправить stale comments и contract-расхождения, найденные ревью (blockedBy: #970)
- #982 crush-fallback state (persistent sentinel — do not complete): sh — armed
- #983 BUG: binary (Uint8Array) field value corrupted on write.insert round-trip
- #984 Wire degraded base-index (Building) state into server readiness/metrics
- #985 Design a unified DDL result contract (operation ID + status) across the wire protocol

### recently completed
- #968 P1-3: Store::transact contract двусмыслен (doc-only fix)
- #967 P1-2: CREATE/RENAME/DROP могут вернуть Err после уже применённой live-мутации
- #966 P1-1: legacy Building-индексы не self-heal-ятся при open
- #965 P1-5: Query Builder паникует через основной Batch API вместо typed error
- #981 e2e gap cluster H (low)
- #980 e2e gap cluster G (low)
- #979 e2e gap cluster F: keyset after_id/NULLS/PaginationInfo
- #978 e2e gap cluster E: rename_db/describe_table/changePassword
- #977 e2e gap cluster D: covering/composite/if_exists
- #976 e2e gap cluster C: functional/FTS breadth

## Decisions

- Scoped every P1 task down to the review's own explicitly-sanctioned "minimum/alpha bar" fallback wherever the review offered one, rather than attempting the larger "full fix" in a single delegated pass — judged the right risk/reward tradeoff given each larger alternative (full open-time self-heal for #966, a wire-protocol "operation ID" contract for #967, a full `Store::transact` API split for #968) would be substantially larger, riskier, and cross-cutting for uncertain marginal benefit within a single 60-minute delegated run. Filed each deferred larger piece as its own tracked follow-up task (#984, #985) rather than silently dropping it.
- For #967, deliberately chose additive error-message enrichment (using the ALREADY-EXISTING `BuilderError`-style typed errors) over the review's suggested rollback mechanism or wire-contract redesign — the idempotent-recovery half was already built this session (#959/#961/#962/#972); the only missing piece was honest error reporting.
- For #968, rejected the review's suggested full trait-method split (`apply_batch_ordered`/`atomic_transact`) as too invasive (6+ production call sites across 2 crates, including this session's own just-landed P0 work) for what is fundamentally a misleading-doc-comment problem, not a runtime bug.
- For #969, discovered the "100k/1M row benchmark" ask was ~90% already satisfiable by extending an EXISTING bench (`f78_writer_latency.rs`, currently only run at 5k rows) rather than writing one from scratch — significantly narrowed the brief's scope once found.
- Restored a brief file (`13-e2e-cluster-e-ddl-singletons.md`) that a crashed crush process had deleted from the working tree after it was already committed — used `git checkout HEAD -- <path>` (safe: recovering committed content, not discarding uncommitted work) rather than treating the disappearance as intentional.

## Open questions

- None outstanding from the user. The only "pause and ask" moments this session were the user interrupting an `AskUserQuestion` tool call twice with plain-text replies ("задай мне вопрос, чтобы встать в паузу" then "остальные задачи будем решать?") — both handled inline without re-invoking the tool, and the user's intent to continue the full chain was reconfirmed both times.

## Repo state

```
 M crates/shamir-client-ts/src/core/builders/ddl.ts
 M crates/shamir-engine/benches/f78_writer_latency.rs
 M crates/shamir-engine/src/table/table_manager_index_mgmt.rs
 M crates/shamir-engine/src/table/table_manager_sorted_index.rs
 M crates/shamir-index/src/base_index/index_manager.rs
?? docs/checkpoints/2026-08-04-1215.md
```

(The 5 modified files above are #969's in-flight, UNCOMMITTED work from the still-running `crush` session — do not commit until independently verified per the "next action" note in the session summary.)

```
a4febf2f docs(prompts): brief for #969 -- P1-4 CREATE INDEX write-stall alpha bar
72ff90ed docs(storage): #968 -- P1-3 fix Store::transact's misleading doc framing
0f39d278 docs(prompts): brief for #968 -- P1-3 transact doc framing + caller audit
be79e1b0 feat(index,engine): #967 -- P1-2 enrich DDL errors after partial persist
057f157d docs(prompts): brief for #967 -- P1-2 DDL partial-mutation error context
```
