# Checkpoint — 2026-08-13 01:30

## Session summary

Long continuation of the standing `/babygoal` session that has now closed EVERY actionable task on the TaskList. This checkpoint follows `2026-08-12-1730.md` and covers the final stretch: `#1100`, `#1104`, `#1101`, `#1095`, and `#1103` — all now completed, gate-verified, and committed.

`#1104` (test-coverage gap in `update_tx_bytes`'s `is_record_touched` closures) closed first — its `/crush` worktree session had honestly reported a `--timeout 60m` deadline expiry, not a real failure; resumed with `--timeout 90m` and it completed cleanly. Personally re-verified fmt/clippy/full suite, merged the worktree branch into `master`, removed the worktree.

`#1100` (DELETE/UPDATE planned from a stale snapshot never removes the record's current posting) took two full rounds. Round 1's `/crush` session chose the right fix approach (commit-time re-derivation, mirroring `rederive_base_index_ops_post_stage`) and honestly reported 2 of 4 new tests still failing. The orchestrator root-caused this personally via temporary `eprintln!` probes (reverted before commit): `read_pre_tx_bytes`/`data_store.get()` reads the RAW physical WAL/data store, but in this codebase's lock-free commit path the actual physical write happens in a **detached `tokio::spawn`** (`materialize_async_tail`) that `commit_tx` never awaits — so an already-MVCC-visible, durably-committed record can still read as `NotFound` through the raw store. Wrote a precise round-2 brief naming this exact race; round 2 replaced the read with `tbl.read_one_tx_bytes(rid, None)` (reads through the MVCC-visible overlay instead). Personally mutation-tested both fixed call sites and ran the full 3-crate gate (3043/3043) before committing.

`#1101` (Step 2's `released -> continue` shortcut skips the durable check for a same-tx net-release) is the session's most interesting verification catch: the `/crush` delegate chose the correct fix shape but **honestly reported it could not construct a concurrent reproduction test**. Rather than accepting that gap, the orchestrator traced Step 1/Step 2's bookkeeping by hand and discovered the delegate's own reproduction attempt was being caught by an UNRELATED mechanism first (Step 1's `unverified_removes` durable check retracting a stale bare `RemovePosting`, which incidentally "un-releases" the key before Step 2 ever runs — masking the actual bug). Constructed a reproduction shape that survives Step 1 untouched (a net-release built from a clean `SetPosting`-then-`RemovePosting` pair for a record the tx itself created, never touching a pre-existing durably-owned record), personally wrote it, confirmed it passes on the fixed code, then confirmed it genuinely fails when the fix is reverted — a real Red→Green mutation test the delegate's own session never achieved.

`#1095` (`SegmentSet::inner` off `std::sync::Mutex`) and `#1103` (`ReaderDrainGate` loom infra) were both PERF/infra investigations that concluded cleanly with **no production code changes** — the correct, successful outcome per each task's own explicit framing. `#1095`'s isolated bench proved `SegmentSet::inner`'s mutex has negligible contention (the WAL append latency rise is fully attributable to `WalGroupCommit`'s own coordination layer, not this specific lock); `#1103` wired loom model-checking infra for `shamir-index` (mirroring `shamir-engine`'s exact pattern) and proved the current `SeqCst` ordering's interleaving contract holds, deliberately declining an `Acquire`/`Release` relaxation as unfamiliar, high-risk territory for modest (~15-17%) gain. Both were personally re-verified: the orchestrator re-ran the actual bench/loom-test numbers itself (not just trusted the delegate's claim), and for `#1103` specifically, mutation-tested the loom model by deliberately reordering the reader's atomics to reproduce the exact bug the model exists to catch, confirming the model is not a vacuous pass.

Two minor documentation issues were caught and fixed directly by the orchestrator during verification: a wrong date (`2026-01-10` instead of `2026-08-13`) in `#1095`'s `CLAUDE.md` update, and confirmation (not a fix — it's pre-existing) that `shamir-engine`'s own established loom pattern shares the identical `cargo clippy --all-targets --features loom` limitation `#1103`'s mirror inherited (loom is a dev-dependency, invisible to the plain lib-target compile) — this is inherent to the established pattern, not a defect either task introduced.

**TaskList is now fully closed** except two structurally non-actionable sentinels: `#982` (persistent crush-fallback sentinel — designed to never complete) and `#1049` (P1-6 follow-up — explicitly "do not start until new data appears"). Every other task from `#1054` through `#1104` is `completed`. The standing `/babygoal`'s final step — `/checkpoint` (this file), CHANGELOG update (done incrementally per-task throughout, already covers every closure with full technical detail), commit all `.md` files (every brief is committed under `docs/dev-artifacts/prompts/release-blockers/`; CHANGELOG.md is committed after every entry; this checkpoint file itself is intentionally left uncommitted per the skill's own instruction) — is now complete except for the final `@oh` adversarial review of ALL the accumulated work, which the user's earlier explicit correction deferred to exactly this point (once every task is resolved).

## Active goal

Standing `/babygoal` Stop-hook: "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу" — this checkpoint marks the "после завершения всей работы" point; the `@oh` review is the one remaining step.

## TaskList

### pending (deliberately non-actionable, not part of "all work")
- #982 crush-fallback state (persistent sentinel — structurally never completes)
- #1049 P1-6 follow-up (non-blocking — explicitly "do not start until new data appears")

### recently completed (this segment, chronological)
- #1104 update_tx_bytes's is_record_touched closures — test-coverage gap closed
- #1100 DELETE/UPDATE from a stale snapshot never removes the record's current posting — 2 fix rounds, root-caused an async-tail MVCC-visibility race personally
- #1101 Step 2's released->continue shortcut skips the durable check for a same-tx net-release — personally constructed the reproduction the delegate couldn't
- #1095 SegmentSet::inner off std::sync::Mutex — isolated, confirmed negligible contention, no migration
- #1103 ReaderDrainGate phase 2 — loom infra for shamir-index, SeqCst ordering proof, no relaxation attempted

(Full history back through #1054 also completed — see prior checkpoints, notably `2026-08-12-1420.md` and `2026-08-12-1730.md`, for the earlier arc.)

## Decisions

- **Deferred ALL `@oh` review to session-end**, per the user's explicit correction earlier this session — no per-task reviews for `#1100`/`#1101`/`#1095`/`#1103`, unlike the pattern this session had drifted into for `#1094`/`#1099`/`#1102`.
- **For `#1100`, traced the actual root cause personally** (temporary `eprintln!` instrumentation, reverted before commit) rather than accepting round 1's "logic looks right but tests fail" report at face value — found the `materialize_async_tail`/detached-`tokio::spawn` staleness race, wrote a brief naming it precisely rather than a vague "debug and retry" instruction.
- **For `#1101`, did not accept "could not construct a reproduction" as sufficient** — traced Step 1/Step 2's bookkeeping by hand to find a reproduction shape immune to the unrelated masking mechanism, and personally proved both directions (fails without the fix, passes with it).
- **For `#1095` and `#1103`, accepted "no code change, investigated and confirmed clean" as a fully successful close** rather than pressuring either delegate toward a speculative migration on the highest-blast-radius (WAL) or highest-unfamiliarity (loom) code — matching each brief's own explicit framing that this is the correct outcome if the data supports it.
- **Personally re-ran every claimed bench/test number** rather than trusting delegate reports verbatim — caught the `#1095` doc date error this way; confirmed `#1103`'s loom-clippy limitation is pre-existing (not a new defect) by testing `shamir-engine`'s own established pattern under the same invocation.

## Open questions

None blocking. The TaskList's only remaining open item is the final `@oh` review of the full accumulated session's work (per the standing `/babygoal`), not yet launched as of this checkpoint.

## Repo state

```
?? devrust.cargo-target-check/
?? docs/checkpoints/2026-08-12-1420.md
?? docs/checkpoints/2026-08-12-1730.md
```
(Working tree otherwise clean — every substantive change committed. The two untracked prior checkpoints and this one are intentionally left for the user to `git add`, per the checkpoint skill's own instruction. `devrust.cargo-target-check/` is a stray scratch directory from an earlier `CARGO_TARGET_DIR` experiment, harmless and untracked.)

```
aa1a620b docs: CHANGELOG entry for #1103's closure (ReaderDrainGate loom infra)
9932d6d8 perf(index): #1103 -- ReaderDrainGate phase 2, loom infra + SeqCst ordering proof
b2b3a280 docs(prompts): brief for #1103 -- ReaderDrainGate phase 2, loom infra for shamir-index
197765b9 docs: CHANGELOG entry for #1095's closure (SegmentSet::inner contention isolation)
390d779a perf(wal): #1095 -- isolate SegmentSet::inner's contention contribution, confirm negligible, no migration needed
```
