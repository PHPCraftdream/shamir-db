# Checkpoint — 2026-07-24 01:00

## Session summary

This session ran a very long, multi-wave remediation campaign on the shamir-db cursor-streaming feature (FG-5) and adjacent areas. It started by closing out the prior RI-15/FG-5 campaign (CHANGELOG, checkpoint, push, green CI), then executed **Wave A** (#760–#766, 7 tasks: cursor ACL, terminal-page leak, page_size validation, byte-budget wiring, keyset tie-breaker, bootstrap-token rotation, docs truth pass), **Wave B** (#767–#775, 9 tasks: concurrent-DELETE snapshot fix, byte-budget upfront-reserve, optional FetchNext page_size, has_more peek-ahead, with_version rejection, TS CAS bigint types, restore reordering+manifest hardening, registry chores, release-workflow gate), and **Wave C** (#776–#780, 5 tasks: polish batch, cursor test-coverage gaps, AsOf batched-lookup perf, streaming backup hash, exact Big-number comparisons) — every single one of these 21 tasks is `completed`, each via the same disciplined cycle: a committed brief in `docs/dev-artifacts/prompts/post-alpha/` before every `@sh` agent launch, then the full diff personally read and fmt/clippy/full-test-suite re-run before committing, never trusting an agent's own "done" claim.

After Wave C finished, the user gave a compound instruction (still being executed): update CHANGELOG, `/checkpoint`, commit all `.md` files, push, then launch `@fh` for a broad review of everything done, then create and execute tasks from that review's findings. This became task #781 (now `completed`): CHANGELOG's `[Unreleased]` section was consolidated with entries for every Wave B/C item, a checkpoint was written (`2026-07-23-0000.md`... actually `2026-07-24-0000.md`, this session's date rolled to 2026-07-24 partway through), everything was pushed to `origin/master` (46 commits at that point), and `@fh` was launched — it found **two release-blocking bugs** the earlier reviews missed: a keyset cursor **livelock** when an ORDER BY tie run exceeds `max_cursor_page_size` (N-1), and **silent row loss** for Null/missing/NaN/mixed-type ORDER BY values on the keyset path (N-2) — plus confirmed a plain `Int`↔`F64` f64-collapse precision bug that CR-C5 had found-but-not-fixed and left untracked (N-3), and several MED/LOW polish items (N-4 through N-9).

This spawned **Wave D** (#782–#786, 5 tasks), sequenced with `blockedBy` chains since most touch `cursor_handlers.rs`. **#782 (CR-D1, the livelock)** and **#783 (CR-D2, the null row-loss)** are both `completed` and pushed — CR-D1 fixed via a one-time, detected-failure-condition fallback from keyset to offset-bookmark pagination when the tie-run ceiling genuinely can't make progress (verified exactly-once correct via a dedicated transition test); CR-D2 fixed via a cheap one-time `IS NULL` existence probe at `CreateCursor` time that falls the whole cursor back to offset mode when the ORDER BY column contains nulls (mixed-type/NaN sub-cases were investigated and explicitly documented as a still-open residual, not fixed, since no cheap detection primitive exists for those — this was an honest, deliberate scope decision, not an oversight). **#784 (CR-D3, exact Int↔F64 comparison)** is `in_progress` — its brief (committed) gives a fully-derived exact comparison algorithm (bounds-check against exact ±2^63 f64 literals, then floor/fract tie-break, no BigInt needed) and the `@sh` agent is currently running in the background implementing it; **this was interrupted by the user's `/checkpoint` command before I could review its diff or run the gate**, so its actual completion state is UNKNOWN as of this checkpoint — the background agent may still be running, may have finished, or may have errored; check for its completion notification before doing anything else with #784.

**Currently uncommitted in the working tree** (visible via `git status --short` at checkpoint time): `CHANGELOG.md`, `crates/shamir-engine/src/query/filter/resolve.rs`, `crates/shamir-engine/src/query/filter/tests/eval_tests/dec_cross_type_tests.rs`, `crates/shamir-engine/src/query/read/order.rs`, `docs/guide-docs/KNOWN_LIMITATIONS.md` — these are presumably the CR-D3 agent's in-progress or just-finished edits per its brief's file scope (`resolve.rs`, `order.rs`, their tests, `KNOWN_LIMITATIONS.md`, and it was also asked to update a CHANGELOG bullet). **None of this has been reviewed or gated yet** — do not commit it blindly; follow the same zero-trust diff-read + fmt/clippy/test cycle used for every other task this session before committing.

**Tasks #785 (CR-D4: cursor double-serialization + ops docs) and #786 (CR-D5: restore error-path polish + hygiene nits)** remain `pending`, blocked in sequence behind #784 → #783 → (already satisfied). No briefs written yet for these two.

## Active goal

None (`/goal` was never armed this session — progress tracked via TaskList + a `/babysit` cron armed early in the session, 15-minute interval, still presumably active; it self-deletes once the TaskList empties). Verify with `CronList` if resuming and the cron's state matters.

## TaskList

### in_progress
- #784 CR-D3. Int<->F64 exact comparison fix + KNOWN_LIMITATIONS.md entry (N-3) — brief committed (`cb350e71`), `@sh` agent launched in background, **interrupted before review** — check for its completion notification first.

### pending
- #785 CR-D4. Cursor path: single-serialization fix + upfront-reserve ops docs (N-4 + N-5)  (blockedBy: #783 — already satisfied, will be ready once #784 completes per its own chain)
- #786 CR-D5. Polish batch: restore error-path, drain_all decision, hygiene nits (N-6 + N-7 + N-9)  (blockedBy: #785)

### recently completed
- #783 CR-D2. Keyset null/incomparable row loss (`0ac28195`)
- #782 CR-D1. Keyset tie-run-ceiling livelock (`c1766ea0`)
- #781 Post-Wave-C wrap-up (CHANGELOG/checkpoint/push/@fh review/Wave D task creation)
- #780 CR-C5. Exact Big-number comparisons (`e916130b`)
- #779 CR-C4. Streaming backup hash (`5c8a8404`)
- (#760–#778 also all completed earlier — see `2026-07-24-0000.md` checkpoint for their commit hashes)

## Decisions

- **CR-D1's fix uses a ONE-TIME, detected-failure-condition mode flip from Keyset to Offset pagination** (not a typed-error fallback, which was the brief's acceptable-minimum option) — chosen because `CursorState::offset` was already being tracked in parallel on the Keyset branch, making the transition provably exactly-once-correct without needing new bookkeeping.
- **CR-D2 fixed ONLY the Null/missing-value sub-case, not mixed-type or NaN** — deliberate scope boundary per the brief's own tiered permission, since `Filter::IsNull` already makes the Null case cheaply detectable at `CreateCursor` time but no equivalent cheap primitive exists for "is this field a different type" or "is this field NaN." Documented precisely as a still-open residual in both `CURSORS.md` and `KNOWN_LIMITATIONS.md`, with a test pinning the current accepted gap.
- **Wave D tasks are chained via `blockedBy` in the same file-conflict-avoidance pattern used in Waves A/B/C** (#783 blockedBy #782, #785 blockedBy #783, #786 blockedBy #785) since most touch `cursor_handlers.rs`.
- **CR-D3's exact-comparison algorithm was fully derived and verified IN THE BRIEF ITSELF** before delegating (bounds against exact ±2^63 f64 literals + floor/fract tie-break) — this was a deliberate choice to reduce risk on a numerically-sensitive fix, giving the implementing agent a pre-verified algorithm to implement and validate rather than inventing one under time pressure.

## Open questions

- **#784's actual state is unknown** — the `/checkpoint` command interrupted the session before the background `@sh` agent's completion notification arrived. The very next action after resuming should be: check for a pending task-notification for that agent; if none has arrived, it may still be running (do not re-launch a duplicate agent — wait for the notification per this session's standing anti-poll discipline); if one already arrived and was missed, retrieve its result and proceed with the normal zero-trust diff review + gate cycle before committing.
- The uncommitted working-tree changes (`CHANGELOG.md`, `resolve.rs`, `order.rs`, their tests, `KNOWN_LIMITATIONS.md`) have NOT been reviewed — treat them exactly as any other delegated agent's output requiring full verification, not as already-trusted.

## Repo state

```
 M CHANGELOG.md
 M crates/shamir-engine/src/query/filter/resolve.rs
 M crates/shamir-engine/src/query/filter/tests/eval_tests/dec_cross_type_tests.rs
 M crates/shamir-engine/src/query/read/order.rs
 M docs/guide-docs/KNOWN_LIMITATIONS.md
```
(These are presumed to be CR-D3's in-progress/completed edits per its brief's file scope — UNVERIFIED as of this checkpoint.)

```
cb350e71 docs(prompts): brief for #784 -- CR-D3 exact Int<->F64 comparison
0ac28195 fix(server): CR-D2 -- keyset cursor silently loses Null ORDER BY rows (release blocker)
4c371826 docs(prompts): brief for #783 -- CR-D2 keyset null/incomparable row loss
c1766ea0 fix(server): CR-D1 -- keyset cursor tie-run-ceiling livelock (release blocker)
046d1333 docs(prompts): brief for #782 -- CR-D1 keyset tie-run-ceiling livelock
```

Branch `master`. `origin/master` was last pushed at commit `3e85e976` (the post-Wave-C wrap-up point, 46 commits). Every commit since then (`bba7effd` through `cb350e71` — the `@fh` review commit plus all of Wave D's briefs/fixes so far) is LOCAL ONLY, not yet pushed. No push has been authorized for the Wave D work specifically yet — the user's earlier "push" authorization was scoped to the post-Wave-C wrap-up, not standing for all future work; confirm before pushing Wave D's commits.
