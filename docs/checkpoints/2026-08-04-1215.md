# Checkpoint — 2026-08-04 12:15 [release-blocker-campaign]

## Session summary

This session continued directly from the prior session's `@oh` read-only review of the new wave (102 commits vs. the previous review point), which produced `docs/dev-artifacts/research/2026-08-03-new-wave-readonly-review.md` — a NO-GO verdict for `v0.1.0-alpha.1` citing 6 P0 correctness/release blockers and a set of P1 items. The user asked to create 15 tasks from that review (#957-971) and one important standalone task (#964: TS e2e must cover the full OQL/DDL surface, not selectively). The user then invoked `/babygoal` with the strategy "implement via /crush, commit between tasks; after everything, checkpoint + changelog + commit all .md + run @oh review".

The session executed a strict prompt-first, zero-trust delegation loop for each task: write a detailed brief to `docs/dev-artifacts/prompts/release-blockers/<NN>-<name>.md`, commit it, launch `crush run --role smart --session release-blocker-<id> --timeout 60-90m` (or, during a `zai` peak-hours window 08:00-12:00 today, fall back to `Agent(subagent_type: "sh")` per an armed `/crush-fallback sh`), then independently re-verify every claim: read the full diff personally, cross-check any novel claim against the actual source (e.g. `RecordId::system()`'s 12-byte truncation collision risk, verified by hand multiple times), re-run `cargo fmt --check` / `cargo clippy --workspace --all-targets -- -D warnings` / `./scripts/test.sh` (scoped or full-workspace as appropriate) myself before committing. No task was committed on the sub-agent's word alone.

**All 7 P0 correctness blockers are now fixed, verified, and committed**, plus one large user-requested rename:
- #960 (P0-4, `5e766820`): corrupt unique-index posting now fails closed (typed `Codec` error) instead of silently treating a corrupted 0/15/17/64-byte posting as "key free".
- #957 (P0-1, `e6b0af98`): added a per-table `ddl_admission: tokio::sync::Mutex<()>` inside `TableManager::begin_write_barrier`, closing the multi-owner write-barrier race where two concurrent DDLs sharing a bit could let a new writer fast-path past an in-progress backfill. New `WriteBarrierGuard.admission` field, drop-order-dependent release (bit cleared before mutex releases).
- #958 (P0-2, `abd63e66`, largest fix): unified tx/index lifecycle reconciliation. Added `IndexManager::generation` (legacy regular+unique, mirroring index2/sorted's existing pattern) + `rederive_legacy_ops_post_stage` in `pre_commit.rs` (2a: regular/unique now rederive at commit like index2/sorted already did); fixed the TOCTOU between snapshot and generation-read by flipping `IndexRegistry::insert`'s ordering to publish-then-`fetch_max(Release)`, with all 6 tx-staging methods now reading generation BEFORE their snapshot (2b); added a `retain`-based retraction of staged ops for legacy indexes DROP'd between stage and commit (2c). One brief-acknowledged deviation: no dedicated deterministic race-condition test for 2b was written (the structural fix + 10 behavioral tests were judged sufficient); accepted as-is after independent review.
- #959 (P0-3, `ec186ae8`) + #972 (P0-3b, `b895a1aa`): durable "Dropping" tombstone + open-time crash recovery for DROP INDEX, closing the crash-resurrection bug (a crash between posting-sweep and metadata-persist left a stale `Ready` descriptor pointing at swept postings). #959 covers legacy regular+unique; #972 extends the SAME pattern to the sorted family and explicitly documents (doc-only, no code change) the index2 family's crash-safety gap as a deliberate, reviewed deferral (F-50 Step 3b / #873 already existed as a different, more invasive design — not silently overridden). Both also close sub-bug 3b (namespace-reuse ghost postings) via a tombstone-checked CREATE guard.
- #973 (`23ac7b7d`, explicit user request mid-session, NOT from the review): renamed the `legacy` module (`crates/shamir-index/src/legacy/` → `.../base_index/`) repo-wide, because the user pointed out "legacy" wrongly implied "safe to delete, pre-release cruft" when it's actually the actively-used first-generation index implementation (hash/unique/sorted), distinct from `index2`. User picked the name `base_index`. ~43 files renamed/modified across `shamir-index`/`shamir-engine`/`shamir-db`/`shamir-tx`/`shamir-storage`/`shamir-client`/`shamir-query-builder`, with careful preservation of the UNRELATED "legacy shape/format" backward-compat terminology (on-disk serialization fallback language, a different meaning of the word). Verified via full-workspace `cargo check --all-targets` + full `./scripts/test.sh` (no `-p` scoping) — 5648/5649 passed, 1 pre-existing flaky vector-recall test confirmed unrelated (comment-only diff to that file, passes in isolation at 114s).
- #961 (P0-5a, `12a0234f`): index2 RENAME INDEX now persists across restart. Root cause: `IndexRegistry::rename_entry` only updated the `by_name` reverse-lookup map, never the backend's own descriptor's `name`/`name_interned`, so `all_descriptors()` (the persistence path) kept emitting the stale name. Fix: introduced a named `BackendEntry` struct (replacing a raw tuple) with authoritative `name`/`name_interned` slots (mirroring the existing `state`-override pattern), updated by `rename_entry`. Sub-agent found and fixed a SECOND bug while implementing: `remove_by_id` was unlinking `by_name` using the backend's stale construction-time name instead of the authoritative slot, which would leave a dangling `by_name` entry after DROP-following-RENAME.
- #962 (P0-5b, `3d5a7785`): sorted RENAME INDEX made resumable after a partial failure. Re-verified against current source that the review's literal framing ("transact() commented as atomic") was already partially superseded by a prior fix (#616, settle-re-scan loop) — the REAL remaining gap was that a `rekey_sorted_prefix` error after `rename_definition` already swapped the definition left the catalog believing the rename succeeded while orphaning postings, with no client-retriable path. Fixed via the SAME durable-tombstone pattern as #959/#972, adapted for rename (`system:sidx_ren` key, collision-checked). `rekey_sorted_prefix` moved from the engine crate into `SortedIndexManager::rekey_postings` so the open-time recovery hook can call it.

**#964** (the user's explicit "important" e2e coverage task) — phase 1 (inventory) is done: `docs/dev-artifacts/research/2026-08-03-e2e-oql-ddl-coverage-matrix.md` (490 lines, committed `48672e32`) enumerates ~138 OQL+DDL capabilities from the actual Rust wire DTOs, cross-referenced against BOTH live e2e suites (the 18-file JS suite AND a previously-uncounted ~26-file TS/vitest suite that quietly covers ~20 capabilities the JS-only view would have flagged as false gaps). Result: ~104 covered, ~15 partial, ~22 genuine gaps, grouped into 8 clusters (A-H, HIGH/MED/LOW priority) filed as tasks #974-981, chained sequentially after #964 and before #965.

**In-flight right now**: task #975 (e2e cluster B — HAVING + EXPLAIN dry-run). Cluster A (#974, replication lifecycle: drop/list/alter/status ops for publications/subscriptions/profiles) is DONE and committed (`67e661cc`, +3 tests, 133/133 e2e passing). Cluster B's test file (`crates/shamir-client-ts/src/__tests__/e2e-explain-having.test.ts`, 13 tests) has been WRITTEN by a sub-agent (during the peak-hours `sh`-fallback window) and I have personally read the full diff — it is well-reasoned (found that HAVING references the SELECT-alias as a flat field, not a nested path; found that EXPLAIN short-circuits before GROUP BY/HAVING; found that `index_used` reports internal index ids not user-chosen names) — but the sub-agent's own test run used `SHAMIR_SKIP_STALE_BINARY_CHECK=1` because a fresh release rebuild was too slow in a shared/contended environment, and it explicitly recommended an independent fresh-binary verification. I am currently rebuilding `shamir-server --release` from scratch in the background (task `b5znabkwk`, `cargo build --release -p shamir-server`) to run that independent verification before committing #975. This checkpoint is being written WHILE that build is still in flight — the very next action after this checkpoint completes is to wait for that build, run the new test file (and the full `crates/shamir-client-ts` vitest suite) myself, then commit if green.

**Fallback mechanics used this session**: `/crush-fallback @sh` was armed mid-session. A `crush run` for #974 failed with a `zai` peak-hours refusal (08:00-12:00 local). Per the armed contract, work was routed through `Agent(subagent_type: "sh")` for #974 and #975 instead, with a durable one-shot cron (`5a81c578`, now fired and auto-deleted) set to remind at 12:00. The user explicitly said "переключаемся на агентов /crush" right as the window closed (~12:01) — the fallback marker task (#982) was reset to `STATUS: armed` (dormant-equivalent) and subsequent NEW work should go back through `crush run`, not `sh`, unless another peak-hours/hard-limit trigger fires. A stray `devrust.cargo-target/` directory (leftover build-artifact junk from an earlier botched `CARGO_TARGET_DIR` env var, NOT from this session's own actions) was found and deleted twice during zero-trust verification passes — it is not real work product and should be deleted again if it reappears.

**babysit cron `98f917ee`** (armed earlier this session, `7,22,37,52 * * * *`) is still active and has been ticking throughout, correctly reporting "still running #<id>" on each tick since the orchestrator (this session) has continuously been the one doing the work — it has not needed to resume anything itself.

## Active goal

None (`/goal` not set). Progress is driven by the TaskList + the original `/babygoal` instruction (implement via delegation, commit between tasks; after ALL tasks: checkpoint + changelog + commit all .md + `@oh` review of the whole campaign) which remains the standing intent for this session.

## TaskList

### in_progress
- #975 e2e gap cluster B: HAVING, EXPLAIN dry-run, PlanType (test file written + reviewed, awaiting my independent fresh-binary test run before commit)

### pending
- #963 P0-6: release perf-gate inoperable — requires REAL operator/hardware action (self-hosted GitHub Actions runner registration + baseline capture), fully documented in `docs/guide-docs/CI_PERF_GATE_RUNBOOK.md`; cannot be completed by any agent in this sandbox. Left `pending`, not `completed` — needs the user's own infrastructure action, or an explicit decision to defer past this release.
- #976 e2e cluster C: FTS/functional index option breadth (blockedBy: #975)
- #977 e2e cluster D: covering/composite index, DROP if_exists (blockedBy: #976)
- #978 e2e cluster E: rename_db/describe_table/change_password/interactive Tx* (blockedBy: #977) — brief must start with a 2-minute check of interactive-tx coverage status before scoping, per the matrix's own flag
- #979 e2e cluster F: keyset after_id/NULLS ordering/PaginationInfo (blockedBy: #978)
- #980 e2e cluster G (low): misc OQL gaps (blockedBy: #979)
- #981 e2e cluster H (low): misc DDL gaps (blockedBy: #980)
- #965 P1-5: Query Builder panics via Batch API instead of typed error (blockedBy: #981)
- #966 P1-1: legacy/base_index Building indexes don't self-heal on open (blockedBy: #965)
- #967 P1-2: CREATE/RENAME/DROP can return Err after a live mutation already applied (blockedBy: #966)
- #968 P1-3: `Store::transact` contract is ambiguous — split into `apply_batch_ordered`/`atomic_transact` (blockedBy: #967)
- #969 P1-4: long CREATE INDEX blocks all writes for the full scan (blockedBy: #968)
- #970 P1-6: index builder is stringly-typed — introduce typed `IndexSpec` (blockedBy: #969)
- #971 fix stale comments/contract mismatches found by the review (blockedBy: #970, intentionally LAST — documents final post-fix state)
- #982 crush-fallback state (persistent sentinel — currently `STATUS: armed`, `AGENT: sh`)

### recently completed
- #964 TS e2e coverage inventory phase 1 (matrix committed `48672e32`)
- #974 e2e cluster A: replication lifecycle ops (committed `67e661cc`)
- #973 rename `legacy` → `base_index` repo-wide (committed `23ac7b7d`)
- #962 P0-5b sorted RENAME resumability (committed `3d5a7785`)
- #961 P0-5a index2 RENAME persistence (committed `12a0234f`)
- #972 P0-3b DROP INDEX tombstone for sorted family (committed `b895a1aa`)
- #959 P0-3 DROP INDEX tombstone for legacy family (committed `ec186ae8`)
- #958 P0-2 unified tx index lifecycle reconciliation (committed `abd63e66`)
- #957 P0-1 DDL admission mutex (committed `e6b0af98`)
- #960 P0-4 corrupt unique posting fail-closed (committed `5e766820`)

## Decisions

- For #972's index2 crash-safety gap, chose to LEAVE it deferred (option b from the brief) rather than force a tombstone fix, because the existing deferral note pointed at a different, more invasive `Dropping`-state design already scoped under F-50 Step 3b/#873 — silently overriding that with a simpler mechanical mirror of the sorted-family fix was judged worse than an honest, cross-referenced deferral.
- For #973 (the rename), decided the ~43-file mechanical rename was worth doing as its own dedicated task BEFORE continuing the P0 chain, inserted directly after #972 in the sequential chain (not deferred to the end), specifically to avoid racing #972's own in-flight edits to the same directory.
- Chose `base_index` (user's own suggestion) over `index1`/`hash`/`classic` (my proposed alternatives) when asked.
- When `crush` hit a `zai` peak-hours refusal, honored the pre-armed `/crush-fallback sh` contract immediately and silently (no confirmation asked) per that skill's own instructions, and reverted back to `crush` the moment the user said so post-window-close, updating the sentinel task rather than deleting it.
- For #963 (perf-gate), decided NOT to delegate to `crush`/`sh` at all after investigating — the remaining work is 100% human/hardware/GitHub-admin action already fully documented in an existing runbook, so delegating would be pointless busywork; left it `pending` with a clear description rather than completing it dishonestly.
- After #964's phase-1 matrix came back with 22 gaps, chose to file 8 cluster-sized follow-up tasks (not 22 individual ones) — sized "one focused crush session per cluster" per the matrix's own proposed grouping, to keep the chain from exploding into an unmanageable number of tiny tasks.
- Currently mid-decision: NOT committing #975 yet until my own from-scratch release-binary rebuild + test run completes, despite the sub-agent's tests already having passed once (with a stale-binary-check bypass) — zero-trust discipline applied consistently with every other task this session.

## Open questions

None from the user. The one open technical item is procedural, not a question: whether cluster E (#978) actually needs new interactive-transaction e2e coverage or whether it's already covered outside the directories #964's inventory walked — the brief for #978 (not yet written) must resolve this with a quick check before scoping test work, per the matrix's own flag.

## Repo state

```
?? crates/shamir-client-ts/src/__tests__/e2e-explain-having.test.ts
```
```
be95bd6b docs(prompts): brief for #975 -- e2e cluster B HAVING/EXPLAIN
67e661cc test(e2e): #974 -- cluster A live e2e coverage for replication lifecycle ops
c759a478 docs(prompts): brief for #974 -- e2e cluster A replication lifecycle ops
48672e32 docs(research): #964 phase 1 -- OQL/DDL e2e coverage inventory and gap matrix
2c6b2afb docs(prompts): brief for #964 phase 1 -- OQL/DDL e2e coverage inventory
```
