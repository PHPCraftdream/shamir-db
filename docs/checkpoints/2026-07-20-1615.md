# Checkpoint — 2026-07-20 16:15 [etap7-complete]

## Session summary

This is a continuation of a very long `/babygoal`-driven session working through `docs/dev-artifacts/research/2026-07-17-release-audit/00-WORK-PLAN.md`, with the standing directive "реализуй задачи с помощью /crush, между задачами делай коммиты, покрой их тестами." Earlier checkpoints (`2026-07-20-0230.md`, `2026-07-20-0245.md`) cover the session through the end of Этап 5 (Compliance & Ops) and the peak-hours provider interruption/fallback. This checkpoint picks up from there.

**Completed since the last checkpoint**: the peak-hours `zai` window closed around 12:00; task 5f (SecretString for `CreateScramUser.password` + `VectorBackendRef::External.api_key_secret`) was finished via a local `sh` sub-agent, independently verified (moved `SecretString` from `shamir-query-types` down to `shamir-types` behind a new `crypto` feature, re-exported for compatibility), and committed. That closed out Этап 5 entirely.

**Этап 6 (Documentation accuracy, 7 tasks 6a-6g) — fully completed**, each via investigate → write brief → commit brief → delegate (crush or local `sh` agent depending on peak-hours state) → zero-trust verify → commit:
- 6a: rebalanced `08-interconnect.md`'s stale "nothing works" framing — replication/changefeed/subscriptions are actually implemented and tested.
- 6b: replaced raw `cargo test` examples with the nextest wrapper in README/CONTRIBUTING/CLAUDE.md (CLAUDE.md's own Pre-commit gate section contradicted its own later ban).
- 6c: fixed stale `redb`→`fjall` references (scoped narrowly — most of the ~150 grep hits were legitimate historical docs or general engine-comparison prose, correctly left alone).
- 6d: implemented the previously-phantom `allow_public_metrics` config knob (real code — added the field to `ObservabilityConfig`, threaded through `server_launcher.rs`, two new boot-path tests).
- 6e: fixed CLAUDE.md's self-contradictory claim that `shamir-bench-utils` "is gone" (it isn't — narrowed scope, not removed).
- 6f: fixed phantom/wrong function names in the funclib docs table (`cast/to_str`→`to_string`, `crypto/hash_sha256`→`sha256`, `value/`→`value_nav/`, removed a phantom `compare/` folder, added missing real folders `null/`/`encode/`/`gen/`/`object/`).
- 6g: added a missing `skipped` field to the TS `QueryResult` type (real client bug), fixed a stale example port (13760→7331, 4 occurrences), and corrected three stale "not wired" Rust doc-comments (WalGroupCommit, RecordView, VersionedOverlay are all now genuinely wired in) — also removed two now-provably-dead `#[allow(dead_code)]` attributes after tracing every use.

**Этап 7 (Test/CI robustness, 7 tasks 7a-7g) — fully completed**, all via `/crush` (peak-hours window had closed by this point, so no more local-agent fallback needed):
- 7a: pinned `cargo-nextest@0.9.137` in CI (was unpinned despite `.config/nextest.toml`'s own documented "pinned baseline" — a real guard-coupling risk).
- 7b: added `shell: bash` to the `test` job (was missing, unlike `integration`; Windows runners default to PowerShell otherwise).
- 7c: investigated `[[profile.ci.overrides]]` — the brief's hypothesized gap (SCRAM/WASM per-test overrides not applying under `--profile ci`) turned out to be **false** on investigation: nextest's override precedence falls through from the selected profile to `default` per-setting, so the overrides already applied. Resolution was documentation-only (explained the real precedence so a future editor doesn't duplicate/misconfigure).
- 7d: added 7 new tests covering write-value marker combination gaps (top-level `$expr`, `$expr`+`$ref` pinning, `$cond` field-condition pinning, `SetOp.key` markers, nested markers) — the "structural gap class that produced a shipped `$fn`+`$ref` bug" per report 08.
- 7e: added a new nightly/scheduled GitHub Actions workflow (`stress-nightly.yml`) plus a new `[profile.stress]` nextest profile (inverse of `ci`'s low-parallelism tuning) running `@oracle --full` + repeated `mvcc_store_tests` at deliberately elevated parallelism, since no environment today recreates the contention that once caught a real MvccStore deadlock. This crush turn crashed once mid-verification (transient "Stream Transport Error") and was successfully re-launched into the same session to finish.
- 7f: wrapped one genuinely-unbounded spin-wait (`overlay_ordering_tests.rs`'s reader loops) in `tokio::time::timeout` per the `quantized_graph_tests.rs` template; investigated four other report-08-cited sites and correctly left them as-is with explanatory comments (they turned out to already have adequate protection via iteration caps or nearby timeout wrappers, not the same risk class).
- 7g: pinned `cargo-cooldown` (was unpinned) and, per the brief's own instruction to sweep for the same class of gap, found and fixed four MORE unpinned installs across `numa.yml` and `supply-chain.yml` that earlier tasks had missed (scoped too narrowly to `ci.yml`).

Every single task this entire session (both Этапы 6 and 7, ~14 tasks) followed the same disciplined loop without exception: investigate the code myself first (never trust the work-plan's citations blindly — several turned out to be imprecise or need correction), write a precise brief to `docs/dev-artifacts/prompts/<area>/NN-*.md`, commit the brief, delegate (crush or local agent), independently read the full diff and re-run tests/fmt/clippy myself before ever committing, and mark the TaskList task complete only after that verification passed.

Two standalone follow-up tasks were created mid-campaign and remain untouched, deliberately out of the sequential Этап chain: **#695** (a row-overlap/lost-update concern in the UPDATE-cascade pipeline, discovered during Этап 3's self-referential-FK work) and **#715** (a needed holistic rewrite of `shamir-storage/src/README.md` and `shamir-server/src/backup.rs`'s stale `redb` content — found during task 6c but explicitly out of that brief's narrow scope since it needs more than a targeted fix).

**Right at the end of this checkpoint's window**: I asked the user (via `AskUserQuestion`) whether to continue into Этап 8 (Performance — explicitly marked "post-blocker, не гейт релиза" in the work plan, i.e. NOT a release gate) or pause. The user rejected/interrupted that question and invoked `/checkpoint` instead — meaning the decision of what to do next is still open and must be asked again (or the user will state it directly) in the next turn. Do NOT assume an answer.

## Active goal

None. No `/goal` Stop hook is armed — the TaskList is the sole source of truth for what's in flight.

## TaskList

### pending
- #679 Этап 8 — Performance (post-blocker, не гейт релиза) — NOT yet decomposed into leaf tasks; work-plan item 8 hasn't been read/decomposed this session yet
- #695 Investigate row-overlap lost-update in UPDATE-cascade pipeline (standalone, unscheduled, no blockedBy)
- #715 Rewrite shamir-storage/src/README.md and shamir-server/src/backup.rs's stale redb content (standalone, unscheduled, no blockedBy)

### recently completed (last 10 of ~50 this session)
- #722 7g. Pin cargo-cooldown version + sweep all workflow files for unpinned installs
- #721 7f. Wrap unbounded spin-waits in tokio::time::timeout
- #720 7e. Add nightly contention/stress lane
- #719 7d. Write-value marker combination tests
- #718 7c. profile.ci.overrides investigation (resolved as docs-only — no real gap)
- #717 7b. shell: bash parity fix in ci.yml
- #716 7a. Pin cargo-nextest@0.9.137
- #714 6g. TS skipped field + stale port + stale not-wired comments
- #713 6f. Funclib phantom function names
- #712 6e. CLAUDE.md shamir-bench-utils claim fix

(Full earlier history — #672, #677/#678 now completed umbrella markers, #680-#714 — all completed, see TaskList tool for the exhaustive list; every single one was independently verified and committed this session or in the session this one continues from.)

## Decisions

- **Этап 8 is explicitly out of the release-blocking critical path** per the work-plan's own framing ("post-blocker, не гейт релиза") — this is a fact already established in the work plan, not a new decision, but worth restating since it changes the urgency calculus for whether to continue immediately.
- **7c's brief hypothesis was wrong, and I acted on the correction rather than the original ask**: the crush agent proved (via nextest's own docs + an empirical `show-config` probe) that per-test overrides in `[profile.default.overrides]` already fall through to `--profile ci` — adding the brief's suggested new CI-specific overrides would have been counterproductive (loosening already-correct tight timeouts). Committed the documentation-only correction instead of the originally-briefed config change.
- **7f: judgment over mechanical application** — of five report-08-cited spin-wait sites, only one (`overlay_ordering_tests.rs`) was fixed with the `tokio::time::timeout` wrap; the other four were investigated and correctly left as-is (already iteration-bounded, or already wrapped in a nearby timeout, or turned out to be single yields rather than spin-loops) — explicitly avoiding the failure mode of applying a fix mechanically across superficially-similar citations.
- **7g: scope expansion was justified and executed** — the brief's own "sweep for the same class of gap" instruction surfaced 4 more unpinned installs beyond the primary cargo-cooldown target; all were fixed in the same commit since they're the identical bug class, not scope creep.
- **A stale git index.lock was encountered and removed** during 7f's commit — investigated first (confirmed via `tasklist`-equivalent that no git process was actually running, 0-byte lock file) before removing, per CLAUDE.md's "investigate before deleting a lock" guidance.
- **One crush session crash (7e) was resolved by re-launching into the SAME session** (not falling back to a local agent) since the failure was a transient "Stream Transport Error", not a rate-limit/quota issue — matching the crush skill's explicit guidance to re-run rather than escalate for transient failures.

## Open questions

- **Whether to continue into Этап 8 (Performance) now, or pause.** I asked this via `AskUserQuestion` and the user interrupted/rejected that specific tool call, then invoked `/checkpoint` — the question itself remains unanswered. This is the first thing to resolve in the next turn.
- Этап 8's own leaf-task decomposition hasn't happened yet (per this session's practice, decomposition happens right before a stage begins, not preemptively) — the work-plan's Этап 8 section hasn't even been read into context yet this checkpoint.
- #695 and #715 remain unscheduled — no decision has been made about when (or whether, in this session) to pick them up; they're standalone and don't block anything.

## Repo state

```
?? docs/checkpoints/2026-07-17-1600.md
?? docs/checkpoints/2026-07-19-1015.md
?? docs/checkpoints/2026-07-20-0230.md
?? docs/checkpoints/2026-07-20-0245.md
```

```
6883a2c6 ci: pin cargo-cooldown and sweep all remaining unpinned tool installs
b4773911 docs(prompts): brief for 7g -- pin cargo-cooldown version in CI
1bb2f3f2 test(tx,engine): wrap genuinely-unbounded spin-waits in tokio::time::timeout
e3f42fe2 docs(prompts): brief for 7f -- wrap unbounded spin-waits in tokio::time::timeout
478a4f68 ci: add nightly contention/stress lane for the Version Oracle area
```

## Active timers

None confirmed still active as of this checkpoint — the babysit heartbeat cron that was running earlier in the session (ticking every 15 minutes, monitoring the TaskList) has been firing throughout this window (visible via the repeated "# babysit tick" prompts in this conversation) and should still be armed; the one-shot peak-hours-recovery timer (`fe2a1258`) already fired and self-deleted per its own design once the 12:00 window passed. Run `CronList` at the start of the next session to confirm current state rather than assuming.
