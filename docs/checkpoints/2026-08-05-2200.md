# Checkpoint — 2026-08-05 22:00

## Session summary

Продолжение release-blocker кампании после закрытия волны #986/#1000/#1002-1005 (два раунда `@oh`-ревью, фиксы зафиксированы, запушены в `origin/master`, 102 коммита, чекпоинт `2026-08-05-2010.md`).

Пользователь запросил новый шаг: широкий readonly-review "новой волны" (#957-#1005, диапазон `72106a90..37cc59a3`, 99 коммитов/215 файлов) с вопросами про корректность, готовность к релизу, качество кода, развитие DDL/OQL/Query Builders и производительность. Запущен `Agent(subagent_type: "fh")` в read-only режиме (только git/файлы, без cargo/тестов/сборки), с детальным брифом, включавшим контекст предыдущего ревью 2026-08-03 (тоже NO-GO, 6 P0) — задача агента была явно проверить статус каждого старого P0 плюс найти новые дефекты в самой волне.

Агент сначала записал ЧЕРНОВУЮ версию отчёта (553 строки, P0-1..P0-8), на основе которой я завёл 17 тасок (#1006-#1022) и **лично перепроверил каждый P0 по исходному коду** (zero-trust — прочитал registry.rs, sorted_index_manager.rs, pre_commit.rs, table_manager.rs, admin_table_index.rs, index_manager.rs, into_batch_op.rs построчно) — все находки подтвердились.

Пришло ОТЛОЖЕННОЕ уведомление о завершении того же фонового агента: он дозаписал ФИНАЛЬНУЮ, самопроверенную версию поверх черновика (файл сократился до 235 строк) — в финале агент сам перепроверил черновик по коду, 5 из 6 старых P0 подтверждены закрытыми (переименованы в NP-1..NP-6 для новых дефектов), а два пункта черновика (#1010 namespace collision, #1011 DROP-vs-readers) **понижены в приоритете** относительно черновика (R1 вместо P0, и "решение а не код" соответственно). Обновил обе таски под финальную классификацию, добавил новую таску #1023 (P2 fail-open backfill skip), которой не было в черновике.

Затем выяснилось, что **параллельно существовал ВТОРОЙ независимый агент** ("codex"), который писал review в файл с ТЕМ ЖЕ ИМЕНЕМ (`docs/dev-artifacts/research/2026-08-05-new-wave-readonly-review.md`) — отсюда путаница с перезаписью. Пользователь попросил не разбираться в причине совпадения имён, а просто попросить codex-агента продублировать свой отчёт под отдельным именем (`...-codex.md`) и закоммитить оба файла + чекпоинт `2026-08-05-2010.md` одним коммитом (сделано, затем амендed после появления codex-файла — итоговый коммит `556db12d`).

Codex-отчёт оказался практически идентичен ЧЕРНОВОЙ (до-финальной) версии fh-отчёта — те же P0-1..P0-8, включая namespace-collision КАК P0 (в отличие от финальной fh-классификации, которая понизила это до R1). Сверил оба отчёта построчно: **ядро (6 дефектов стыков DDL/tx-lifecycle) подтверждено ДВУМЯ независимыми агентами** — сильный сигнал, что это реальные баги, не галлюцинация. Расхождения (namespace-collision priority, DROP-vs-readers framing) зафиксированы прямо в описаниях тасок #1010/#1011 как открытый вопрос приоритизации для оператора. Добавил 3 новые таски по пунктам, которые были в обоих отчётах, но тасок под них не было: #1024 (OQL SelectItem::Expression — dead wire surface), #1025 (unified DROP INDEX без клиентского unique:bool, blockedBy #1010), #1026 (консолидированный perf-backlog после alpha — зонтичная таска, декомпозировать при взятии в работу).

Никакой код в этом сегменте не менялся — только исследование (два независимых readonly-агента) + заведение/уточнение тасок + два doc-коммита (чекпоинт + оба отчёта). Работа по самим P0/P1 задачам (#1006 и далее) ещё НЕ начата.

## Active goal

Нет активной `/goal` Stop-hook условия.

## TaskList

### pending (сентинел, никогда не завершается)
- #982 crush-fallback state (persistent — do not complete): sh — ACTIVE (hard-limit), сброс ~2026-08-06 17:01

### pending (release-blocker работа, из ревью 2026-08-05, НЕ начата)

**P0 — блокеры корректности (закрывать первыми, в этом порядке):**
- #1006 P0-1/NP-1: IndexRegistry generation watermark (insert_ticket vs generation divergence)
- #1007 P0-2/NP-2: sorted RENAME не двигает generation — staged tx пишет в старый namespace
- #1008 P0-3/NP-3: reconcile не удаляет stale ops (ABA по имени) — blockedBy #1007
- #1009 P0-4/NP-4: IndexRegistry::insert — частичная вставка без rollback (by_id без by_name)
- #1012 P0-7/NP-5: единый DDL coordinator — admission не покрывает sorted/index2 DROP и RENAME
- #1013 P0-8/NP-6: startup recovery fail-closed — не Ready после ошибок восстановления
- #1010 R1 (было P0-5 в черновике/codex): единый namespace имён между семействами — расхождение приоритета между двумя ревью, зафиксировано в описании таски
- #1011 P0-3a: DROP vs активные readers — таска-решение (документировать ограничение ИЛИ реализовать grace period), не таска-код

**P1 — до публичной alpha:**
- #1014 doctor/repair в операторский интерфейс (CLI/admin API)
- #1015 DDL result contract из RFC #985 (сейчас только дизайн-документ)
- #1016 убрать panic-by-default из Batch builders (`.expect()` в IntoBatchOp)
- #1017 typed public CreateIndex API (не stringly)
- #1018 решение по CREATE INDEX write stall (online build vs enforced alpha limits)
- #1019 Windows integration CI красный — найти причину (открытый тред с прошлого чекпоинта)
- #1023 P2: fail-open пропуск недекодируемых записей в backfill create_unique_index

**Release engineering:**
- #1020 REL-1: perf-gate runner + baseline (требует действий оператора — регистрация машины)
- #1021 REL-2: конфликт версии/CHANGELOG (alpha.1 heading уже занят — решение по alpha.1 vs alpha.2 за оператором)
- #1022 REL-3: состав продукта (server-only vs SDK) + ручной release drill

**Development backlog (после/вместе с correctness-freeze):**
- #1024 OQL: SelectItem::Expression — dead wire surface, реализовать или убрать
- #1025 unified DROP INDEX без клиентского unique:bool — blockedBy #1010
- #1026 PERF backlog (streaming/DROP-memory/unique-build-memory/spill/batching) — зонтичная таска, после alpha

### recently completed
- #986, #1000, #1002, #1003, #1004, #1005 — вся предыдущая волна (см. чекпоинт `2026-08-05-2010.md` за деталями)

## Decisions

- Запустил `@fh` в строго read-only режиме (git+файлы, без cargo) для широкого стратегического ревью новой волны — по прямой просьбе пользователя, без предварительного коммита брифа (не код-генерация, а анализ — полная prompt-first-commit церемония CLAUDE.md не требовалась).
- После находки, что параллельно существовал второй независимый ревьюер с тем же именем файла — НЕ стал разбираться в причине совпадения (по прямому указанию пользователя), просто попросил продублировать под отдельным именем и закоммитил оба.
- При расхождении приоритета между fh-финалом и codex-отчётом (namespace-collision: R1 vs P0) — не стал произвольно выбирать сторону, а зафиксировал расхождение прямо в тексте таски #1010 как открытый вопрос для оператора.
- Не стал заводить отдельные таски на каждый из ~8 пунктов perf-backlog из отчётов — свёл в одну зонтичную таску #1026 с явной пометкой "декомпозировать при взятии в работу", чтобы не засорять TaskList низкоприоритетными пунктами до того, как до них дойдёт очередь.
- Установил blockedBy-связи там, где отчёты явно указывают технологическую зависимость: #1008 blockedBy #1007 (общий примитив instance identity), #1025 blockedBy #1010 (unified DROP по имени бессмысленен без единого namespace).

## Open questions

- **Приоритет #1010** (namespace collision между семействами индексов): P0-блокер тега (codex) или R1/после-correctness-freeze (fh-финал)? Требует решения оператора перед тем, как секвенировать работу.
- **#1011** (DROP vs активные readers): документировать как известное alpha-ограничение в `KNOWN_LIMITATIONS.md`, или инвестировать в read-side grace period/reader-epoch механизм уже сейчас? Явно не решено ни одним отчётом — оба называют это решением, не готовым кодом.
- **С чего начинать исполнение?** Оба отчёта единогласно рекомендуют #1006→#1007→#1008 (registry watermark + instance identity + reconcile) как связку, закрывающую наибольшую площадь дефектов одним архитектурным решением (immutable IndexInstanceId + typed op provenance). Ждёт подтверждения пользователя перед началом реализации.
- Красный Windows CI (#1019, `run 31032501528`) — по-прежнему не решено: rerun или разбираться глубже? (Тред открыт с прошлого чекпоинта, `2026-08-05-2010.md`.)
- `/crush`'s хард-лимит сбрасывается ~2026-08-06 17:01 — до этого делегированная код-генерирующая работа (не readonly-ревью) должна идти через `sh`, по сентинелу #982.

## Repo state

```
(clean — нечего закоммитить на момент чекпоинта)
```
```
556db12d docs: checkpoint 2026-08-05-2010 + two readonly reviews of #957-#1005 wave
37cc59a3 docs(checkpoints): 2026-08-05-1955 -- #1005 fix + follow-up review wave
c5d60ec8 fix(engine): #1005 follow-up -- regression test didn't discriminate, doc nit
6140d9cf docs(changelog): note #1005's #1003/#1004 corrections
df0bab54 test(query-builder,client-ts): #1005 -- #1004's variant-coverage check was a count, not real coverage
```
