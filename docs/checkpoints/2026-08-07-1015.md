# Checkpoint — 2026-08-07 10:15

## Session summary

Continuation of a very long multi-day session governed by the standing `/babygoal` instruction: "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу." This checkpoint captures the state roughly midway through the sprint — most of the P0/P1/F-* backlog from the prior segment (already committed) plus the P1-1..P1-6 + REL-* wave worked through this segment.

This segment closed, in order, with zero-trust verification on every one: **#1014** (doctor/repair CLI — 3 crush rounds, caught a non-compiling first draft and a broken test-invocation self-report), **#1015** (DDL result contract sub-slice A — 3 crush rounds, caught a non-compiling draft and a real routing bug: `get_ddl_op_status` grabbed "the first available db/repo" instead of routing to the correct per-table `info_store`), **#1016** (removed panic-by-default from Batch builders — 3 crush rounds, caught unreliable sed-based mechanical migration attempts, landed clean), **#1017** (typed public CreateIndex API — 1 crush round, caught and reverted an unnecessary `IndexSpec` visibility widening that would have reopened a validation-bypass gap, plus a missed unused-import clippy failure), **#1018** (CREATE INDEX write-stall — user chose the full architectural fix over alpha-limits; `/crush`'s provider hit 6 consecutive stream-stall failures over ~1 hour, escalated to the user twice, who redirected delegation to `Agent(subagent_type: "sh")` until 12:00 and had me set a `CronCreate` one-shot reminder for then; the `sh` agent delivered an 839-line, fully-cited online-CREATE-INDEX RFC, verified clean), **#1019** (Windows CI batch-e2e timeout — 2 crush rounds; round 1 delivered an unevidenced timeout bump which I rejected after pulling the actual CI log myself via `gh run view --log-failed` and finding a ~140s stall against a reported "30s budget" error, pointing at `ExecutionDeadline`'s documented "cooperative, not preemptive" design; round 2 investigated honestly and reported inconclusive; landed diagnostic slow-op logging myself since neither round found the root cause — tracked as follow-up #1049), **#1020** (perf-gate runner — investigated registering the session's own dev machine as a self-hosted GitHub Actions runner via `gh api`, confirmed technically feasible, but the user decided against ANY self-hosted infra: benchmarks must never run automatically anywhere; removed the `perf-gate` job from `release.yml`'s automatic DAG entirely, updated the runbook and roadmap doc), **#1021** (CHANGELOG/version conflict — confirmed via `git tag`/`gh release list` that alpha.1 was never actually tagged, merged `[Unreleased]` into `[0.1.0-alpha.1]` with today's date per the user's choice, no version bump). **#1022** was deleted outright at the user's explicit request ("удали эту задачу, она не важна. Фокусируемся на коде").

Currently about to start **#1024** (OQL `SelectItem::Expression` dead-surface — decide implement-vs-remove, brief already scoped to have the crush delegate propose+justify a recommendation for orchestrator approval before implementing).

Two follow-up tasks were spawned mid-sprint from findings during this segment: **#1048** (P1-2 sub-slice B — tombstone `op_id` carry + recovery status-write, deferred from #1015, blocked on it, now unblocked), **#1049** (P1-6 follow-up — identify the actual Windows-CI stalling operation using the new diagnostic logging once it reproduces in a real CI run, explicitly non-blocking/not to start until new data exists), **#1050** (P1-5 slice 1 — implement the online CREATE INDEX RFC's regular/hash-family slice, explicitly gated on the operator reviewing/approving the RFC's specific technical design before implementation starts, since it's marked "DRAFT — pending review").

A `CronCreate` one-shot durable reminder (job `3f942c34`, fires `0 12 7 8 *` = 2026-08-07 12:00 local) is armed to prompt trying `/crush` again (provider "zai" may have recovered) and resuming the standard delegation strategy for the rest of the sprint.

Remaining pending work after this checkpoint: #1024, #1025, #1034, #1037, #1038, #1039, #1040-1047 (PERF backlog, mostly blocked on #1040), #1048, #1049 (deferred/non-blocking), #1050 (gated on RFC approval). The governing `/babygoal` Stop-hook condition (checkpoint + CHANGELOG + md commit + `@oh` review) remains explicitly deferred until ALL of these are exhausted or explicitly resolved as out-of-scope/deleted, per the user's own correction earlier in the session ("почему у нас больше 12 невыполненных тасок, а ты уже ревью запустил?").

## Active goal

Session-scoped Stop hook, still armed: **"реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу."** Per the user's mid-sprint instruction, delegation currently uses `Agent(subagent_type: "sh")` instead of `/crush` until the 12:00 cron fires and `/crush` is retried.

## TaskList

### in_progress
(none — about to mark #1024 in_progress)

### pending
- #1024 OQL: SelectItem::Expression dead surface (implement vs remove — brief scoped, not yet dispatched)
- #1025 DDL: unified DROP INDEX by name (blockedBy #1010, resolved — ready)
- #1034 Flake: HNSW recall test below calibrated floor on ubuntu (needs empirical recalibration)
- #1037 P0-3a Slice 2: ReaderDrainGate for sorted index family (blockedBy #1011, resolved — ready)
- #1038 P0-3a Slice 3: index2 lease-based drain guard (blockedBy #1011, resolved — ready)
- #1039 Engine gap: unique-index validation doesn't cross-validate same-tx uncommitted claims
- #1040 PERF: baseline benchmark matrix (no blocker — gates #1041-1044/1046/1047)
- #1041-1044, #1046, #1047: PERF sub-tasks (blockedBy #1040)
- #1045 PERF: FULL_SCAN_BATCH → tunables (no blocker)
- #1048 P1-2 sub-slice B: tombstone op_id carry + recovery status-write (blockedBy #1015, resolved — ready)
- #1049 P1-6 follow-up: identify Windows-CI stalling operation (non-blocking, waits on new CI data)
- #1050 P1-5 slice 1: implement online CREATE INDEX regular/hash family (gated on RFC review/approval)
- #982 crush-fallback sentinel (never completes by design)

### recently completed (this segment, most recent first)
- #1021 CHANGELOG/version conflict — merged Unreleased into alpha.1
- #1020 perf-gate — removed from automatic release DAG (no self-hosted infra)
- #1019 Windows CI investigation — diagnostic instrumentation landed, root cause open (→ #1049)
- #1018 CREATE INDEX write-stall — RFC delivered via sh agent
- #1017 typed public CreateIndex API
- #1016 removed panic-by-default from Batch builders
- #1015 DDL result contract sub-slice A
- #1014 doctor/repair CLI

### deleted
- #1022 REL-3 product composition — deleted at explicit user request ("не важна, фокусируемся на коде")

## Decisions

- User chose the full online-CREATE-INDEX architectural fix over the cheaper alpha-limits workaround for #1018.
- User decided against ANY self-hosted CI infrastructure (including registering the session's own dev machine, confirmed technically feasible) for perf-gate — benchmarks run manually only, never automated, anywhere.
- User confirmed alpha.1 was never tagged/published, chose to merge Unreleased into it rather than bump to alpha.2.
- User deleted #1022 (product composition decision) outright as out of scope for now.
- User redirected delegation from `/crush` to `Agent(subagent_type: "sh")` after 6 consecutive provider stream-stall failures over ~1 hour, with an explicit 12:00 cron reminder to retry `/crush`.
- Continued strict zero-trust re-verification on every delegated result regardless of which delegation mechanism (crush or sh agent) — this caught real bugs in #1015, #1016, #1017, #1019's first-round deliverables.

## Open questions

- Whether `/crush`'s "zai" provider has recovered will be checked when the 12:00 cron fires (job `3f942c34`) — not yet known.
- #1024's implement-vs-remove decision for `SelectItem::Expression` is explicitly deferred to the crush/sh delegate's own recommendation-with-justification, for orchestrator approval before implementation — not yet dispatched at checkpoint time.
- #1050 (online CREATE INDEX implementation) is blocked on the operator reviewing the RFC's specific technical design (not just the general direction, already approved) — not yet requested.

## Repo state

```
?? docs/checkpoints/2026-08-06-1650.md
```
(the prior checkpoint file remains untracked, as left previously; this new checkpoint file will also appear untracked until committed)

```
48dab158 docs(changelog): #1021 -- merge Unreleased into 0.1.0-alpha.1 (never actually tagged/published)
7a0e9b64 chore(ci): #1020 -- remove perf-gate from the automatic release DAG (explicit operator reversal of F-62)
61ffee5b docs(research): #1018 -- online CREATE INDEX design RFC (snapshot -> CDC -> catch-up -> short barrier)
c7668c5d investigate(engine): #1019 -- Windows CI batch-e2e timeout: real evidence gathered, root cause still open, diagnostic instrumentation added
8978d4f6 docs(prompts): follow-up brief for #1019 -- reject the naive timeout bump, point at the real evidence (~140s stall vs 30s reported budget)
1b3f8012 docs(prompts): brief for #1019 -- Windows CI batch-e2e timeout investigation
```
