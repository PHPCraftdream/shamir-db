# Checkpoint — 2026-08-09 19:11

## Session summary

Continuation of the very long multi-day session governed by the standing `/babygoal` Stop-hook: "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу." This segment (since the last checkpoint, `2026-08-09-0945.md`) did three things in sequence: (1) independently audited the online-CREATE-INDEX RFC and corrected four real gaps in it before the operator approved v2; (2) ran an independent Codex readonly-review and my own second `@oh` readonly-review of the whole "new wave," surfacing ~30 distinct findings (4 P0/CRITICAL, several MEDIUM/LOW, perf, doc-mismatches); (3) began working the resulting backlog sequentially via `/crush`, one task at a time, each result verified personally (diff read + tests re-run + fmt/clippy) before commit — never accepted on self-report alone.

**RFC v2 (online CREATE INDEX) — approved and being implemented.** The operator approved two gating decisions on 2026-08-09: fix the CREATE INDEX write-stall now (slice 1, not defer it), and implement the full crash-safe DDL status contract now (not experimental). RFC v2 is committed (`98b22655`) with 5 corrections, all independently verified against code: removed a stale open-question (a `pub` watermark accessor already exists), corrected the `snapshot_stream` framing (it's a small parameterization of already-version-pinned code, not new machinery), added a `SnapshotGuard` requirement against MVCC GC (missing entirely from v1 — a real correctness gap I found by reading `mvcc_gc.rs`'s `min_alive()`/`gc_below()` mechanics), rewrote Phase B's capture mechanism around the ACTUAL shared-choke-point finding from the write-path audit (not a fictional "live write-hook"), and threaded the operator's dirty-set (not CDC-log) design through the whole document.

**Slice 1 (regular/hash family online build) — in progress, 5 of ~9 sub-tasks landed.** Completed and committed: `#1056` (`MvccStore::snapshot_stream`, 1 round), `#1057` (`SnapshotGuard` for Phase A, 2 rounds — round 1 only proved `min_alive()`'s numeric value, round 2 added the real end-to-end proof via actual `gc_below()` + `snapshot_stream` readback), `#1058` (in-flight build registry + dirty-set capture inside the shared `IndexManager::plan_record_*` choke point, 2 rounds — round 1 used `std::sync::Mutex` for the registry despite the brief explicitly requiring lock-free `scc::HashMap`, with a doc comment falsely claiming "the brief allows this"; round 2 fixed it correctly). A follow-up `drain_dirty_set` primitive (prerequisite for Phase C) was added and hand-fixed after the delegate's own test used a fictional `IndexInfo::update_async` API that doesn't exist — I wrote the correct fix myself, committed separately (`e9b477ed`).

**The original `#1059` (full Phase A→D orchestration) was attempted in one shot and got only ~5% done** — the delegate honestly disclosed this (did not fabricate completion) and recommended splitting by phase. Decomposed into `#1087` (Phase B+A: registration + barrier-free backfill), `#1088` (Phase C+D: catch-up loop + publish barrier), `#1089` (wiring into `TableManager::create_index` + `doctor::repair()` + integration tests). `#1059` itself deleted from the TaskList.

**`#1087` is CURRENTLY IN PROGRESS, round 2 running in the background as this checkpoint is written.** Round 1 delivered correct Phase B logic (barrier, snapshot pin, registration, mark-in-flight — verified against the brief) but Phase A's stream-decode attempt did not compile: it invented `RecordId::try_from(&[u8])`/`InnerValue::try_from(&[u8])` (neither trait exists — the correct APIs are `RecordId::try_from_bytes(&[u8]) -> Option<Self>` and `InnerValue::from_bytes(...) -> Result<Self, _>`, both already used elsewhere e.g. `pre_commit.rs`), and reached directly into `IndexManager`'s private `info_store`/`posting_cache` fields from a different crate. I additionally found (via direct code reading, not from the delegate's own report) a latent variable-shadowing bug: `posting_writes`/`cache_index_keys` were hoisted outside the per-batch loop instead of declared fresh inside it (as the proven, existing `create_index_from_stream` does), and a `let posting_writes: Vec<(RecordKey, Bytes)> = posting_writes.into_iter()...` inside the batch-write branch shadows and consumes the outer accumulator, so a trailing `.clear()` would silently target the wrong (already-moved) binding rather than resetting the real per-scan accumulator between batches. Round 2's brief (`docs/dev-artifacts/prompts/release-blockers/58-p1087-round2-fix-decode-and-visibility.md`, committed) fixes all three: correct decode calls, a new `IndexManager::write_postings_batch` method (proper crate-boundary API instead of reaching into private fields), and the per-batch-declaration fix mirroring the existing proven pattern. **Round 2's result has not yet been checked** — that is the very next thing to do once its background `crush run` (session `p1087-phase-b-a`) completes.

**Backlog after slice 1 finishes**: 4 P0s from the two readonly reviews (`#1064` GetDdlOpStatus ACL, `#1065` DDL status crash-safe contract [gated on the same operator decision that also unblocked slice 1], `#1066` index2 RENAME atomicity, `#1072` `shamir-server doctor` blind to wire-created tables — the CRITICAL finding from my own second review, an operator tool reporting healthy with exit 0 while seeing zero tables), plus ~20 MEDIUM/LOW/perf/doc tasks (`#1067`-`#1086`), none started yet. `#1084` (the alpha-scope gate) is closed; its 3 non-blocking documentation sub-items live in `#1086`.

One incident this segment: a stale `.git/index.lock` blocked a commit — confirmed via `tasklist` that no live git process held it (a genuine anomaly check, not routine polling) before removing it; no data was at risk since the lock predates any pending write.

## Active goal

Session-scoped Stop hook, still armed: **"реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу."** Not yet satisfied — `#1087` is mid-flight, ~28 tasks remain pending, and the closing sequence (checkpoint/changelog/`@oh` review) is intentionally deferred until the backlog is exhausted or legitimately blocked, per this session's established practice.

## TaskList

### in_progress
- #1087 Слайс 1d-1: Phase B (микро-барьер) + Phase A (barrier-free backfill через snapshot_stream) — round 2 running in background

### pending
- #982 crush-fallback state (persistent — sentinel, never completes by design)
- #1049 P1-6 follow-up: Windows CI stall — waits on a real recurrence, not actionable now
- #1060 Слайс 1e: crash recovery для online build (blockedBy: #1089)
- #1061 Слайс 1f: тесты свойств пайплайна (blockedBy: #1089)
- #1062 Слайс 1g: бенч f78_writer_latency + KNOWN_LIMITATIONS/CHANGELOG (blockedBy: #1061)
- #1064 P0 SECURITY: GetDdlOpStatus не проверяет ACL
- #1065 P0: DDL status crash-safe contract
- #1066 P0: index2 RENAME неатомарен
- #1067 P1: DDL op-log — семейства + op_id (blockedBy: #1065)
- #1068 P1: DDL op-log retention (blockedBy: #1065)
- #1069 P1 OQL: SELECT * / alias коллизии
- #1070 HNSW recall floor/документация
- #1071 Release evidence: perf gate
- #1072 CRITICAL: shamir-server doctor слеп к wire-таблицам
- #1073 MEDIUM: CursorRegistry::register race
- #1074 MEDIUM: #1039 ложный UniqueViolation
- #1075 MEDIUM: CreateIndex builder молча теряет опции
- #1076 MEDIUM: CLAUDE.md Mutex-заявление не соответствует коду
- #1077 LOW: PerIpLimiter lock (blockedBy: #1073)
- #1078 LOW: DropIndexOp::unique в HMAC
- #1079 PERF: sorted range-scan, lease_by_field_and_kind O(N), audit-checkpoint blocking
- #1080 nit: QueryResult Default erosion
- #1081 ReaderDrainGate perf-hardening + livelock-proof
- #1082 CachedStore::flush busy-poll + потеря ошибки
- #1083 Batch::try_build codec round-trip + expect()
- #1085 Watchdog + cancellation-safety audit
- #1086 Docs: sizing guide (cursor/FTS-rebuild/vector quality)
- #1088 Слайс 1d-2: Phase C+D (blockedBy: #1087)
- #1089 Слайс 1d-3: wiring + integration tests (blockedBy: #1087, #1088)

### recently completed (most recent first)
- #1058 Слайс 1c: in-flight registry + dirty-set capture (2 rounds)
- #1057 Слайс 1b: SnapshotGuard (2 rounds)
- #1056 Слайс 1a: snapshot_stream
- #1055 RFC v2 revision (approved by operator)
- #1054 Write-path audit (2 rounds — round 1 wrongly claimed tx-staged path already covered)
- #1063 P0: BumpFtsStats provenance fix (3 rounds)
- #1084 GATE: alpha-scope decisions (2 of 5 resolved gating decisions; 3 non-blocking docs items → #1086)

### deleted
- #1059 (original monolithic Phase A→D task — replaced by #1087/#1088/#1089)
- #1040-#1047 (PERF cluster, deleted earlier this session per explicit user decision)

## Decisions

- Operator approved RFC v2 and both its gating sub-decisions (fix CREATE INDEX write-stall now via slice 1; implement full crash-safe DDL status contract now, not experimental) — 2026-08-09, via `AskUserQuestion`.
- `#1058`'s in-flight registry corrected from `std::sync::Mutex` to lock-free `scc::HashMap` after round 1 deviated from the brief and falsely attributed the deviation to "the brief allows it" — caught by re-reading the diff against the brief, not trusted from the self-report.
- `#1059` (monolithic Phase A→D) split into 3 sub-tasks after the delegate honestly reported ~5% completion and recommended phase-based decomposition — followed that recommendation rather than re-attempting the same oversized brief with a bigger timeout.
- `#1087` round 1's compile errors were diagnosed personally (not just accepted from the delegate's own honest report) — independently ran `cargo check -p shamir-engine --lib`, found the exact root causes (wrong decode API, private-field access) PLUS a latent shadowing bug the delegate's own report did not mention at all.

## Open questions

None outstanding requiring user input right now. Two prior gating decisions were resolved this segment (RFC v2 approval). The remaining ~28 backlog tasks have clear, self-contained descriptions and do not currently block on any further operator input.

## Repo state

```
 M crates/shamir-engine/src/table/table_manager.rs
 M crates/shamir-engine/src/table/table_manager_index_mgmt.rs
 M crates/shamir-engine/src/table/tests/mod.rs
 M crates/shamir-index/src/base_index/index_keys.rs
 M crates/shamir-index/src/base_index/index_manager.rs
?? crates/shamir-engine/src/table/tests/p1087_phase_b_a_tests.rs
?? docs/checkpoints/2026-08-09-0945.md
?? docs/dev-artifacts/research/2026-08-09-new-wave-review-claude.md
```

Note: the 5 modified/untracked files above are `#1087` round 2's uncommitted work-in-progress (crush is running against them in the background as this checkpoint is written) — do NOT commit them until round 2's result is personally verified (diff read, `cargo check`, fmt/clippy, tests) per this session's established zero-trust practice.

```
8a97ae8c docs(prompts): brief for #1087 round 2 -- fix decode API, private-field access, shadowing bug
35bf0ffb docs(prompts): brief for #1087 -- Phase B+A (registration + barrier-free backfill)
e9b477ed feat(index): add IndexManager::drain_dirty_set, prerequisite for Phase C
9d177220 docs(prompts): brief for #1059 -- Phase A-D orchestration for regular CREATE INDEX
7660415b feat(index): #1058 -- in-flight build registry + dirty-set capture (regular family)
```
