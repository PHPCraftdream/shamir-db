# Checkpoint — 2026-07-17 08:15

## Session summary

This session completed all 4 OQL (Object Query Language) roadmap epics (#624-627: Sequencing, $cond/$expr, Conditional execution/when, Loops/for_each) plus 6 follow-up bug/gap fixes discovered along the way (#634, #641, #643, #651 CRITICAL, #659 design-only, #660), all implemented via prompt-first `@sh`/`@fl` agent delegation with zero-trust orchestrator verification (independent fmt/clippy/test re-runs before every commit) and a commit between every task, per the standing user directive ("решши оставшиеся задачи, используй агентов @sh, между задачами делай коммиты"). Epic04's Phase B suffered a serious process incident (two concurrent sub-agent continuations edited the same files simultaneously) — caught by the user, resolved by stopping both agents and diagnosing/fixing the salvageable state directly rather than discarding it. #651 (CRITICAL: `when` field-based comparisons always folding to a fixed result) was fixed via a new `Filter::ValueCompare` variant. #634 (TS client chown/addGroupMember BigInt bug) turned out NOT to be the hypothesized msgpack signed/unsigned-int64 issue — real root cause was `createScramUser` missing a mandatory HMAC field after an earlier commit (#604) added the gate server-side without updating the TS client.

After all originally-planned tasks completed, the user requested a final `@fh` (Fable high) review of the whole session's work. That review found the campaign's craftsmanship generally high (all claimed test counts/fmt/clippy honestly re-verified by me and matched) but surfaced 4 genuine new problems, filed as tasks #661-667 (later cleaned up — see below): **F1/#661 (HIGH, CONFIRMED by me independently)** — `BatchOp::Batch`/`BatchOp::ForEach` bodies nested inside an outer transaction recurse via `execute_batch_impl` WITHOUT threading the outer `TxContext`, so nested writes commit independently instead of participating in the outer transaction — breaking the documented "any iteration failure aborts the whole batch" guarantee for BOTH `Batch` and `ForEach` (not ForEach-specific as the review initially framed it). **F2/#662 (HIGH, CONFIRMED and FIXED+COMMITTED this session)** — `BatchLimits.max_iterations` (added in #653) had no `#[serde(default)]`, breaking every TS client calling `.limits()` (TS side never knew the field existed) — fixed with `#[serde(default = "default_max_iterations")]` + TS parity + a real e2e test proving both directions. **F3/F4/#663 (MEDIUM)** — #651's plan-time guard against field-based comparisons in `when` only covers 7 of ~15 field-based `Filter` variants (In/NotIn/Like/etc. still silently fold), and the same silent-fold class exists in #641's `$cond` markers inside write values. **F5-F7 (#665/#666/#667, LOW/MEDIUM)** — untested `cond_cache` hit path (#643), ForEach DoS-gate weaknesses (isolated-not-composed nesting check, unclamped `max_iterations`, unenforced `max_execution_time_secs`), and undocumented `ValueCompare` null-comparison semantics.

I decomposed the umbrella review findings properly: deleted an initial umbrella task (#664) and split it into #665/#666/#667 (one per finding), folded F8 (a #660 docs mis-statement) into #661 since its correct wording depends on #661's fix-vs-document decision, and chained all of #661→#663→#665→#666→#667 with `blockedBy` in the review's recommended priority order. #662 was completed and committed (`7171f39e`) before #661 started. For #661, I asked the user to choose between "really fix atomicity" vs "document current behavior as accepted limitation" — user chose **fix it properly**. I then did deep independent investigation (not just delegation) confirming the exact fix is feasible and low-risk: `execute_plan_tx_impl` (`batch_execute.rs:367`) already exists and executes a plan directly against an open `&mut TxContext`; `execute_transactional_impl` already does RAII-rollback (drops `tx` without commit) on ANY `Err` from the plan (`batch_execute.rs:505-516`) — so threading the outer tx into nested `Batch`/`ForEach` recursion via this existing function, instead of the tx-less `execute_batch_impl`, gives correct abort-on-failure semantics for free, with zero changes needed to the commit/abort logic itself. Wrote a precise, fully-scoped brief (`docs/dev-artifacts/prompts/bugfix-661/01-thread-outer-tx-into-nested-recursion.md`, committed `6c11013d`) mandating the fix for BOTH `BatchOp::Batch` and `BatchOp::ForEach` arms consistently (the bug is identical in both, not ForEach-specific), specific discriminating tests (a genuinely-successful-then-failing iteration/sub-batch, proving real rollback rather than "nothing ever ran"), and a docs correction pass.

An `@sh` agent (agentId `a2e1af5fda75de404`) was launched against this brief and is CURRENTLY RUNNING IN THE BACKGROUND at the moment of this checkpoint — `git status --short` shows uncommitted, in-progress changes to `query_runner.rs`, `for_each_tests.rs`, `sub_batch_tests.rs`, `batch_for_each_e2e.rs`, and doc files, consistent with the brief's scope. This work is NOT yet verified by the orchestrator or committed. The user interrupted with `/checkpoint` right after the agent launch, before any completion notification arrived — do NOT assume the agent is done; check for a task-notification for `a2e1af5fda75de404` before doing anything else with #661. Once it reports back, the standing zero-trust discipline applies: read the diff myself, re-run `./scripts/test.sh -p shamir-engine -p shamir-query-types --full` + the e2e `for_each` tests + fmt + workspace-wide clippy independently before committing — do NOT trust the agent's own claimed results, especially given #661's severity and the subtlety of Rust borrow-checker reborrows (`self.tx.as_deref_mut()`) the fix requires.

A `# babysit tick` cron (`7cd17c89`, every `7,22,37,52 * * * *`) has been active and monitoring the TaskList throughout — it will resume #661 if this session's agent silently stalls, mirroring exactly what happened earlier this session with #634's release-build stall (diagnosed via `tasklist`/log-tail inspection, fixed by `SendMessage` to the same agentId rather than spawning a fresh duplicate). A session-scoped `/goal` Stop hook is also active with condition: "давай реализуем все задачи, используй агентов @sh, между тасками делай коммиты, в конце запусти ревью агента @fh (agent)" — meaning after #661/#663/#665/#666/#667 are all resolved, ANOTHER final `@fh` review round is expected per this explicit standing instruction, before the session can be considered fully "done" against this goal.

## Active goal

"давай реализуем все задачи, используй агентов @sh, между тасками делай коммиты, в конце запусти ревью агента @fh (agent)" — implement all remaining tasks using @sh agents, commit between tasks, run a final @fh review agent at the end. This is a session-scoped Stop hook — it will keep firing until this condition is judged satisfied.

## TaskList

### in_progress
- #661 BUG (HIGH, найден @fh-ревью): for_each внутри транзакции не атомарен — тело не наследует внешний tx (an @sh agent is actively working this per the brief at docs/dev-artifacts/prompts/bugfix-661/01-thread-outer-tx-into-nested-recursion.md, committed 6c11013d — NOT yet verified/committed)

### pending
- #663 GAP (MEDIUM, найден @fh-ревью): #651-guard в when неполон + тот же silent-fold класс проник в #641's $cond (blockedBy: #661)
- #665 F5 (LOW/MEDIUM, @fh-ревью): #643 cond_cache cache-hit путь без тестов (blockedBy: #663)
- #666 F6 (LOW/MEDIUM, @fh-ревью): слабости DoS-гейта ForEach (blockedBy: #665)
- #667 F7 (LOW, @fh-ревью): null-семантика ValueCompare не задокументирована (blockedBy: #666)

### recently completed
- #662 BUG (HIGH, найден @fh-ревью): BatchLimits.max_iterations ломает TS .limits() — fixed + committed (7171f39e)
- #659 FUTURE: while-циклы в OQL — design exploration only, no code (committed 9b4f9ef1)
- #634 BUG: TS client chown/addGroupMember BigInt wire — real root cause was missing HMAC on createScramUser, not the hypothesized msgpack issue (committed a0557171)
- #643 PERF: $cond compile_filter per-row → CondCache (committed 352e7112)
- #641 GAP: $cond/FilterValue write-value resolution (committed c286b199)
- #660 BUG: distinct_repos() recursion into Batch/ForEach bodies (committed 024c70b5)
- #651 CRITICAL BUG: when field-based comparisons via new Filter::ValueCompare (committed 3315f414)
- All of OQL Epic 04 (#652-658) and Epics 01-03 (#624-650) — full roadmap, closed

## Decisions

- User chose "fix real atomicity" over "document as accepted limitation" for #661 — the harder but correct path.
- Both `BatchOp::Batch` and `BatchOp::ForEach` get the #661 fix consistently, since independent investigation confirmed the bug is identical in both arms, not ForEach-specific as the review initially implied.
- #664 (an umbrella task bundling F5/F6/F7/F8) was deleted and properly decomposed into #665/#666/#667; F8 (a #660 docs mis-statement) was folded into #661 rather than kept separate, since correct docs wording depends on #661's fix-vs-document outcome.
- Review findings (#661-667) chained sequentially via `blockedBy` in the @fh review's own recommended priority order, per explicit user preference for serial (non-parallel) execution to avoid test-run contention.
- Chose to independently verify the @fh review's two most severe claims (F1/#661, F2/#662) myself via direct code reading before trusting them enough to file as tasks — both confirmed with concrete file:line evidence, not taken at face value.

## Open questions

- None blocking right now — #661's user-facing fork (fix vs. document) was already resolved by the user before this checkpoint. The only open item is operational: confirm the in-flight `@sh` agent (`a2e1af5fda75de404`) actually completes #661 correctly and passes independent verification once it reports back.

## Repo state

```
 M crates/shamir-client/tests/batch_for_each_e2e.rs
 M crates/shamir-engine/src/query/batch/query_runner.rs
 M crates/shamir-engine/src/query/batch/tests/for_each_tests.rs
 M crates/shamir-engine/src/query/batch/tests/sub_batch_tests.rs
 M docs/dev-artifacts/design/oql-05-while-loop-exploration.md
 M docs/guide-docs/guide/01-queries.md
```

```
6c11013d docs(prompts): brief for #661 — thread outer tx into Batch/ForEach body recursion
7171f39e fix(query-types,client-ts): #662 — max_iterations serde default + TS parity
29fe643c docs(prompts): brief for #662 — max_iterations serde default + TS parity
9b4f9ef1 docs(design): #659 — while-loop design exploration (no code)
17a1c404 docs(prompts): brief for #659 — while-loop design exploration (no code)
```

All of the above uncommitted changes belong to the IN-FLIGHT `@sh` agent working #661 — do not commit them until independently verified (fmt/clippy/full test suite + the specific new discriminating tests the brief mandated) per this session's zero-trust discipline.
