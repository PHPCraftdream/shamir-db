# Checkpoint — 2026-06-22 03:30 (hidden-O(N) sweep done; awaiting push)

## Session summary

Multi-campaign perf session, now at a clean resting point. The arc this
sub-session: Op #2 incremental drainer cursor (committed + pushed earlier:
be0bc1f, plus post-Op#2 cleanup 92311d4 + b68fa1c), then a **hidden-O(N)
sweep** campaign hunting the bug-class that bit the drainer twice (hidden
O(N) disguised as O(1); spurious O(W) from a wrong predicate).

Hidden-O(N) sweep stages (all done): **Stage 0** measured `gc_upto`'s
overlay-GC cliff — real in code (14× at 20k) but NEVER bites (Op #2 keeps
overlay depth ≤3 even on fjall under burst), so **Stage 1 (version-major
index) was DELETED as gold-plating** — verdict driven by measurement, not
preference. **Stage 2** migrated decode/deliver subscription caches from
`DashMap::retain` (O(cache) per watermark) to `scc::TreeIndex` with a
CV-first key → `remove_range` (O(evicted+log N)). **Stage 3** banned
`scc::*::len()` (it's `iter().count()`, O(N)) via `clippy.toml`
disallowed-methods, audited+annotated ~18 existing sites across 6 crates,
added a CLAUDE.md pillar-#3 note.

**Bonus (found during Stage 2 verification):** a real pre-existing
wire-contract regression on origin/master — the #128 top-K `LIMIT` fast
path hardcoded `pagination: None`; its sibling (sorted-index ORDER BY+LIMIT
`read_order_limit_fast`) had the same bug. Fixed **structurally**: both
fast paths now route through one `exec::fast_path_pagination` helper, locked
by a completeness-critic test `limit_queries_all_emit_pagination_contract`.
`@server --full` green again (531/531, first clean run since b2b1280).

Mid-session a stray `; git stash pop` (unconditional, after a failed
`git stash push` on an untracked path) popped an unrelated OLD stash and
left conflict markers in 7 query files — root-caused, cleaned with one
`git checkout HEAD -- <paths>` (zero loss; the stash stays retained). Two
false alarms retracted (not pre-session corruption; no stale green gates).

Final gate all green: clippy --workspace --all-targets -D warnings clean,
lib 3646/3646, @oracle 1422/1422, @server 531/531, fmt clean.

Currently in flight: NOTHING — campaign complete, TaskList empty, babysit
cron cancelled. Awaiting the user's decision on (a) push, (b) the dangling
checkpoint/roadmap docs, (c) the 3 orphaned WIP stashes.

## Active goal

None (`/goal` not set). No babysit cron (cancelled when TaskList emptied).

## TaskList

Empty — all tasks for the hidden-O(N) sweep (#166, #168, #169, #170) and the
pagination regression (#171, #172) completed; #167 deleted (gold-plating).

## Decisions

- **Stage 1 deleted by measurement** — Stage 0 probe (overlay depth ≤3 in
  every realistic scenario incl. fjall burst) proved the version-major GC
  index is gold-plating. Chose "restore drainer health if overlay grows"
  over "optimize the GC".
- **clippy disallowed-methods over a grep test** for the scc-len guard —
  type-aware, zero false positives, rides the existing clippy gate. The
  per-site `#[allow] // O(N) ack:` annotations ARE the documentation.
- **Pagination fixed structurally** (one shared `fast_path_pagination`
  helper + a contract/enumeration test) rather than two point-patches —
  "two instances of one bug = a structural bug, fix it structurally".
- **Per-stage commits**; the cross-cutting lint is its own `chore(clippy)`.
- **Did NOT drop the 3 orphaned stashes** — can't verify their content is
  superseded (esp. stash@{2} deletes 4359 lines); keeping costs nothing.

## Open questions

- **Push.** 5 commits ahead of origin/master unpushed (3eb7601, 0a9571f,
  604cc47, a37c950, 0f5de6b). Earlier Op#2-cleanup commits (92311d4,
  b68fa1c) are ALSO unpushed. Awaiting explicit "пуш".
- **Dangling docs.** `docs/checkpoints/2026-06-22-0145.md`,
  `hidden-on-sweep-complete.md`, this file, and the modified
  `docs/perf/perf-roadmap-2026-06-21.md` (outcomes section appended) are
  uncommitted — fold into a docs commit on sanction.
- **3 orphaned WIP stashes** — `stash@{0}` mpack!/json-sweep (15f,
  +291/-629, likely superseded by f19d593), `stash@{1}` cycles-9-11 perf
  (4f, +112/-46), `stash@{2}` test-extraction (7f, +76/-4359). User didn't
  recognize them. Options offered: A) leave, B) convert each to `wip/*`
  branch (zero-loss declutter), C) drop specific ones they recognize as
  dead. Awaiting choice. I will NOT drop without explicit say.

## Repo state

```
 M docs/perf/perf-roadmap-2026-06-21.md
?? docs/checkpoints/2026-06-22-0145.md
?? docs/checkpoints/hidden-on-sweep-complete.md
?? docs/checkpoints/2026-06-22-0330.md
```

```
0f5de6b chore(clippy): ban scc O(N) len() — hidden-O(N) sweep Stage 3
a37c950 fix(read): sorted-index ORDER BY+LIMIT must emit pagination — #128 sibling
604cc47 fix(read): top-K ORDER BY+LIMIT must emit pagination — #128 regression
0a9571f perf(tx,engine): hidden-O(N) Stage 0 — overlay GC measurement (cliff is theoretical)
3eb7601 perf(subscriptions): decode/deliver cache eviction → TreeIndex range-drain
```

5 commits ahead of origin/master (7 if counting 92311d4 + b68fa1c). 3
orphaned WIP stashes retained. Nothing pushed.
