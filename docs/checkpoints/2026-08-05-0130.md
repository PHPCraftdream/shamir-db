# Checkpoint — 2026-08-05 01:30

## Session summary

This session completed a large `/babygoal`-driven campaign, originally instructed as: *"реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу"*. The campaign remediates a 2026-08-03 readonly review (`docs/dev-artifacts/research/2026-08-03-new-wave-readonly-review.md`) that returned a NO-GO verdict for `v0.1.0-alpha.1`.

**Fully completed and committed:** the entire P0 correctness wave (#957-962, #972 — write-barrier multi-owner race, tx/index lifecycle staleness, DROP INDEX crash-safety across all 4 index families, corrupt-unique fail-open→fail-closed, index2+sorted RENAME INDEX persistence); the repo-wide `legacy`→`base_index` module rename (#973, 44 files); the full 8-cluster e2e/TS-SDK OQL/DDL coverage sweep (#964, #974-981 — JS e2e suite 130→147 tests, TS vitest suite 1028→1055 tests, including a real SDK gap closed with `changePassword`); and the full P1 hygiene wave (#965-971 — typed `TryIntoBatchOp`, doctor Building-state visibility, DDL partial-mutation error enrichment, `Store::transact` doc reframing, CREATE INDEX write-stall visibility/benchmarking, CREATE INDEX cross-type validation, and a final stale-comment/`if_exists` cleanup). Every task followed the mandatory prompt-first discipline (a committed brief in `docs/dev-artifacts/prompts/release-blockers/` before each `crush run`) and every sub-agent's diff was independently re-verified (full diff read, tests personally re-run) before committing — never trusting a sub-agent's self-report.

**After the 15-task chain finished**, `CHANGELOG.md` was updated with a comprehensive `[Unreleased]` entry covering the whole campaign, all `.md` files (two session checkpoints + the changelog) were committed, and a full `@oh` (Opus, high effort) adversarial review was launched over the entire diff range (`33f8e8fa..HEAD`, ~45 commits). That review found the campaign's work was largely sound but surfaced **one confirmed, empirically-reproduced P0 regression** and three smaller should-fix items, all now fixed and committed:

- **#987 (P0, the critical one):** `pre_commit_prelock`'s Phase 2.6 (the only site raising `TxError::UniqueViolation`) ran BEFORE `rederive_base_index_ops_post_stage` — the function that retroactively records `UniqueGuard`s for a unique index created mid-transaction. A tx staged before `CREATE UNIQUE INDEX` therefore committed a silent duplicate AND its `SetPosting` overwrote the pre-existing owner's posting at the same deterministic key (real index corruption, not just an unenforced constraint). Fixed by moving the rederive call to run after Phase 2.5's lock acquisition but before Phase 2.6. I personally verified the fail→fix→pass cycle via a temporary `git stash` (not just trusting the sub-agent's report) — the new regression test genuinely fails against the old ordering and passes against the new one.
- **#989:** `handle_drop_index`/`handle_rename_index`'s `if_exists` early-exit ran BEFORE `authorize_access` — a pre-auth existence oracle for an unauthorized caller. Fixed by reordering auth first in both handlers.
- **#990:** #970's own cross-type validation sweep missed `include` — `.include([...]).index_type("fts")` was still silently ignored, the exact bug class #970 was filed to close. Fixed in all 3 layers (server/Rust try_build/TS builder), plus closed a pre-existing TS-side gap (no include validation existed there at all).
- **#991:** CHANGELOG entry for #970 didn't call out `vector_dim` becoming mandatory as a breaking change — added.

Four more findings from the same review were filed as tracked-but-not-urgent tasks rather than fixed immediately, each with the reviewer's own reasoning for why it's non-blocking: **#988** (index2/fts/vector/functional DROP INDEX still lacks crash-safety — a real gap, but one the original #972 brief explicitly pre-sanctioned as deferred scope, confirmed still open); **#992** (`IndexRegistry::insert`'s generation-tagging isn't linearizable across concurrent inserts, currently masked by #957's own admission mutex); **#993** (sub-bug 2c's base_index-ops retain filter uses a key-length heuristic instead of an explicit discriminant — fragile for a hypothetical future index2 backend, nothing live affected today); **#994** (two minor residuals — SSI predicate staleness after index2 rename, and #967's error-variant collapse to `Internal` — both currently unreachable).

A Stop-hook check flagged (correctly) that the /checkpoint written earlier in the session (`2026-08-04-2112.md`) predated the review-driven fixes (#987/#989/#990/#991), and that those specific fixes had been verified by me directly but not by a fresh `@oh` pass. This checkpoint is that requested fresh one, written after all currently-planned work is done. **Immediately following this checkpoint, a follow-up `@oh` review scoped specifically to commits `944cc5a0..HEAD` (the #987/#989/#990/#991 fixes) will be launched** to close that loop before ending the turn.

The `/babysit` cron (job id `909f6b8b`, ticking every 15m) has been running throughout and correctly reporting `still running #<id>` on live crush sessions; it will keep ticking as long as any task remains `pending`/`in_progress` (currently: #963, #982, #988, #992, #993, #994 pending — none of which are meant to auto-start into `crush` work without a deliberate decision, see below).

## Active goal

None — this session uses `/babygoal` + `/babysit` (TaskList-driven), not a `/goal` Stop-hook condition. (A generic Stop-hook DOES exist in this environment and is what flagged the checkpoint/review gap above — it checks the ORIGINAL `/babygoal` instruction's literal completion criteria, not a user-set `/goal`.)

## TaskList

### in_progress
- (none)

### pending
- #963 P0-6: release perf-gate неработоспособен — нет bench-baseline.json и не зарегистрирован runner (NOT delegatable — needs a human operator with real hardware + GitHub repo-admin access; `docs/guide-docs/CI_PERF_GATE_RUNBOOK.md` already documents every manual step)
- #982 crush-fallback state (persistent sentinel — do not complete): sh — armed
- #988 index2 (fts/functional/vector) DROP INDEX still lacks crash-safety (deferred by #972, confirmed still open by @oh review) — real gap, pre-sanctioned deferral, larger scope than #987/#989/#990
- #992 IndexRegistry::insert generation tagging is not linearizable across concurrent inserts (masked today by #957) — low priority
- #993 Sub-bug 2c's base_index-ops retain filter identifies ops by key-length heuristic, not an explicit tag — low priority
- #994 Two minor residuals noted by @oh review: SSI predicate staleness after index2 rename; #967 error-variant collapse to Internal — low priority
- #983 BUG: binary (Uint8Array) field value corrupted on write.insert round-trip (found during #980, root cause not yet located)
- #984 Wire degraded base-index (Building) state into server readiness/metrics (deferred from #966)
- #985 Design a unified DDL result contract (operation ID + status) across the wire protocol (deferred from #967)
- #986 Introduce typed IndexSpec Rust enum + wire-compatible DTO + shared Rust/TS fixture-matrix codegen for CREATE INDEX (deferred from #970, post-alpha)

### recently completed
- #990 #970's cross-type validation missed `include`
- #989 Security-adjacent: DROP/RENAME INDEX's if_exists ran before authorize_access
- #987 P0 REGRESSION: unique-index staged-before-CREATE duplicate not validated
- #991 Document #970's vector_dim-now-required as a breaking change
- #971 Final P1 cleanup: stale comments + RENAME INDEX if_exists
- #970 P1-6: CREATE INDEX cross-type validation
- #969 P1-4: CREATE INDEX write-stall visibility
- #968 P1-3: Store::transact doc framing
- #967 P1-2: DDL partial-mutation error enrichment
- #966 P1-1: doctor Building-state visibility

## Decisions

- Scoped every P1 task (and the post-review #987/#989/#990 fixes) to the narrowest change that closes the concrete, verified defect, filing every larger suggested redesign as its own standalone tracked task (#984, #985, #986, #988, #992, #993, #994) rather than attempting it inline or silently dropping it.
- For #987, personally re-verified the fail/pass cycle via a temporary `git stash` rather than trusting the sub-agent's self-reported repro — this caught nothing wrong in that specific case, but is the same zero-trust standard applied to every task this session.
- For #989/#990, judged both fixable immediately (small, mechanical, unambiguously correct) rather than deferred, given their moderate severity (a security-adjacent oracle; a validation gap in the exact bug class just closed).
- For #988, judged NOT to fix immediately despite being a real gap — it requires a new tombstone mechanism (larger surface than a mechanical fix) and was already a deliberate, documented deferral from #972's own brief; re-confirmed by the review as still non-critical (requires a specific crash timing).
- Chose to write this SECOND checkpoint (rather than treating the earlier `2026-08-04-2112.md` as sufficient) because a Stop-hook check correctly identified it predated the review-driven fixes — checkpoint content should reflect the true final state, not a mid-campaign snapshot.

## Open questions

- None from the user directly. The Stop-hook feedback that triggered this checkpoint also asked for a fresh `@oh` review pass over the #987/#989/#990/#991 fixes specifically — that review is being launched immediately after this checkpoint is written, in the same turn.
- Not yet asked, but worth surfacing to the user once this turn ends: whether to proceed with #988 (larger index2 DROP crash-safety fix) now or leave it queued: whether #992-994 (all explicitly low-priority per the reviewer) should be worked now or left for a dedicated future pass: and whether `/babysit`'s cron should keep running given the only pending tasks left are either non-delegatable (#963), a sentinel (#982), or the reviewer-confirmed-low-urgency items above.

## Repo state

```
(clean — no uncommitted changes)
```
```
542d03c3 fix(db,query-builder,client-ts): #990 -- close include-on-non-btree gap
7fb300fc docs(prompts): brief for #990 -- close include-on-non-btree validation gap
1c982b7a fix(db): #989 -- auth runs before if_exists existence probe (DROP/RENAME INDEX)
f3555d26 docs(prompts): brief for #989 -- auth must run before if_exists existence probe
944cc5a0 fix(engine): #987 -- P0 regression, unique staged-before-create not validated
```
