# Checkpoint — 2026-08-12 03:30

## Session summary

Continuing the standing `/babygoal` Stop-hook session: "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу". This checkpoint follows `2026-08-12-0230.md` and closes out a THIRD `@oh` adversarial review round over `#1096`'s chain of fixes, triggered by Stop-hook feedback specifically pointing out that the review step's completion wasn't visible in the (limited/stale) transcript view it had access to — a recurring pattern this whole session.

**Full arc of `#1096` across three review rounds, for the record:**
1. Original fix (`1600ea0f`) — designed correctly, but `/crush`'s generated diff needed several manual mechanical fixes (bad `#[cfg(test)]`, bad visibility, non-existent API calls, a bad global rename) before it even compiled/passed. First `@oh` review found a CRITICAL bypass and a HIGH unfixed production path.
2. Bypass fix (`dbc6299e`) — added `touched_records_in_tx` as a second mandatory tolerance condition, applied at all 4 real call sites, 6 new regression tests. Second `@oh` review confirmed the bypass fix sound (proven non-vacuous by reverting each half), found a PERF regression the fix itself introduced (unconditional O(tx) walks not gated behind `has_unique_indexes()`) and a separate, unrelated, pre-existing CRITICAL bug (`#1097`, `RemovePosting` has no owner — out of scope, tracked not fixed).
3. Perf-gating fix (`88eabd34`) — gated all four sites. Third `@oh` review confirmed this a semantically exact no-op wrapper (verified by literal diff comparison + re-running the gate), found a coverage gap (no test exercised `update_tx_bytes` or asserted `DuplicateKey` out of any UPDATE path — confirmed exploitable by mutation testing) and a second unrelated pre-existing hazard (`#1098`, a generation-read-ordering race around mid-tx `CREATE UNIQUE INDEX` — out of scope, tracked not fixed).
4. Closed the coverage gap (`40692cd6`) — two new tests (`update_tx` and `update_tx_bytes` both asserting a genuine durable-conflict rejection). Full gate re-verified one final time: fmt clean, clippy clean, `shamir-engine --full` 1999/1999.

**Every commit in the chain has its own CHANGELOG entry** (`d65c2780`, `d8223edb`, `4b4284ef`, `7f546b58`) documenting what was found and fixed at each stage, in order, so the full arc is readable from CHANGELOG.md alone without needing this checkpoint or the git log.

**This is now genuinely, adversarially verified done** — three independent review passes, each finding something real, each finding fixed or correctly triaged as out-of-scope-and-tracked. No review pass found nothing; the pattern of "review finds something real → fix → review the fix" repeated three times before a review pass found the diff sound with only a coverage gap (not a correctness defect) remaining. This is the strongest evidence this session has produced that the zero-trust-verification discipline is load-bearing, not ceremonial — a single review pass, or trusting test-suite-green without adversarial scrutiny, would have shipped a genuine uniqueness-bypass.

## Active goal

Standing `/babygoal` Stop-hook (verbatim, unchanged across the whole session): "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу"

## TaskList

### pending
- #982 crush-fallback state (persistent sentinel — do not complete, structurally unsatisfiable)
- #1049 P1-6 follow-up (non-blocking, waits on a real recurrence)
- #1092 PERF: ReaderDrainGate architectural hardening — needs a loom model test before touching proven-correct ordering
- #1094 Watchdog for stuck batch ops — needs new OS-thread-based infrastructure design
- #1095 PERF: SegmentSet::inner off std::sync::Mutex — needs bench-level contention-isolation instrumentation first
- #1097 CRITICAL: pre_commit.rs Step 1 RemovePosting has no owner — can cancel a different record's live in-tx unique claim (found round 2, pre-existing from #1074)
- #1098 HIGH: unique-guard/generation read-order race can silently skip durable unique check for a mid-tx-created unique index (found round 3, pre-existing from P0-2/#958)

### recently completed
- #1096 tx-aware unique check for release-then-reclaim of a durable value — three full review rounds, bypass found+fixed, perf regression found+fixed, coverage gap found+closed, gate green throughout, fully committed (1600ea0f through 7f546b58)
- #1093 Batch::try_build typed visitor — fully implemented, tested, verified
- #1091 lease_by_field_and_kind investigation — documented blocker, no code change
- #1090 post_auth_bucket lock-free migration (partial: SegmentSet half deferred to #1095)

## Decisions

- **Ran a THIRD `@oh` review over the perf-gating commit specifically**, rather than treating the second review's findings as fully resolved once fixed — an early-exit gate wrapped around a correctness-critical check is exactly the shape of change that can silently skip the check, so it warranted its own dedicated adversarial pass, not just a green test suite.
- **Closed the coverage gap the third review found immediately** (two cheap, targeted tests) rather than just tracking it — unlike `#1097`/`#1098`, this was a same-session, same-task, low-cost fix directly strengthening confidence in code already believed correct.
- **Tracked both `#1097` and `#1098` as separate, out-of-scope, pre-existing bugs** rather than fixing them in this already very long session — both are explicit results of the reviewers correctly distinguishing "found while reviewing this task" from "caused by this task"; both have real reproductions or clear failure-scenario write-ups recorded in their own task descriptions so a future session doesn't need to re-derive them.

## Open questions

None blocking. `#1096` is complete. `#1097` and `#1098` each have a recorded reproduction/failure scenario and a sketched (not fully designed) fix direction — either should be picked up as its own dedicated investigation-then-implementation cycle, following the same pattern (`#1090`/`#1091`/`#1093`/`#1096`) that worked well this session: investigate deeply, confirm the fix shape against a reproduction BEFORE implementing, full gate, then an adversarial `@oh` review before considering it done — do not implement free-hand from the task description's sketch alone.

## Repo state

```
?? devrust.cargo-target-check/
```

```
7f546b58 docs: CHANGELOG entry for #1096's third review round and #1098
40692cd6 test(engine): #1096 -- close update-path coverage gap found by third @oh review
81f6a9a8 docs: checkpoint after #1096's two-round bypass-fix-then-verify closure
4b4284ef docs: CHANGELOG entry for #1096's perf fix and #1097 tracked follow-up
88eabd34 perf(engine): #1096 follow-up -- gate released/touched walks behind has_unique_indexes()
d8223edb docs: CHANGELOG entry for #1096's follow-up bypass fix
dbc6299e fix(engine): #1096 follow-up -- close a genuine uniqueness-bypass found by @oh review
cdf6da69 docs: checkpoints for the #1096 crush-dispatch and its manual-fix-then-verify completion
d65c2780 docs: CHANGELOG entry for #1096's fix
1600ea0f fix(engine): #1096 -- tx-aware unique check for release-then-reclaim of a durable value
dbe1f197 docs(prompts): brief for #1096 -- tx-aware stage-time unique check for release-then-reclaim of durable values
93b1df71 docs: checkpoint after second extended wave (#1090/#1091/#1093 + 3 @oh-found fixes)
```
