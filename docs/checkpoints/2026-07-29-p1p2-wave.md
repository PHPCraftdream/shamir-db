# Checkpoint — 2026-07-29 [p1p2-wave]

## Session summary

This session began from checkpoint `2026-07-28-p0-wave2.md` and closed the ENTIRE remaining P0 wave (F-50's Step 1/Step 2/Step 3a/Step 3b), then continued into the full P1/P2 wave from the 2026-07-28 readonly review, all under the standing `/babygoal` contract: decompose into tasks, delegate each via `/crush` with a committed prompt-first brief, zero-trust-verify every diff personally (full read, personal `fmt`/`clippy`/test re-run, and — the discipline refined further this session — a personal red-then-green reproduction of the actual fix mechanism for EVERY task, not just a trust in the agent's own claim), then commit.

**F-50 fully closed** (online index build lifecycle): Step 1 spike (commit `07076dde`) settled the generation-gated ops-plan re-derivation design and prototyped it for the functional-backend/INSERT case. Step 2 (`630c9bc0`) extended it to all mutation kinds, vector staged_vectors, an FTS double-count guard, and a sorted-index generation-gate fix. A separate finding from Step 1's investigation — index2 backends had NO `DROP INDEX` support at all — was filed and closed as its own task (#872, commit `60c2f0c8`). Step 3a spike (`d4f159fb`) proved (and importantly, DISPROVED one lead first) the bincode forward-compat mechanism for a new persisted `IndexState` field, and settled restart-from-scratch as the crash-recovery strategy. Step 3b (`a9f436de`) landed the full Building/Ready lifecycle state, the planner Ready-gate, and self-healing crash/restart recovery. F-50's own umbrella task and all sub-tasks are `completed`.

**P1 truthfulness/release wave**: F-51 (`883af6dc`) fixed the base/large deploy profiles' undocumented Argon2 concurrency ceilings and removed the dead-on-arrival `CreateRepo::path` builder method (server has unconditionally rejected it since F-43); independently re-verified and closed KNOWN_LIMITATIONS.md's FK/RI-barrier overclaim as ALREADY fixed by this session's own F-46 commit, so no action was needed there. F-52 (`5cf98b1a`) made every GitHub Actions release/CI workflow SHA-pin its third-party actions (independently spot-checked 3 of 8 pins via `gh api`, including correctly catching that `Swatinem/rust-cache@v2`'s moving tag points past its own `v2.9.1` release — an honest `# v2` comment rather than a fabricated version), added self-contained release archives (LICENSE + README + sample config + a new VERIFY.md), added least-privilege `permissions:` blocks to 5 previously-unscoped workflow files, and documented (without bumping) the Node client package's intentional exclusion from the version-consistency check.

**F-53 performance wave** (decomposed into 4 sub-tasks + a deferred Step 3, mirroring the F-50 precedent): F-53a (`36373fea`) fixed streaming top-K to bound memory DURING the scan (not after) in both `read_collecting` and the AsOf/cursor path `read_as_of`, via a new shared `TopKHeap` — 1.9× lower peak allocation, ~27% faster, personally reproduced via a real bench regression when the fix was disabled. F-53b was split into a spike (Step 1, `bf3f37a2`) that DISPROVED the review's own primary lead (the sorted index's covering-projection version stamp doesn't apply to non-covering single-column indexes) and instead PROVED a narrower mechanism (`MvccStore::version_of` classification against a pinned snapshot) works for the INSERT-after-pin case but requires a mandatory per-index mutation high-water gate because UPDATE/DELETE-after-pin are irreducibly unservable by a current-state index (two negative tests proved this). Step 2 (`54e8f0ef`) landed the gate + the seek arm for one-shot `Temporal::AsOf` queries, but HONESTLY DEFERRED wiring the cursor's own `FetchNext` path into it after discovering the original brief's premise (that the cursor already tracks the last RecordId) was inaccurate — filed as Step 3 (#879, not yet started). F-53c (`ea165157`) wired the SAME index-fast-path RESTRICT already had into CASCADE/SET NULL actions (fk_actions.rs, fk_on_update.rs), 17-23× faster on a benchmark, with the F-40b RI-barrier's predicate-recording ordering carefully preserved and its own regression suite (`fk_ri_barrier_tests.rs`) re-confirmed passing. F-53d (CI perf gates) is not started, blocked by F-53's other sub-tasks.

**F-54 is IN PROGRESS right now** (backgrounded `/crush` session `f54-remove-dead-group-commit`, brief committed as `4fd966d8`): after an `AskUserQuestion`, the user explicitly chose to REMOVE (not revive) `group_commit.rs`'s 705-line unreachable group-batching path (`run_leader` has zero production call sites, confirmed by grep) rather than wire it into production — this session's own F-46 had been extending its dead correctness logic as if it were live. The brief also requires correcting two stale doc references (`finalize.rs`'s module comment, `KNOWN_LIMITATIONS.md:181`'s "3 guard sites" claim) without touching the historical `f40b-ri-barrier-spike.md` research memo.

**Remaining after F-54**: F-53d (CI perf gates, blocked by F-53b Step 3), F-53b Step 3 (cursor `Pagination::After` conversion — deferred from Step 2, needs its own investigation-first brief), F-55 (public-repo hygiene — explicitly requires user sign-off before starting per a standing rule protecting `docs/dev-artifacts/`), and #868 (the recurring SLOW ANN test investigation, deliberately scheduled last).

**Delegation channel**: `/crush` throughout this entire window (no `@sh`/Agent-tool switch occurred this session). Standing babysit cron (from a prior session) continues to tick and has been responding "still running #N" or advancing tasks correctly across dozens of ticks this session.

## Active goal

None via `/goal`. Operative contract is `/babygoal`'s TaskList-driven directive: continue solving tasks via `/crush`, commit between tasks — reaffirmed multiple times this session with identical wording.

## TaskList

### in_progress
- #865 F-54 (P2): remove group_commit.rs's unreachable run_leader batching path — `/crush` session `f54-remove-dead-group-commit` running as of this checkpoint, not yet reviewed/committed.

### pending
- #866 F-55 (P2): public-repo hygiene sweep — requires explicit user sign-off before starting (touches `docs/dev-artifacts/`, protected by a standing rule against deletion/archival without agreement)
- #868 (P-low): investigate/fix the recurring SLOW `filtered_ann_tests::vr5_cofilter_sees_staged_and_filters_residual` test — scheduled to run LAST
- #877 F-53d (P2): CI release performance gates (blocked by #879)
- #879 F-53b Step 3 (P1): convert cursor_handlers.rs's boundary-filter+OFFSET pagination to `Pagination::After` so `FetchNext` reaches the already-landed AsOf keyset-seek arm — needs its own investigation-first brief per Step 2's findings (the cursor does NOT already track a RecordId, contrary to the original assumption)
- #864 F-53 (P1/P2, umbrella): blocked by #877, #879

### recently completed (this window, in order)
- #862 F-51, #863 F-52, #874 F-53a, #875 F-53b Step 1, #876 F-53c, #878 F-53b Step 2 — all P1/P2 items closed this window.
- (prior window, same session) #857-#861/#867/#869-#873 — the entire F-46 through F-50 P0 wave.

## Decisions

- User explicitly chose "remove/feature-gate" (specifically: remove) over "wire into production + benchmark" for F-54's dead `group_commit.rs` code, via `AskUserQuestion` this session.
- F-53's scope was decomposed into 4 independent sub-tasks (a/b/c/d) up front, mirroring the F-50 Step-based precedent, rather than attempted as one giant delegation — F-53b further split into Step 1 (spike)/Step 2 (implement, partial)/Step 3 (deferred, cursor conversion) once investigation revealed genuine design complexity (the AsOf/pinned-snapshot interaction with a current-state index).
- F-53b Step 2's delegated agent HONESTLY deferred the cursor-shape conversion after finding the brief's own premise (cursor already tracks a RecordId) was wrong — this was recognized as a legitimate, well-reasoned deferral (not corner-cutting) and turned into its own follow-up task (#879) rather than either rejecting the work or forcing a rushed completion.
- KNOWN_LIMITATIONS.md's FK/RI-barrier section was independently re-verified (via a dedicated Explore-agent check) to already be accurate after F-46's own commit, rather than assumed to need the fix the original review recommended — avoided redundant/unnecessary doc churn.
- Zero-trust discipline this window additionally required, for every task, a PERSONAL reproduction of the red-then-green proof (not just reading the delegated agent's own test output) — this caught nothing wrong in any single task this window, but the discipline held without exception across F-51 through F-54 (in progress).

## Open questions

- None pending user input at checkpoint time. F-54's only open item is whether the in-flight `/crush` session's diff will need any follow-up once reviewed (not yet known — session was still running when this checkpoint was written).

## Repo state

```
?? docs/checkpoints/2026-07-28-0100.md
?? docs/checkpoints/2026-07-28-p0-wave2.md
```
```
4fd966d8 docs(prompts): brief for F-54 -- remove group_commit.rs's unreachable batching path (#865)
54e8f0ef perf(engine,index): F-53b Step 2 -- AsOf-aware cursor index-seek for one-shot queries; cursor shape conversion deferred (#878)
076408b8 docs(prompts): brief for F-53b Step 2 -- land the AsOf-aware cursor index-seek (#878)
ea165157 perf(engine): F-53c -- index-assisted lookup for FK CASCADE/SET NULL, closing the scan-heavy gap RESTRICT already avoided (#876)
f9f9806e docs(prompts): brief for F-53c -- wire index-assisted lookup into FK CASCADE/SET NULL actions (#876)
```

Note: the working tree is otherwise clean (every task this window has been committed immediately after zero-trust verification) except for the two prior checkpoint files, which remain untracked by design (checkpoint files are not auto-committed). The current `/crush` session `f54-remove-dead-group-commit` has NOT yet produced a diff to review as of this checkpoint (still running).
