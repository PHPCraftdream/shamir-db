# Checkpoint — 2026-07-22 17:47

## Session summary

This window completed the ENTIRE alpha-gate campaign. The headline: **RI-13 (#742, frozen commit) is DONE** — full local gate (fmt/clippy/`./scripts/test.sh --full`, 5967/5967 green), 146+ commits pushed to `origin/master` (first push of the whole campaign, explicitly authorized by the user), and after five push-fix-push CI iterations, a fully green remote CI across all three workflows (CI/numa/supply-chain) at commit `0c73a827`.

Getting the remote CI green surfaced and fixed **9 distinct issues**, two of them genuine bugs, not flakes: (1) a CRITICAL intra-batch lost-update in `group_commit.rs::run_leader` — keyed SSI read-set and FG-7's Phase CAS validated against `provider.version_of` (published state only), blind to earlier-validated-but-unpublished batch peers; fixed by mirroring the existing P3a `batch_footprints` pattern with an exact-key accumulator (commit `a0c48154`; note the function currently has no live callers — defense-in-depth). (2) A deterministic `Instant`-underflow: `expired_tx_rejected_at_commit` backdated `started_at` by 600s, which panics on any host with <10min uptime (freshly-booted CI VMs); fixed via a `#[cfg(test)]` `TEST_MAX_TX_LIFETIME_OVERRIDE` OnceLock in `tx/commit.rs` (`ff25c566`). The other seven were pre-existing timing-margin/ANN-recall flakes fixed individually (vr5_cofilter slow-timeout override; filtered_ann_low_selectivity dataset growth + recall-tolerance assert; concurrent_cas_exactly_one_wins test-design fix — WHERE predicate matched the field the update itself overwrote; argon2id saturation window; audit retention sweep sleep; spike_file_dump_u8 dataset growth; truncation-ceiling drain retry; and a second-order fix to my own WAL poll condition — poll must check `!is_dirty() && fsync_count() >= 1` because `take_dirty()` clears before `sync_now()` increments).

A user-requested `@fm` consultation confirmed the systemic-cause question: **no single infrastructure regression** — accumulated timing-margin debt across independently-authored tests, amplified by the documented CI oversubscription (`test-threads=4` × `worker_threads=4` on 4-vCPU runners, `.config/nextest.toml:67-89`), surfaced now only because RI-13 is the first campaign to repeatedly run the full CI matrix. `@fm` recommended an optional future sweep task (fixed-sleep + `Instant::now() - Duration` audit) — NOT yet created as a task.

After RI-13 closed, the user asked to investigate the two remaining post-alpha items, then delete the old umbrella tasks and create proper ones. Old #747 (FG-5 umbrella) and #749 (RI-15) were deleted; replaced by **#754 (RI-15, single executable task)** and **#755–#759 (FG-5a–e decomposed chain)** with correct blockedBy edges (755→756→{757∥758}→759). All code anchors in the new task descriptions were re-verified against the working tree this session. A detailed narrative plan was then written to `docs/dev-artifacts/roadmap/POST_ALPHA_EXECUTION.md` (currently uncommitted — the commit of all docs is the immediate next step after this checkpoint).

Remaining release-track work NOT yet started (from RI-13's own description, steps 3–4): the release-gate checklist (Docker smoke RI-2/RI-4, TS/Node e2e RI-5, backup→restore→ticket-invalidation smoke RI-11) and the multi-day nightly stress on the frozen commit before tagging `v0.1.0-alpha.1` (tag requires an explicit user command).

## Active goal

Standing campaign directive (Stop-hook armed at campaign start): «реализуй задачи с помощью /crush, между задачами делай коммиты, покрой их тестами. Если /crush войдет в пиковые часы, то переключайся на агентов @sh». Standing amendment (explicit user instruction, still in force): **use `@sh` directly, do NOT attempt `/crush` until the user says otherwise** (its quota reset 2026-07-23 17:01 does not lift this).

## TaskList

### pending
- #754 RI-15 (post-alpha). Global inflight response-memory budget — ready, no blockers, single-shot
- #755 FG-5a (post-alpha). Cursor wire protocol + spec — ready, no blockers
- #756 FG-5b (post-alpha). Engine/session cursor (blockedBy: #755)
- #757 FG-5c (post-alpha). Rust SDK async Stream (blockedBy: #756)
- #758 FG-5d (post-alpha). TS SDK async iterator (blockedBy: #756)
- #759 FG-5e (post-alpha). Cursor e2e Rust+TS (blockedBy: #757, #758)

### recently completed
- #742 RI-13 (frozen commit — full local gate + push + green remote CI), #753 (group-commit intra-batch SSI/CAS fix), #752 (filtered_ann flake fix), #751 FG-7, #750 FG-6, #748 RI-14, #746 FG-4, #745 FG-3, #744 FG-2, #743 FG-1, #730–#741 (RI-1…RI-12) — the entire alpha campaign.

### deleted
- 2 (old umbrella #747/#749, superseded by #754–#759)

## Decisions

- **Push authorized and executed** — user said "Да, push" after the local gate went green; every subsequent CI-fix commit was pushed without re-asking (fix-forward on an already-authorized push train).
- **CI flake strategy: fix one at a time to a green run** — user chose this over stopping for a bulk sweep or accepting local-green-only. Took 5 CI iterations, ended green.
- **Old umbrella tasks deleted, replaced by decomposed ones** — explicit user instruction («удали старые таски, заведи новые»).
- **RI-15 before FG-5** when post-alpha work starts — my recommendation, recorded in POST_ALPHA_EXECUTION.md §3 (smaller; ByteBudget reused by cursors; FG-5 reduces but doesn't replace the need).
- **`@fm` systemic-flake verdict accepted**: no single root cause; optional sweep task deliberately NOT created yet.

## Open questions

- Whether to proceed now with RI-13's remaining release-checklist steps (Docker smoke, TS/Node e2e, backup→restore smoke, nightly stress, tag) — awaiting user direction; the tag explicitly requires a user command.
- Whether to create the `@fm`-recommended flake-sweep task (fixed-sleep/Instant-arithmetic audit across the workspace).

## Repo state

```
?? docs/dev-artifacts/roadmap/POST_ALPHA_EXECUTION.md
```

```
0c73a827 fix(wal): poll on both dirty-cleared AND fsync_count in background_fsync_clears_dirty_on_success (CI gate)
308d8056 test(engine): retry drain_all in truncation ceiling test's final assertion (CI gate)
c88f4ccb test(wal): poll instead of fixed-sleep for background fsync assertions (CI gate)
f6016bce test(index): fix flaky spike_file_dump_u8_works self-query recall (CI gate)
ff25c566 fix(engine): avoid Instant-subtraction underflow in expired-tx test (CI gate)
```

Branch `master`, in sync with `origin/master` (all pushed, remote CI green at HEAD).

## Active timers

A babysit cron ticks every 15 minutes (`# babysit tick` prompt; id rotates — re-verify via CronList). With all pending tasks now post-alpha and no in_progress task, ticks will report idle/blocked until the user starts post-alpha work or the remaining release-checklist steps.
