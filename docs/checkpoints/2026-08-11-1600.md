# Checkpoint — 2026-08-11 16:00

## Session summary

Continuing the same standing `/babygoal` Stop-hook session as the prior checkpoint (`2026-08-11-0930.md`): "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу". Since that checkpoint, worked straight through the P0/P1/MEDIUM/LOW/PERF release-blocker backlog sourced from the independent code review, in strict TaskList id order, using the same established pattern for every item: investigate the code personally first, either fix directly in single-context (when the task's own strategy said so and the scope held) or write a grounded brief (`docs/dev-artifacts/prompts/release-blockers/<NN>-*.md`), commit the brief, dispatch to `crush run` in the background, then zero-trust-verify every delegated diff by reading it in full, re-running `fmt`/`clippy`/the relevant test suites myself, and committing only after personally confirming everything green.

Completed and committed this segment (in order): **#1074** (intra-tx unique-guard false `UniqueViolation` on release-then-reclaim — delegate correctly walked `tx.index_write_set` for true final ownership, verified via revert-and-check), **#1075** (typed `CreateIndex` builder silently discarding already-set options — delegate added `ConflictingBuilderState` detection across all 9 typed constructors plus empty-fields validation, breaking-change-tolerant since alpha), **#1076** (CLAUDE.md's false "sole remaining Mutex exception" claim — done single-context by me: personally re-verified ~14 `std::sync::Mutex`/`parking_lot::Mutex` sites across the workspace via direct reading, found the claim was provably false, replaced it with three durable rule-based categories instead of an enumerable list that would go stale again, explicitly called out two hot-by-frequency sites — `SegmentSet::inner`, `Session::post_auth_bucket` — as NOT covered, deferred their migration to new task **#1090** rather than risk a same-session rewrite of crash-critical/security-critical code), **#1077** (`PerIpLimiter::try_acquire`'s misleading doc comment claiming the shard lock is released before the CAS loop — investigated and found the CURRENT code is actually safe and the comment is just wrong; critically, discovered that the "obvious fix" the task suggested would reintroduce the EXACT race #1073 fixed, so did a doc-only correction, no behavior change), **#1078** (`DropIndexOp::unique` dead-for-resolution field still perturbing the HMAC signature, plus `if_exists` asymmetry vs `DROP TABLE` — escalated to `/crush` after investigation revealed the true scope was much larger than "small edit" implied, spanning Rust query-types/query-builder/server/db and TypeScript/e2e-JS simultaneously; delegate did both fixes correctly, I personally caught and fixed one quality issue in the delegate's error code choice — `"query_error"` renamed to the more specific `"index_not_found"`, updating both new tests to match), **#1079** (sorted range-scan `BTreeSet`-per-element + audit-checkpoint missing `spawn_blocking` — done single-context: fixed `SortedIndexManager::lookup_range`'s return type from `BTreeSet<RecordId>` to `Vec<RecordId>` mirroring the hash family's established pattern, and added a `blocking_io()` helper around `AuditChainWriter`'s fsync calls that correctly checks `runtime_flavor()` before reaching for `block_in_place` — necessary because several existing `audit_appender.rs` tests exercise the trait as plain synchronous `#[test]` functions with no tokio runtime at all, which a naive `block_in_place` wrap would have panicked on; deliberately did NOT touch `IndexRegistry::lease_by_field_and_kind`'s O(N) resolve — the third of three independent points — after investigating and finding `registry.rs` is one of the most carefully-reasoned concurrent files in the workspace with an explicit doc comment warning against exactly the side-map approach the task suggested, deferred to new task **#1091**; during verification hit 3 flaky `shamir-engine` test failures — `wal_segment_count_bounded_under_drain`, `vr5_cofilter_sees_staged_and_filters_residual`, `p1061_bounded_barrier_duration_constant_across_sizes` — confirmed all three as environmental flakes from concurrent load during this long session by re-running each in isolation, all passed clean, none reference the touched code).

**Currently in flight: #1080** (`QueryResult { .., ..Default::default() }` at ~133 sites disabling exhaustive field-checking — a "nit" severity task). Investigated personally first: sampled actual call-site shapes directly (not just trusted the review's framing) and found they are NOT uniform — some spell out 7-8 of 10 fields and only rely on the default for `op_id`/`ddl_status` (the two fields `#1015` added later), others spell out far fewer, meaning no single regex/sed substitution is safe across all 133 sites without per-site attention when the compiler flags a field conflict. Wrote a detailed brief (`docs/dev-artifacts/prompts/release-blockers/80-p1080-query-result-exhaustive-defaults.md`, committed `bbe9b92e`) confirming option (b) — helper constructors like `QueryResult::rows(records)` for the common shapes — over option (a) — spelling out all fields everywhere — since it addresses the root cause (future field additions only need reviewing a handful of helpers, not 133 call sites) rather than just the symptom, and dispatched to `crush run --session p1080-queryresult-defaults` (background task, now showing `.crush/stdin/p1080.out` populated with a `final_text` JSON blob — the run has produced output and appears complete, but I have NOT yet read/verified its content or diff; this checkpoint was triggered by the user's explicit `/checkpoint` invocation arriving before I could start that verification pass). The working tree currently shows 21 modified files spanning `shamir-engine`, `shamir-query-types`, `shamir-query-builder`, `shamir-db`, `shamir-server` from this in-flight, uncommitted delegate run.

**Two new follow-up tasks created this segment, both deliberately deferred rather than attempted same-session**: **#1090** (migrate `SegmentSet::inner` WAL-append-path and `Session::post_auth_bucket` post-auth-rate-limit-path off `std::sync::Mutex` to lock-free — both are hot-by-call-frequency but currently argue low actual contention via single-writer/bounded-concurrency models; explicitly NOT sanctioned as permanent exceptions in the CLAUDE.md fix, tracked as real debt instead) and **#1091** (`IndexRegistry::lease_by_field_and_kind`'s O(N) per-resolve scan of index2 backends — deferred because `registry.rs`'s `insert()`/`BackendEntry::gen` carry extensive, hard-won atomicity reasoning that explicitly rejects a side-map approach for the exact reason the task's suggested fix would introduce one).

**Delegation channel this whole segment: `/crush` exclusively** (per the user's earlier explicit "после завершения агента переходим на /crush" instruction from before the prior checkpoint) — no `@sh`/Agent-tool dispatches this segment. A prior double-backgrounding mistake (adding a trailing `&` inside a `crush run` command ALSO passed to `run_in_background: true`, producing a false early completion notification from the shell wrapper rather than the real process) was caught once early in this segment and corrected — subsequent dispatches used `run_in_background: true` alone, no `&`, which produces correct completion signaling since the tool backgrounds the whole synchronous command properly.

**Governing hook status**: the hook's own text requires, only AFTER ALL tasks are done: `/checkpoint` (in progress — this file), CHANGELOG update, committing all `.md` files, and an `@oh` review agent pass. None of those closing steps have been started yet — the backlog itself (`#1080` verification, then `#1081`–`#1086`, `#1090`, `#1091`) is not yet complete either. This checkpoint is a mid-flight snapshot triggered by explicit user invocation, not a claim that the governing hook's terminal condition is met.

## Active goal

Standing `/babygoal` Stop-hook (verbatim, unchanged across the whole session): "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу"

## TaskList

### in_progress
- #1080 nit: QueryResult { ..Default::default() } × 133 мест отключает проверку компилятором новых полей (crush session `p1080-queryresult-defaults`, output produced, NOT yet personally verified — next action)

### pending
- #982 crush-fallback state (persistent sentinel — do not complete)
- #1049 P1-6 follow-up: identify actual stalling operation using new diagnostic logging, then fix (non-blocking, waits on a real recurrence)
- #1081 ReaderDrainGate: перф-hardening (3×SeqCst, unbounded spin, gate на всё семейство) + неверное доказательство livelock-freedom
- #1082 CachedStore::flush — unbounded busy-poll в двух местах, ошибка фоновой записи теряется молча
- #1083 Batch::try_build делает полный msgpack round-trip на каждый op + 4 expect() в публичном не-тестовом пути
- #1085 Watchdog на границе request/batch + аудит cancellation-safety перед реальным timeout
- #1086 Docs: sizing guide числа для cursor rescans, FTS/functional rebuild time, vector quality статус (item 3 answerable from #1070's real matrix data; items 1-2 permit extrapolation, no fresh bench runs needed per standing user instruction)
- #1090 PERF: мигрировать SegmentSet::inner и Session::post_auth_bucket с std::sync::Mutex на lock-free (new this segment, deferred from #1076's investigation)
- #1091 PERF: IndexRegistry::lease_by_field_and_kind — O(N) per-resolve сканирование index2-бэкендов (new this segment, deferred from #1079's investigation)

### recently completed (this segment)
- #1079 PERF: sorted range-scan BTreeSet + audit-checkpoint spawn_blocking (2 of 3 points; 3rd deferred to #1091)
- #1078 LOW: DropIndexOp::unique dead HMAC field + if_exists asymmetry vs DROP TABLE
- #1077 LOW: PerIpLimiter::try_acquire misleading doc comment (no behavior change — the "fix" would have been a regression)
- #1076 MEDIUM: CLAUDE.md false Mutex-exception claim
- #1075 MEDIUM: typed CreateIndex builder silent option loss
- #1074 MEDIUM: #1039 unique-guard false UniqueViolation on release-then-reclaim

## Decisions

- **#1076 done single-context, not delegated**: the task's own investigation (re-verifying ~14 Mutex sites personally) was fast enough and the CLAUDE.md edit itself contained enough nuance (three rule-based categories vs an enumerable list) that direct authorship was more reliable than a brief-and-verify round trip for a documentation-accuracy task.
- **#1076/#1079's genuinely-hot Mutex sites explicitly NOT sanctioned, tracked as debt instead (#1090)**: rather than either ignoring the review's "these should probably be lock-free" observation or rushing an unreviewed same-session rewrite of WAL-append/auth-rate-limit code, chose the middle path — document them honestly as known debt with a dedicated follow-up task, since both sit on crash-critical/security-critical paths that warrant dedicated stress-testing before a migration.
- **#1077: fixed the doc comment, explicitly did NOT apply the task's suggested code fix** — direct investigation showed the suggested "obvious" fix (clone the counter out, drop the entry guard early) would reintroduce the exact TOCTOU race #1073 had just fixed in the structurally-identical `CursorRegistry`. This is the clearest example this segment of the standing zero-trust discipline catching a plausible-but-wrong task premise before acting on it, not just before accepting a delegate's report.
- **#1078 escalated from single-context to /crush mid-investigation**: the task description's own "small edit" framing was contradicted almost immediately once I started reading the actual call sites (HMAC canonical bytes touch Rust AND TypeScript AND e2e JS fixtures) — escalating early, before writing any code, avoided sunk-cost pressure to under-scope the eventual brief.
- **#1079's third point (registry.rs) deliberately deferred, not attempted**: `IndexRegistry`'s existing atomicity documentation explicitly argues against the exact fix pattern (`BackendEntry::gen`'s own doc comment: "the two-maps-out-of-sync hazard a parallel side-map would introduce") — treated that as a real, considered constraint from the original authors, not an obstacle to route around under a "nit"/"not urgent at current N" task.

## Open questions

None blocking — the immediate next step is unambiguous and was interrupted only by this explicit `/checkpoint` invocation: read `.crush/stdin/p1080.out`'s full `final_text` content and the actual git diff for `#1080`'s ~21 currently-modified files, apply the same zero-trust verification (confirm the delegate's chosen helper-constructor shapes actually match the site survey, re-run `cargo fmt`/`clippy`/the full multi-crate gate the brief specified, check whether the delegate flagged any suspected latent bugs per the brief's explicit instruction not to silently fix those), then commit and continue down the TaskList in id order (#1081 next). After the numbered backlog (#1081, #1082, #1083, #1085, #1086, #1090, #1091) is exhausted, the governing hook's closing sequence — `/checkpoint` (already satisfied by this file, though a fresh one may be warranted after further progress), CHANGELOG update, commit all `.md` files, launch an `@oh` review pass — still needs to happen before the hook's terminal condition is met.

## Repo state

```
 M crates/shamir-db/src/shamir_db/execute/function_invoker.rs
 M crates/shamir-db/src/shamir_db/execute/tests/filter_value_tests.rs
 M crates/shamir-engine/benches/filter_eval.rs
 M crates/shamir-engine/src/query/batch/query_runner.rs
 M crates/shamir-engine/src/query/batch/tests/executor_tests/query_runner_tests.rs
 M crates/shamir-engine/src/query/batch/tests/query_value_serializer_tests.rs
 M crates/shamir-engine/src/query/filter/tests/eval_tests/collection_tests.rs
 M crates/shamir-engine/src/query/filter/tests/eval_tests/cond_gap_tests.rs
 M crates/shamir-engine/src/query/filter/tests/eval_tests/in_query_ref_column_tests.rs
 M crates/shamir-engine/src/query/filter/tests/eval_tests/query_ref_tests.rs
 M crates/shamir-engine/src/query/filter/tests/query_ref_cache_tests.rs
 M crates/shamir-engine/src/table/read_asof_seek.rs
 M crates/shamir-engine/src/table/read_exec.rs
 M crates/shamir-engine/src/table/read_index_scan.rs
 M crates/shamir-engine/src/table/read_temporal.rs
 M crates/shamir-engine/src/table/tests/filter_stream_tests.rs
 M crates/shamir-engine/src/table/tests/recordview_cutover_parity_tests.rs
 M crates/shamir-engine/src/table/tests/s3_bytes_path_parity_tests.rs
 M crates/shamir-query-builder/src/response/tests/response_tests.rs
 M crates/shamir-query-types/src/read/query_result.rs
 M crates/shamir-query-types/src/wire/tests/db_message_tests.rs
 M crates/shamir-server/src/db_handler/tests/subscribe_handler_tests.rs
?? devrust.cargo-target-check/
?? docs/checkpoints/2026-08-09-0945.md
?? docs/checkpoints/2026-08-09-1911.md
?? docs/checkpoints/2026-08-10-0842.md
?? docs/checkpoints/2026-08-11-0930.md
?? docs/dev-artifacts/research/2026-08-09-new-wave-review-claude.md
```

```
bbe9b92e docs(prompts): brief for #1080 -- QueryResult { .., ..Default::default() } exhaustive-checking nit
dab5760a perf(index,engine,server): #1079 -- drop per-element BTreeSet in sorted range-scan, block_in_place around audit-appender fsyncs
ce860dbc fix(query-types,query-builder,server,db,client-ts): #1078 -- drop dead DropIndexOp::unique HMAC field, fix if_exists asymmetry vs DROP TABLE
9394d000 docs(prompts): brief for #1078 -- DropIndexOp::unique dead-field HMAC perturbation + if_exists asymmetry vs DROP TABLE
9ac2760e docs(server): #1077 -- fix conn_limiter.rs claim that try_acquire's CAS loop doesn't hold the shard lock
0b24a472 docs(claude-md): #1076 -- fix false "sole remaining Mutex exception" claim, list all confirmed exceptions
d19761db fix(query-builder,client-ts): #1075 -- typed CreateIndex constructors reject stale builder state and empty fields
d4169f30 docs(prompts): brief for #1075 -- typed CreateIndex constructors silently discard already-set builder options
```
