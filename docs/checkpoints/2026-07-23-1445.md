# Checkpoint — 2026-07-23 14:45

## Session summary

This session continued directly from the RI-15+FG-5 campaign closing (CHANGELOG update, checkpoint, push, green CI — all done earlier this session, commits up through `bc3c99b8`). The user then asked me to review an in-chat-relayed external code review of the whole recent wave (`33109972..8a499694`), which found 6 P0 release blockers concentrated in the new cursor feature (ACL bypass, terminal-page resource leak, `page_size=0` infinite loop, keyset-pagination row loss on duplicate ORDER BY values, cursor responses bypassing the RI-15 byte budget, and an unrelated bootstrap-token-not-truly-one-time issue) plus High/optimization-tier findings. I verified several of the P0 claims directly against the code before trusting them, then created Wave A (#760–#766, one task per P0 + a final docs task), armed babysit, and began executing sequentially via `@sh` sub-agents with the established prompt-first + zero-trust-verify discipline (brief written and committed BEFORE each agent launch; every agent's diff read in full and the fmt/clippy/test gate re-run personally before committing, never trusting the delegated agent's own claim).

Mid-wave, the user asked me to also launch `@fx` (in an isolated worktree, in parallel) for a broader, independent release-readiness review explicitly scoped to skip the 6 already-known P0s. That review (`docs/dev-artifacts/research/2026-07-23-wave-review-followup.md`, copied out of the worktree and committed) surfaced significant NEW findings: **R-1 (HIGH, release blocker)** — the cursor's "stable snapshot" guarantee is actually broken by a concurrent DELETE, since `read_as_of` enumerates the CURRENT id set, not the pinned-version id set; **R-2 (HIGH, release blocker in its current oversold form)** — the RI-15 byte budget is acquired AFTER execution+serialization, so it only bounds write-path residency, not the ~64 GiB burst-allocation scenario the CHANGELOG claims to close; plus R-6 (cursor `FetchNext` is O(table) per page and doesn't actually reduce server-side memory, only wire/client-side), and about a dozen MED/LOW items (registry leak, stale docs, `has_more` off-by-one, hasher-convention violation, test-coverage gaps).

I then wrote a consolidation document (`docs/dev-artifacts/research/2026-07-23-review-consolidation.md`) mapping every finding from BOTH reviews to a disposition, and expanded the TaskList into three waves: **Wave A** (#760–#766, the original P0s + docs), **Wave B** (#767–#775, nine new tasks from the @fx findings plus rev-1's High-tier items: concurrent-DELETE fix, byte-budget upfront-reserve, `FetchNext` default-page-size dead field, `has_more` peek-ahead, `with_version`-through-cursor rejection, TS bigint CAS types, restore reordering+manifest hardening, registry chores, release-workflow gate), and **Wave C** (#776–#780, five polish/optimization tasks: SDK edge cases + ByteBudget fast-path + micro-nits batch, cursor test-coverage gaps, AsOf batched-lookup optimization, streaming backup hashing, exact Big-number comparisons). Dependencies are wired so file-conflicting tasks serialize correctly (e.g. Wave B's cursor-code tasks all `blockedBy` #764, the last Wave-A task touching `cursor_handlers.rs`).

Wave A execution status as of this checkpoint: **#760 (CR-A1, ACL enforcement)**, **#761 (CR-A2, terminal-page leak)**, **#762 (CR-A3, page_size validation)**, **#763 (CR-A5, byte budget wiring)**, and **#764 (CR-A4, keyset tie-breaker — the riskiest task in the wave)** are ALL done, verified, and committed (`b860765c`, `9f7b9d55`, `f048b218`, `a02dcd03`, `ccf02009` respectively). CR-A4 in particular required careful verification since the delegated agent deviated from the brief's literal design (a `_id`-based skip mechanism) after discovering `RecordId` isn't available on the AsOf read path, substituting a mathematically-equivalent ordinal tie-count (`tie_skip`) approach instead — I verified this substitution's correctness reasoning line-by-line and re-ran the new tests 3 extra times to rule out timing-sensitivity in its retry logic before committing.

**Currently in flight:** **#765 (CR-A6, bootstrap token rotation)** — the `@sh` agent has already reported completion and I have already reviewed its full diff (all 4 files: `bootstrap.rs`'s new `rotate_bootstrap_credential_to_random` helper, `handshake.rs`'s first-login wiring, `server_launcher.rs`'s TTL-sweep wiring with the username-read-before-consume ordering fix, plus 4 new/modified test files including a genuine e2e proving a second login with the same token now fails). The diff looked correct and thorough. I ran `cargo fmt`/`clippy` myself (both clean) and had JUST launched the full `./scripts/test.sh -p shamir-server --full` run in the background when this checkpoint was requested — **that test run's result has not yet been seen; this is the very next thing to check** (background task, will arrive via notification). A stray scratch file `cra6_test.log` is sitting in the repo root (untracked, will be deleted before committing, same pattern as every prior task this session).

**Not yet started:** #766 (CR-A7, docs truth pass — blocked on all of Wave A) and all of Wave B (#767–#775) and Wave C (#776–#780).

babysit cron `2c7d3bc8` (every 15 min, session-only) is armed and has been ticking normally throughout — most recent ticks correctly reported "still running #765" since the sub-agent/orchestrator verification was genuinely in progress each time.

## Active goal

Session-scoped Stop hook (armed earlier this session, still in force): "реализуй задачи с помощью @sh , между задачами делай коммиты, покрой их тестами." — not yet satisfiable (TaskList still has 15 pending/in-progress tasks across Waves A/B/C).

## TaskList

### in_progress
- #765 CR-A6. Bootstrap token: truly one-time — rotate SCRAM credential on first login / TTL (diff reviewed and looks correct; fmt/clippy clean; full test suite run launched in background, result not yet seen)

### pending
- #766 CR-A7. Docs truth pass: KNOWN_LIMITATIONS / README / CHANGELOG vs actual state (blockedBy: #760, #761, #762, #763, #764, #765)
- #767 CR-B1. Cursor snapshot stability vs concurrent DELETE — fix read_as_of enumeration (R-1, release blocker)
- #768 CR-B2. Byte budget: upfront estimate-reserve then shrink + single serialization (R-2 + P-2) (blockedBy: #763)
- #769 CR-B3. FetchNext page_size: Option<u32> + stored default fallback (R-5) (blockedBy: #764)
- #770 CR-B4. has_more via page_size+1 peek-ahead (B-3 / rev-1 High) (blockedBy: #769)
- #771 CR-B5. Reject with_version=true at CreateCursor (rev-1 High) (blockedBy: #764)
- #772 CR-B6. TS CAS types: number|bigint for versions/expected_version (rev-1 High)
- #773 CR-B7. Restore: invalidate tickets BEFORE swap + manifest hardening (rev-1 High)
- #774 CR-B8. Registry chores: by_session entry leak + THasher convention (blockedBy: #764)
- #775 CR-B9. Release workflow: full gate before tag release
- #776 CR-C1. Polish batch: config validation, SDK edge cases, ByteBudget fast path, micro-nits (blockedBy: #770, #771)
- #777 CR-C2. Cursor test-coverage gaps (B-2) (blockedBy: #770)
- #778 CR-C3. AsOf read path: batch per-record MVCC lookups (R-6) (blockedBy: #767)
- #779 CR-C4. Backup/restore: streaming SHA-256 + copy (blockedBy: #773)
- #780 CR-C5. Exact Big-number comparisons: eliminate f64 rounding (rev-1)

### recently completed
- #764 CR-A4. Keyset tie-breaker (riskiest task, `ccf02009`)
- #763 CR-A5. Cursor byte budget + page-size cap (`a02dcd03`)
- #762 CR-A3. page_size validation (`f048b218`)
- #761 CR-A2. Terminal-page cursor leak (`9f7b9d55`)
- #760 CR-A1. Cursor ACL enforcement (`b860765c`)

## Decisions

- **CR-A4's `_id`-based design was replaced with an ordinal `tie_skip` count** — `RecordId` is not carried through the AsOf read path's projection helpers, so the brief's literal "skip past last_id" mechanism was infeasible; the substitute exploits the SAME stable-sort/deterministic-enumeration guarantee the brief already verified, and I independently re-verified the substitution's correctness before committing.
- **@fx's R-1 and R-2 findings were NOT folded into the in-flight Wave A tasks** — they became separate Wave B tasks (#767, #768) specifically to avoid scope creep on already-in-progress/already-completed Wave A work; #763/#764's task descriptions were explicitly annotated to warn future readers not to expand into #767/#768's territory.
- **Wave C (optimizations/polish) was created but explicitly deprioritized** — lowest-id-first sequential execution means Wave A and B will exhaust before Wave C starts naturally, no special gating needed beyond normal id ordering.
- **Docs task (#766) is blocked on ALL of Wave A** (not just the security-critical ones) so it describes the fully-post-fix state rather than a moving target.

## Open questions

- None blocking. The full `./scripts/test.sh -p shamir-server --full` run for CR-A6 was launched in the background right before this checkpoint and its result is the immediate next thing to check — not a question, just unfinished verification.

## Repo state

```
 M CHANGELOG.md
 M crates/shamir-server/src/bootstrap.rs
 M crates/shamir-server/src/connection/handshake.rs
 M crates/shamir-server/src/server/server_launcher.rs
 M crates/shamir-server/src/tests/bootstrap_tests.rs
 M crates/shamir-server/src/tests/mod.rs
 M crates/shamir-server/tests/bootstrap_token_lifecycle_e2e.rs
?? cra6_test.log
?? crates/shamir-server/src/tests/bootstrap_rotation_changepw_tests.rs
```
(`cra6_test.log` is a scratch background-test-output file, will be deleted before the CR-A6 commit — same pattern as every prior task's `craN_test.log` this session.)

```
2f25d1d6 docs(prompts): brief for #765 -- CR-A6 bootstrap token rotation
ccf02009 fix(server): CR-A4 -- keyset tie-breaker: no lost rows on duplicate ORDER BY values
0e5bdfda docs(prompts): brief for #764 -- CR-A4 keyset tie-breaker
a02dcd03 fix(server): CR-A5 -- route cursor responses through byte budget + page-size cap
dc9185aa docs(prompts): brief for #763 -- CR-A5 cursor byte budget + page size cap
```

Branch `master`, in sync with `origin/master` as of the last push (`bc3c99b8`) — all Wave A commits since then (`b860765c` through the pending CR-A6 commit) are LOCAL ONLY, not yet pushed (no push requested/authorized this session for the Wave A/B/C work).
