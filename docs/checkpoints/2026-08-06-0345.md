# Checkpoint — 2026-08-06 03:45

## Session summary

Продолжение сессии после завершения волны R0 (correctness-freeze, чекпоинт `2026-08-06-0230.md`) и адверсариального ревью `@oh`, нашедшего 2 реальных регрессии (F-1 CRITICAL, F-2 HIGH — обе исправлены и закоммичены) плюс 6 менее приоритетных находок (F-3, F-4, F-5-F-8), заведённых как таски #1029-#1031.

Пользователь запустил новый `/babygoal` с формулировкой "давай наладим все тесты, ci" — новая session-scoped цель, требующая зелёного CI и закрытия всех открытых тасок. Ход работы:

1. **Проверил статус `@oh`'а через консультационный `Agent`-вызов** (не ревью, а совет) по всем открытым архитектурным вопросам (#1011, #1018, #1021, #1019, приоритеты F-3/F-4). Ключевая находка агента: **вся волна R0 (14 коммитов) никогда не проходила через CI** — ветка не была запушена.
2. **Запушил волну** (`c6fdaa7b`). CI показал: из 13 джобов только один красный — `cargo test integration (ubuntu-latest)`, `TIMEOUT [600.015s] rename_table_durability::rename_populated_survives_cold_restart`. Windows-интеграция (старая находка #1019) оказалась зелёной — похоже была транзиентной.
3. **F-4 исправлен** (#1029, коммит `72357383`, запушен `cccf2d16`-веткой): `IF NOT EXISTS` для sorted/index2 CREATE был сломан преflight-проверкой из R0-C; фикс — `any_index_exists` в хендлере.
4. **Расследовал зависание** (#1032): прочитал реальный CI-лог (уже была инструментация с прошлого расследования F-68/#895/#124), нашёл точную точку — лог обрывается на `"rename_table_stores: calling drain_to_history from=users"`, фоновый Drainer продолжает тикать (рантайм не заморожен, зависла одна конкретная future внутри `MvccStore::drain_to_history`). Три попытки локального воспроизведения (изолированно + под полной нагрузкой) не смогли поймать гонку — подтверждает узкое, нагрузочно-зависимое окно (тот же паттерн, что и в прошлых CI-only находках #946/#949). Добавил более точную инструментацию (до/после каждого шага внутри `drain_to_history`, вокруг `Store::transact` в `write_committed_to_history` — общая функция с фоновым Drainer, покажет, не гонятся ли оба пути за один физический стор). Закоммичено (`cccf2d16`) и запушено — ждём следующего реального CI-повторения зависания для точного лога.
5. **Пользователь через Stop-hook feedback указал**: работа была только диагностикой, не фиксом; #1030 (F-3) и #1031 не начаты; #1033 заблокирована. Требование: продолжать, не останавливаться на диагностике.
6. **Начал F-3** (#1030): написал детальный бриф (`docs/dev-artifacts/prompts/ddl-lifecycle/08-f3-cascade-repair-admission.md`, коммит `d2355900`) — `DROP TABLE CASCADE` и `doctor::repair()` мутируют реестры индексов НАПРЯМУЮ, в обход `ddl_admission`, нарушая единственное обоснование безопасности слияния счётчиков из R0-A. Запустил `@sh` в фоне.

**Прервано пользователем на `/checkpoint` в момент, когда агент `@sh` уже работал над F-3** — рабочее дерево показывает незакоммиченные изменения агента (`admin_table_index.rs`, `doctor.rs`, plus два новых тестовых файла) — **эти правки ещё НЕ проверены zero-trust и НЕ закоммичены**. Следующий шаг после этого чекпоинта: дождаться завершения агента (или проверить его текущий прогресс), затем провести обычную проверку (прочитать дифф, прогнать fmt/clippy/тесты лично) и закоммитить.

CI на SHA `cccf2d16` (F-4 + инструментация) на момент чекпоинта ещё **in_progress** — результат неизвестен.

## Active goal

Session-scoped Stop hook: **"давай наладим все тесты, ci"**. НЕ удовлетворена — пользователь явно подтвердил через feedback, что требуется: все тесты зелёные И все открытые таски закрыты/завершены. Условие остаётся в силе, продолжать работу.

## TaskList

### in_progress
- #1030 F-3: DROP TABLE CASCADE и doctor::repair() обходят ddl_admission — агент `@sh` работает, результат ещё не проверен/закоммичен
- #1032 HANG (CI-BLOCKING): rename_populated_survives_cold_restart — диагностика продвинута (точная точка зависания найдена), инструментация запушена, фикс ещё не написан — ждём следующего CI-повторения

### pending
- #982 crush-fallback state (sentinel, never completes) — sh ACTIVE (hard-limit), сброс ~2026-08-06 17:01
- #1011 P0-3a: DROP vs активные readers — решение оператора (не код)
- #1014-#1017, #1019, #1020, #1022 — R1/release backlog, готовы к делегированию
- #1018, #1021 — решения оператора (не код)
- #1024-#1026 — development backlog
- #1031 F-5..F-8 LOW: doc/hygiene хвосты — не блокер
- #1033 Push финального фикса + подтвердить зелёный CI — blockedBy #1032

### recently completed
- #1006-#1010, #1012, #1013, #1023 (вся волна R0)
- #1027 (F-1 CRITICAL), #1028 (F-2 HIGH), #1029 (F-4 MEDIUM)

## Decisions

- По совету `@oh` (консультационный проход): пушить волну и смотреть реальный CI ПЕРЕД тем, как продолжать F-3/F-4 — оказалось критичным, т.к. вся волна вообще не проходила CI.
- Не стал перезапускать красный CI-джоб вслепую (31078031951) — вместо этого прочитал полный лог, нашёл точную причину (зависание, не флака), и это же зависание оказалось ТЕМ ЖЕ тестом, что уже был известен по прошлым CI-инцидентам (#946, #949) — не новая регрессия этой волны.
- При неудачных попытках локального воспроизведения зависания — не стал форсировать дальше (3 попытки, ~25% исторический hit rate), а последовал уже установленному в проекте прецеденту (F-68 брифа): добавить точную инструментацию, положиться на CI как надёжную среду воспроизведения, не гадать вслепую.
- После явного фидбека пользователя о том, что диагностика — не фикс — переключился на параллельную работу над F-3 (#1030), не дожидаясь результата CI-прогона с инструментацией (правильная стратегия — не сидеть без дела в ожидании внешнего события).

## Open questions

- Результат CI-прогона на `cccf2d16` — неизвестен на момент чекпоинта (либо зависание повторилось — тогда есть точный лог для фикса #1032, либо прошло — тогда красный CI временно устранён, но гонка не закрыта).
- Результат работы агента `@sh` над F-3 — не проверен (агент запущен, изменения в рабочем дереве есть, но не верифицированы и не закоммичены).
- #1011, #1018, #1021 — по-прежнему решения оператора, не код (см. предыдущие чекпоинты и консультационный ответ `@oh`).

## Repo state

```
 M crates/shamir-db/src/shamir_db/execute/admin_table_index.rs
 M crates/shamir-db/tests/ddl_wire_e2e/main.rs
 M crates/shamir-engine/src/table/doctor.rs
 M crates/shamir-engine/src/table/tests/mod.rs
?? crates/shamir-db/tests/ddl_wire_e2e/admission_barrier_tests.rs
?? crates/shamir-engine/src/table/tests/f3_cascade_repair_admission_tests.rs
?? devrust.cargo-target-check/
```
(незакоммиченные правки принадлежат работающему агенту `@sh` над F-3 — НЕ проверены, НЕ трогать вручную до завершения агента)
```
d2355900 docs(prompts): brief for F-3 -- route DROP TABLE CASCADE + doctor::repair() through ddl_admission
cccf2d16 test(diagnostics): #1032 -- finer-grained instrumentation for the drain_to_history hang
72357383 fix(db): F-4 -- restore IF NOT EXISTS for sorted/index2 CREATE (#1029)
4563421a docs(prompts): brief for F-4 -- restore IF NOT EXISTS for sorted/index2 CREATE
c6fdaa7b docs: CHANGELOG entry for F-1/F-2 fixes + session checkpoint
```
