# Checkpoint — 2026-08-12 14:20

## Session summary

Continuing the standing `/babygoal` Stop-hook session: "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу". This checkpoint follows the `#1096`/`#1097`/`#1098` correctness-fix arc documented in earlier checkpoints (not repeated here — see `2026-08-12-0410.md` for the full `#1096` closure and the CHANGELOG for the complete `#1097`/`#1098` narratives) and covers the subsequent work: closing `#1097` (3 review rounds, each finding real defects, all fixed), closing `#1098` (3 review rounds — round 1's reader-side fix alone was found insufficient by round 2, which fixed the writer's own publish order too; round 3 verified both sound and found one more sibling defect in a different family, tracked as `#1099`-`#1102`), completing `#1092`'s benchmark phase (catching and fixing a real methodology bug in the `/crush`-generated bench — it used a single-threaded tokio runtime that masked all genuine cross-core contention), and then — per an explicit user instruction mid-session ("используй worktress если задачи можно решать параллельно") — dispatching `#1099` and `#1102` as two genuinely independent `/crush` runs in separate git worktrees (`​.claude/worktrees/p1099-touched-probe` and `.claude/worktrees/p1102-regular-index-order`), both still running as of this checkpoint.

Both `#1099` and `#1102` are HIGH/PERF fixes with fully-scoped, committed briefs (`docs/dev-artifacts/prompts/release-blockers/85-*.md`, `86-*.md`) — `#1099` replaces `touched_records_in_tx`'s O(N) upfront set-build with an on-demand O(1) probe (mirroring `pre_commit.rs`'s existing Step 2 pattern), scoped to touch only `table_manager_tx_ops.rs`/`pre_commit.rs`; `#1102` fixes the SAME read-order defect class `#1098` closed but in the regular (non-unique) hash-index family, scoped to `crates/shamir-index/` only — the two worktrees were deliberately chosen for zero file overlap. `crush sessions watch`/`locks`/`why` turned out to be session-DB-scoped to the CWD they're invoked from — a `sessions watch` launched from the main repo directory silently can't see a session started from inside a worktree (`Session not found`); both watchers were relaunched `cd`'d into their respective worktree directories, which fixed it. This is worth remembering for any future worktree-isolated `/crush` dispatch this session or a later one.

This session has by now completed an extraordinarily long, deep adversarial-review arc: `#1096` (4 rounds), `#1097` (3 rounds), `#1098` (3 rounds), each round finding and fixing a real defect before the next round found something new — a demonstrated, not theoretical, case for the zero-trust-verification discipline this whole session has followed (mutation-testing every fix, personally re-running every gate, never trusting a sub-agent's or reviewer's claim without independent verification). Along the way, 6 new follow-up tasks were correctly triaged as out-of-scope-but-tracked rather than chased indefinitely in the same sitting: `#1099`, `#1100`, `#1101`, `#1102` (now `#1099`/`#1102` in flight), `#1103` (ReaderDrainGate phase 2, explicitly low priority per its own honest benchmark data), and `#1100`/`#1101` remain untouched pending.

## Active goal

Standing `/babygoal` Stop-hook (verbatim, unchanged across the whole session): "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу"

## TaskList

### in_progress
- #1099 PERF: update_tx_bytes still O(N²) on tables WITH a unique index — released/touched walks recomputed per row — /crush running in worktree `.claude/worktrees/p1099-touched-probe`, session `p1099-touched-probe`
- #1102 HIGH: regular (non-unique) hash index publishes has_indexes AFTER bump_generation — same defect class as #1098, unfixed — /crush running in worktree `.claude/worktrees/p1102-regular-index-order`, session `p1102-regular-index-order`

### pending
- #982 crush-fallback state (persistent sentinel — do not complete, structurally unsatisfiable)
- #1049 P1-6 follow-up (non-blocking, waits on a real recurrence)
- #1094 Watchdog for stuck batch ops — 3 prior in-process attempts all reverted after reproducing stack overflow; needs an out-of-band (OS-thread) mechanism, own dedicated design cycle
- #1095 PERF: SegmentSet::inner off std::sync::Mutex — highest-blast-radius code in the workspace (WAL durability); prior investigation found contention growth but could NOT cleanly attribute it to this lock vs. the group-commit coordination layer above it; needs new bench instrumentation to isolate before any rewrite — deliberately NOT parallelized this session, too risky to rush
- #1100 HIGH: DELETE planned from a stale snapshot value never removes the record's CURRENT posting — permanent dangling posting (different root cause than #1097/#1098's fixes — an op that should exist is never planned, not an existing op misbehaving)
- #1101 HIGH: Step 2's "released -> continue" shortcut skips the durable check for a same-tx net-release, letting it silently delete an unrelated concurrently-committed posting
- #1103 PERF (low priority): ReaderDrainGate phase 2 — loom infra for shamir-index + evaluate ordering relaxation against real (now honestly measured) contention data

### recently completed
- #1092 ReaderDrainGate benchmark (phase 1) — corrected a real single-threaded-runtime methodology flaw in the /crush draft, found genuine (if modest) contention scaling, honestly recorded in the task's own follow-up (#1103)
- #1098 unique-guard/generation read-order race — 3 adversarial review rounds; round 2 found round 1's fix alone insufficient (writer's own publish order also needed fixing); round 3 verified both sound
- #1097 RemovePosting has no owner — 3 adversarial review rounds, each found and fixed a real defect
- #1096 release-then-reclaim of a durable unique value — 4 adversarial review rounds (see earlier checkpoint for full detail)

## Decisions

- **Personally caught and fixed a real bench-methodology bug** in #1092's /crush-generated first draft (single-threaded tokio runtime silently defeating the whole point of a 1/8/32/64-thread contention benchmark) rather than trusting the sub-agent's clean-looking numbers and confident "no further work needed" conclusion — re-ran with a corrected multi-thread runtime and got materially different (and more informative) results.
- **Marked #1092 completed at the end of phase 1** (benchmark delivered, decision documented) rather than leaving it in_progress indefinitely waiting for an unscoped "phase 2" — spun phase 2 (loom infra + ordering relaxation) into a new, explicitly low-priority task (#1103), matching this session's established `#1096`→`#1099` pattern of closing what's done and tracking what's genuinely separate.
- **Adopted git-worktree isolation for parallel /crush dispatch** per an explicit user instruction mid-session, after confirming #1099 and #1102 have zero file overlap (verified by reading both tasks' full descriptions, not just assuming from their titles) — deliberately did NOT add #1095 or #1100/#1101 to this same parallel batch: #1095 is flagged by its own task description as needing careful, non-rushed investigation on the highest-blast-radius code in the workspace (WAL durability), and #1100/#1101 both still need their own fix-shape investigation before a brief can be written with the same precision #1099/#1102's briefs have.
- **For #1102 specifically, investigated the read-side ordering fix shape myself before writing the brief** (unlike #1098's unique-flag fix, `has_indexes` is read with `Relaxed` not `SeqCst`, so a writer-order swap alone is insufficient) — presented the implementer with the two viable approaches (fold into the existing `write_barrier_flags` packed SeqCst word vs. standalone `Acquire` load) and the reasoning for preferring the fold, but left the final choice up to the implementer's own verification once it starts working, with an explicit instruction to write out the happens-before argument rather than shipping a bare ordering change.

## Open questions

None blocking. Both in-flight worktree dispatches (`#1099`, `#1102`) have fully-scoped, committed briefs and will need the same zero-trust verification (diff review, mutation testing, full gate) this session has applied to every prior fix before being merged back to `master` and the worktrees cleaned up. `#1100`/`#1101`/`#1094`/`#1095`/`#1103` each have a recorded reproduction/analysis and a sketched fix direction in their own task descriptions but explicitly need their own dedicated investigation-then-implementation cycles — do not implement any of them free-hand from the sketch alone.

## Repo state

```
?? devrust.cargo-target-check/
```
(Both worktree directories `.claude/worktrees/p1099-touched-probe` and `.claude/worktrees/p1102-regular-index-order` are untracked-but-present git worktrees, not shown by `git status` from the main tree — see `git worktree list` below.)

```
e110b448 docs(prompts): briefs for #1102 (regular-index gen/flag order) and #1099 (touched_records_in_tx on-demand probe) -- parallel worktree dispatch
a082c47c perf(index): #1092 phase 1 -- ReaderDrainGate concurrency benchmark, corrected to use a real multi-thread runtime
2e827964 docs(prompts): brief for #1092 phase 1 -- benchmark ReaderDrainGate before any architectural change
8af2ee21 docs: CHANGELOG entry for #1098's third (verification) review round and #1102 tracked follow-up
7061fefe docs(engine,index): #1098 -- fix two stale writer-order doc claims and a misplaced comment
```

Active worktrees for this session's in-flight dispatches:
```
D:/dev/rust/shamir-db/.claude/worktrees/p1099-touched-probe        e110b448 [wt/1099-touched-probe]
D:/dev/rust/shamir-db/.claude/worktrees/p1102-regular-index-order  e110b448 [wt/1102-regular-index-order]
```
