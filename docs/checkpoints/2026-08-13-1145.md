# Checkpoint — 2026-08-13 11:45

## Session summary

Continuation of the standing `/babygoal` session. This segment covers the final `@oh` adversarial review the babygoal itself requires ("после завершения всей работы... запусти ревью агента @oh") and two full rounds of remediation it triggered.

**Round 1**: launched a full-session `@oh` review over commit range `59e8c532..HEAD` (the `#1096`-`#1103` arc). It found 5 real issues: `#1105` CRITICAL (`op_watchdog.rs` leaked one OS thread per batch op — `init_watchdog`'s `std::thread::spawn` ran before the `OnceLock` dedup instead of inside it), `#1106` HIGH (`#1100`'s `rederive_stale_value_ops_post_stage` was owner-blind in its UPDATE-branch dedup, re-opening `#1100` for the unique family, and silently dropped ALL regular-index removals since `plan_record_deleted` always emits `owner: None` for that family), `#1107` MEDIUM (`#1100`'s "zero-cost on common path" gate never fired since `note_base_index_stage_gen` is called unconditionally), `#1108` MEDIUM (`#1099`'s `released_unique_keys_in_tx` was still a full O(N) walk per call, a residual O(N²)), `#1109` MEDIUM/docs (`#1095`'s CLAUDE.md claim about `SegmentSet::inner` rested on a bench amortization artifact — dividing a fan-out-and-join round trip by N and misreading amortization as the lock getting cheaper under contention).

`/crush` hit a hard weekly/monthly provider quota mid-dispatch (`#1107`). No `/crush-fallback` was configured; per the user's explicit direction, work continued via direct `sh` Agent sub-agents (some worktree-isolated for genuine parallelism, per the user's "use worktrees for parallelizable work" instruction) until the quota reset (confirmed back online via a throwaway `crush run` later in the session). All 5 findings were fixed and independently zero-trust-verified (diff reads, personal fmt/clippy/test/bench re-runs, personal mutation testing by reverting each fix). `#1107`'s own bench claim (15-30% improvement) did NOT reproduce under independent re-measurement — recorded honestly as noise-dominated rather than repeated as fact. `#1108`'s and `#1109`'s bench claims DID reproduce cleanly.

**Round 2**: ran a smaller, targeted follow-up `@oh` review over just the round-1 remediation commits, specifically probing the `#1106`/`#1107` interaction and `#1108`'s cache. It found 2 more real issues: `#1110` HIGH (`#1107`'s gate compared against `last_committed()`, which per a `materialize.rs` trace only advances at Phase 6 — a real false-negative window on paper) and `#1111` MEDIUM (`#1108`'s incremental cache still ended with `cache.released.clone()` on every call, itself O(N²) since the released set grows ~1 key/row).

`#1111` was fixed cleanly (split into `refresh_released_unique_cache` + a caller-side borrow, verified near-linear scaling personally). **`#1110` is the session's most significant finding of this segment**: while building the brief's own MANDATORY pause-seam reproduction test (required because a HIGH-severity concurrency claim needs empirical proof, not just a plausible-looking argument), the orchestrator discovered the test could not discriminate between the old and new gate — both behaved identically. Root-caused via targeted, fully-reverted `eprintln!` instrumentation across `pre_commit.rs`, `materialize.rs`, and `version_guard.rs`: `last_committed_version` is NOT published solely at Phase 6 as the original review (and this task's own round-1 delivery) assumed — `mvcc_store/mvcc_history.rs`'s `apply_committed_visible` (called from Phase 5a) ALSO calls `gate.publish_committed_max` directly and eagerly, a detail documented in that file's own comment but missed by every trace of `materialize.rs` alone (including the orchestrator's own initial one). The specific interleaving described does not reproduce for MvccStore-backed tables. The version-allocation-based gate was KEPT (it's still the more defensible primitive independent of this), but the happens-before proof comment and CHANGELOG entry were rewritten to state this honestly, and the non-discriminating pause-seam test infrastructure was fully removed rather than left in the tree misleadingly.

Round 1 of `#1110` itself also had its own defect the orchestrator caught before accepting: the delegate's happens-before proof claimed an `Acquire` reader synchronizes with a `Relaxed` `fetch_add` writer — false per the Rust/C++11 memory model. Fixed by strengthening `assign_next_version` to `AcqRel`.

**TaskList is now fully closed** except `#982` (persistent sentinel, designed to never complete) and `#1049` (explicit "do not start until new data appears") — both structurally non-actionable, established repeatedly this session. Every fix this segment is committed as a paired `fix`/`perf`/`docs` commit + a separate `CHANGELOG.md` commit, following the session's established convention. All briefs are committed under `docs/dev-artifacts/prompts/release-blockers/` before their corresponding dispatch, per `CLAUDE.md`'s prompt-first discipline.

## Active goal

Standing `/babygoal` Stop-hook: "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу" — the literal final step (launch `@oh` review) has now been executed TWICE (full-session pass + targeted follow-up pass), and every finding from both passes has been fixed and independently verified. This checkpoint marks that point.

## TaskList

### pending (deliberately non-actionable, not part of "all work")
- #982 crush-fallback state (persistent sentinel — structurally never completes)
- #1049 P1-6 follow-up (non-blocking — explicitly "do not start until new data appears")

### recently completed (this segment, chronological)
- #1105 CRITICAL op_watchdog.rs OS-thread leak (fixed: spawn moved inside OnceLock::get_or_init)
- #1106 HIGH #1100 round 3 — owner-aware dedup + regular-family fix in rederive_stale_value_ops_post_stage
- #1107 MEDIUM real repo-global staleness gate (version_allocation_high_water_mark, later further hardened by #1110)
- #1108 MEDIUM released_unique_keys_in_tx incremental cache (later found still-O(N²) by #1111)
- #1109 MEDIUM/docs SegmentSet::inner bench methodology corrected (sustained_append scenario)
- #1110 HIGH #1107's gate false-negative window — fixed, then honestly corrected after the required reproduction test failed to discriminate (root cause: MvccStore's own early Phase-5a publish)
- #1111 MEDIUM #1108's residual O(N²) clone eliminated (two-step refresh+borrow pattern)

(Full history back through #1054 also completed — see prior checkpoints, notably `2026-08-13-0130.md`, for the earlier arc this segment continues from.)

## Decisions

- **When `/crush` hit a hard quota limit with no fallback configured**, asked the user rather than guessing — user chose "set an 11:11 wakeup, meanwhile switch to the `sh` Agent sub-agent as a temporary fallback." Followed exactly; resumed `/crush` once confirmed back online via a throwaway `crush run`.
- **Used git-worktree isolation for `#1109`** (run in parallel with `#1108`, which had a disjoint file set) per the user's explicit "use worktrees for parallelizable work" instruction — merged by diffing the corrected files directly against master tip (not a full `git merge`) since the worktree's branch point had drifted behind several since-landed commits.
- **Did not accept `#1110` round 1's delivery at face value** despite it passing its own stated gate — personally built the brief's own required reproduction test, found it non-discriminating, and traced the actual root cause rather than assuming the review's original claim was correct just because the code compiled and tests passed. This is the segment's clearest instance of the session's zero-trust discipline catching something a surface-level "gate passed" check would have missed entirely.
- **Removed (not kept-but-disabled) the non-discriminating pause-seam test infrastructure** for `#1110` — a seam that can't prove what it claims to prove would be actively misleading left in the tree, even as inert dead code.
- **Corrected two prior CHANGELOG entries retroactively** (`#1107`'s and `#1108`'s) once follow-up review + personal investigation showed their original framing was inaccurate, rather than leaving stale/wrong explanations standing alongside the newer correct ones.

## Open questions

None blocking. One OPEN (non-blocking) investigative thread noted in `#1110`'s own commit/CHANGELOG text: whether the `base.transact` fallback path (tables with no `MvccStore` attached) has a REAL version of the false-negative window the original `#1107` review described for the MvccStore-backed path — not confirmed either way, not asserted as a live bug, no task opened for it (would need its own investigation if it matters in practice).

## Repo state

```
?? devrust.cargo-target-check/
?? docs/checkpoints/2026-08-12-1420.md
?? docs/checkpoints/2026-08-12-1730.md
?? docs/checkpoints/2026-08-13-0130.md
```
(Working tree otherwise clean — every substantive change committed. The three prior checkpoint files and this one are intentionally left for the user to `git add`, per this skill's own instruction. `devrust.cargo-target-check/` is a long-standing harmless stray scratch directory from an earlier `CARGO_TARGET_DIR` experiment.)

```
34ffaeb1 docs: CHANGELOG entry for #1111's fix + correction to #1108's residual-cause explanation
8c8b4ba8 perf(engine): #1111 -- eliminate #1108's residual O(N^2) clone in released_unique_keys_in_tx
47778c30 docs: CHANGELOG entry for #1110 (version-allocation gate + honest correction after failed reproduction)
8bab9de4 docs(engine): #1110 follow-up -- correct the false-negative-window claim after failing to reproduce it
184e6f99 docs(prompts): brief for #1110 follow-up -- pause-seam reproduction test for the version-allocation gate fix
b58984b9 fix(engine,tx): #1110 -- close #1107's gate false-negative window via version-allocation high-water mark
4d359bf0 chore: regenerate bench-iters.txt with #1109's sustained_append calibration entries
9f46cc8e docs(prompts): briefs for #1110/#1111 -- round-2 findings from follow-up @oh review of the #1105-#1109 wave
```
