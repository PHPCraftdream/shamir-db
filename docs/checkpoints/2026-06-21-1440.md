# Checkpoint — 2026-06-21 14:40

## Session summary

Длинная многофазная кампания оптимизации S.H.A.M.I.R. Database. Сессия началась
с post-wave bench delta (regression discovery → L12/L13/L6 fixes → backend
swap redb→fjall → win-snapshot). Затем backend_matrix steady-state бенч показал,
что в самом старом drain_throughput была cold-start bench-bug — реальный fjall
выдаёт 188K e/s vs прежних 2.4K. Дальше — три раунда research'а от пользователя
по «обвязке», каждый я ревьюил по коду (раунд 1 = WAL/drainer/markers оценки
без замеров; раунд 2 = «дешёвые wins» оказались contract-violations; раунд 3
нашёл настоящие read-path обрывы: S1/S2/S3). Финальный master-doc
(`obvyazka-research-master-2026-06-21.md`) переориентировал кампанию: write-path
≈in_memory ceiling, copать в read-path.

Затем кампания через ao46l/o46l агентов в isolated worktrees:
- **Phase 0a/b/c** — peak_mem infra + read_path_matrix harness + baseline.
  Baseline вскрыл что fast_path не triggers (priority shift от данных).
- **Phase 1** — оказалось: bench misconfig (sorted_index не создавался через
  create_index_v2) + missing iter_range_stream override в InMemoryStore.
  Fix дал **106,475× на fast_path/1M** (14.8s → 139µs). Top-K heap для
  no-index case тоже добавлен (memory O(N)→O(K)).
- **Phase 2** — try_plan_and_range_index_scan для AND. Selective range
  даёт +4-15×, **non-selective range регрессирует 3-16×** (classic DB tipping
  point без statistics). Принято с honest disclosure + добавлен
  s3_range_and_selective shape как win demo.
- **Phase 3** — ts-ordered index (in-memory scc::TreeIndex с lazy rebuild)
  для AsOf O(total) → O(log N). 4 unit tests proving correctness. End-to-end
  AsOf bench не isolates ts_index — он dominated full snapshot read.

Параллельно user попросил план bench-infra (#132-#135) — корень проблем
залипания benches (Phase 2/3 + 0c уходили в часы). 4 sequential задач:
audit → tune_tiered+SMOKE → tier all benches → guidelines doc.

В конце user указал на мою ошибку: остановил babysit cron (`9768de83`)
посчитав «goal achieved», игнорируя pending #132-#135. Создал новый cron
`273ae37e` с TaskList-driven stop condition. Написал feedback разработчику
/babysit skill: goal text — снапшот, TaskList — durable contract.

7 коммитов на master ahead of origin, не push'ил (запрет соблюдён).

## Active goal

`реализовать read-path план obvyazka-research-master-2026-06-21.md через
sequential ao46l-агенты (Phase 0a/b/c → S2 → S3 → S1); done = (1)
read_path_matrix bench committed с baseline curves в
docs/perf/read-path-baseline-2026-06-21.md; (2) S2 top-K + index-ordered scan
committed с memory-curve flattened vs baseline; (3) S3 range-extract from AND
committed с correctness fuzz green; (4) S1 ts-ordered index committed с
crash-recovery test green; out-of-scope: write-path (parked), S4/S6/J/parked
levers`

**Status:** все 4 done-критерия выполнены. Stop-hook должен auto-clear'иться.
Но в TaskList осталось 4 pending bench-infra задачи — следующая сессия
должна продолжить с #132.

## TaskList

### in_progress
(none)

### pending
- #132 Bench-infra A — audit + worst-case map существующих бенчей
- #133 Bench-infra B — per-cell timeout + SMOKE mode в shamir-bench-utils (blockedBy: #132)
- #134 Bench-infra C — tier existing benches + cap heavy shapes (blockedBy: #132, #133)
- #135 Bench-infra D — bench guidelines + CI suggestion doc (blockedBy: #133, #134)

### recently completed (read-path кампания)
- #125 Фаза 0a — Memory-measurement tooling + bench-fix K
- #126 Фаза 0b — Read-path bench harness (read_path_matrix)
- #127 Фаза 0c — Baseline run + curves docs
- #128 Фаза 1 — S2 bounded ORDER BY + LIMIT (top-K heap + index-ordered scan)
- #129 Фаза 2 — S3 range-extraction из AND → sorted-index scan
- #130 Фаза 3 — S1 ts-ordered index для AsOf

## Decisions

- **Cron prompt rewritten to TaskList-driven** (не goal-driven): goal — снапшот
  в момент `/goal`, TaskList — durable contract. Заведён feedback developer'у
  /babysit skill.
- **Phase 2 accept-as-is + honest disclosure**: non-selective range regression
  принят как classic DB optimizer tipping point без statistics. Selectivity
  heuristic = follow-up, не блок. Бенч добавлен selective shape (1%
  selectivity) для win demo + non-selective оставлен как regression guard.
- **Phase 3 bench Shape 5 — env-gated за `BENCH_READ_PATH_HUGE`**: end-to-end
  AsOf cost dominated full snapshot read (минуты на 100K), не подходит для
  default tier. Реальное доказательство O(log N) — 4 unit tests'а
  `ts_index_tests.rs`.
- **Phase 1 InMemoryStore iter_range_stream override**: добавлен batch-resume
  через scc::TreeIndex::range — раньше default impl делал full scan + filter,
  что обнуляло sorted-index optimization. Это **storage change**, не engine.
- **Bench-infra plan заведён, не запущен**: 4 sequential задачи (#132-#135),
  следующий шаг — Bench-infra A (audit existing benches, диагностика, без
  кода). Не блокирует никакую внешнюю работу.

## Open questions

- **Push разрешение** — 7 коммитов ждут на master ahead of origin/master,
  push требует явной user-санкции (запрет в CLAUDE.md).
- **Bench-infra приоритет** — запускать сразу следующую цепочку (#132 → ...)
  или подождать пока user явно скажет.
- **Persistent ts-index** — текущий in-memory с lazy rebuild on open. Persistent
  version (sorted storage entry, recoverable без полного history scan) — отдельный
  проект, не в плане сейчас.

## Repo state

```
?? docs/perf/backend-decision-2026-06-19.md
?? docs/perf/obvyazka-research-master-2026-06-21.md
?? docs/perf/obvyazka-research-review-2026-06-19.md
?? docs/perf/obvyazka-research-round2-review-2026-06-19.md
```

```
e2bd151 perf(mvcc): Фаза 3 — ts-ordered index для AsOf O(log N) (#130)
a2ea47a bench(engine): add s3_range_and_selective shape для Phase 2 win demo (#129)
f876229 perf(planner): Фаза 2 — try_plan_and_range_index_scan + bench selectivity disclosure (#129)
1801f63 perf(read): Фаза 1 — fast_path fix + top-K heap для bounded ORDER BY+LIMIT (#128)
bf56f8c docs(perf): Фаза 0c — read-path baseline curves + критическая находка (#127)
```

Состояние master: **7 коммитов ahead of origin/master**, 4 untracked
documentation файла (research artifacts из обвязка-исследований).

Babysit cron `273ae37e` (15-мин, session-only) активен с TaskList-driven
stop condition.
