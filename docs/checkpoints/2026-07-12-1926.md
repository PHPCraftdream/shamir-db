# Checkpoint — 2026-07-12 19:26

## Session summary

Continuing the standing audit-remediation campaign (`/babygoal`, TaskList-driven; the Stop-hook's literal text "Используй агентов sx (agent). Реализуй все задачи" is superseded in *mechanism* by the user's earlier `/crush` invocation — implementation delegation runs through `crush run` background sessions; adversarial review still uses the `Agent` tool/sx). This window closed out #555, #556, #557, #558 in the fxx-designed #548/#549 combined identity/privilege redesign chain, and is now mid-flight on #559 — the LAST and explicitly LARGEST/RISKIEST step per the design doc's own risk-flagging. The standing pipeline held throughout: investigate → write brief to `docs/prompts/audit/NN-*.md` → commit brief alone → launch `crush run` → independently verify (own diff read, own `cargo check`/`fmt`/`clippy`/`./scripts/test.sh` run — never trust the crush agent's self-report) → adversarial review via the `Agent` tool (sx) → fix any found defects directly → second review if a fix was made → commit.

**#555** (`Actor::Admin` + `principal64`, delete `principal_id`) — implemented (one crash/restart on a transient provider error), independently verified, adversarial review found and I fixed a real regression (chown-to-`OWNER_SYSTEM` lockout guard checked `actor != Actor::System` only, locking out every real superuser wire session post-change). Committed `7ba23698`.

**#556** (`FjallUserDirectory` v2 — principal64 keyspace, `remove()`, boot normalization) — implemented (one crash/restart), verified clean (350/350), review found nothing. Committed `3d0775f9`.

**#557** (superuser as a first-class flag: reservation, `SetSuperuser` op, handshake wiring) — implemented cleanly. **I caught and reverted a real scope violation**: the crush agent's own `cargo fmt` run had accidentally reformatted 4 unrelated pre-existing-drift files outside its scope — reverted via `git checkout --` before proceeding further. Verified (960/960), review found nothing exploitable (confirmed, not newly discovered, that resumed sessions already silently lost superuser status — explicitly flagged as #558's job). Committed `63093cf7`. Filed follow-up task #564 (CreateScramUser silently discards an update_roles error — pre-existing, unchanged by #557).

**#558** (Ticket v2 — resume re-verifies against the directory) — the DIRECT fix for the gap #557's review surfaced. `TicketPlain.roles` dropped entirely, wire version bumped 1→2, `UserStateLookup`'s return type widened to a new `ResumeUserState` struct, `process_resume`'s session construction hoisted out of the refresh/no-refresh branch split and built once from a FRESH directory lookup instead of the ticket's stale snapshot. Two existing tests that encoded the exact bug were rewritten (not patched) to prove the opposite; two new red tests added. Verified (666/666), adversarial review found nothing exploitable across 7 hunt areas including the `plain.username_nfc` vs `state.username` distinction and epoch/lookup mechanism composition. Committed `21493fa5`.

**#559** (retire shamir-db's own ineffective "Store B" users/roles tables via a `PrincipalResolver`/`UserAdminPort` seam) — this is the LARGEST task in the whole campaign per the design doc's explicit risk-flag ("should land alone"). Investigation for this one was unusually deep: delegated initial fact-finding to an Explore agent (BatchOp shapes, all 7 admin_users_roles.rs handler bodies, authorize_user_lifecycle, system-store schemas, HMAC canonical forms, server-boot wiring), then personally read `admin_users_roles.rs` in full (770 lines) and `ShamirDb`'s core.rs struct before writing the brief, since this task deletes 3 handlers (CreateRole/DropRole/RenameRole) and re-targets 4 others (CreateUser/DropUser/GrantRole/RevokeRole) through a brand-new cross-crate trait seam. Found and specified several things NOT spelled out mechanically in the design doc itself: (a) `PersistedUser` needs a new `database: Option<String>` field for `authorize_user_lifecycle`'s owner-delegation scope-lookup to keep working through the resolver — schema move only, no auto-import; (b) deleting `CreateRole`/`DropRole`/`RenameRole` from the `BatchOp` enum is a HARD compile-dependency that also forces deleting the Rust query-builder's constructors for them in THIS task (not deferred to #560 like the TS side); (c) `ListOp::Roles` should be deleted alongside the trio for consistency; (d) the design doc's "absent resolver → names resolve to null" framing must NOT be read as "keep using the interim `principal64_from_username` bridge from #555 forever" — the brief corrects this explicitly; (e) #557's already-correct `SetSuperuser` wire handler must NOT be re-routed through the new port "for consistency" — explicit scope-creep guard. Brief written and committed (`f50af37f`, `docs/prompts/audit/77-stores-549-shamir-db-retirement.md`). **Currently IN FLIGHT** via crush session `task-559-store-b-retirement` — confirmed genuinely in progress (a new `crates/shamir-db/src/shamir_db/ports.rs` file exists, `lib.rs`/`mod.rs` show real diffs, session message/token counts climbing between babysit-tick checks: 3→99 messages, 0→148011 tokens). Given the brief's own explicit warning that this may take more than one turn, a crash/restart or a long single run would both be unsurprising — no completion notification yet as of this checkpoint.

Task #560 (TS/Rust client builder sync — `SetSuperuser` client builder support, remove role-object op builders) is blocked by #559 and will be the next task once #559 lands. #561 (chown/chgrp/addGroupMember target validation via the new `PrincipalResolver`) is also blocked by #559 and consumes the exact seam #559 is building. Task #519 (napi-rs bump) stays explicitly parked at the user's own prior request. Task #529 FINAL-GATE stays blocked until the whole #548/#549 chain (#555-561) completes — #559/#560/#561 are the last three links. Tasks #562 (pre-existing clippy `type_complexity`), #563 (group-record TOCTOU race), #564 (CreateScramUser silently discards update_roles error) are queued, low-priority follow-ups, not started.

## Active goal

Session-scoped Stop hook, condition verbatim: `Используй агентов @"sx (agent)". Реализуй все задачи`. Superseded in *mechanism* (not intent) by the user's explicit `/crush` invocation earlier in the session — `crush run` is the standing implementation-delegation channel; the `Agent` tool (sx) is still used for the adversarial-review step of the pipeline.

## TaskList

### in_progress
- #559 IMPLEMENT stores-549-shamir-db-retirement (PrincipalResolver/UserAdminPort ports, retire shamir-db users/roles tables) — crush session `task-559-store-b-retirement` running, not yet verified/committed. Explicitly the largest/riskiest task in the chain; may need more than one turn.

### pending
- #519 CLIENT: node-binding typed error .code/.retryable (needs napi-rs 3.x bump — user explicitly declined this session; parked)
- #529 FINAL-GATE: full fmt + clippy + test --full across workspace (blocked by #548/#549's implementation chain, #559-561 remain)
- #560 IMPLEMENT clients-549-surface-sync (TS + Rust client builders: remove role-object ops, add setSuperuser) — blockedBy #559
- #561 IMPLEMENT validation-543-followup (chown/chgrp/addGroupMember targets resolve via PrincipalResolver) — blockedBy #559
- #562 CHORE(clippy): fix pre-existing type_complexity in read_planner.rs blocking every -p shamir-db clippy gate
- #563 FIX: unlocked owner/membership TOCTOU race on group records (found during #552 review)
- #564 FIX: CreateScramUser silently discards update_roles error on create (found during #557 review)

### recently completed
- #558 IMPLEMENT resume-549-authoritative-lookup (Ticket v2 drops roles, resume re-verifies against directory)
- #557 IMPLEMENT privilege-549-superuser-flag (PersistedUser.superuser bool + SetSuperuser op + reservation)
- #556 IMPLEMENT identity-548-directory-v2 (shamir-server: PersistedUser v2, principal64 keyspace, remove(), boot normalization)
- #555 IMPLEMENT identity-548-actor-principal64 (shamir-types: Actor::Admin, principal64, delete principal_id)
- #554 IMPLEMENT: CreateFunctionOp per-field wire gating for security/secret_grants/visibility
- #553 IMPLEMENT: wire-admin DAC gate explicit per-op allowlist
- #552 IMPLEMENT: Root/User/Group real permission model
- #551 SECURITY: extend destructive-op HMAC confirmation to group-mutating ops
- #550 DECISION: Root/User/Group always-open meta + wire-admin DAC posture + wire Definer/secret_grants gap
- #549 DECISION: unify parallel superuser/role privilege axis + reconcile desynced user/role stores

## Decisions

- **#555's chown-to-System lockout bug, #557's out-of-scope fmt drift**: both caught by orchestrator-level verification (not the implementing agent), fixed/reverted before commit — see prior checkpoint for full detail, unchanged this window.
- **#559's `PersistedUser.database` field**: added as a schema-only move (no auto-import from shamir-db's Store B), mirroring #556/#557's additive-field pattern — an operator must manually re-create any previously-scoped delegation.
- **#559's CreateRole/DropRole/RenameRole deletion scope**: explicitly includes the Rust query-builder constructors (hard compile dependency), explicitly EXCLUDES the TS client builder (deferred to #560, no compile coupling forces it now).
- **#559's absent-PrincipalResolver fallback**: degrades to no-name-resolution (`owner_name: null`), NOT a continuation of #555's interim `principal64_from_username` hash-bridge — corrected a literal misreading risk in the design doc's own wording before it reached the brief.
- **#559's #557-handler boundary**: `SetSuperuser`'s existing, already-correct wire handler must NOT be re-routed through the new `UserAdminPort` "for consistency" — explicit anti-scope-creep instruction in the brief.

## Open questions

- None new this window — #559's crush run has not yet surfaced anything needing user input.
- The stray `D\357\200\272devrustshamir-db_audit_diff.patch` untracked file — still unexplained, still not investigated (carried over unchanged from prior checkpoints).

## Repo state

```
 M CLAUDE.md
 M crates/shamir-db/src/lib.rs
 M crates/shamir-db/src/shamir_db/mod.rs
?? "D\357\200\272devrustshamir-db_audit_diff.patch"
?? crates/shamir-db/src/shamir_db/ports.rs
?? docs/audits/2026-07-11-security-permission-triage.md
?? docs/checkpoints/... (prior-session checkpoint files, pre-existing clutter)
?? docs/design/identity-privilege-unification-548-549-decision.md
?? docs/design/principal-id-identity-548-decision.md
?? docs/design/root-user-group-dac-posture-550-decision.md
?? docs/prompts/audit/crush-launch-command.md
?? logs/
(plus a large number of stray root-level *.log/*.pdb files from earlier bench/test sessions — pre-existing clutter, not from this window's work)
```

The 3 changed files above (`lib.rs`, `shamir_db/mod.rs`, new `ports.rs`) are #559's in-flight, uncommitted, unverified work from the live crush session — not yet reviewed by the orchestrator.

```
f50af37f docs(prompts): brief for #559 shamir-db Store B retirement
21493fa5 fix(connect): Ticket v2 — resume re-verifies against the directory (#558)
160e036e docs(prompts): brief for #558 ticket v2 authoritative resume lookup
63093cf7 feat(server): superuser as a first-class flag (#557)
61797cf6 docs(prompts): brief for #557 superuser first-class flag
```
