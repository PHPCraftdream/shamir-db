# Checkpoint — 2026-07-24 04:30

## Session summary

This session completed the final two waves of a very long multi-wave remediation campaign on shamir-db's cursor-streaming feature (FG-5) and adjacent areas (RI-15 byte budget, RI-11 backup/restore). It resumed from a `/checkpoint` written mid-Wave-D (while a background CR-D3 agent was still running), finished verifying and committing CR-D3 (#784, exact Int↔F64 comparison), then completed CR-D4 (#785, cursor single-serialization + RI-15 concurrency-math ops docs) and CR-D5 (#786, restore.rs error-path polish + `drain_all` doc correction + hygiene nits) — closing out Wave D (#782–786) entirely.

Per the user's standing compound instruction (re-issued this session and never rescinded), once Wave D finished: CHANGELOG updated, a checkpoint written, all `.md` changes committed, everything pushed to `origin/master`, then an `@fh` (Fable-5, high effort) agent launched for an independent review of the whole Wave D diff. That review (`docs/dev-artifacts/research/2026-07-24-wave-d-review.md`) found **one HIGH-severity regression** — `cmp_i64_f64`'s tie-break (added by this session's own CR-D3) used `f.fract() > 0.0` for its Equal-case tie-break, silently assuming floor-based fractional semantics; Rust's `f64::fract()` is actually TRUNC-based (sign-preserving), so `Int(-1)` wrongly compared `Equal` to `F64(-0.5)`. This was independently reproduced via a standalone `rustc` compile before being filed as task #788 and fixed immediately (commit `10fd6d27`) as an URGENT release blocker, ahead of the rest of the review's findings.

The review's other six findings (W-2 through W-7) were grouped into two more tasks per its own suggested grouping and both completed: **#789** (W-2+W-3+W-7 — a keyset cursor over a `Bin`/`List` ORDER BY column silently dropped rows past page 1, same shape as CR-D2's already-fixed `Null` case; a `Dec`/`Big` ORDER BY column hard-errored on every `FetchNext` past page 1; and a doc-only clarification that the offset-mode fallback can duplicate, not just lose, rows — commit `a715291e`) and **#790** (W-4+W-5+W-6 — CR-D2's null probe escaped the RI-15 admission gate; `restore.rs`'s step-5 first-rename failure orphaned the staged temp dir with no error-message pointer to it, plus a TOCTOU in the temp-dir-creation check; and an undisclosed `access_denied`-vs-`unknown_db` behavior change from CR-D5's `fetch_next` reorder — commit `c9f7170b`). During #790's zero-trust review, the orchestrator caught and fixed an inaccuracy in the delegated agent's own doc comment (it claimed the `access_denied` came from `resource_meta`'s fail-closed path, but the agent's OWN test proved the real mechanism was the target table's still-persisted ACL) — corrected before committing, consistent with this campaign's "never trust an agent's own claim" discipline holding even for comments, not just code.

Every one of these ~10 tasks this session followed the same disciplined cycle used throughout the whole campaign: a committed brief in `docs/dev-artifacts/prompts/post-alpha/` before every `@sh` agent launch, the agent's full diff read personally, `cargo fmt`/`clippy`/`./scripts/test.sh` re-run by the orchestrator (never trusting the agent's own "gate passed" claim), and a commit only after independent verification. One test-suite anomaly was investigated during #790's verification: two consecutive `./scripts/test.sh -p shamir-server --full` runs each failed on a DIFFERENT, unrelated test (first run: `slow_loris`/`duplex_loop`/`audit_appender`/`server_meta` timing tests; second run: a `cursor_registry_tests` concurrency test) — neither touched by the diff being verified. A third clean run passed 546/546, confirming this was environmental resource contention from the session's own heavy sequential `cargo`/`nextest` invocations, not a real regression; documented as a diagnosed-and-dismissed anomaly, not silently ignored.

All 27 commits from this session (Wave D + the post-Wave-D `@fh` review + Wave E's #788/#789/#790) are pushed to `origin/master`, most recently at `c9f7170b`. The `babysit` cron (armed earlier this session, 15-minute interval) self-deleted once the TaskList emptied — every task from #760 through #790 is now `completed`, and no new tasks are queued as of this checkpoint.

**Currently in flight (this exact turn, not yet done)**: the user just asked to (1) `/checkpoint` (this file), (2) update `CHANGELOG.md` with Wave E's findings (W-1 through W-7 are NOT yet reflected in the CHANGELOG — only Wave D's own bullets were folded in during the #787 wrap-up, before the `@fh` Wave D review even ran), (3) "commit amend all .md files" (AMBIGUOUS — could mean a fresh commit covering `.md` changes, or a literal `git commit --amend` on the last pushed commit `c9f7170b`, which would require a force-push and rewrite already-shared history; this needs clarification from the user before acting, per the standing rule against amending published commits without explicit, scoped authorization), and (4) "наладь CI" ("set up/fix CI" — vague; requires first checking the actual current CI status via `gh` before assuming what's broken or missing). None of these four items has been executed yet as of this checkpoint being written — this file itself is the very first step.

## Active goal

None (`/goal` was never armed this session). The `babysit` cron that was driving progress has already self-deleted (TaskList emptied). No cron is currently active.

## TaskList

### in_progress
(none)

### pending
(none)

### recently completed
- #790 W-4+W-5+W-6. Polish batch: null-probe budget exemption, restore.rs step-5 first-rename cleanup, fetch_next reorder disclosure (`c9f7170b`)
- #789 W-2+W-3+W-7. Keyset cursor bookmark typing: Bin/List/Dec/Big (`a715291e`)
- #788 W-1 (URGENT). cmp_i64_f64 trunc-vs-floor fract() regression (`10fd6d27`)
- #787 Wave D wrap-up: CHANGELOG, checkpoint, commit, push, @fh review, follow-up tasks
- #786 CR-D5. Polish batch: restore error-path, drain_all decision, hygiene nits (`b10e7ab8`)
- (#760–#785 also all completed — see `2026-07-24-0100.md`/`2026-07-24-0200.md` checkpoints for their commit hashes)

## Decisions

- **W-2's fix chose the narrower "detect as unsafe, fall back to offset" option over extending `compare_values` with a real `Bin`/`List` total order** — investigated first: `order.rs`'s `QvSortKey` machinery already treats `Bin`/`Set`/`List`/`Map` as an explicitly UNSORTABLE bucket (`QvSortKey::Other`), so there was no existing total order to mirror/reuse, and inventing one would also require changing `ORDER BY`'s own sort semantics to stay self-consistent — judged out of scope for a polish-batch task.
- **W-4 chose to reorder the RI-15 budget reserve earlier (before the null probe) rather than just documenting an exemption** — verified `enforce_page_budget`'s shrink/grow step only cares about the final measured size, never when the reservation started, so widening the single reservation to cover both reads was a clean, safe hoist with no control-flow complication.
- **The orchestrator corrected a delegated agent's own doc comment during #790's review** (W-6's mechanism was originally described as "fail-closed `resource_meta`," but the agent's own regression test proved the real mechanism was the target table's still-persisted ACL) — fixed the comment to match the verified mechanism before committing, rather than accepting a plausible-but-inaccurate explanation.
- **Three consecutive test-suite runs during #790's verification, two of which failed on different unrelated tests** — diagnosed as environmental resource contention (this session's own heavy sequential `cargo`/`nextest` load) rather than a regression, based on the failing tests never overlapping between runs and never touching the code under review; confirmed via a third clean 546/546 run before committing.

## Open questions

- **"коммит аменд всех мд" is ambiguous and involves a potentially hard-to-reverse action** (amending an already-pushed commit `c9f7170b` would require a force-push, rewriting shared history) — needs clarification from the user before any git-mutating action is taken on this front.
- **"наладь CI" is vague** — needs investigation (current `gh`/workflow status) before any concrete action can be scoped; not yet investigated as of this checkpoint.

## Repo state

```
(clean — git status --short produced no output)
```

```
c9f7170b fix(server): W-4+W-5+W-6 -- final polish batch (Wave D review)
60f8cf9b docs(prompts): brief for #790 -- W-4+W-5+W-6 polish batch
a715291e fix(server): W-2+W-3 -- keyset cursor bookmark typing: Bin/List/Dec/Big (#789)
5990d841 docs(prompts): brief for #789 -- W-2+W-3+W-7 keyset bookmark typing
10fd6d27 fix(engine): W-1 -- cmp_i64_f64 tie-break used trunc-based fract(), not floor-based (release blocker)
```

Branch `master`, fully in sync with `origin/master` (last push was this exact commit, `c9f7170b`).
