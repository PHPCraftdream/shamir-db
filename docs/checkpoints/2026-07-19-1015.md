# Checkpoint — 2026-07-19 10:15

## Session summary

This session picked up from a prior `/checkpoint` restore (2026-07-17-1600.md), verified and committed a critical ACL-bypass fix (ForEach/Batch bypassing per-table authorization — commit `6d33fe9e`), then ran a broad 10-topic parallel research audit of the whole codebase (security, permissions, compliance, logical-correctness, incomplete-features, concurrency-deadlock-hazards, performance, test-coverage/CI, documentation-accuracy, v0.10 release-readiness — reports in `docs/dev-artifacts/research/2026-07-17-release-audit/`). Read and synthesized all 10 reports into `00-SUMMARY.md` and `00-WORK-PLAN.md` (8 stages, committed `9aba02cf`).

The user then invoked `/goal` (redirected to `/babygoal`) with: "реализуй задачи с помощью /crush, между задачами делай коммиты, покрой их тестами" — implement the work-plan stages using `/crush` sub-agents, one task at a time, with a commit + tests after each. A `/babysit` cron (every 15 min, job id `bc06cce1`) is armed and has been ticking throughout, confirming "still running #N" each time by checking `git log` + the crush session's `.err` output for fresh tool-call activity (never polling raw process state).

**Execution pattern established and followed rigorously for every single task so far** (this is the core operating loop for the rest of the session): (1) read the relevant code myself to pin exact file:line references, (2) write a precise, scoped prompt-first brief to `docs/dev-artifacts/prompts/<area>/`, explicitly listing what's in/out of scope and what tests must prove, (3) commit the brief BEFORE launching, (4) launch `/crush` in the background on that brief, (5) when it completes, independently verify — read the full diff myself (never trust the agent's own claimed summary), reason through whether the new tests would actually have failed pre-fix, re-run the full test gate myself (`./scripts/test.sh <scope> --full`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`), clean up any stray debug-log files the agent left in the repo root, (6) commit only after all of that passes, with a detailed commit message, (7) mark the TaskList task completed and move to the next.

**Этап 1 (correctness-blockers, 5 bugs) — fully complete**: 1a FK-on-update dedup bug (commit `a9b79db2`; the crush agent also found and fixed a second related first-match-wins bug in the same file, needed to satisfy its own required test), 1b `$contains_all` fast-path duplicate-counting bug (commit `0be91b4a`; agent chose an O(field_len)-preserving fix over the brief's simpler O(n·m) suggestion, correctly justified), 1c diamond FK cascade false-cycle rejection (commit `f3334de5`; clean RAII-guard implementation), 1d Dec/Big-aware comparison layer across filters/aggregates/ORDER BY (commit `dd5c8571`; the largest fix — also surfaced a real architectural fact along the way: `Value::Dec`/`Big` decay to `Str` over msgpack round-trip, confirmed via an existing test, so the fix correctly targets in-memory-only Dec values), 1e UPSERT MERGE `created_at` overwrite (commit `6e9e2a94`; agent empirically falsified the fix by reverting it and confirming the new test fails, then restored it — strong non-vacuity evidence).

**Этап 2 (concurrency deadlock hazards, 7 sites total — H1 through H6 plus one bonus finding — all fully complete)**: 2a H1+H2 `active_snapshots`+`locks` entry_async→entry_sync (commit `621776bd`, HIGH confidence, same class as the already-fixed #589 `cells`-map deadlock), 2b H3 vector-index 5-map async/sync mixing in `hnsw_adapter.rs`/`vector_backend.rs` (commit `dcfaf825`, largest mechanical sweep — 484/484 tests; the implementing agent independently found a 6th hazardous map, `rid_map`, NOT in the original 5-map scope, and correctly left it untouched, flagging it as a follow-up), 2c H4+H5 `per_table_mvcc`+`token_names` read_async→read_sync (commit `2d0f4115`, MEDIUM confidence — needs an exclusive DDL writer in the mix, unlike H1-H3's pure hot-key pattern; carefully preserved `table_by_token_if_live`'s documented non-instantiating property), 2d H6 `layered_interner` hygiene (commit `60ff2270`, LOW risk — per-TxContext map, no cross-task interleaving exists today, fixed for convention/future-proofing only; also fixed an unrelated stale comment in `mvcc_history.rs`), 2e the bonus `rid_map` fix found during 2b (commit `d2aa4ed7`, same mechanical shape as H3). All 7 deadlock-hazard sites from the concurrency research report are now closed.

**Этап 3 (honesty fixes) — decomposed into 5 leaf tasks (3a-3e), currently in progress**: 3a is DONE (commit `bdd7bbb3`) — two fixes: (i) DDL-time rejection of multi-segment `default`/`auto_now`/`auto_now_add` schema rules, mirroring the existing `validate_unique_indexes` precedent exactly; (ii) reject `Call` ops inside a transactional batch/interactive tx with a new `call_in_tx_not_supported` error, mirroring the existing `nested_tx_not_supported` precedent. During 3a's verification I hit a REAL transient issue worth remembering: a full `shamir-db --full` run TIMEOUT'd on an entirely unrelated test (`rename_populated_survives_cold_restart`), which then caused a Windows `LNK1104` file-lock cascade on the next build (3 stray `rename_table_durability-*` processes still holding the test binary open). I killed the stray PIDs via `taskkill`, confirmed the test passes cleanly in isolation (2.2s) and the full suite passes cleanly on a clean re-run (584/584) — concluded this was transient load-induced contention unrelated to the diff, not a real regression, and documented this reasoning in the commit message.

**3b is CURRENTLY IN PROGRESS** (agentId/session `honesty-3b`, launched from committed brief `f4b2edc3`) — two warn-log additions: (i) `write_helpers.rs`'s `ComputedDefault` transform silently swallows an evaluation error with no diagnostic (fail-open, by design — this fix only adds a `warn!` log, does NOT change the fail-open behavior); (ii) `shamir-db/src/shamir_db/execute/helpers.rs`'s `filter_value_to_query_value` silently collapses `$ref`/`$fn`/`$expr`/`$cond` Call-params to `Null` (also by design — same treatment, warn-log only). Per `git status` at checkpoint time, the crush agent has made UNCOMMITTED, NOT-YET-VERIFIED changes to `helpers.rs`, `execute/mod.rs`, `apply_transforms_tests.rs`, plus a new `execute/tests/` directory — I have NOT yet read this diff or run the verification gate on it.

Remaining in Этап 3 after 3b: 3c (cleanup tail A — coercing Int/F64 set-probes in InSet/ContainsAnySet, threading `ScalarResolver` into 5 non-WHERE contexts, `count_distinct`/`mode` over Set/Map structural equality), 3d (cleanup tail B — self-referential FK enforcement, FK Int↔F64 child-matching coercion), 3e (cleanup tail C — small checked-arithmetic fixes: `$expr mod` overflow, Sum accumulator overflow, `datetime::diff_secs` overflow, Big↔Int exact compare, Big cast support). After 3e, Этап 4 (funclib top-up for v0.10 — `coalesce`/`nullif`/`is_null`, wire-level aggregate params, `datetime/format`+`parse`, `uuid_v4`, `arrays/sort` cross-type fix, `parse_json`/`to_json`) unblocks, then Этапы 5-8 (Compliance & Ops, Documentation accuracy, Test/CI robustness, Performance) in sequence — none of these later stages have been decomposed into leaf tasks yet; that decomposition should happen just before each stage starts, matching the pattern used for Этапы 1-3.

## Active goal

No `/goal` Stop hook is set (the user used `/babygoal`, which governs progress via the TaskList + a `/babysit` cron instead of a Stop-hook condition). The `/babysit` cron (job `bc06cce1`, every 15 min, session-only) is the operative heartbeat — it must keep ticking and resuming/advancing the TaskList until empty.

## TaskList

### in_progress
- #691 3b. Warn-logs for fail-open computed default + Null Call params

### pending
- #692 3c. Cleanup tail A: coercing set-probes + ScalarResolver threading + count_distinct/mode (blockedBy: #691)
- #693 3d. Cleanup tail B: self-referential FK + FK Int↔F64 coercion (blockedBy: #692)
- #694 3e. Cleanup tail C: checked arithmetic (mod/i64/Big compare/Big cast) (blockedBy: #693)
- #675 Этап 4 — funclib top-up для v0.10 (P0/P1) (blockedBy: #694) — NOT yet decomposed into leaf tasks
- #676 Этап 5 — Compliance & Ops (blockedBy: #675) — NOT yet decomposed
- #677 Этап 6 — Documentation accuracy (blockedBy: #676) — NOT yet decomposed
- #678 Этап 7 — Test / CI robustness (blockedBy: #677) — NOT yet decomposed
- #679 Этап 8 — Performance, post-blocker (blockedBy: #678) — NOT yet decomposed

### recently completed
- #690 3a. DDL-time rejection: nested-path transforms + Call-in-tx (commit `bdd7bbb3`)
- #689 2e. Fix rid_map async/sync mixing (commit `d2aa4ed7`)
- #688 2d. Fix H6 — layered_interner hygiene (commit `60ff2270`)
- #687 2c. Fix H4+H5 — per_table_mvcc + token_names (commit `2d0f4115`)
- #686 2b. Fix H3 — vector index 5-map async/sync mixing (commit `dcfaf825`)
- #685 2a. Fix H1+H2 — active_snapshots + locks (commit `621776bd`)
- #684 1e. Fix UPSERT MERGE overwriting created_at (commit `6e9e2a94`)
- #683 1d. Add Dec/Big-aware comparison layer (commit `dd5c8571`)
- #682 1c. Fix diamond FK cascade false-cycle rejection (commit `f3334de5`)
- #681 1b. Fix $contains_all duplicate-counting bug (commit `0be91b4a`)
- #680 1a. Fix FK on-update dedup bug (commit `a9b79db2`)
- #672 RO-audit: Security posture (whole project) — pre-existing from the 10-topic research sweep

## Decisions

- User authorized an explicit, sustained `/crush`-driven execution loop for the whole work plan, with mandatory per-task commits and test coverage — this is now the standing operating mode for the rest of the session, not a one-off.
- For every task, chose to decompose each Этап into concrete leaf tasks (mirroring #660/#666's established pattern) rather than run each Этап as one giant crush call — keeps the TaskList resumable and each commit small/reviewable.
- For the 3a `rename_table_durability` TIMEOUT: chose to investigate (kill stray PIDs, re-test in isolation, re-test the full suite clean) rather than either ignoring it or treating it as a regression requiring a fix — concluded genuine transient contention, documented the reasoning in the commit message per CLAUDE.md's "hunt and fix hangs, never tolerate" mandate (this WAS the hunt; the conclusion was "not a real hang in this diff," which is an acceptable outcome when backed by isolation + clean-rerun evidence).
- For 1d (Dec comparison), explicitly scoped OUT extending `ScalarRef` itself and OUT fixing Big's f64-precision-loss — kept the fix narrow to the reachable, high-frequency Dec case; Big precision and self-referential FK/other lower-priority items were deliberately deferred to later tail-cleanup tasks (3c/3d/3e) rather than scope-creeped into earlier fixes.

## Open questions

- None requiring user input right now. The only pending operational step is: wait for the `honesty-3b` crush session's completion notification, then apply the same zero-trust verification (read diff in full, reason through test non-vacuity, `./scripts/test.sh @engine --full` + `-p shamir-db --full`, fmt, workspace clippy) before committing.
- Not yet decided/started: whether Этапы 5-8 will get the same fine-grained leaf-task decomposition as Этапы 1-3, or a coarser treatment given they're lower-priority (compliance docs, documentation accuracy, CI config, performance) — should decide this when Этап 4 completes and Этап 5 is about to start.

## Repo state

```
 M crates/shamir-db/src/shamir_db/execute/helpers.rs
 M crates/shamir-db/src/shamir_db/execute/mod.rs
 M crates/shamir-engine/src/table/tests/apply_transforms_tests.rs
 M crates/shamir-engine/src/table/write_helpers.rs
?? crates/shamir-db/src/shamir_db/execute/tests/
?? docs/checkpoints/2026-07-17-1600.md
```
(the 4 modified files + new `execute/tests/` dir are the IN-FLIGHT, uncommitted, NOT-YET-VERIFIED 3b warn-log fix from crush session `honesty-3b` — do not commit until independently verified)

```
f4b2edc3 docs(prompts): brief for warn-logs on fail-open computed default + Null Call params
bdd7bbb3 fix(engine,db): reject nested-path schema transforms at DDL time; reject Call inside a transaction
4a9e0155 docs(prompts): brief for DDL-time nested-path rejection + Call-in-tx rejection
d2aa4ed7 fix(index): rid_map mixed scc _async/_sync lock hazard (follow-up to H3)
5ab797da docs(prompts): brief for rid_map async/sync mixing (follow-up to H3)
```
