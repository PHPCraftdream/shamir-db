# Checkpoint — 2026-07-26 02:30

## Session summary

Continuing the Wave F release-hardening campaign (tasks #791–#805, derived
from a pasted release-readiness review of shamir-db). This session resumed
from an earlier checkpoint (`2026-07-25-2152.md`) with F-1 through F-9
already committed and verified. Since then, F-10 through F-14 (#800–#804)
were completed and committed one by one, each following the established
disciplined cycle: write a brief to
`docs/dev-artifacts/prompts/post-alpha/<NN>-<name>.md`, commit the brief,
delegate implementation to a `/crush` agent (session id matching the task
slug, `--timeout` 60–90m), personally read the full diff, personally re-run
`cargo fmt`/`clippy`/`./scripts/test.sh` (never trusting the agent's own
claim), then commit the verified code. Routing stayed on `/crush` for the
whole session (no `@sh`/Agent-tool fallback was needed — no quota/peak-hour
blocks occurred this time).

**F-10 (#800):** `QueryResult.corrupt_records` — corrupt/malformed records
hit during a scan are now reported instead of silently dropped. Verified
and committed as `b75bb746`.

**F-11 (#801):** shipped `deploy/server.small.example.ktav` and
`server.medium.example.ktav` were missing `max_inflight_response_bytes`
(RI-15), leaving that budget unbounded. Set at the same 4× ratio the
reference profile/ops-guide already documents (small=128 MiB,
medium=256 MiB). Crush agent flagged that the REFERENCE profile
(`server.example.ktav`) itself ALSO lacks the field (my brief incorrectly
assumed it already had it) — left as an open, documented, non-blocking
follow-up rather than silently expanding scope. Committed as `774b452e`.

**F-12 (#802):** `backup`/`restore`'s file copy and manifest write had no
`sync_all`; `restore`'s atomic swap had no post-rename directory fsync —
added both, following `shamir-wal/wal_segment.rs`'s exact existing
`fsync_parent_dir` precedent (file-sync propagates errors, directory-fsync
logs only, no-op on non-unix). New regression + fault-injection tests in
`restore_tests.rs`. Verified (including 3 extra standalone re-runs of the
new Windows-only watcher-thread fault-injection test to check for timing
flakiness — all passed). Committed as `d1ad2e9b`.

**F-13 (#803):** Rust/TS query-builder parity — `having()` without
`group_by()`, `page=0`/`page_size=0`, and 5 builders
(`Delete`/`Update`/`Upsert`/`AddSchemaRuleBuilder`/`AlterSubscriptionBuilder`)
using `.expect()`-panics or a `QueryValue::Null` sentinel instead of a
proper `Result`. Added a NON-breaking `Query::try_build()` (mirrors this
crate's own `Batch::try_build()`/`BuildError` precedent) rather than
breaking `Query::build()`'s ~822 existing call sites; converted the 5
smaller builders' `build()` to return `Result` directly (small blast
radius, confirmed via caller-count greps first). Also fixed, on my own
initiative mid-verification: `shamir-numa`'s `Cargo.toml` was the one
crate in the workspace missing `doctest = false` (added it, matching the
established repo-wide convention — the user separately confirmed doctests
are banned project-wide). Committed as `1c87708a`. One PRE-EXISTING
flaky/slow test (`vr5_cofilter_sees_staged_and_filters_residual`) timed
out during verification, unrelated to this diff — this triggered a
mini-investigation (see below).

**F-14 (#804):** CHANGELOG.md previously only had `## [Unreleased]`
(meaning the release workflow's tag-based "Extract release notes" step
would always fall back to a generic message). Added a fresh empty
`## [Unreleased]` above, renamed the existing content to
`## [0.1.0-alpha.1] - 2026-07-26`, and appended one bullet per Wave F fix
(F-1 through F-13) plus a "Known limitations accepted for this release"
closing note (engine-level cursor streaming, migration API staying
experimental). Verified the release workflow's actual gate check (a
simple `grep`) passes against the new heading; did NOT touch
`release.yml` itself (out of scope, belongs to an earlier wave). Did
single-context (not delegated — pure editorial/doc work). Committed as
`1c4b58fd`.

**Investigation #807 (now closed, false positive):** the
`vr5_cofilter_sees_staged_and_filters_residual` TIMEOUT that appeared
during F-13's verification, and again in F-15's first `--full` run,
turned out to be a PRE-EXISTING, ALREADY-DOCUMENTED condition —
`.config/nextest.toml` already has a dedicated override for this exact
test (240s kill, "~90s legit", git-blamed to commit `30d976c0`, explicitly
classified as "not a deadlock, same CPU-starvation-under-load class as
the wasm_function override"). A quiet re-run (no other cargo/clippy
processes running) passed in 88.9s, matching the documented expectation
exactly. Task #807 was created prematurely (before checking
`nextest.toml`'s own history) and then closed as a false positive once
this was found — a reminder to check existing git-blamed context before
assuming a "new" bug.

**F-15 (#805, IN PROGRESS — the final release gate):** `cargo fmt --all
--check` clean, `cargo clippy --workspace --all-targets -- -D warnings`
clean (ran twice, both clean). `./scripts/test.sh --full` run TWICE:
first run had exactly 1 timeout (the known vr5_cofilter flake above,
accepted). Second run had 8 failed + 4 timed out — ALL of them
timing/interval-sensitive tests (scheduler `*_runs_at_interval`,
`slow_loris`, `crash_simulation_preserves_state`, wasm-compile-timeout
tests, and F-12's own new watcher-thread fault-injection tests).
Investigated via `tasklist` (itself took 120s to return — confirming
genuine severe machine load) and found several orphaned `crush.exe` +
unexplained `cargo.exe` processes. Asked the user before touching
anything; **the user explicitly said NOT to touch those processes — the
machine is shared with OTHER agents working on OTHER projects right now,
which is exactly why it's noisy, and confirmed bench-regression testing
should be SKIPPED entirely for this reason** (F-15's own task
description asked for a bench pass; this is now a deliberate,
user-directed omission, not a silent gap). Re-ran all 12 failed/timed-out
individual test names ONE AT A TIME (not the full 6227-test suite) — ALL
12 passed cleanly and quickly, confirming zero real regressions; every
failure was pure resource-contention noise from neighboring unrelated
agent activity on the shared machine.

**New real finding during F-15's e2e step:** built a fresh debug
`shamir-server` binary (`cargo build -p shamir-server`; lands under the
env-var-redirected `CARGO_TARGET_DIR=D:\dev\rust\.cargo-target\debug\`,
NOT the repo-local `target/` — this tripped me up briefly) and ran the
full TS suite via `SHAMIR_SERVER_BIN=... npx vitest run` from
`crates/shamir-client-ts`. 2 of 1005 TS tests failed: both
`e2e-ddl.test.ts` migration-lifecycle tests
(`startMigration→migrationStatus→rollbackMigration` and
`startMigration→commitMigration→...`). Root cause: F-5 (#795) correctly
gated `StartMigration` behind `ShamirDb::enable_experimental_migration_api()`,
but the LIVE `shamir-server` binary has **zero way** to call that method
— no CLI flag, no config field (`grep` confirmed). Asked the user how to
close this; **the user chose to add a proper live-server config opt-in**
(over the alternative of just weakening the 2 e2e tests to expect
rejection). Investigated the boot path via an `Explore` sub-agent:
`ServerLauncher::launch` (`server_launcher.rs:366-372`) is the single
`ShamirDb` construction site; `admin_migration.rs`'s gate reads off that
exact same `Arc`, so one boot-time toggle suffices. Wrote and committed
brief `docs/dev-artifacts/prompts/post-alpha/49-f15-live-server-migration-api-opt-in.md`
(commit `e103bf4c`) specifying a NEW `SecurityConfig::enable_experimental_migration_api`
config-file-only field (no CLI flag — no established CLI+config
precedent exists in this codebase to justify one), wired at the boot
site, plus updating the TS e2e harness's own generated test-server config
to set it `true` so the two existing e2e tests can run with their
ORIGINAL assertions unchanged (not weakened).

**Currently in flight (task #808, `/crush` session
`f15-migration-opt-in`, 60m timeout):** the crush agent appears to be
actively working — `git status --short` at checkpoint time shows
in-progress (uncommitted) changes to `config.rs`, `server_launcher.rs`,
`server_handle.rs`, e2e-ddl.test.ts, e2e-harness.ts,
`config_tests.rs`, `max_connections.rs`, `slow_loris.rs`,
`slow_loris_pre_tls.rs`, plus a new untracked
`crates/shamir-server/tests/migration_api_gating.rs` — matching the
brief's scope. This has NOT yet been verified or committed by me; the
agent has not yet reported completion (no `<task-notification>` has
arrived for it as of this checkpoint). babysit ticks have been reporting
"still running #808" the last two cycles.

**Session-wide standing context:** babysit cron `1c88fc6a` (every
7,22,37,52 min) is active and has been driving progress via
`# babysit tick` messages throughout. A `/goal` Stop hook is armed (see
below). The user separately confirmed the machine is genuinely shared
with other unrelated agents right now, and explicitly told me not to
kill any stray processes I find and not to run benchmarks.

## Active goal

`продолжай решать задачи с помощью агентов /crush, меджу задачами делай коммиты.`

(Set via `/goal` earlier this session; per its own semantics this Stop
hook blocks the session from ending until this condition holds —
i.e., until there is no more Wave-F-related work to solve via `/crush`
with commits between tasks.)

## TaskList

### in_progress
- #805 F-15: финальный release gate — fmt/clippy/test/e2e/bench regression
- #808 F-15 follow-up: live-server config opt-in для experimental migration API (crush session `f15-migration-opt-in` running, not yet verified/committed)

### pending
- #806 F-16: пост-волновой ревью Wave F агентом @oh (blockedBy: #805)

### recently completed
- #807 [закрыто, false positive] vr5_cofilter TIMEOUT — уже задокументированная CPU-contention чувствительность
- #804 F-14: CHANGELOG — реальная секция [0.1.0-alpha.1]
- #803 F-13: query-builder parity (Rust/TS)
- #802 F-12: backup/restore — fsync перед rename + crash-тест
- #801 F-11: конечные memory budgets в deploy profiles
- #800 F-10: corrupt record молча пропускается в read paths
- #799 F-9: cursor reaper может удалить курсор во время активного fetch
- #798 F-8: QueryValueSerializer::serialize_u64
- #797 F-7: with_version=true молча теряет versions
- #796 F-6: count_total=true + ORDER BY + LIMIT top-K путь теряет total

(#791–#795 also completed, from before this session's most recent
resumption — F-1 through F-5.)

## Decisions

- **F-11's reference-profile gap** (`server.example.ktav` missing
  `max_inflight_response_bytes` too) — left as a documented open
  follow-up, not folded into F-11's scope retroactively.
- **F-13's fallible-build design** — added a non-breaking `try_build()`
  sibling for `Query` (822 existing `.build()` callers made a breaking
  signature change disproportionate) but changed `build()`'s signature
  directly for the 5 smaller write/DDL builders (small, confirmed
  blast radius).
- **#807 (vr5_cofilter flake)** — NOT a new bug; already documented and
  accepted in `nextest.toml` since before this session (commit
  `30d976c0`). Closed as false positive rather than pursuing a
  "root-cause" investigation that would have found nothing new.
- **F-15's bench-regression requirement** — explicitly SKIPPED per
  direct user instruction, because the shared dev machine is genuinely
  busy with other unrelated agents' work right now, making any
  benchmark comparison meaningless.
- **Stray crush.exe/cargo.exe processes found via `tasklist`** — left
  untouched per direct user instruction (belong to other agents/projects
  sharing the machine).
- **Migration-API e2e gap** — chose to add a real live-server config
  opt-in (`SecurityConfig::enable_experimental_migration_api`, config-file
  only, no CLI flag) rather than weakening the 2 TS e2e tests to just
  assert rejection — user's explicit choice among 3 offered options.

## Open questions

- None pending on the user's side right now — the last `AskUserQuestion`
  (migration-API e2e gap) was answered and is being acted on via task
  #808's in-flight crush session.
- Whether F-15's gate should be considered "done" once #808 lands, or
  whether the full `--full` + TS e2e run should be repeated ONE more
  time end-to-end after #808's commit to get a single clean combined
  signal — not yet decided; likely yes, given the discipline the rest
  of this session has followed (re-run everything after the last
  change lands, not just the newly-touched piece).

## Repo state

```
 M crates/shamir-client-ts/src/__tests__/e2e-ddl.test.ts
 M crates/shamir-client-ts/src/__tests__/e2e-harness.ts
 M crates/shamir-server/src/config.rs
 M crates/shamir-server/src/server/server_handle.rs
 M crates/shamir-server/src/server/server_launcher.rs
 M crates/shamir-server/src/tests/config_tests.rs
 M crates/shamir-server/tests/max_connections.rs
 M crates/shamir-server/tests/slow_loris.rs
 M crates/shamir-server/tests/slow_loris_pre_tls.rs
?? crates/shamir-server/tests/migration_api_gating.rs
?? docs/checkpoints/2026-07-25-2152.md
```

(The 9 modified + 1 new file above are the IN-FLIGHT, NOT YET VERIFIED
OR COMMITTED output of task #808's crush session — do not assume they
are correct without reading the diff and re-running fmt/clippy/tests
first, per this session's own zero-trust discipline.)

```
e103bf4c docs(prompts): brief for F-15 follow-up -- live-server migration API opt-in
1c4b58fd docs(changelog): F-14 -- real [0.1.0-alpha.1] section (#804)
1c87708a fix(query-builder): F-13 -- Rust/TS builder parity: having/group_by, page validation, fallible build() (#803)
28b9d075 docs(prompts): brief for #803 -- F-13 query-builder parity (Rust/TS)
d1ad2e9b fix(server): F-12 -- fsync durability for backup/restore copy + atomic rename (#802)
cc57f567 docs(prompts): brief for #802 -- F-12 backup/restore fsync durability
774b452e fix(server): F-11 -- finite max_inflight_response_bytes in small/medium deploy profiles (#801)
b77833b7 docs(prompts): brief for #801 -- F-11 deploy profile memory budgets
```
