בְּשֵׁם יהוה הָרַחֲמָן וְהַחַנּוּן

# Checkpoint — 2026-06-21 23:20

## Session summary

День состоял из трёх крупных этапов; **третий завершён, TaskList пуст**.

**Этап 1 (раньше):** bench-infra A→D (#132-#135), затем удаление 4 storage engines (redb/nebari/persy/canopy) и rewrite redb-инфраструктуры на fjall. Закоммичено + запушено как `af2ff2d`. Чекпоинт `2026-06-21-1840.md`.

**Этап 2 (deep research):** запустил workflow с 8 oxx-агентами параллельно — исследовали все hot-path dimensions. Synthesis написал `docs/perf/perf-roadmap-2026-06-21.md` — 39 opportunities, ROI-ранжирование, рекомендована Phase 1 с 3 ops.

**Этап 3 (текущий, ЗАВЕРШЁН):** реализовал Phase 0 + Phase 1 + Phase 2 routing.
- **Phase 0 (3 probes):** все закрыты. (0a) fjall.range=lex-ordered ✓, insert не возвращает prior. (0b) fast_path bail УЖЕ ПОЧИНЕН (138µs на N=1M). (0c) baselines — пришлось снимать вручную из-за двукратного failure o46l-агентов.
- **Phase 1A (Op A, scan_prefix_stream fjall):** Sonnet high agent сделал — commit `98f256d`, **164×**.
- **Op A.2 (sled mirror):** я сам сделал по просьбе пользователя — commit `7140f82`, **50×**.
- **Phase 1B (Op B, interner Arc<str>):** Sonnet high agent правильно написал код, но застрял в собственном bench-livelock (N=5000 OLD blob-persist = 813s/sample). Я остановил workflow, измерил вручную через stash/restore — commit `35ebd40`, **4×@N=5k** (растёт линейно с N).
- **Phase 1C (Op C, MemBuffer sentinel):** сам сделал в main thread — commit `d2d3504`. Расширил roadmap (был только insert_many, оказалось ВСЕ три `*_many` без sentinel). Регрессионный тест ловит баг (FAIL без фикса, PASS с).
- **Bench-infra E (#154):** сам сделал — commit `c64fd41`. Capped `read_path_matrix/s3_range_and` N=500K чтобы не нарушать 10-min budget (это та самая cell которая агенту вгоняла в 3-часовой livelock).
- **Phase 2 routing:** документ `docs/perf/phase2-routing-decision-2026-06-21.md`. Следующая campaign = **Op #2 incremental drainer cursor** (ROI 82, large/medium). **Не запущена** — нужна явная санкция пользователя.

**Workflow opera:** `wf_267d4b4f-f48` был остановлен и resume'нут 4 раза (livelock, prompt-discipline-failure, agentType switch o46l→sh, livelock второго рода). В итоге workflow механизм провалил себя — финальные 3/5 шагов сделал я в main thread.

**Подтверждённая гипотеза:** Sonnet high (sh) лучше следует prompt'у чем Opus 4.6 low (o46l) — но даже sh попал в bench-livelock (sleep+poll pattern на собственном heavy bench). Workflow-агенты не подходят для bench-heavy /opti циклов без жёстких per-tool timeout'ов.

**Cron timers:** все остановлены. Cron `417ee33f` (babysit) был активен во время Phase 1, CronDelete'нут когда TaskList опустел.

## Active goal

Нет активной цели (`/goal` Stop hook не установлен). Контракт — TaskList; он пуст.

## TaskList

### in_progress
*(нет)*

### pending
*(нет)*

### recently completed (10)
- #155 Op A.2 — sled scan_prefix_stream range-seek (50×)
- #154 Bench-infra E — cap s3_range_and N at 500K
- #153 Phase 2 — routing decision (→ Op #2 drainer cursor)
- #152 Phase 1C — MemBuffer *_many sentinel fix (correctness)
- #151 Phase 1B — interner Arc<str> spine (4× @ N=5k)
- #150 Phase 1A — fjall scan_prefix_stream (164×)
- #149 Phase 0c — baselines (manual)
- #148 Phase 0b — fast_path bail re-measure (denied)
- #147 Phase 0a — fjall API verify (confirmed)
- #146 umbrella

## Decisions

- **Перешёл с o46l на sh** для Phase 1+ ops (after o46l дважды провалил prompt discipline на Phase 0c).
- **Phase 0c сделал вручную** вместо третьей попытки агента. Заменил `agent()` call в workflow на literal Promise.resolve.
- **Op A.2 sled добавил как отдельный коммит** вне workflow по просьбе пользователя — симметричный fjall fix.
- **Op B baseline получил через stash/restore** — извлёк bench file из stash, замерил с OLD impl, восстановил все changes.
- **Op C расширил на все три `*_many`** — roadmap упоминал только insert_many, но set_many и remove_many имели тот же баг.
- **Phase 1B Sonnet agent's 10-file diff принят как есть** после tests pass + бенчмарк показал ≥ 2×. Не делал review каждой downstream строчки — компилируется, 1836 тестов проходят, гипотеза подтверждена.
- **Bench-infra E как cap N=500K, не env-gate** — пользователь явно сказал "все shapes проверяются всё, но max 10 min". Cap N сохраняет shape, держит budget.

## Open questions

- **Запускать ли Op #2 (incremental drainer cursor) кампанию?** ROI 82, large effort, medium risk. Нужна явная санкция (xlarge multi-week потенциально). Документ `phase2-routing-decision-2026-06-21.md` готов как референс.
- **Делать ли коммит + пуш текущих изменений?** Uncommitted: `docs/checkpoints/2026-06-21-2206.md`, `docs/perf/phase2-routing-decision-2026-06-21.md`. Жду явного "коммит и пуш".
- **Bench-infra hardening** (упомянуто в commit `c64fd41`): улучшить `tune_tiered` max_wall_secs guard чтобы он был wall-clock-aware (runtime kill), а не estimate-aware (pre-computed). Это вытащит другие потенциальные ловушки. Открытый вопрос — стоит ли тратить ресурс сейчас или ждать когда снова укусит.

## Repo state

```
?? docs/checkpoints/2026-06-21-2206.md
?? docs/perf/phase2-routing-decision-2026-06-21.md
```

```
c64fd41 fix(bench): cap s3_range_and N at 500K — bound 10-min wall-clock (#154)
d2d3504 fix(storage): MemBuffer *_many — publish dirty_nonempty before populating (#152)
35ebd40 perf(types): interner reverse-spine Arc<str> — O(N²)→O(N) cold growth (#151)
7140f82 perf(storage): scan_prefix_stream range-seek on sled — O(M²/bs)→O(log N+bs) (#155)
98f256d perf(storage): scan_prefix_stream range-seek — O(N+M²)→O(log N+M) on fjall (#146)
```

5 коммитов ahead of origin/master: af2ff2d, 98f256d, 7140f82, 35ebd40, d2d3504, c64fd41 (актуально 6 если считать с af2ff2d). Push не делал — жду санкции.
