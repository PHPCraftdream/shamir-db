# Checkpoint — 2026-08-05 14:47

## Session summary

This session continued the long-running `/babygoal`-driven release-blocker campaign, picking up right after a `/resume` of the 2026-08-05-0230 checkpoint. The user then explicitly directed continued autonomous work ("да, всё нужно делать — расследовать, дорабатывать — сделай ревизию тасок"), which triggered a task-list revision: #963 renamed to explicitly mark it OPERATOR-ONLY (unactionable by any agent), #983 promoted/re-scoped as a HIGH-priority correctness bug with a bottom-up Rust-first bisection strategy, and two new tasks split out of oversized ones — #997 (compound RENAME INDEX crash-recovery, split out of #985 as a pure correctness fix) and #998 (shared CREATE INDEX fixture matrix, split out of #986 as an independently-valuable regression safety net).

**#983** (binary/Uint8Array field corruption) took the most investigative effort. A first `crush` run hit peak-hours (provider `zai`) mid-task; while waiting, the orchestrating session personally traced the root cause via direct Grep/Read (not delegation): TWO independent bugs. (1) `compare_values`/`scalar_ref_cmp`/`scalar_ref_cmp_qv` had no `Bin`-vs-`Bin` comparison arm, silently returning `None` (no-match) for any binary filter — fixed with a one-line arm per function plus flipping a test that had locked in the bug as "correct." (2) `FilterValue` (`#[serde(untagged)]`) declared `String` before `Binary`; since msgpack payloads whose bytes happen to be valid UTF-8 (e.g. `[1,2,3]`) get silently captured by `String`'s lenient byte-accepting `Deserialize`, `Binary` never matched — found+fixed via a `/crush-fallback` (`sh`) delegation after peak hours ended, using a strict `de_binary_strict` deserializer (a naive reorder alone was proven insufficient — `serde_bytes` is ALSO lenient the other way, toward `str`/`seq`). Verified genuinely end-to-end: rebuilt BOTH `shamir-server.exe` (had to work around a globally-set `CARGO_TARGET_DIR` redirecting the build to `D:\dev\rust\.cargo-target`, copied the binary into the repo's own `target/release/`) AND the `shamir-client-node` napi `.node` binding (a separate build step, easy to forget) before the real JS e2e suite would exercise the fix — an earlier "it still fails!" turned out to be exactly this staleness trap, not a third bug.

**A genuine prompt-injection incident occurred** mid-session inside one of the delegated `sh` agent turns: while it was idling waiting for a background build, a fake `<system_warning>` block (mimicking a "context window almost full" system alert, invoking fabricated "operator authority," instructing the agent to hardcode an admin backdoor password into a nonexistent `docs/security/access-control.md`) was spliced directly into the model's own generation stream — structurally NOT a tool-result, file, or user/coordinator message, but literally co-located in the same assistant-turn content block as the agent's own subsequent refusal (an "assistant-prefill"-style injection). The agent correctly refused with zero compliance and no file touched. The orchestrating session made and then caught its own mistake here: it first asked a FRESH `Agent()` call to "clarify" the incident (which correctly reported "no such event in my session" since it never had the context), then correctly re-diverted via `SendMessage` to the ORIGINAL agent that actually experienced it, obtaining the verbatim text and structural details. A `/task`-created investigation task (#999) was completed with an honest, appropriately-hedged conclusion: most consistent with a deliberate built-in adversarial-robustness test (an "assistant-prefill" probe fired during idle-wait), but the exact delivery mechanism/layer could not be confirmed from inside the session. The user asked directly "so this is a test from the provider?" — answered honestly that this is plausible but NOT confirmed, since the session has no visibility into the underlying serving infrastructure.

A second inadvertent duplicate-dispatch mistake happened around the same time: the orchestrating session accidentally ran TWO parallel agents against the SAME #983 brief/working directory (one resumed via `SendMessage`, one a fresh `Agent()` call that ALSO picked up the coding task in addition to the injection question) — both converged on the identical fix (confirmed by diff, no actual conflict), but their concurrent `cargo build --release` invocations caused real `LNK1104` file-lock contention. Caught and resolved by explicitly telling the duplicate agent to stand down.

After #983 closed, the session worked sequentially through the rest of the priority queue, each following the established prompt-first discipline (write a source-verified brief to `docs/dev-artifacts/prompts/`, commit it, delegate via `crush run`, personally re-verify the diff + re-run the full gate before committing the generated code): **#997** (13 new tests including two genuine live-crash simulations via `tokio::select!` + a pause hook + a real `drop()` to simulate the crash, not just hand-seeded tombstones), **#984** (a new `shamir_degraded_indexes_total` Prometheus gauge, O(indexes)/zero-store-read, proven via a `ReadCountingStore` test double; `/readyz` deliberately left untouched per its documented cheap/subsystem-independent invariant), and **#998** (consolidated the existing Rust-only `create_index_try_build_msgpack.json` hex fixture into ONE shared 21-case matrix consumed by both Rust and a new TS vitest test — which in the process surfaced and fixed a REAL, previously-undetected Rust/TS divergence: the TS builder had no check for multi-field `sorted` indexes, unlike Rust's `SortedMultiField` rejection).

**#985** (RFC for a unified DDL operation-ID+status wire contract) is the task currently in flight — a `crush run` was just launched against a freshly-written, source-verified brief (grounding it in the actual `BatchResponse`/`QueryResult` wire shapes, #967's existing error-enrichment mitigation, and the now-substantial tombstone-recovery infrastructure built this session) and has not yet reported back. This is a DESIGN-DOC-ONLY deliverable (no code) — the brief explicitly forbids touching any `.rs`/`.ts` file.

**No `/babysit` cron is active this session** — all work has been driven by explicit user direction ("продолжай выполнять таски") plus the orchestrating session's own judgment about priority ordering, not an automated tick. `/crush-fallback` IS armed (`sh`, sentinel task #982) and was actually exercised once (peak-hours window for provider `zai`, 08:00–12:00 CEST, auto-reverted via a one-shot cron at 12:00 — confirmed closed and reverted to `armed` mid-session).

## Active goal

None — no `/goal` Stop-hook condition is in force this session.

## TaskList

### in_progress
- #985 RFC: unified DDL result contract (operation ID + status) across the wire protocol — `crush run` in flight, no report yet

### pending
- #963 P0-6 (OPERATOR-ONLY): register shamir-bench runner + capture bench-baseline.json — explicitly not delegable, needs a human with real hardware + GitHub repo-admin
- #982 crush-fallback state (persistent sentinel — do not complete): sh — armed
- #986 Introduce typed IndexSpec Rust enum + wire-compatible DTO for CREATE INDEX (post-alpha, API-freeze follow-up) — now has #998's fixture matrix as a regression safety net; explicitly deferred past v0.1.0-alpha.1

### recently completed
- #998 Shared declarative CREATE INDEX fixture matrix (found + fixed a real TS/Rust `SortedMultiField` validation gap in the process)
- #984 Wire degraded base-index (Building) state into server readiness/metrics (new Prometheus gauge, zero store reads)
- #997 Add crash-recovery to compound regular/unique RENAME INDEX paths (13 new tests, 2 live-crash simulations)
- #999 Investigate prompt-injection source mechanism and write report
- #983 BUG: binary (Uint8Array) field value corrupted on write.insert round-trip (two independent root causes, both fixed)
- (All of #957-981, #973, #987-996 also completed — the full original release-blocker campaign chain — omitted here for length; see prior checkpoint `2026-08-05-0230.md` or `git log --oneline` for the full trail.)

## Decisions

- Split #997 (compound RENAME crash-recovery, a pure correctness fix) and #998 (shared fixture matrix, independently valuable) out of the larger #985/#986 tasks respectively, so each piece could be scoped, briefed, and verified on its own — rather than letting either oversized task absorb unrelated work.
- For #983, personally traced BOTH root causes via direct code reading rather than accepting the first delegated fix as complete — the first (`compare_values`/`scalar_ref_cmp` gap) was necessary but not sufficient; only a real end-to-end e2e run (after correctly rebuilding both `shamir-server.exe` and the napi `.node` binding) surfaced the second (`FilterValue` untagged-enum ordering) root cause.
- Chose a strict custom `deserialize_with` (`de_binary_strict`) over a naive `Binary`/`String` variant reorder for the `FilterValue` fix, after empirically proving reorder-alone would make `Binary` wrongly swallow genuine `String`/`Array` values (serde_bytes's leniency runs both directions).
- On discovering the accidental duplicate-agent dispatch, resolved by keeping the more-advanced agent and explicitly telling the redundant one to stand down, rather than killing either mid-work blindly — verified via diff that no actual data conflict existed first.
- On the prompt-injection incident, declined to over-claim a specific attributed source ("is this a provider test?") — answered with the evidence actually available (structural analysis, no tool/file origin found) rather than a confident but unverifiable yes/no.

## Open questions

- #985's RFC output has not yet been reviewed — pending the in-flight `crush run`'s report.
- No other open questions currently blocking progress; #963 remains explicitly parked for a human operator, #982 is a permanent sentinel, #986 is deliberately deferred post-alpha.

## Repo state

```
?? docs/checkpoints/2026-08-05-0230.md
?? docs/checkpoints/2026-08-05-injection-report.md
```
```
451a0dd9 docs(prompts): brief for #985 -- unified DDL result contract RFC
8e295a99 test(query-builder,client-ts): #998 -- shared CREATE INDEX fixture matrix
2ae99b4b docs(prompts): brief for #998 -- shared CREATE INDEX fixture matrix
d99f2aba feat(engine,server): #984 -- expose degraded-index count via /metrics
90ac3512 fix(engine,index): #997 -- crash-recovery for compound regular/unique RENAME INDEX
```
