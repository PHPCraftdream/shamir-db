# Checkpoint — 2026-07-14 10:30

## Session summary

This session began by finishing the "7-stage Dependabot PR review" campaign from the previous session (tasks #573–#579: trivial bumps merged, tungstenite verified against a stale PR branch and merged, rand 0.8→0.9 migrated by hand across 5 crates, bincode 3.0.0 discovered to be a deliberate tombstone release from an abandoned project and its PR closed with `deny.toml` exception instead). Along the way a REAL regression was found and fixed: task #576 had merged `dtolnay/rust-toolchain` bumped to a nonexistent Rust version `1.100.0` — confirmed by real GitHub CI failing with "could not download nonexistent rust version"; reverted to 1.93.0 directly on `master` (task #581).

The user then asked for a full public-OSS treatment of `bench-scale-tool` (a companion benchmark-harness crate at `D:/dev/rust/bench-scale-tool`, already published to crates.io 0.1.0 earlier). Delivered: a workload substring filter (mask) for `Harness::run`/`--calibrate`, a rewritten `bench-cli run` as a focused optimize-loop (`run <crate> <bench> [mask] [-n N] [--scale s] [--keep-baseline]`, aggregating best/median/mean and diffing against a rolling per-bench baseline file), a layout-agnostic CLI (`find_workspace_root`/`discover_targets` no longer assume `crates/`), README Recipes section covering all three stated goals, and a `release.yml` GitHub Action that publishes to crates.io on `v*.*.*` tag push. Tagged and published `bench-scale-tool` 0.1.1 for real (verified live on crates.io from a fresh scratch project). The repo was later made **public** at the user's explicit request (`gh repo edit --visibility public --accept-visibility-change-consequences`) — task #587 ("prepare for full public OSS release": badges, docs, GitHub topics, CLAUDE.md for that repo) is **in_progress**, only the visibility-change step is done so far.

Switching `shamir-db`'s `bench-scale-tool` dependency from a local absolute path (`D:/dev/rust/bench-scale-tool`, which only existed on this one dev machine) to the crates.io version (task #583) triggered a MUCH larger-than-expected chain of discoveries, because it was the first time `cargo deny check` ever ran to actual completion in this repo's history (the path bug broke `cargo metadata` before the advisory scan could start). Findings and fixes, all pushed to `master`:
- **aws-lc-sys** 0.38.0 — 2 real CVEs (RUSTSEC-2026-0044/0048) — fixed via `cargo update -p aws-lc-rs` (pulls patched aws-lc-sys 0.42+).
- **spin** 0.9.8 — yanked — `cargo update -p spin`.
- **rustls-pemfile** — unmaintained (RUSTSEC-2025-0134) — migrated `shamir-transport-tcp` to `rustls-pki-types`'s `PemObject` trait directly.
- **fxhash** — unmaintained (RUSTSEC-2025-0057) — migrated ~20 call sites across 8 crates to `rustc-hash` (removed 2 genuinely-unused `fxhash` dev-deps along the way).
- **bincode** — unmaintained (RUSTSEC-2025-0141) — confirmed (again, independently) no safe upgrade exists; added a documented `deny.toml` ignore entry rather than migrating now.
- **scc** 2.2 — real `unsound` advisory RUSTSEC-2026-0205 (`Array::insert` double-free if a user `K::compare` panics mid-insert; fixed upstream in 3.8.4). This was the big one: migrated across 7 shamir-db crates (dozens of files — MVCC store, drainer, tx gate, versioned overlay, wasm-host registries, vector HNSW adapter) via a **delegated agent** (`Agent` tool, `sl` = Sonnet low) against a committed brief (`docs/prompts/deps/01-scc-2-to-3-migration.md`, prompt-first discipline per CLAUDE.md) — every scc method with both sync+async forms needed an explicit `_sync` suffix (`insert`→`insert_sync`, `scan`→`iter_sync` with a bool return, etc.), `scc::ebr::Guard`→`scc::Guard`. Independently re-verified (diff read, `cargo check`/`clippy` clean, full test suite rerun twice to rule out transient contention). BUT scc 2.4.0 also turned out to be pulled in by two OTHER paths not covered by the direct bump: the workspace's own pinned `captrack` dependency, and `serial_test` 3.3.1 (dev-dep) — both fixed (captrack got its own migration+release, serial_test just needed `cargo update` to 3.5.0 which dropped scc entirely for `sdd`).
- **captrack** (`D:/dev/rust/captrack`, another of the user's own published crates) needed the SAME scc 2.2→3.8 migration done by hand (smaller blast radius — its own registry.rs/dump.rs plus ~15 test files with `.scan()`/`.insert()` calls on `Tracked*` wrapper types that Deref to real scc types). This ALSO forced an MSRV bump 1.74→1.85 (scc 3.8.4 declares `rust-version = "1.85.0"` / edition 2024 — confirmed on real CI, a 1.74 toolchain can't even parse scc's manifest). Committed, pushed (hit a transient "Recv failure: Connection was reset" on `git push` that required `git config http.postBuffer 524288000` + `http.version HTTP/1.1` to work around), verified green on real captrack CI (all 3 OS + msrv job), tagged `v0.1.1`, published via captrack's own `workflow_dispatch`-based `publish.yml` (had one real failure — a stale `Cargo.lock` `version = "0.1.0"` self-reference that needed a follow-up commit — then succeeded). Verified live on crates.io independently.

Back in shamir-db: bumped the exact `captrack = "=0.1.1"` pin, `cargo update -p serial_test`, and **`cargo deny check` now reports fully clean** ("advisories ok, bans ok, licenses ok, sources ok") for the first time ever, confirmed on real GitHub CI (`supply-chain` workflow green).

While chasing `numa.yml`/`ci.yml` on real CI to confirm the fix, found and fixed THREE MORE genuinely pre-existing, previously-invisible bugs (all only surfaced because CI is finally exercising all 3 OSes for the first time in over a month, since `ci.yml` had been `disabled_manually` and is now re-enabled per task #581):
1. `numa.yml` and `ci.yml`'s test/integration jobs invoked raw `cargo test` directly, which this repo's cargo-runner perimeter guard refuses outright — fixed by switching to `./scripts/test.sh` (with a nextest `-E 'kind(test)'` filterset for the integration-only job, to preserve its original "don't re-run lib tests" intent).
2. `scripts/test.sh` had never had its POSIX executable bit set in git (`100644` instead of `100755`) — invisible locally because `core.fileMode=false` on Windows, but broke every `./scripts/test.sh` invocation on real Linux/macOS runners with "Permission denied". Fixed via `git update-index --chmod=+x`.
3. `cargo fmt --all -- --check` drift (3 files, import-ordering, leftover from the manual fxhash→rustc-hash edits never having been re-formatted) and `crates/shamir-numa/src/linux.rs` using a bare `std::collections::HashMap` instead of the workspace's mandated `TFxMap` (this file is `cfg(target_os = "linux")`-only, so it was NEVER compiled/linted on Windows or by any prior successful CI run — a genuinely dormant clippy violation). Both fixed.

The LAST open thread: even after wiring nextest's existing-but-never-selected `[profile.ci]` (600s kill vs `[profile.default]`'s 180s) via a `CI=true` auto-detect in `scripts/test.sh`, TWO concurrency stress tests (`shamir-tx::overlay_ordering_reader_sees_version_implies_value_history_arm`, `shamir-engine::oracle_stress_disjoint_table_concurrent_monotonic` — different shapes: one busy-spin readers, one 50 one-shot tx tasks) hit the FULL 600s timeout on real ubuntu-latest CI with apparently zero progress, despite passing cleanly and fast locally (isolation AND full 16-core local suite, twice). This rules out "just needs a bit more wall-clock time" and points toward genuine CPU oversubscription at the OS level (many nextest test-binary processes, each spinning up its own `worker_threads=4` tokio runtime, all competing for GitHub's much-more-constrained real core count) rather than a bug in the test code itself. Per the user's explicit call, this was split off into its own task (#589) rather than chased further in this already very extended session — task #583's actual goal (cargo-deny clean + captrack migration) was independently confirmed achieved and the task was marked completed.

A separate, unrelated real bug was also found and deferred as task #588: `crates/shamir-index/src/vector/simd.rs`'s NEON dot-product intrinsics (`vdotq_u32`) require the unstable Rust feature `stdarch_neon_dotprod` and fail to compile on stable Rust on real macOS ARM64 CI runners (again, never previously exercised since `ci.yml` was disabled) — needs a stable-compatible rewrite of that SIMD path, out of scope for this session.

Working hypothesis for #589 (not yet investigated): nextest's `[profile.ci]` should probably get an explicit `test-threads` cap (binary-level parallelism, distinct from the existing `wasm-heavy` test-GROUP throttle) to avoid GitHub's ~4-vCPU runners being oversubscribed by dozens of concurrently-launched multi-worker-thread test binaries.

## Active goal

None. No `/goal` Stop hook is set this session.

## TaskList

### in_progress
- #587 bench-scale-tool: подготовить к полноценному публичному OSS-релизу (только видимость репозитория сделана public; остались бейджи, доки, GitHub topics, CLAUDE.md, прочая OSS-гигиена)

### pending
- #588 CI: macOS ARM64 SIMD NEON — стабилизировать/переписать nightly-only dotprod интринсики
- #589 CI: расследовать CPU-oversubscription под nextest на GitHub-раннерах (стресс-тесты упираются в 600с timeout)

### recently completed
- #583 После публикации bench-scale-tool: переключить shamir-db на crates.io-зависимость (снять numa/cargo-deny CI failures) — cargo-deny check полностью зелёный на реальном CI, подтверждено
- #581 URGENT: revert CI's dtolnay/rust-toolchain pin 1.100.0 → 1.93.0 (nonexistent Rust version)
- #580 INFRA: расследовать 3 предсуществующие проблемы CI
- #579 Этап 7: PR #3 — bincode 1.3.3→3.0.0 (закрыт, не смёржен — заброшенный проект)
- #578 Этап 6: PR #6 — rand 0.8.5→0.9.2
- #577 Этап 5: PR #7 — tungstenite 0.24.0→0.29.0
- #576 Этап 4: PR #1 — dtolnay/rust-toolchain (смёржен, затем откачен как регресс в #581)
- #575 Этап 3: PR #4 — futures-util
- #574 Этап 2: PR #5 — quote
- #573 Этап 1: PR #8 — actions/checkout
- #586 bench-scale-tool: layout-агностичный CLI + починка доков
- #585 bench-scale-tool: focused `run` с before/after diff
- #584 bench-scale-tool: фильтр по одному workload

## Decisions

- **bincode PR closed, not migrated** — the only newer crates.io release (3.0.0) is a deliberate tombstone (`compile_error!`) from an abandoned/harassed-out-of-maintenance project; documented `deny.toml` ignore added instead of chasing a migration that has no real target.
- **scc migration delegated to a sub-agent against a committed prompt-first brief**, then independently re-verified (diff read + tests rerun twice) — not trusted on the agent's own report alone, per CLAUDE.md's crush/agent zero-trust discipline.
- **captrack and bench-scale-tool are treated as first-class published dependencies**, not vendored/path deps — when they needed the same scc fix, the fix was done IN THEIR OWN REPOS with their own commits/releases, not patched around in shamir-db.
- **`CI=true` auto-selects nextest's `[profile.ci]`** rather than bumping `[profile.default]`'s global timeout — activates an already-designed-for-this-exact-purpose profile that was simply never wired in, rather than a blind global timeout increase.
- **Did not chase the remaining CPU-oversubscription stress-test timeouts further this session** — split into task #589 per explicit user instruction, since root-causing needs its own dedicated investigation (nextest test-threads tuning, possibly a new test-group) rather than more guessing.

## Open questions

- Task #589's actual root cause is unconfirmed — CPU-oversubscription is the leading hypothesis (different-shaped tests, both pass locally/isolated, both hit the exact profile timeout ceiling with no visible progress) but not yet proven (e.g., never checked `nproc` on the actual GitHub runner, never tried an explicit `test-threads` cap).
- Task #587 (bench-scale-tool OSS polish) is only partially done — visibility is public; badges/docs/topics/CLAUDE.md for that repo are still open.

## Repo state

### D:\dev\rust\shamir-db
```
(clean — no output from git status --short)
```
```
0a19e558 fix(ci): wire up nextest's existing [profile.ci] via CI=true auto-detect
b8e3b441 fix(ci): fmt drift from rustc-hash migration + shamir-numa disallowed HashMap
d27536f7 fix(ci): set the executable bit on scripts/test.sh
af9381f1 fix(deps): captrack 0.1.0 -> 0.1.1, serial_test 3.3.1 -> 3.5.0 (closes RUSTSEC-2026-0205)
e07ee9c2 fix(deps): scc 2.2 -> 3.8.4 migration (RUSTSEC-2026-0205)
```

### D:\dev\rust\bench-scale-tool
```
(clean — no output from git status --short)
```
```
9e1b7b8 feat: workload filter/mask + focused optimize-loop run + layout-agnostic CLI
a27f98c ci: publish to crates.io on version tag push
78e3f10 ci: bump actions/checkout to v7 to clear the Node.js deprecation warning
76ba6b5 fix(cargo): correct rust-version to 1.71 — tokio's actual MSRV floor
7dba7d2 ci(msrv): drop the repo's newer Cargo.lock before the 1.65 check
```

### D:\dev\rust\captrack
Not re-checked at checkpoint time; last known state (from this session) is clean, pushed through commit `06a5cf2` ("chore: sync Cargo.lock's captrack self-version entry to 0.1.1"), tagged `v0.1.1`, published to crates.io, CI green on all 3 OS + msrv.
