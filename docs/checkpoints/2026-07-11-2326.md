# Checkpoint — 2026-07-11 23:26

## Session summary

Continuing the long-running audit-remediation campaign (tasks #481-#550) on
the S.H.A.M.I.R. database. This window covered: verifying and committing
task #532 (MvccStore Bytes→RecordKey re-key), #533 (keyset-tiebreaker design
decision doc, no code), #534 (index2 CREATE INDEX write-barrier + crash-
orphan-id fix, partial — non-tx path only, tx-commit gap filed as #538),
#535 (MemBuffer `dirty_nonempty` clear-race, two rounds — round 1 closed the
check-then-act gap, round 2 closed a narrower stall-across-await gap found
by adversarial review, a THIRD residual filed as #539), #536 (fjall
write-only worker for insert/transact, scoped down from a mixed
insert-wins/set-regresses bench result, plus a real eager-thread-spawn
regression found and fixed via lazy `OnceLock` init), and #537 (keyset-
pagination record-id tie-breaker — a genuine client-visible wire-protocol
change, user explicitly declined to bump any crate/package version;
adversarial review by a fresh `sx` agent instance found and I fixed directly
a real bug where the tie-breaker was dropped on the internal stale-posting
retry loop's 2nd+ iteration within a single request).

Mid-session the user invoked `/babygoal` asking to continue using `@sx`
(Sonnet, effort=xhigh) as the delegated agent type instead of the
established `@oh`/`@fl` pairing used earlier in the campaign. I complied:
re-armed `/babysit` (new cron job `5bc3e6be`, since the old one had expired/
been cleared), and have been using `sx` for both implementation delegation
and adversarial review passes since then (previously `oh` implemented, `fl`
reviewed).

Using `sx`, launched task #538 (index2 lost-write race residual on the
TX-COMMIT path, as opposed to #534's non-tx-only fix) in the background.
It just completed: Part A (extend the tx-commit pipeline's Phase 2.5
prelock to also cover index2-only tables under an active
`create_index_v2`) is closed; Part B (stage-time-vs-commit-time index2
ops-plan staleness — a harder, independent problem) was explicitly
scoped down per the brief's own permission, left open, and pinned by a
dedicated regression test that documents the residual rather than
silently passing. The implementing agent's own report flags a
self-caught bug worth an independent re-check: its first attempt used
`table_by_token` to peek a table's write-barrier flag, which had the
side effect of FORCING lazy `TableManager` instantiation — this silently
changed `apply_data_batch`'s MVCC-vs-raw-store routing and broke a
pre-existing stress test (`oracle_stress_same_table_snapshot_all_succeed`).
Fixed by adding a new non-instantiating peek, `table_by_token_if_live`.
**I have NOT yet independently verified this fix** — I was mid-way through
my own `cargo check` (passed) and had just started my own scoped
`./scripts/test.sh -p shamir-engine -p shamir-index -p shamir-tx` run (the
user manually backgrounded that command, job id `bl1vpff3n`, output at
`C:\Users\Computer\AppData\Local\Temp\claude\D--dev-rust-shamir-db\...\tasks\bl1vpff3n.output`)
when the `/checkpoint` command interrupted this flow. The #538 diff is
UNCOMMITTED. Per the established discipline (never trust a self-report,
and this task explicitly requires an adversarial review pass before
commit given the commit-pipeline blast radius), the next step is: read my
own backgrounded test output (or re-run if needed), read the actual diff
across the 5 touched files, then send it through an `sx`-based adversarial
review — paying special attention to the `table_by_token_if_live`
change the implementing agent itself flagged as the highest-risk spot —
before committing.

Separately, per an explicit user request mid-session ("запусти @oxx
агента, пусть проанализирует отчёты D:\dev\rust\shamir-db\docs\audits\
2026-07-10-security*.md и заведёт таски"), I launched an `@oxx` (Opus,
max effort) agent to triage 6 permission-system security audit reports
(5 detailed findings + 1 synthesis) that were produced by an EARLIER,
separate `/fxx`-based review pass not previously part of this campaign's
tracked TaskList. The user's explicit guidance for the WASM-function
findings: WASM modules will continue to be compiled by us (the operator),
so the fix direction must be "build a code sanitizer/verifier," never
"stop accepting WASM." `oxx`'s environment lacked TaskCreate/TaskList/
TaskUpdate tools, so it returned a complete, ready-to-register triage as
its final report instead of registering tasks itself. I registered all
11 of its recommended tasks directly as `#540`-`#550` (8 direct-fix
"bucket a" tasks + 3 decision-required "bucket b" tasks mirroring the
#512/#533 investigate-then-ask pattern), cross-checked for overlap with
already-tracked work (none found — #510/#527 already cover the
subscription/SSRF findings the new audits merely reconfirm as closed),
and added the CRITICAL/HIGH items (`#540` fail-open resource_meta, `#541`
definer-function owner-missing escalation to System, `#542` missing HMAC
confirm on privilege-grant/chmod/purge ops, `#545` WASM sanitizer,
`#548`/`#549` the two identity/privilege-model DECISION tasks) to task
`#529` (FINAL-GATE)'s `blockedBy`. Per the user's follow-up request, I
also wrote `oxx`'s full report verbatim (plus my own registration
follow-up note) to `docs/audits/2026-07-11-security-permission-triage.md`
— this file is currently UNCOMMITTED; I told the user it's ready to
commit whenever they want (per standing rule, never commit without
explicit request).

The `/babysit` cron (`5bc3e6be`, every 15 min, off-minutes 7/22/37/52) is
still active and has been ticking correctly throughout — reporting
"still running #538" on each tick while the `sx` background agent worked
(now complete, awaiting my verification+commit).

## Active goal

None (`/goal` Stop-hook machinery not in use this session — the campaign
is driven by the `/babygoal`-installed TaskList + `/babysit` cron, per the
skill's own design: "No `/goal` machinery: progress is governed by what's
on the TaskList").

## TaskList

### in_progress
- #538 SECURITY/CORRECTNESS: close index2 lost-write race on the tx-commit path (residual from #534) — implementation complete (Part A closed, Part B honestly left open + regression-tested), NOT YET independently verified or committed by the orchestrator

### pending
- #519 CLIENT: node-binding typed error .code/.retryable (needs napi-rs 3.x bump — blocked on an explicit user decision not yet asked this session)
- #529 FINAL-GATE: full fmt+clippy+test--full across workspace (blocked by: #507, #515, #516, #538, #539, #540, #541, #542, #545, #548, #549)
- #539 FIX: MemBuffer dirty_nonempty transient reader-visible window + get() tombstone-poisoning (residual from #535 round-2 review)
- #540 SECURITY: resource_meta fail-open on storage error → fail-closed (model-core F1) — HIGH, blocks #529
- #541 SECURITY: effective_fn_actor missing-owner escalates definer function to System (model-core F3) — HIGH, blocks #529
- #542 SECURITY: extend destructive-op HMAC confirmation to privilege/create/chmod/retention ops (admin-ddl #3) — MEDIUM→HIGH, blocks #529
- #543 FIX: validate chown/chgrp/addGroupMember target ids resolve to real principals (admin-ddl #5, identity #5) — MEDIUM, non-blocking
- #544 FIX: WASM per-function egress capability + gate global_set into env.* namespace (wasm-functions #2) — MEDIUM, non-blocking
- #545 SECURITY: WASM module sanitizer/verifier before instantiation (wasm-functions #1, CRITICAL residual) — blocks #529
- #546 HARDENING: unified authz-mapping registry + doc-guards + create-TOCTOU + coverage matrix (gate-coverage cluster) — LOW, non-blocking
- #547 FIX: wire changePassword flow into the live server request dispatcher (identity-session #3) — MEDIUM, non-blocking
- #548 DECISION: unbind security-Actor from fxhash(username), bind to stable user_id (model-core F2, admin-ddl #4, identity #1+#2) — HIGH systemic, blocks #529
- #549 DECISION: unify parallel superuser/role privilege axis + reconcile desynced user/role stores (admin-ddl #1+#2, identity #4, model-core F4) — HIGH, blocks #529
- #550 DECISION: Root/User/Group always-open meta + wire-admin DAC posture + wire Definer/secret_grants gap (model-core F5, gate-coverage #2, wasm #3) — LOW/design, non-blocking

### recently completed
- #537 IMPLEMENT: keyset-pagination record-id tie-breaker (wire-protocol change, per #533 decision) — commit a3c8acf5
- #536 PERF: redesign fjall worker-loop as write-only worker + sharded read pool — commit 9d1b4003
- #535 FIX: MemBuffer dirty_nonempty flag clear-race — commit 98aa0c46
- #534 SECURITY/CORRECTNESS: index2 CREATE INDEX lost-write race + crash-orphan-postings — commit 06179964
- #533 DESIGN: keyset tie-breaker decision doc — commit cb45ad22
- #532 PERF: MvccStore RecordKey re-key — commit 7a703b25
- #531 G6: WAL test-hardening
- #530 G5: vector-index perf residual
- #528 G4: test flakes
- #527 G3: security-residual subscription/SSRF

## Decisions

- User explicitly declined to bump crate/package versions for #537's wire-protocol change ("Не поднимать версии (рекомендую)") — matches CLAUDE.md's standing "never bump versions without explicit request" rule; the change stays strictly additive/backward-compatible with no version bump.
- User asked mid-session to switch delegated-agent usage from the established `@oh` (implement) / `@fl` (review) pairing to `@sx` (Sonnet, effort=xhigh) for both roles going forward — complied, currently using `sx` for both implementation and adversarial review.
- For #536's mixed bench result (insert/transact genuinely faster via a write-only worker thread, but set/remove regressed ~1.45x because their embedded `contains_key` read lost spawn_blocking-pool parallelism when serialized), chose to scope DOWN rather than ship a net-negative change: kept insert/transact on the worker, reverted set/remove to the original per-op `spawn_blocking` path with their original doc comments restored.
- For #538, accepted the brief's own explicit permission to scope down: closed Part A (commit-time prelock gap) fully, left Part B (stage-time ops-plan staleness) open with an honest regression test documenting the residual rather than attempting a risky rewrite of the commit pipeline's phase structure.
- For the new permission-system audit findings (#540-#550), followed the established #512/#533 precedent: findings requiring an architecture/representation decision (identity model, role-axis unification, Root/User/Group meta semantics) became separate DECISION tasks requiring investigation + a decision doc + explicit user sign-off BEFORE any code, rather than being folded into direct-fix tasks.
- WASM sanitizer task (#545) explicitly framed per user directive: the fix is "build a structural verifier over the compiled .wasm artifact," never "refuse to accept WASM" — since WASM modules are always compiled by the operator, not accepted as arbitrary anonymous bytecode.

## Open questions

- **#538's `table_by_token_if_live` change is NOT YET independently verified.** This is the immediate next step: read the backgrounded test output (job `bl1vpff3n`) or re-run `./scripts/test.sh -p shamir-engine -p shamir-index -p shamir-tx` myself, read the actual diff across `pre_commit.rs`/`repo_instance.rs`/`table_manager.rs`/`table_manager_index_mgmt.rs`/`index2_create_barrier_tests.rs`, then send through an `sx` adversarial review pass (the implementing agent itself flagged the lazy-instantiation-side-effect fix as the highest-value thing to re-check) before committing.
- Should `docs/audits/2026-07-11-security-permission-triage.md` be committed now? Told the user it's ready; no explicit "yes, commit" received yet.
- #519 (napi-rs 3.x version bump for the node-binding client) still needs an explicit user decision not yet asked this session — does not block FINAL-GATE.
- Several of the newly-filed #540-#550 tasks will need their own prompt-first briefs, `sx` implementation launches, and (for the two DECISION tasks #548/#549 marked HIGH-blocking) explicit user sign-off before any code — none of that work has started yet.

## Repo state

```
 M CLAUDE.md
 M crates/shamir-engine/src/repo/repo_instance.rs
 M crates/shamir-engine/src/table/table_manager.rs
 M crates/shamir-engine/src/table/table_manager_index_mgmt.rs
 M crates/shamir-engine/src/table/tests/index2_create_barrier_tests.rs
 M crates/shamir-engine/src/tx/pre_commit.rs
?? docs/audits/2026-07-11-security-permission-triage.md
?? docs/checkpoints/2026-07-08-audit-remediation-a10-inflight.md
?? docs/checkpoints/2026-07-08-audit-remediation-a4-inflight.md
?? docs/checkpoints/2026-07-08-audit-remediation-babygoal.md
?? docs/checkpoints/2026-07-09-audit-remediation-perf-radical-2.md
?? docs/checkpoints/2026-07-09-audit-remediation-perf-radical.md
?? docs/checkpoints/2026-07-10-audit-remediation-500-inflight.md
?? docs/checkpoints/2026-07-10-audit-remediation-520-inflight.md
?? docs/checkpoints/2026-07-10-audit-remediation-532-inflight.md
?? docs/prompts/audit/crush-launch-command.md
?? (a large number of stray *.log files and a logs/ dir at repo root from
   earlier bench/test runs across many prior sessions — not from this
   session, pre-existing clutter, not touched)
```
(CLAUDE.md's own modification predates this session — a `crush`-related
guidance addition already visible in the system-prompt copy of the file;
not something this session's work touched, left as-is.)

```
adcb9e49 docs(prompts): brief for #538 index2 tx-commit-path lost-write gap
a3c8acf5 feat(query): keyset-pagination record-id tie-breaker (#537)
997d30e1 docs(prompts): brief for #537 keyset-pagination tie-breaker implementation
9d1b4003 perf(storage): fjall write-only worker for insert/transact (#536)
57c613e8 docs(prompts): brief for #536 fjall write-only worker redesign
```
