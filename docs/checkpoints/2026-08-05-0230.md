# Checkpoint — 2026-08-05 02:30

## Session summary

This session completed the entire `/babygoal`-driven campaign originally instructed as: *"реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу"*. The campaign remediates a 2026-08-03 readonly review (`docs/dev-artifacts/research/2026-08-03-new-wave-readonly-review.md`) that returned a NO-GO verdict for `v0.1.0-alpha.1`.

**The full 15-task original chain (#957-971)** is done: all 7 P0 correctness blockers, a repo-wide `legacy`→`base_index` rename (#973), the 8-cluster e2e/TS-SDK OQL/DDL coverage sweep (#964, #974-981 — JS suite 130→147 tests, TS vitest 1028→1055), and the 6-task P1 hygiene wave (#965-971).

**After that chain finished**, CHANGELOG.md was updated, all `.md` files were committed, and a full `@oh` (Opus, high effort) adversarial review ran over the entire diff range. That first review found the wave was largely sound but surfaced one confirmed, empirically-reproduced P0 regression (#987 — a tx staged before `CREATE UNIQUE INDEX` could commit a silent duplicate AND corrupt the pre-existing owner's posting, because the retroactive `UniqueGuard`-recording rederive ran AFTER the only site that validates guards) plus three smaller issues (#989 security-adjacent pre-auth existence oracle, #990 a missed `include` validation case, #991 a CHANGELOG gap). All four were fixed and committed, each independently verified (diffs read personally, tests re-run personally; for #987 the fail→fix→pass cycle was manually reproduced via a temporary `git stash`).

**A Stop-hook then correctly flagged** that the checkpoint written after the original chain predated these 4 fixes, and that a fresh `@oh` pass hadn't reviewed them. A second, fresh checkpoint was written, and a SECOND `@oh` review ran, scoped specifically to the #987/#989/#990/#991 diff. That review confirmed #987 solid, but found: #990 had caused a latent (dist/-masked) JS e2e test regression (#996), #989's fix only covered 2 of ~10 handlers sharing the identical defect pattern (#995), and reiterated two already-known lower-priority items (#992 a non-linearizable generation counter, #993 a fragile key-length heuristic) — plus confirmed #988 (index2 DROP INDEX crash-safety, deferred since #972) was still genuinely open. All of #987-996 (10 tasks total: the 4 first-review fixes + #988/#992/#993/#995/#996 found by the second review + #994 a no-action documentation item) are now fixed, verified, and committed — nothing was left as a silent landmine; every deferred/scoped-down decision was made deliberately by the orchestrating session (not the delegated sub-agent) and is recorded either inline in the relevant commit message or as a freshly-filed follow-up task.

**Every single task in this campaign** — all ~35 completed tasks — followed the mandatory prompt-first discipline: a committed brief in `docs/dev-artifacts/prompts/release-blockers/` (numbered 00 through 31) before each `crush run`, followed by the orchestrating session independently re-verifying the sub-agent's diff (full `git diff` read, tests personally re-run via `./scripts/test.sh`, never trusting a sub-agent's self-report) before committing. Several fixes required extra manual verification beyond the standard gate: #987 and #996 both had their core claim (a fail→fix→pass cycle, or a "the rebuilt client actually behaves differently" claim) personally reproduced by the orchestrating session using a temporary `git stash` or a fresh `npm run build` + full e2e run, specifically because the whole point of this second-review pass was to not just rubber-stamp sub-agent claims.

**At the end of the campaign**, the `/babysit` cron (job id `909f6b8b`) was deliberately disarmed after 4 consecutive ticks reported "blocked" with no state change — every remaining pending task is either explicitly non-delegatable (#963, needs a human with hardware + GitHub admin access), a persistent sentinel that must never complete (#982), or genuinely needs a fresh user decision before more work should start (#983's root cause is unlocated; #984-986 are large deferred post-alpha designs). Repeated identical "blocked" tick reports were adding no value, so the cron was cancelled rather than left spinning.

## Active goal

None — this session used `/babygoal` + `/babysit` (TaskList-driven), not a `/goal` Stop-hook condition. A generic environment-level Stop-hook DID exist and correctly caught two gaps mid-session (a missing checkpoint, a missing follow-up review) — both were addressed before this checkpoint was written.

## TaskList

### in_progress
(none)

### pending
- #963 P0-6: release perf-gate неработоспособен — нет bench-baseline.json и не зарегистрирован runner (NOT delegatable — needs a human operator with real hardware + GitHub repo-admin access; `docs/guide-docs/CI_PERF_GATE_RUNBOOK.md` already documents every manual step)
- #982 crush-fallback state (persistent sentinel — do not complete): sh — armed
- #983 BUG: binary (Uint8Array) field value corrupted on write.insert round-trip (found during #980's verification; root cause NOT yet located — several conversion sites already ruled out and documented in the task body; needs fresh investigation, not yet delegated)
- #984 Wire degraded base-index (Building) state into server readiness/metrics (deferred from #966, large scope)
- #985 Design a unified DDL result contract (operation ID + status) across the wire protocol (deferred from #967, large scope)
- #986 Introduce typed IndexSpec Rust enum + wire-compatible DTO + shared Rust/TS fixture-matrix codegen for CREATE INDEX (deferred from #970, post-alpha API-freeze follow-up, large scope)

### recently completed
- #995 Broaden #989's auth-before-existence-probe fix to remaining handlers (8 more handlers across 4 files)
- #993 Sub-bug 2c's base_index-ops retain filter safety-net (doc contract + regression test)
- #992 Made IndexRegistry::insert generation tagging linearizable (root-cause fix, not just documented)
- #996 Fixed #990's latent JS e2e test regression (rebuilt client, ran full e2e suite personally)
- #994 Two minor residuals noted by @oh review — closed as no-action-needed documentation items
- #988 index2 DROP INDEX crash-safety (full tombstone + recovery pattern, mirroring #959/#972)
- #991 CHANGELOG updated for #970's breaking change + the post-wave review fixes
- #990 Closed #970's missed include-on-non-btree validation gap
- #989 Fixed pre-auth existence oracle in DROP/RENAME INDEX (2 handlers)
- #987 Fixed P0 regression: unique-index staged-before-CREATE duplicate not validated

(All 15 original-chain tasks #957-981 plus #973 also completed — omitted here for length, see prior checkpoints `2026-08-04-2112.md`/`2026-08-05-0130.md` for their detail, or `git log --oneline` for the full commit trail.)

## Decisions

- Scoped every review finding to the narrowest change that closes the concrete, verified defect, filing every larger suggested redesign as its own standalone tracked task (#984, #985, #986 from the first wave; none new from the second) rather than attempting an oversized fix or silently dropping the larger idea.
- For #987 (the most severe finding), personally re-verified the fail/pass cycle via a temporary `git stash` rather than trusting the sub-agent's self-reported repro.
- For #989/#990 (moderate severity, mechanical, low-risk fixes), fixed immediately rather than deferring, since the fix shape was small and unambiguous.
- For #992, deliberately fixed the ROOT CAUSE (a dedicated ticket counter) rather than just documenting the fragile coupling to #957's admission mutex the task's own description offered as an alternative — reasoned that leaving a second instance of the exact #987 failure class as a documented landmine was not acceptable for a release-track fix.
- For #993, scoped DOWN from the task's own suggested fix (an explicit family tag on `IndexWriteOp`, which would have touched 13+ construction sites across 3 crates) to a doc-contract + regression-test safety net, since the actual risk was verified zero-live-backends-affected and the full redesign was disproportionate.
- For #988 (the largest of the second-wave fixes), operationalized #972's own already-detailed deferral note rather than redesigning from scratch — chose tombstone (option a) over state-based (option b) exactly as that note recommended, adapting only for `IndexRegistry`'s architecture (free functions in `persistence.rs`, since `IndexRegistry` doesn't own an `info_store` the way `SortedIndexManager` does).
- Disarmed the `/babysit` cron after 4 consecutive identical "blocked" tick reports, since every remaining pending task genuinely needs either a human (#963), is a sentinel (#982), or needs a fresh user decision (#983druck root cause unknown; #984-986 large deferred designs) — continuing to tick would have produced no new information.

## Open questions

- **What to do next** — posed directly to the user at the end of the last turn, not yet answered: (a) investigate #983 (the Uint8Array/binary corruption bug, root cause not yet located), (b) start one of the three deferred post-alpha redesigns (#984/#985/#986), or (c) stop here. The campaign's core release-blocker work (everything the two `@oh` reviews found) is fully closed either way.

## Repo state

```
(clean — no uncommitted changes)
```
```
18f6b768 fix(db): #995 -- broaden auth-before-existence-probe fix to 8 more handlers
75a6e685 docs(prompts): brief for #995 -- broaden auth-before-existence-probe fix
40066694 test(engine): #993 -- safety net for sub-bug 2c's retain key heuristic
92b03bdc docs(prompts): brief for #993 -- safety net for the 2c retain key heuristic
9dc40c75 fix(index): #992 -- make IndexRegistry::insert generation tagging linearizable
```
