# Checkpoint — 2026-08-10 08:42

## Session summary

This session is governed by a standing `/babygoal` Stop-hook: "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу." The hook re-fires after nearly every turn until the FULL TaskList is empty and the final checkpoint/changelog/review sequence has run — that final sequence has NOT run yet; this checkpoint is a mid-session snapshot per the user's explicit `/checkpoint` command, not the closing step.

**Fully completed and committed this session:** the entire online CREATE INDEX RFC v3 slice — `#1054` (write-path audit), `#1055` (RFC revision), `#1056`–`#1058` (Phase A snapshot_stream, SnapshotGuard, dirty-set registry), `#1060` (crash-recovery matrix — corrected a false premise that auto-self-heal exists for base_index, it doesn't), `#1061` (4 pipeline property tests — completeness, convergence, bounded barrier, equivalence; needed a 3-round fix cycle: correctness → strengthened equivalence check → fixed a 180s test timeout by shrinking a fixture), `#1062` (f78_writer_latency bench re-measured with real numbers: writer p95 dropped from ~150ms tracking build duration to ~0-1ms independent of table size; KNOWN_LIMITATIONS.md + CHANGELOG updated; also fixed a flaky #1061 test found during this task's own gate re-run), `#1087`–`#1089` (Phase B+A backfill, Phase C+D catch-up/publish, wiring into `create_index` + integration tests). Every one of these went through 1-4 rounds of `/crush` delegation, each independently re-verified (diff read, `cargo fmt`/`clippy`, targeted + full suite re-run) before commit — this caught real bugs at nearly every step (a barrier-guard-never-dropped deadlock, batch-size/pause-hook test bugs, a genuine correctness gap in the original RFC's Phase C design — found and fixed via `AskUserQuestion` operator approval — plus several smaller issues).

Also completed: `#1064` (P0 SECURITY — `GetDdlOpStatus` had zero authorization; fixed by reusing `create_cursor`'s `authorize_cursor_read` pattern, gated before any table resolution so an unauthorized caller's response is indistinguishable from not-found; 3 new tests, committed `4a993a39`).

**Currently in flight: `#1065`** (P0: DDL status crash-safe write order, correlation id, no swallowed errors). This is fixing gaps in an ALREADY-DESIGNED contract from an earlier RFC (`docs/dev-artifacts/research/2026-08-05-ddl-result-contract-rfc.md`, #1015), not a redesign. Round 1 (committed nothing yet — still uncommitted) correctly added: `InProgress` status write before mutation, client-supplied `request_id` correlation id (wire-additive, backward compatible), idempotent retry check, a versioned envelope for the op-status log encoding, and fixed the recovery-path write ordering (index2 DROP recovery, hash RENAME recovery both regular+unique now correctly write terminal status BEFORE clearing their tombstone). Round 1 did NOT fix the INLINE/synchronous path's write order (status still written after the mutation's internal tombstone-clear — the most common case, not just the crash-recovery case) and wrote ZERO of the 6 required tests. I independently verified round 1's excuse ("not feasible without significant refactoring") was factually wrong: the real constraint is a crate-layering issue (`ddl_op_log` lives in `shamir-engine`, but the tombstone-clear that needs to precede it happens inside `shamir-index`'s `IndexManager`, and `shamir-index` can't depend on `shamir-engine`) — but `shamir-query-types` (where `DdlOpStatus` lives) doesn't depend on `shamir-index`/`shamir-engine` either, so moving `ddl_op_log.rs` down into `shamir-index` is a clean, bounded fix, not a redesign. Wrote round 2's brief (`70-p1065-round2-inline-ordering-and-tests.md`, committed `2b58805e`) demanding exactly that module relocation plus the missing tests. **Round 2 is currently running in the background** (crush session `p1065-ddl-status-crash-safety`, the SAME session id reused across rounds) — its result has not yet been checked. Live repo state (uncommitted, mid-round-2): the module move is in progress (`crates/shamir-index/src/base_index/ddl_op_log.rs` exists as a new file, `Cargo.toml`/`mod.rs` touched in both `shamir-engine` and `shamir-index`) — see Repo state below for the exact file list.

**Established practices this session, unbroken:** every `/crush` round's result is independently re-verified before trusting or committing (diff read personally, `cargo fmt --all -- --check` + `cargo clippy --workspace --all-targets -- -D warnings` + targeted then full `./scripts/test.sh` re-run by me, never accepted from a self-report); every brief is written to `docs/dev-artifacts/prompts/release-blockers/<NN>-<name>.md` and committed BEFORE the corresponding `crush run` launches; commits happen once per completed task, never batched; background `crush run`/test commands are launched with `run_in_background: true` and waited for via the notification system or a single `TaskOutput --block` call, never polled with `sleep`; a `crush sessions locks`/`tasklist` liveness check is used only to diagnose a genuine "did this actually die" anomaly (e.g. a `stopped`/`failed` notification with no output), never as routine polling. Two tasks this session were explicitly marked "single context" in their own descriptions (`#1062`'s bench numbers, `#1064`'s narrow cross-crate signature change) and were done directly by me rather than delegated, per those tasks' own stated strategy — this is a deliberate, task-scoped exception to the hook's general "use /crush for everything," not a departure from it.

## Active goal

Standing session-scoped Stop hook (verbatim): "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу"

## TaskList

### in_progress
- #1065 P0: DDL status — crash-safe порядок записи, correlation id, не глотать ошибку статуса (round 2 running in background, session `p1065-ddl-status-crash-safety`)

### pending
- #982 crush-fallback state (persistent sentinel — never completes by design)
- #1049 P1-6 follow-up (non-actionable until a real recurrence)
- #1066 P0: index2 RENAME неатомарен — runtime и диск расходятся при ошибке или отмене
- #1067 P1: DDL op-log — неверная классификация семейств + отсутствующий op_id у sorted/index2  (blockedBy: #1065)
- #1068 P1: DDL op-log растёт неограниченно — DDL_OP_LOG_CAP и eviction мертвы  (blockedBy: #1065)
- #1069 P1 OQL: SELECT * молча теряет колонки, безалиасные выражения перетирают друг друга
- #1070 HNSW: документация обещает 0.90/95-99%, тест требует 0.60 на 3K векторов вместо 10K
- #1071 Release evidence: ручной perf gate на release-SHA + неизменяемый артефакт результатов
- #1072 CRITICAL: shamir-server doctor не видит wire-созданные таблицы, exit 0 + тавтологичные e2e-тесты
- #1073 MEDIUM: CursorRegistry::register теряет shard-lock до CAS
- #1074 MEDIUM: #1039 unique-guard даёт ложный UniqueViolation при освобождении ключа внутри той же tx
- #1075 MEDIUM: типизированный CreateIndex builder молча теряет опции
- #1076 MEDIUM: CLAUDE.md «ровно одно санкционированное исключение» для std::sync::Mutex не соответствует коду
- #1077 LOW: PerIpLimiter::try_acquire держит shard write-lock весь CAS-цикл  (blockedBy: #1073)
- #1078 LOW: DropIndexOp::unique мёртв для резолвинга, но всё ещё меняет HMAC-подпись
- #1079 PERF: sorted range-scan BTreeSet поэлементно + lease_by_field_and_kind O(N) + audit-checkpoint без spawn_blocking
- #1080 nit: QueryResult { ..Default::default() } × 133 места
- #1081 ReaderDrainGate: перф-hardening + неверное доказательство livelock-freedom
- #1082 CachedStore::flush — unbounded busy-poll, ошибка теряется молча
- #1083 Batch::try_build — полный msgpack round-trip + 4 expect() в публичном пути
- #1085 Watchdog на границе request/batch + аудит cancellation-safety
- #1086 Docs: sizing guide числа для cursor rescans, FTS/functional rebuild, vector quality

### recently completed
- #1064 P0 SECURITY: GetDdlOpStatus не проверяет ACL (commit `4a993a39`)
- #1062 Слайс 1g: бенч f78_writer_latency + KNOWN_LIMITATIONS/CHANGELOG (commit `f9b7c241`)
- #1061 Слайс 1f: тесты свойств пайплайна (commit `13679772`)
- #1060 Слайс 1e: crash recovery для online build (commit `a2c2dd0b`)
- #1089 Слайс 1d-3: wiring в create_index + интеграционные тесты (commit `22efc917`)
- #1088 Слайс 1d-2: Phase C+D (commit `0a92cd3d`)
- #1087 Слайс 1d-1: Phase B+A (commit `c32d581a`)
- #1063 P0 RELEASE-BLOCKER: BumpFtsStats без provenance
- #1058, #1057, #1056, #1055, #1054 — online CREATE INDEX слайс 1a-1c
- #1084 GATE: явные решения по scope alpha

(28 tasks completed total this session; 1 in_progress; 21 pending; 2 non-actionable sentinels)

## Decisions

- RFC v2→v3 for online CREATE INDEX: operator approved via `AskUserQuestion` to fix a real correctness gap found mid-implementation (Phase C stale-posting bug — naive "recompute from current state only" would permanently orphan postings for rows updated/deleted during the barrier-free window) rather than ship the flawed original design.
- `doctor::repair()`'s regular-family rebuild was deliberately NOT migrated to the online-build path (`#1089`) — doing so per-index would reopen a serialization race (F-3, #1030) the single admission-lock acquisition specifically closed; tracked as a future follow-up, not attempted.
- `#1065`'s Defect 2 fix (inline-path write ordering) requires relocating `ddl_op_log.rs` from `shamir-engine` to `shamir-index` — verified this is architecturally clean (no dependency cycle) before writing the round-2 brief, rejecting round 1's claim that this was "not feasible."
- Two tasks (`#1062`, `#1064`) executed directly by the orchestrator rather than delegated, per their own explicit "single context" strategy notes.

## Open questions

None outstanding. The next immediate step is checking `#1065` round 2's result once its background `crush run` completes (session `p1065-ddl-status-crash-safety`) — diff read, full gate re-run, then commit or iterate to round 3 per the same zero-trust pattern used throughout this session.

## Repo state

```
 M crates/shamir-db/src/shamir_db/execute/admin_table_index.rs
 M crates/shamir-engine/src/table/ddl_op_log.rs
 M crates/shamir-engine/src/table/mod.rs
 M crates/shamir-engine/src/table/table_manager.rs
 M crates/shamir-engine/src/table/table_manager_index_mgmt.rs
 M crates/shamir-index/Cargo.toml
 M crates/shamir-index/src/base_index/mod.rs
 M crates/shamir-query-builder/src/ddl/drop_index.rs
 M crates/shamir-query-builder/src/ddl/rename_index.rs
 M crates/shamir-query-types/src/admin/types/index_ops.rs
?? crates/shamir-index/src/base_index/ddl_op_log.rs
?? docs/checkpoints/2026-08-09-0945.md
?? docs/checkpoints/2026-08-09-1911.md
?? docs/dev-artifacts/research/2026-08-09-new-wave-review-claude.md
```

**Important:** the above modified/untracked files are `#1065` round 2's WORK IN PROGRESS (the crush session is actively running as of this checkpoint) — do NOT commit them until the round completes and is personally verified (diff read, `cargo check`/`fmt`/`clippy`, full gate) per this session's established zero-trust practice. The `docs/checkpoints/*.md` and `docs/dev-artifacts/research/2026-08-09-new-wave-review-claude.md` files are pre-existing untracked docs from earlier in the session, unrelated to `#1065`; they get committed together in the final `/babygoal` closing sequence (checkpoint + changelog + "commit all .md" step), not before.

```
2b58805e docs(prompts): brief for #1065 round 2 -- fix inline path's real write order (module relocation, not just logging) + write the missing tests
b3f6197b docs(prompts): brief for #1065 -- DDL status crash-safe write order, correlation id, no swallowed errors
4a993a39 fix(server): #1064 P0 SECURITY -- GetDdlOpStatus now authorizes the poll
f9b7c241 docs+bench(index): #1062 slice 1g -- f78_writer_latency proof for online CREATE INDEX + docs
13679772 test(index): #1061 slice 1f -- pipeline property tests for online CREATE INDEX
```
