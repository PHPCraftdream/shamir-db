בְּשֵׁם יהוה הָרַחֲמָן וְהַחַנּוּן

# Checkpoint — 2026-06-24 22:30 [phase-e-campaign-planned]

## Session summary

Сессия после победы над багом #236 (Phase D — FK ON DELETE). Текущая работа уже
**закоммичена и запушена** (4 коммита: builder `foreign_key_on_delete`, fix
on_delete-каталог + drop-guard + новый in-process тест, un-skip TS e2e, docs
DONE.md + выпил выполненного). master на `5a0ad7e`.

Затем по просьбе пользователя **спланирована СЛЕДУЮЩАЯ кампания**. Изучил
`docs/research/` (после выпила), ранжировал остаток по линзам «похожее на Phase
D/keyset» × «наибольшая польза». Созерцание → `docs/research/NEXT-CAMPAIGN.md`
(4 кандидата: ① DDL-lifecycle, ② OQL-surface, ③ A2 access-дефолты, ④ headline-
e2e). Решено: слить **①+②+④ в ОДНУ кампанию «Phase E — Completeness &
Operability»**; **A2 (③) и unify-uniqueness — ОТДЕЛЬНО следом** (другой класс
риска — A2 меняет дефолтное поведение всей системы, может замаскировать
регрессии, нужна security-линза).

Заземлил план чтением кода (file:line). Ключевые находки: дропы непоследовательны
при отсутствии (drop_db no-op, drop_index ошибка); `cascade` только на db/repo;
`if_exists` нигде; rename только function/validator; `DeleteOp` без returning,
`UpdateOp.select` есть; **ни один TS e2e не создаёт fts/vector-индекс и не зовёт
`call`**; DESCRIBE надо скомпоновать из существующих reads.

**Завёл 9 тасков #241–249** (pending), план дописан в NEXT-CAMPAIGN.md. НИЧЕГО
по Phase E ещё не реализовано — только планирование. Старт ждёт слова
пользователя. babysit НЕ армлен (планирование, не исполнение). `/goal` снят
(предыдущая цель достигнута).

## Active goal
Нет активного `/goal` (цель «победим баг» достигнута и снята).

## TaskList
### pending
- #241 E.1 — if_exists на всех drop-ops (идемпотентность DDL)
- #242 E.2 — table-level cascade на drop (blockedBy: #241)
- #243 E.3 — DropFunction-as-validator guard
- #244 E.4 — RENAME table/repo/index (+db/role/group/folder follow-on)
- #245 E.5 — RETURNING-симметрия INSERT/DELETE
- #246 E.6 — DESCRIBE / SHOW CREATE
- #247 E.7 — EXPLAIN / dry-run plan (ОПЦИОНАЛЬНО)
- #248 E.8 — e2e FTS / vector / call (headline)
- #249 E.9 — unit Phase B/C constraints (C2) + doc-fixes F1–F5
### recently completed
- #236 Phase D.2/D.3 (CASCADE/SET NULL/drop-guard) — побеждён, закоммичен/запушен.
- #229–235, #237–240 — Phase D.0/D.1, keyset, B1/B3, блок1a/b.

## Decisions
- ①+②+④ → одна кампания «Phase E»; A2(③)+unify-uniqueness — отдельно (риск-класс:
  A2 меняет глобальный access-дефолт → может замаскировать регрессии остальных
  треков, нужна security-линза и поэтапный rollout).
- Порядок фаз — по ID (E.1→E.9), кроме E.2←E.1 (общий drop-handler surface).
  Стратегия: single-context, sequential, commit-per-phase.
- E.4 RENAME ограничен скоупом table/repo/index (db/role/group/folder — follow-on).
- E.7 EXPLAIN — опциональна (низкий приоритет).
- Phase D-работа закоммичена 4 логическими коммитами + запушена (по явной просьбе).

## Open questions
- **Старт Phase E** — жду слова пользователя («погнали»/`/babygoal`, или указать
  фазу/порядок/скоуп). Таски pending, ничего не начато.
- `.recovery-backup/` (untracked мусор с аварии) — прибрать? (предлагал, ответа нет)
- `docs/research/NEXT-CAMPAIGN.md` — untracked, в git не добавлен.

## Repo state
```
?? .recovery-backup/
?? docs/research/NEXT-CAMPAIGN.md
```
```
5a0ad7e docs(research): DONE.md + выпил выполненного из плановых доков
f0ac57e test(client-ts): un-skip Phase D.2/D.3 e2e (#236 fixed)
f0c64a6 fix(engine,db): Phase D — persist FK on_delete + catalogue-based drop-guard (#236)
3c57230 feat(query-builder): foreign_key_on_delete для Rust/TS паритета
909b650 test(client-ts): Phase D e2e — D.1 RESTRICT green; D.2/D.3 skipped (e2e gap)
```
