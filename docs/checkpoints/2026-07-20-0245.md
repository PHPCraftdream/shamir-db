# Checkpoint — 2026-07-20 02:45 [compliance-ops-peak-hours-fallback]

## Session summary

Continuation of the same standing `/babygoal` directive ("реализуй задачи с помощью /crush, между задачами делай коммиты, покрой их тестами") described in the prior checkpoint (`docs/checkpoints/2026-07-20-0230.md`, written ~15 minutes ago — read that file for the full session history through the end of task 5d). This checkpoint only records what changed since then.

**What happened since the last checkpoint**: the `/crush` session for task 5e (SBOM artifact in the supply-chain CI workflow) hit the `zai` provider's peak-hours gate (08:00–12:00 local) — the SAME class of interruption that occurred earlier in this session during the Этап 3 cleanup-tail work. The session had already produced a partial, uncommitted diff in `.github/workflows/supply-chain.yml` (48 lines) before being cut off.

Per the crush skill's fallback policy (never self-add `--allow-peak-hours`; surface the refusal and ask), I asked the user how to proceed. **This time the user chose a THIRD option not previously used this session**: switch to a local sub-agent specifically of type `sh` (Sonnet 5, high effort) via the `Agent` tool, rather than either (a) waiting out the window and resuming the same `/crush` session, or (b) stopping entirely (both of which were chosen at different points earlier in the session for similar interruptions). A local `sh` agent was launched with a fully self-contained prompt: the complete 5e brief content, plus an explicit instruction to first read the existing partial diff via `git diff` and either finish or redo it (not blindly trust it), plus the same git-mutation prohibition and verification requirements every crush brief in this campaign carries. That agent is running in the background as of this checkpoint — not yet complete, not yet reviewed.

The user then asked me to set a timer for 12:00 to switch back to `/crush` — done via `CronCreate` (one-shot, `2 12 20 7 *`, session-only, job id `fe2a1258`), with a prompt instructing whoever picks it up to verify/commit whatever the local-agent fallback produced (if not already done) and then resume using `/crush` (not local agents) for task 5f and beyond, once the peak-hours window has closed.

**Two timers are now active**: the existing 15-minute babysit heartbeat (`459f9e60`, recurring, monitors the TaskList generally) and this new one-shot 12:00 peak-hours-recovery reminder (`fe2a1258`, fires once then self-deletes).

Nothing new has been committed since the last checkpoint (`97e10067` is still HEAD — that was 5d's brief commit... no, wait: re-check — `97e10067` is 5e's BRIEF commit, already made before the crush session was launched and then interrupted; the actual 5e IMPLEMENTATION is what's pending, sitting partially done in the working tree). Task #706 (5e) remains `in_progress` on the TaskList; nothing has regressed.

## Active goal

None (same as prior checkpoint — no `/goal` Stop hook is armed; the TaskList is the sole source of truth).

## TaskList

### in_progress
- #706 5e. Add SBOM artifact to supply-chain CI workflow (blockedBy: none — brief committed at `97e10067`; a local `sh` sub-agent is currently working the implementation in the background after the `/crush` session hit peak-hours mid-work; diff not yet reviewed/verified/committed)

### pending
- #707 5f. SecretString for CreateScramUser.password + VectorBackendRef api_key_secret (blockedBy: #706) — the item the user specifically flagged as wanting careful treatment
- #677 Этап 6 — Documentation accuracy (blockedBy: #707)
- #678 Этап 7 — Test / CI robustness (blockedBy: #677)
- #679 Этап 8 — Performance, post-blocker, не гейт релиза (blockedBy: #678)
- #695 Investigate row-overlap lost-update in UPDATE-cascade pipeline (blockedBy: none — standalone, unscheduled, discovered mid-campaign during task 3d)

### recently completed
- #705 5d. Fix dead audit.retention_days config knob
- #704 5c. Expand data-protection.md §2 erasure remnants doc
- #703 5b. Fix 07-operations.md backup docs (stop-and-copy, not live)
- #702 5a. Audit-coverage doc fix (AuditSink bridge is P1/deferred)
- #701 4f. parse_json/to_json functions
- #700 4e. Fix arrays/sort cross-type compare() + sort_desc
- #699 4d. uuid_v4() (+ optional random/random_bytes) as pure:false
- #698 4c. datetime/format(ts, pattern) + parse(s, pattern) via chrono
- #697 4b. Wire percentile/string_agg params + honor/reject distinct
- #696 4a. Null-functions: coalesce/if_null/nullif/is_null

(Full earlier history — #680-#694, the Этап 1-3 correctness/deadlock/DDL-rejection wave — in the prior checkpoint, unchanged.)

## Decisions

- **Peak-hours fallback strategy (NEW this checkpoint)**: when `/crush` hits the `zai` peak-hours gate a second time in the same session, the user chose local-`sh`-agent fallback over waiting or stopping — this is now the THIRD distinct resolution the user has picked for the same recurring interruption class across this session (stop-entirely; wait-and-resume-same-session; local-agent-fallback), confirming there is no standing default — each occurrence needs to be asked about fresh, per the crush skill's own policy (never self-add `--allow-peak-hours`, never assume a prior resolution carries forward).
- **Timer over manual polling**: user asked for a scheduled reminder (`CronCreate` one-shot) rather than the agent manually sleeping/polling until 12:00 — consistent with this project's CLAUDE.md ban on manual sleep-polling for backgrounded work.
- (All decisions from the prior checkpoint — self-ref-Cascade DDL rejection, generic DistinctWrapper, hand-rolled uuid_v4, etc. — remain unchanged, see `2026-07-20-0230.md`.)

## Open questions

- Whether the local `sh` agent's 5e implementation will be usable as-is or need rework once reviewed — unknown, it's still running.
- Same as prior checkpoint: Этап 4's P1 items (calendar/array-set-ops/object-path/string-case/min_by-max_by) — decompose now or defer past Этапы 5-8 — still not decided.
- 5f (secrets) investigation not yet started — next after 5e closes, flagged by the user as wanting careful treatment.

## Repo state

```
 M .github/workflows/supply-chain.yml
?? docs/checkpoints/2026-07-17-1600.md
?? docs/checkpoints/2026-07-19-1015.md
?? docs/checkpoints/2026-07-20-0230.md
```

```
97e10067 docs(prompts): brief for 5e -- add SBOM artifact to supply-chain CI workflow
e71f64a4 fix(server): implement audit.retention_days sweep (was a dead config knob)
98628e23 docs(prompts): brief for 5d -- implement audit.retention_days sweep
5e2855de docs: expand data-protection.md SS2 with index/replica erasure-remnant caveats
71de573b docs(prompts): brief for 5c -- expand data-protection.md SS2 erasure remnants
```

## Active timers

- `459f9e60` — recurring, `7,22,37,52 * * * *`, babysit heartbeat (TaskList monitor, general-purpose, session-only).
- `fe2a1258` — one-shot, `2 12 20 7 *` (12:02 local, 2026-07-20), fires once then self-deletes: reminder to verify/commit the local-agent 5e work (if not already done) and switch back to `/crush` for task 5f onward once zai's peak-hours window has closed.
