# Checkpoint — 2026-08-12 17:30

## Session summary

Long continuation of the standing `/babygoal` Stop-hook session: "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу". This checkpoint follows `2026-08-12-1420.md` (which covered the #1096→#1099 correctness/perf arc) and documents the subsequent, very long arc since then: closing `#1099` (PERF, O(N²) fix + real before/after bench numbers) and `#1102` (HIGH, regular-index read-order race — 2 fix rounds, round 1's own `@oh` review caught a genuine write-barrier-predicate regression the fold-into-packed-word approach introduced, round 2 fixed it with a `BARRIER_BITS` mask), both dispatched in parallel via git worktrees per an explicit user instruction, both independently re-verified end-to-end (diff read, gate re-run personally, mutation-tested) rather than trusting either `/crush` session's own claims — this repeatedly paid off: caught a bench-scope typo, a false "gate passed" claim that only ran a filtered test subset, and the write-barrier regression.

Then investigated and closed `#1094` (the OS-thread watchdog for stuck batch ops, deferred since `#1085` after 3 prior in-process attempts all overflowed the stack) via a genuine three-probe investigation (recorded directly in `execution_deadline.rs`'s module doc): measured baseline stack headroom (ample, 2 MiB default, 99.8%+ free), then a leaf-level probe through one representative DDL test (no crash), then a full-suite 200 KiB threshold-trip run across all 2730 `shamir-db`+`shamir-engine` tests (never fired) — conclusively ruling out "thin stack budget" as the cause and confirming the prior overflows were from the WRAPPER'S OWN size, not call depth. This premised a real OS-thread-based watchdog implementation, delivered via 2 `/crush` rounds (round 1 had a sound design but shipped zero behavioral tests plus an out-of-scope `pub(crate)` sweep across every sibling test module, reverted; round 2 added the 4 missing tests, all independently re-verified and mutation-tested by the orchestrator) and closed with a personally-re-run full regression check (2718/2736 passed, 18 pre-existing unrelated WASM-SDK-path failures, zero stack overflow).

**User explicitly interrupted an attempted `@oh` review of `#1094`** with "не нужно запускать ревью пока есть невыполненные задачи" (don't run review while unfinished tasks remain) — corrected course: `#1094`'s adversarial review is now deliberately DEFERRED to the very end, per the `/babygoal`'s own original instruction ("после завершения всей работы... запусти ревью"), not per-task as this session had drifted into doing. `#1094` was marked completed based on the orchestrator's own independent verification (gate, mutation test, full regression) without a separate `@oh` pass — that pass will happen in the final batch review.

**User then gave a new standing instruction**: "используй wrktrees & /crush, если задачи можно решать параллельно" (use worktrees + /crush for parallelizable tasks) — applied immediately by launching `#1100` (in the main repo directly) and `#1104` (in a NEW isolated worktree, `.claude/worktrees/p1104-update-tx-bytes-tests`) in parallel, after confirming zero file overlap (explicitly reasoned that `#1101` — also pending — was EXCLUDED from this parallel batch because it very likely touches the same `pre_commit.rs` region `#1100`'s fix does, and `#1095` was excluded because its own task description explicitly demands a careful, non-rushed dedicated investigation on the highest-blast-radius code in the workspace). Both `#1100` and `#1104` are still running as of this checkpoint.

**Mid-session side task (not `/babygoal`-related)**: user asked whether `sccache` was enabled; investigation found it WAS already correctly configured globally (`~/.cargo/config.toml`'s `rustc-wrapper = "sccache"`) but showed a suspicious 0% hit rate reading — a controlled experiment (rebuild same crate, same source path, fresh target-dir) proved caching DOES work (50-88% hit rates achieved live), concluding the earlier 0% reading was measurement artifact (accumulated stats dominated by legitimately-cold first compiles and by this session's own mutation-testing revert/restore cycles, which ARE legitimate cache misses since source content genuinely differs at each step) — no config change was needed for that. User then asked to bump the cache size to 500GB (was 30GiB, already full/evicting) — done via `setx SCCACHE_CACHE_SIZE 500G` (persistent) + server restart (picks up the new limit immediately); confirmed via `sccache --show-stats` showing `Max cache size: 500 GiB`.

## Active goal

Standing `/babygoal` Stop-hook (verbatim, unchanged across the whole session): "реализуй ВСЕ задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу"

Standing correction from this segment: **do NOT run per-task `@oh` reviews while any task remains unfinished — batch the review to the very end**, per the user's explicit interruption. Standing addition: **use git-worktree isolation + `/crush` for any genuinely parallelizable tasks**, per the user's explicit instruction this segment.

## TaskList

### in_progress
- #1100 HIGH: DELETE/UPDATE planned from a stale snapshot value never removes the record's CURRENT posting — permanent dangling posting. `/crush` running directly in the main repo (session `p1100-stale-delete`, `--timeout 60m`). Brief committed at `docs/dev-artifacts/prompts/release-blockers/90-p1100-stale-snapshot-delete-posting.md`. As of this checkpoint, uncommitted local diff already visible (`pre_commit.rs`, `tx/tests/mod.rs`, new `p1100_stale_snapshot_delete_posting.rs`) — genuine progress, not yet verified by the orchestrator.
- #1104 MEDIUM: `update_tx_bytes`'s `is_record_touched` closures (`#1099`) have zero test coverage. `/crush` running in isolated worktree `.claude/worktrees/p1104-update-tx-bytes-tests` (branch `wt/1104-update-tx-bytes-tests`, session `p1104-update-tx-bytes-tests`, `--timeout 60m`). Brief committed at `docs/dev-artifacts/prompts/release-blockers/91-p1104-update-tx-bytes-touched-coverage.md`, explicitly scoped test-only (no production code changes).

### pending
- #982 crush-fallback state (persistent sentinel — do not complete, structurally unsatisfiable)
- #1049 P1-6 follow-up (non-blocking, waits on a real recurrence — explicitly "do not start until new data appears")
- #1095 PERF: `SegmentSet::inner` off `std::sync::Mutex` — highest-blast-radius code in the workspace (WAL durability); prior investigation found contention but could NOT cleanly attribute it to this lock vs. the group-commit coordination layer above it; needs new bench instrumentation to isolate before any rewrite — deliberately excluded from this segment's parallel batch, needs its own careful cycle
- #1101 HIGH: Step 2's "released -> continue" shortcut skips the durable check for a same-tx net-release — deliberately excluded from this segment's parallel batch (very likely touches the same `pre_commit.rs` region `#1100`'s fix does; sequencing after `#1100` completes is safer)
- #1103 PERF (low priority): ReaderDrainGate phase 2 — loom infra for shamir-index + evaluate ordering relaxation against real contention data

### recently completed
- #1094 OS-thread-based live watchdog for stuck batch ops — 3-probe stack-headroom investigation + 2 `/crush` implementation rounds, independently re-verified (gate, mutation test, full 2736-test regression) by the orchestrator; `@oh` review DEFERRED to the final batch review per user instruction
- #1102 regular-index read-order race — 2 fix rounds (round 1's own `@oh` review caught a real write-barrier-predicate regression the fold introduced; round 2 fixed it with `BARRIER_BITS`)
- #1099 `touched_records_in_tx` O(N)→O(1) probe — real before/after bench numbers (7.82×→4.04× scaling for 4× rows), `@oh` review found a MEDIUM test-coverage gap (now tracked as `#1104`) plus doc nits (fixed)
- #1096/#1097/#1098 — the earlier long correctness-fix arc (see `2026-08-12-1420.md` for full detail)

## Decisions

- **Deferred `#1094`'s adversarial `@oh` review to the end of the session**, correcting course after the user's explicit interruption — the original `/babygoal` instruction always said "after ALL work is complete," and this segment had drifted into running per-task reviews immediately after each fix, which the user does not want repeated for the remaining tasks.
- **Adopted worktree+`/crush` parallelization for `#1100`+`#1104`, but explicitly excluded `#1101` and `#1095` from the same batch** — `#1101` for probable file overlap with `#1100` in `pre_commit.rs` (unconfirmed but likely enough to not risk a race), `#1095` because its own task description demands a dedicated, unrushed investigation on the highest-blast-radius code in the workspace, not a same-session parallel dispatch.
- **For `#1100`, personally investigated the fix-shape question before writing the brief** rather than delegating that investigation — traced `delete_tx`'s exact stale-snapshot-read call site, found the existing `rederive_base_index_ops_post_stage`/`read_pre_tx_bytes` precedent in `pre_commit.rs` (established technique: read current durable state at commit time, bypassing the tx's own snapshot) but confirmed its existing gate (`mgr.generation() == stage_gen`) is for DEFINITION changes, not VALUE changes, so it does NOT already cover `#1100`'s bug — the brief asks the implementer to choose between reusing that technique under a NEW trigger condition vs. a narrower fix at `delete_tx`'s own stage-time read, with explicit reasoning required either way.
- **sccache: no config fix needed, only a cache-size bump** — investigated the reported 0% hit rate with controlled experiments before touching any config, concluded it was a measurement artifact (not a real caching failure), and only acted on the user's SEPARATE, explicit ask to raise the cache size limit (30GiB → 500GiB, via `setx` + server restart).

## Open questions

None blocking. Both in-flight `/crush` dispatches (`#1100`, `#1104`) need the same zero-trust verification (diff review, gate re-run, mutation testing) this session has applied to every prior fix before being considered done. `#1101` should follow `#1100` sequentially once the latter's fix shape is confirmed and merged (to avoid stepping on the same `pre_commit.rs` region blind). `#1095`/`#1103`/`#1049`/`#982` remain deliberately deferred per their own task descriptions. The full-session `@oh` batch review (per the user's correction) has not yet run — it should be the LAST step, only once every task above reaches a resolved state (completed or explicitly re-deferred with the user's acknowledgment).

## Repo state

```
 M crates/shamir-engine/src/tx/pre_commit.rs
 M crates/shamir-engine/src/tx/tests/mod.rs
?? crates/shamir-engine/src/tx/tests/p1100_stale_snapshot_delete_posting.rs
?? devrust.cargo-target-check/
?? docs/checkpoints/2026-08-12-1420.md
```
(The uncommitted diff above is `#1100`'s `/crush` session actively writing its reproduction test + fix in the main repo working tree — not yet reviewed or committed by the orchestrator. `docs/checkpoints/2026-08-12-1420.md` is the PRIOR checkpoint, untracked because checkpoints are intentionally left for the user to `git add` themselves. `#1104`'s worktree has its own separate uncommitted state, not shown here — see `git -C .claude/worktrees/p1104-update-tx-bytes-tests status --short` separately.)

```
90f79fc6 docs(prompts): brief for #1104 -- close update_tx_bytes's is_record_touched test-coverage gap
ef7d570e docs(prompts): brief for #1100 -- DELETE/UPDATE planned from a stale snapshot never removes the record's current posting
cfe02ab0 docs: CHANGELOG entry for #1094's closure (OS-thread watchdog for stuck batch ops)
d5d77709 feat(engine): #1094 -- OS-thread-based live watchdog for stuck batch ops
2452f7f3 docs(prompts): brief for #1094 round 2 -- add missing core watchdog tests + complete the regression check
```

Active worktree for this segment's in-flight dispatch:
```
D:/dev/rust/shamir-db/.claude/worktrees/p1104-update-tx-bytes-tests  ef7d570e [wt/1104-update-tx-bytes-tests]
```
(Several pre-existing, unrelated worktrees from other tools/sessions — opencode, zenflow, ab-post/ab-pre, agent-a80117412c1bff614, agent-a8545ce6ef4fb805b, shamir-db-verify-f28 — remain present and untouched.)
