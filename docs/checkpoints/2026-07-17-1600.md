# Checkpoint — 2026-07-17 16:00

## Session summary

This session started by completing a second wave of bugfix/gap-closure tasks (#661-670) discovered by an earlier `@fh` review of the OQL epic work: #661 (nested-tx atomicity for Batch/ForEach), #663 (field-based-comparison guard gaps), #665 (CondCache test coverage), #666 (ForEach DoS gates — `max_iterations` clamp + `max_execution_time_secs` enforcement), #670 (interactive-tx filter-depth guard). Each was implemented via a committed prompt-first brief + an `@sh` agent, independently zero-trust-verified (fmt/clippy/full test re-runs) before committing.

A second `@fh` review of the #666 fix found two HIGH-severity cancel-safety defects in its original `tokio::time::timeout`-wrapped `execute_batch`: it could race `commit_tx` (explicitly documented as non-cancel-safe) and could permanently leak pessimistic locks on cancellation (no `Drop` impl on `TxContext`). After the user chose to fix properly, an `@fx` agent replaced the preemptive timeout with a cooperative `ExecutionDeadline` mechanism (checkpoints at existing stage/iteration boundaries, never racing `commit_tx`), landed and independently verified.

The user then asked to commit, push, and fix CI ("наладь CI"). This surfaced THREE real, independent problems on GitHub Actions, each investigated and fixed in its own commit: (1) a cross-platform floating-point tolerance flake in an SQ8 SIMD test (fixed with a combined rel+abs tolerance), (2) a genuine regression where a `$fn` computed-value-on-write with a same-document `$ref` argument broke (root-caused to an earlier #641 commit; fixed by deferring `$ref`-bearing `$fn` markers to the table layer's existing correct resolver), and (3) — the most serious — a REAL, reproducible whole-runtime deadlock in `MvccStore`'s `cells` map (`entry_async`/`retain_async` lock-handoff semantics colliding with synchronous `read_sync` hot-path readers under hot-key contention), root-caused via an isolated repro crate and fixed by switching to `entry_sync`/`retain_sync` (matching an already-established sync convention elsewhere on the same map). CI is now fully green (all 11 jobs) after that third fix landed. A follow-up task (#671) was filed for 3 other sites with the same structural sync/async-mixing pattern, not yet fixed.

There was a rocky stretch mid-session where delegated `@sh`/`@fx` agents kept backgrounding their own test/build commands and then ending their turn before the command finished, forcing repeated manual `SendMessage` resumes — the user flagged this had already been "fixed" once before and I'd reintroduced it. The corrective instruction ("run test/build commands in the FOREGROUND, never background your own commands") is now being included explicitly in every delegated agent prompt.

Most recently, the user asked for a broad parallel research sweep: 10 read-only `@fxx` (max-effort) agents were launched in the background, each investigating one topic across the WHOLE codebase (security, permissions/ACL, compliance/data-governance, logical-correctness bugs, incomplete features, concurrency/deadlock hazards, performance, test-coverage/CI-robustness, documentation accuracy, and v0.10 release-readiness/funclib breadth), each writing its own report to `docs/dev-artifacts/research/2026-07-17-release-audit/`. All 10 completed and their reports were committed (`c69207c4`).

**The most important open thread right now**: two of the ten reports (security posture, and permissions/ACL correctness) INDEPENDENTLY found the SAME critical, confirmed access-control bypass: `BatchOp::Batch`/`BatchOp::ForEach` return `None` from `required_access()`, so the per-op authorization pre-check loops in `ShamirDb::execute_as` (`db_execute.rs`) and `tx_execute_as` (`db_tx.rs`) are flat, one-level walks that NEVER check tables touched by a `Batch`/`ForEach`'s nested body — an authenticated non-superuser can read/write a forbidden table just by wrapping the op in a top-level `ForEach`/`Batch`. I independently confirmed this myself by reading `batch_op.rs`, `db_execute.rs`, and `db_tx.rs` directly, and confirmed the WASM host's `FacadeDbGateway::execute` also goes through the same vulnerable `execute_as` path (one fix covers both). I wrote a precise brief (`docs/dev-artifacts/prompts/security-fix/01-foreach-batch-acl-bypass.md`, committed `0f60fc86`) specifying the exact fix: mirror the existing `distinct_repos`/`collect_repos` recursive pattern (from the earlier #660 fix) with a new `collect_required_access` function in `shamir-query-types`, swapping both authorization loops to use it. An `@sh` agent (agentId `aef8dba68385b4039`) is CURRENTLY RUNNING IN THE BACKGROUND implementing this fix — `git status --short` at checkpoint time shows in-progress, UNCOMMITTED, NOT-YET-VERIFIED changes to `db_execute.rs`, `db_tx.rs`, `enforcement_dml_e2e.rs`, `query_runner.rs`'s mod.rs, `batch/mod.rs`, and `query_entry.rs`.

Just before this checkpoint, the user asked to commit "all docs" — I correctly scoped that to ONLY the 10 research report files (committed as `c69207c4`), explicitly leaving the in-flight, unverified ACL-fix code changes uncommitted, since they have not yet been zero-trust-verified by me.

## Active goal

None currently set via an explicit `/goal` Stop hook in this immediate context (the earlier session-long goal about the OQL wave was effectively satisfied and superseded by this new research/security-fix thread). No standing Stop hook condition is known to be active right now — if one still fires, it would be from the earlier "давай реализуем все задачи, используй агентов @sh..." goal, which should be considered satisfied for its original scope.

## TaskList

### pending
- #671 Audit other scc maps for mixed sync/async lock-acquisition deadlock hazard (found during the MvccStore deadlock investigation; 3 sites flagged: `repo_tx_gate.rs:415`, `mvcc_locks.rs:54`, `layered_interner.rs:61` — plus TWO NEW sites the concurrency-hazard research agent found today: the vector index's `hnsw_adapter.rs`/`vector_backend.rs` deleted-tombstone maps, and `per_table_mvcc`/`token_names` in shamir-engine — none of these are fixed yet)
- #672 RO-audit: Security posture (whole project) — this task's underlying agent already completed and its report is committed (`01-security-posture.md`); the task itself was never marked completed (a TaskList hygiene loose end, not a real blocker)

Note: only these 2 tasks exist in the TaskList even though 10 research agents were launched — the other 8 were launched without a matching TaskCreate (the user rejected an early batch of TaskCreate calls mid-decomposition, so only the first 2 topics got tracked before I stopped creating tasks for the rest and just launched agents directly).

### recently completed (not tracked as formal tasks, but done and committed)
- #661-670 wave (nested-tx atomicity, field-based-comparison guards, CondCache tests, ForEach DoS gates + cooperative ExecutionDeadline redesign, interactive-tx filter-depth) — all committed.
- Three CI fixes: sq8 tolerance, `$fn`+`$ref` write-value regression, MvccStore deadlock — all committed, CI fully green.
- 10-topic parallel research audit — all reports committed (`c69207c4`).

## Decisions

- User chose to fully fix (not just document) the #666 cancel-safety defects — led to the cooperative `ExecutionDeadline` redesign.
- User explicitly corrected the agent-backgrounding-then-dying anti-pattern a second time; the fix (never background test/build commands inside a delegated agent) is now mandatory boilerplate in every Agent() prompt this session going forward.
- For the CI-blocking MvccStore timeout, user chose "investigate now via @fx" over reverting/documenting — led to finding and fixing a genuine production deadlock, not just a CI-config tuning issue.
- For "commit all docs", scoped strictly to the 10 committed research reports, deliberately EXCLUDING the in-flight, unverified ACL-fix code — matches this session's standing zero-trust discipline (never commit unverified agent output).
- Chose to independently verify the critical ACL-bypass finding myself (reading `batch_op.rs`/`db_execute.rs`/`db_tx.rs` directly) before trusting the two research agents' reports enough to act on them — confirmed real, then wrote a precise fix brief modeled directly on the proven `#660` `distinct_repos`/`collect_repos` pattern.

## Open questions

- None requiring user input right now — the ACL-bypass fix's design is fully decided (mirror #660's pattern) and is currently being implemented by an in-flight agent. The only pending action is operational: wait for agentId `aef8dba68385b4039`'s completion notification, then apply the same zero-trust verification (read the diff, re-run `./scripts/test.sh -p shamir-query-types -p shamir-db --full` + `-p shamir-engine --full`, fmt, workspace-wide clippy) before committing.
- Task #671 (the other sync/async deadlock-hazard sites, now expanded with 2 more candidates from today's concurrency research report) has no committed fix yet and no brief written — a natural next step after the ACL fix lands, but not yet started.
- The remaining 9 research reports (compliance, logical-bugs, incomplete-features, concurrency, performance, test-coverage, documentation, release-readiness) contain many findings below "drop everything and fix immediately" severity — no explicit user decision yet on which (if any) to act on next beyond the critical ACL bypass already in flight.

## Repo state

```
 M crates/shamir-db/src/shamir_db/execute/db_execute.rs
 M crates/shamir-db/src/shamir_db/execute/db_tx.rs
 M crates/shamir-db/tests/enforcement_dml_e2e.rs
 M crates/shamir-engine/src/query/batch/mod.rs
 M crates/shamir-query-types/src/batch/mod.rs
 M crates/shamir-query-types/src/batch/query_entry.rs
```
(all of the above are the IN-FLIGHT, uncommitted, NOT-YET-VERIFIED ACL-bypass fix from agentId `aef8dba68385b4039` — do not commit until independently verified)

```
c69207c4 docs(research): 2026-07-17 release-readiness audit — 10 parallel reports
0f60fc86 docs(prompts): brief for CRITICAL — ForEach/Batch bypass per-table ACL
7a4abf62 fix(tx): real deadlock in MvccStore's cells map — entry_async/retain_async → sync
581ffb4a docs(prompts): brief for CI investigation — overlay_ordering macos timeout
6dea4e4f fix(engine): CI regression — \$fn+\$ref computed-value-on-write
```
