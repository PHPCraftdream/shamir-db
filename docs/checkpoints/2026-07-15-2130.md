# Checkpoint — 2026-07-15 21:30

## Session summary

Executing a `/babygoal`-style pipeline implementing 4 OQL (Object Query Language) roadmap plans (`docs/dev-artifacts/roadmap/oql/01-04`), each as one epic task, using prompt-first `@sh` agent delegation with zero-trust orchestrator verification (independent fmt/clippy/test re-runs before every commit) and a commit between every task. Epics 01 (Sequencing explicitness), 02 ($cond/$expr value evaluation), and 03 (Conditional execution — `when`/switch) are **fully completed**, each through all their phases (ADR where required → engine → builders → tests → e2e → benchmarks → docs → epic closure), with real bugs found along the way spun out as separate tracked tasks rather than silently absorbed: #634 (unrelated TS principal64 bug), #641 ($cond doesn't compose into write SET-values), #643 (perf: $cond re-compiles filter per-row, 29-190x overhead), #651 (CRITICAL: `when` field-based comparisons always evaluate to a fixed result due to a scratch interner in `resolve_skip` — this significantly limits Epic 03's practical value and is honestly documented in the guide docs with a prominent warning).

Currently mid-flight on **Epic 04 (Loops/for-each)**: ADR (#652) is committed (`e267406b`) and Phase B (engine `BatchOp::ForEach`, task #653) is **in-progress with real, uncommitted, working code** — NOT yet committed. This phase hit a serious process failure: two sub-agent continuations of the same lineage ended up running truly concurrently (confirmed by the user, then confirmed by observing a file "grow beyond what I wrote" mid-edit), both editing the same files at once. Both were stopped via `TaskStop`. Rather than discard the resulting diff, a `@fl` agent was dispatched read-only to diagnose it and concluded the implementation is **structurally sound and near-complete** — 4/12 new `for_each` tests were passing, 8 were failing from exactly two mechanical, precisely-diagnosed bugs (not fundamental corruption). I applied the fixes directly myself (not via a fresh agent, since the diagnosis was precise):
1. `ForEachOp.batch` field needed `#[serde(rename = "for_each")]` so the wire format doesn't collide with `SubBatchOp`'s `"batch"` key (both are `BatchRequest`-wrapping structs).
2. `batch_op.rs`'s dispatcher needed to check `has("for_each")` before `has("batch")` (defense in depth after fix 1).
3. `resolve_filter_query` (`crates/shamir-engine/src/query/filter/resolve.rs`) was missing a `FilterValue::Array` arm entirely — added it (recursively resolves each element, collects into `QueryValue::List`), which also required deleting the now-unreachable trailing `_ => None` (all 13 `FilterValue` variants are now explicitly matched — clippy correctly flagged this as dead code).
4. Two test call sites used bare function names (`"slice"`, `"now"`) instead of the funclib's folder-qualified names (`"arrays/slice"`, `"datetime/now"` — confirmed via `crates/shamir-funclib/src/lib.rs`'s `reg.in_folder(...)` registration pattern) — fixed both.

After these fixes: all 12/12 `for_each`-filtered tests pass, `cargo fmt -p shamir-query-types -p shamir-engine -- --check` is clean, and a **full workspace-wide** `cargo clippy --workspace --all-targets -- -D warnings` just came back clean (exit 0, confirmed via the just-received task-notification) — this was deliberately run workspace-wide because every single prior phase in this session broke *some other* crate by growing a struct/enum without updating all call sites elsewhere (a recurring, now well-drilled lesson).

**Not yet done for this turn**: the full `./scripts/test.sh -p shamir-query-types -p shamir-engine --full` regression run (only the `for_each`-filtered subset has been re-verified), and the commit itself. User also flagged mid-session two design considerations for **future** loop work (both explicitly deferred, NOT in scope for the current Epic 04 phase): (a) `ForEach.over` must be able to source from `$query`, `$fn`, or literal values — already true by construction since `over: FilterValue` covers all three, confirmed structurally; (b) `over` (including a function call) must resolve exactly ONCE before the loop starts, not per-iteration — already the implemented behavior (confirmed by a dedicated test, `for_each_over_resolves_exactly_once_before_loop`, which passed). A further user idea — supporting while-style loops where the condition is re-evaluated every iteration, toggled via a flag on the condition itself rather than a separate loop primitive — was explicitly deferred to a new backlog task, **#659**, not started.

## Active goal

No `/goal` Stop-hook condition is separately active in this checkpoint's view, but the session has been operating under a standing user directive (re-affirmed repeatedly via Stop-hook feedback): implement all 4 OQL roadmap plans, one epic task per plan, sub-tasks created before each epic starts, implementation via `@sh` agents, commit between tasks.

## TaskList

### in_progress
- #627 OQL Epic 04: Loops (data-dependent for-each/repeat)
- #653 Epic04/B: движок — ForEachOp, планировщик, K-кратный executor

### pending
- #634 BUG: TS client chown/addGroupMember with resolved principal64 fails (BigInt wire)
- #641 GAP: $cond/FilterValue не компонуется в write SET-значениях (QueryValue vs FilterValue split)
- #643 PERF: $cond эвалюация вызывает compile_filter на каждую строку (~29-190x оверхед)
- #651 CRITICAL BUG: when-фильтры на основе полей всегда false (фиктивный interner в resolve_skip)
- #654 Epic04/C: билдеры Rust+TS — for_each (blockedBy: #653)
- #655 Epic04/D: юнит-тесты loops (blockedBy: #654)
- #656 Epic04/E: e2e Rust+TS loops (blockedBy: #655)
- #657 Epic04/F: бенчмарки loops (blockedBy: #656)
- #658 Epic04/G: доки loops + закрытие эпика + финальная сводка всех 4 планов (blockedBy: #657)
- #659 FUTURE: while-циклы в OQL — пересчёт condition на каждом шаге, флаг в самом condition

### recently completed
- #652 Epic04/A: ADR — ForEachOp форма, представление результатов, лимиты, семантика ошибки
- #650 Epic03/G: доки conditional execution + закрытие эпика
- #649 Epic03/F: бенчмарки conditional execution
- #648 Epic03/E: e2e Rust+TS conditional execution
- #647 Epic03/D: юнит-тесты conditional execution
- #646 Epic03/C: билдеры Rust+TS — when/switch-хелперы
- #645 Epic03/B: движок — QueryEntry.when, planner, executor skip-семантика
- #644 Epic03/A: ADR — форма примитива when/if, каскадный skip, авторизация, skipped-статус
- #642 BUG: BatchPlanner не рекурсирует в $cond/$expr/FnCall при извлечении зависимостей (closed as part of #645)
- #640 Epic02/F: доки $cond + закрытие эпика

## Decisions

- Two concurrent sub-agent continuations editing the same files was a process error (never launch overlapping resumes on the same lineage without confirming the prior one finished) — both stopped via `TaskStop`, diagnosed read-only via `@fl` rather than blindly discarded, since the underlying implementation was salvageable and mostly correct.
- Chose to fix the two diagnosed bugs directly myself (not via a fresh agent) since the `@fl` diagnosis was file:line-precise and mechanical — no need for another delegation round.
- `ForEach.over: FilterValue` already structurally supports `$query`/`$fn`/literal sources (confirmed, not redesigned) — user's concern was addressed by verification, not new code.
- `over` resolves exactly once before the loop (not per-iteration) — already correctly implemented and tested; user's requirement was already met.
- While-style loops (condition re-evaluated per-step) explicitly deferred to new task #659 — conflicts with static batch DAG planning per the ADR, not undertaken mid-Phase-B.
- Task #651 (field-based `when` always false) is treated as CRITICAL but was NOT fixed inline in Epic 03 — documented prominently instead, per established pattern from #641 (Epic02) of spinning off found-but-not-fixed issues as separate tasks rather than blocking epic closure.

## Open questions

- None pending user input at this exact moment — the next action (commit the Phase B fixes, then continue to the full regression suite and Phase C) is unambiguous per the standing directive. If resumed, the next step is: run `./scripts/test.sh -p shamir-query-types -p shamir-engine --full`, verify clean, commit Epic04/Phase B, then proceed to Phase C (#654, Rust+TS builders for `for_each`).

## Repo state

```
 M crates/shamir-engine/src/query/auth/session.rs
 M crates/shamir-engine/src/query/batch/query_runner.rs
 M crates/shamir-engine/src/query/batch/tests/mod.rs
 M crates/shamir-engine/src/query/batch/tests/planner_tests.rs
 M crates/shamir-engine/src/query/filter/mod.rs
 M crates/shamir-engine/src/query/filter/resolve.rs
 M crates/shamir-query-builder/src/batch/tests/batch_tests.rs
 M crates/shamir-query-types/src/batch/batch_error.rs
 M crates/shamir-query-types/src/batch/batch_limits.rs
 M crates/shamir-query-types/src/batch/batch_op.rs
 M crates/shamir-query-types/src/batch/mod.rs
 M crates/shamir-query-types/src/batch/planner.rs
 M crates/shamir-query-types/src/batch/query_entry.rs
 M crates/shamir-query-types/src/batch/tests/mod.rs
 M crates/shamir-server/src/db_handler/handler.rs
?? crates/shamir-engine/src/query/batch/tests/for_each_tests.rs
?? crates/shamir-query-types/src/batch/for_each_op.rs
?? crates/shamir-query-types/src/batch/tests/for_each_op_tests.rs
```

```
2c35844d docs(prompts): brief for OQL Epic04/Phase B — ForEachOp engine implementation
e267406b docs(design): OQL Epic04/A — loops/for-each ADR (#652)
9b195d8e docs(prompts): brief for OQL Epic04/Phase A — loops/for-each ADR
6ab3a573 docs(guide): OQL Epic03/G — when/switch usage + prominent #651 warning (#650)
5f6a2c6b docs(prompts): brief for OQL Epic03/Phase G — when docs + honest closure (critical #651 warning)
```

All uncommitted work above compiles clean workspace-wide (`cargo clippy --workspace --all-targets -- -D warnings` exit 0) and passes all 12 `for_each`-filtered tests, but has NOT yet been through the full `--full` regression suite or been committed.
