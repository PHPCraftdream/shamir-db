בְּשֵׁם יהוה הָרַחֲמָן וְהַחַנּוּן

# Checkpoint — 2026-06-23 22:44 [ts-client-interning-phase1-in-flight]

## Session summary

Эпик **декларативных схема-валидаторов** (Rust) ПОЛНОСТЬЮ завершён и закоммичен ранее в
сессии: Phase 0 (валидаторный план RecordValidator/RecordFields) + A (schema-вертикаль +
раскрытие застабленного server-execution) + B (scalar/format/cross-field + interner-persist
фикс) + C (foreign_key + unique + concurrency-no-deadlock). 8 коммитов; на origin — по `7506cc8`
(Phase 0/A/B), непушены 4 коммита Phase C (`f02c5f1`/`87f31a3`/`3817357`/`046d5bb`). Каждую
фазу верифицировал СВОИМИ руками (durable reopen + батч + gate), не по envelope агентов.

ТЕКУЩИЙ ФОКУС переключён на **TS-клиент**. Юзер: «в первую очередь нужно построить полный
query builder на TS, чтобы он тачил и подтягивал значения интернера» — т.е. КЛИЕНТСКОЕ
интернирование (на провод идут id полей записей, не строки). Я заземлился: интернер-инфра на
TS уже есть (`field-map.ts` FieldMap/InternerCacheRegistry, `client.ts` touchFields/
mergeInternerDelta/interner_epochs), НО `execute()` НЕ пакует имена записей в id — шлёт строки,
интернирует сервер. Эталон — Rust `execute_with_touch` (crates/shamir-client/src/
interner_cache_ops.rs): collect имена из INSERT.values/SET.key+value/UPDATE.set (рекурсия) →
touch → литеральные INSERT-записи в id-keyed msgpack `records_idmsgpack` ($fn остаются строками)
→ result_encoding=Id → deintern ответа. Юзер зафиксировал 2 развилки: (1) клиент пакует → id на
проводе (по дизайну doc 04); (2) интернируются ТОЛЬКО имена/пути полей записей (не сущности).

Написал ПЛАН-ДОК `docs/dev-artifacts/design/ts-client-interning-and-e2e.md` — 7 фаз: Ф1 interner-packing, Ф2
полнота билдера, Ф3 общий e2e-харнесс (эфемерный порт — сейчас PORT=13760 фиксирован), Ф4 DDL-
lifecycle e2e, Ф5 валидаторы e2e, Ф6 права доступа e2e, Ф7 данные e2e.

В ПОЛЁТЕ: **o46l-агент `a467025b` реализует Ф1 (#208)** — encodeRecordIdMsgpack/qvHasFnMarker/
executeWithTouch/deinternResponse, паритет с Rust. Сейчас правит protocol.ts. Когда вернётся —
мой zero-trust: сам прогоню e2e round-trip (новые имена → id на проводе → имена на чтении) +
проверю, что формат id-keyed msgpack совпал с серверным decode (КРИТИЧНО — иначе тихая порча).

Стратегия: @o46l, по агенту на фазу, ПОСЛЕДОВАТЕЛЬНО (TS-гейт поднимает сервер на порту →
параллель конфликтует). Per-фаза: tsc build + vitest зелёным → мой zero-trust → атомарный коммит.

Кронов НЕТ (babysit не вооружал — веду активно). Anthropic-500 outage днём прошёл; crush больше
не использую — юзер велел все таски на @o46l.

## Active goal
None. /goal не установлен. Прогресс по TaskList + активное ведение.

## TaskList
### in_progress
- #208 TS: client-side interner packing (smart-write path, parity с Rust execute_with_touch) — o46l `a467025b`
### pending
- #209 TS e2e: full DDL lifecycle (blockedBy #208)
- #210 TS e2e: declarative schema validators (blockedBy #208)
- #211 TS e2e: права доступа / access-control (blockedBy #208)
- #212 TS e2e: вся работа с данными (blockedBy #208)
- #195 Phase 0 follow-up: function record-navigation by-name (doc 08 item 6) — отдельно, не блокирован
### deleted
- 15 задач Rust-эпика (Phase 0 review-notes + A + B + server-exec + C0-C4) удалены ранее.

## Decisions
- **Клиент пакует → id на проводе** (doc 04); НЕ оставлять server-side string-интернирование.
- **Интернируются только имена/пути полей записей** (write-записи), не сущности, не фильтры —
  паритет с Rust collect_field_names.
- **Эталон = Rust execute_with_touch**; TS encodeRecordIdMsgpack обязан совпасть с серверным
  id-keyed decode (риск тихой порчи — главный).
- **Агенты последовательны** (TS-гейт = сервер на порту); @o46l (директива юзера), не crush.
- **План записан как док** docs/dev-artifacts/design/ts-client-interning-and-e2e.md (7 фаз).

## Open questions
- **Утвердить план?** Юзер просил «опишем поэтапный план» — написан; ждёт «идём / правим».
- **Пуш?** 4 коммита Phase C непушены (origin=7506cc8).
- **autocommit-caveat** FK/unique (пропуск в single-op autocommit) — закрывать ли (Rust-сторона).
- **#195 func-nav** — делать или отложить.

## Repo state
```
 M crates/shamir-client-ts/src/core/protocol.ts   (#208 agent in-flight)
?? docs/dev-artifacts/design/ts-client-interning-and-e2e.md
?? docs/dev-artifacts/checkpoints/*.md
```
(Дерево «грязное» только из-за работающего агента #208 + untracked доки.)
```
046d5bb test(schema): Phase C4 — combined FK+unique e2e + concurrency no-deadlock
3817357 feat(schema): Phase C3 — declarative unique (index-backed, read-your-own-writes)
87f31a3 feat(schema): Phase C2 — declarative foreign_key (forward-only)
f02c5f1 feat(schema): Phase C1 — tx-scoped read-only validator db-handle (ValidatorDb)
7506cc8 docs(checkpoints): declarative schema design + libraries-update session checkpoints
```
(4 коммита ahead of origin/master=7506cc8.)
