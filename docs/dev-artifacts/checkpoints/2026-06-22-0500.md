# Checkpoint — 2026-06-22 05:00 (VersionWindow closed at gate; clean rest)

## Session summary

Long multi-campaign perf session, now at a clean resting point with an empty
TaskList and no crons. Arc of the session: Op #2 incremental drainer cursor
(committed earlier) → post-Op#2 cleanup (offer O(1) depth + abort-aware reseed,
crash_mid_drain test fix) → hidden-O(N) sweep (Stage 0 measured overlay-GC cliff
as theoretical so Stage 1 deleted as gold-plating; Stage 2 migrated subscription
caches DashMap→TreeIndex; Stage 3 banned scc::*::len() via clippy.toml) → the
#128 pagination wire-contract regression found + fixed structurally (top-K +
sorted-index fast paths both dropped pagination; one shared `exec::fast_path_pagination`
helper + a contract test) → a rigorous before/after benchmarking detour the user
pushed for ("вдруг проиграли?"), which revealed the cache get regressed 3×
(DashMap O(1) → TreeIndex O(log N)) while eviction improved 15–475× — net win
because eviction is per-event, but the user then adopted the /opti measure-first
discipline → the VersionWindow unification campaign.

VersionWindow: extract a shared `VersionWindow<K,V>` primitive collapsing the 3
version-primary sliding-window stores (drainer window, commit_write_log,
decode/deliver caches; overlay EXCLUDED as version-secondary). Ran Stage 0
(probe + design, o46m agent) under /opti measure-first. Stage 0's gate fired the
honest verdict: **architectural consolidation, NOT a perf win** — caches stay
tiny (≤30-60) under healthy consumers so O(log N) get ≈ O(1); the only measurable
win (commit_log_len O(N)→O(1)) is worthless telemetry; and the "bug-class
extinction" is already covered by the clippy scc-len ban shipped in the prior
campaign. After /oxx contemplation, the user chose **A — STOP**. The campaign was
closed; its 6 tasks deleted. The real value (design doc + cache-depth proof) is
captured as docs/perf artifacts — a "captured option" extractable in half a day
if a 4th version-window store ever appears. babysit cron cancelled.

The session's meta-arc: learned to MEASURE before building (cache 3× regression
discovery → /opti adoption → trusting the gate that said STOP). Nothing in
flight. No active hypotheses. Awaiting only the user's commit/push call for a
large backlog of unpushed work.

## Active goal

None (`/goal` not set). No babysit cron (cancelled). TaskList empty.

## TaskList

Empty. All VersionWindow tasks (#173-#178) deleted (campaign closed at Stage 0
gate). Earlier campaigns' tasks (Op #2, hidden-O(N), pagination #171/#172) all
completed+deleted in prior turns. Recent deleted count this turn: 6.

## Decisions

- **VersionWindow campaign CLOSED at the Stage 0 gate (chose A — stop)** —
  measured no perf win; DRY value thin (stores share only a ~10% substrate);
  bug-class already killed by the clippy scc-len ban; commit_log_len O(1) win is
  worthless telemetry. Rejected B (build primitive speculatively — YAGNI) and C
  (full migration — unearned cost + SSI/durability risk). The beauty was in the
  non-action driven by measurement.
- **Adopted /opti measure-first as a standing principle** (user directive) —
  every optimization gets baseline→change→re-measure on the SAME bench;
  regression → STOP. This is what fired the VersionWindow gate.
- **Deleted closed tasks** to keep TaskList clean (campaign artifacts live in docs).
- **Did NOT commit/push** — standing rule; awaiting explicit sanction.
- Earlier: dropped 3 orphaned pre-serde_json WIP stashes (confirmed dead vs HEAD).

## Open questions

- **Commit + push the backlog.** 6 commits ahead of origin/master (be0bc1f Op#2
  + 92311d4, b68fa1c, 3eb7601, 0a9571f, 604cc47, a37c950, 0f5de6b, 94a88e4 — the
  rev-list shows 6 ahead; some Op#2 commits may already be pushed). PLUS the
  uncommitted VersionWindow Stage 0 artifacts (design doc, cache_depth_probe,
  cache_struct_tradeoff bench, 2 checkpoints, Cargo.toml + tests/mod.rs edits).
  Awaiting "коммит" / "пуш".
- **The uncommitted Stage 0 artifacts** — keep as a docs commit when sanctioned
  (they're durable knowledge: the VersionWindow design option + the cache-depth
  proof that vindicates Stage 2's get regression as moot).

## Repo state

```
 M crates/shamir-server/Cargo.toml
 M crates/shamir-server/src/subscriptions/tests/mod.rs
?? crates/shamir-server/benches/cache_struct_tradeoff.rs
?? crates/shamir-server/src/subscriptions/tests/cache_depth_probe_tests.rs
?? docs/dev-artifacts/checkpoints/versionwindow-paused-stage0.md
?? docs/dev-artifacts/checkpoints/2026-06-22-0500.md
?? docs/dev-artifacts/perf/versionwindow-stage0.md
```

```
94a88e4 docs(perf): hidden-O(N) sweep checkpoints + roadmap outcomes
0f5de6b chore(clippy): ban scc O(N) len() — hidden-O(N) sweep Stage 3
a37c950 fix(read): sorted-index ORDER BY+LIMIT must emit pagination — #128 sibling
604cc47 fix(read): top-K ORDER BY+LIMIT must emit pagination — #128 regression
0a9571f perf(tx,engine): hidden-O(N) Stage 0 — overlay GC measurement (cliff is theoretical)
```

6 commits ahead of origin/master; nothing pushed. VersionWindow Stage 0
artifacts uncommitted (Stage 0 changed NO production code — probe test + bench +
design doc only).
