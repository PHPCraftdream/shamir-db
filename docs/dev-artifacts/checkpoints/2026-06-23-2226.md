בְּשֵׁם יהוה הָרַחֲמָן וְהַחַנּוּן

# Checkpoint — 2026-06-23 22:26 [declarative-schema-feature-complete]

## Session summary

Реализовали ЦЕЛИКОМ эпик **декларативных схема-валидаторов** для S.H.A.M.I.R. (по
дизайну в `docs/dev-artifacts/design/declarative-schema-validators/`) — третий вид валидаторов,
задаётся данными (массив правил полей), хранится по-таблично в каталоговой записи,
персистентен, переживает reopen. Фича готова сквозно через 4 эпик-фазы + предварительный
рефактор, всё закоммичено:

- **Phase 0** (`5b1955b`, `fdab32a`): рефактор — валидатор стал узкой ролью `RecordValidator`
  + by-name `RecordFields` (интернирование скрыто), миграция native/wasm; `ValidatorCtx`
  сужен (interner приватен, только `field_name`). Фундамент под A/B/C.
- **Phase A** (`44b352c` движок + `097dee2` server-execution): `SchemaValidator`,
  TypeTag/Constraints/FieldRule, builtin-checks, ХРАНЕНИЕ в каталоге + `schema_validator_id`
  + compile-on-boot, DDL-хендлеры (раскрыты из authz-only заглушек — это была КРУПНАЯ
  находка моего zero-trust ревью: «Phase A done» по факту значило «движок done, серверное
  исполнение застаблено»), per-table lock + expected_version, права Action::Write, TS-клиент.
- **Phase B** (`097dee2`): scalar-bridge + format(email/url/uuid/date) + cross-field compare;
  `ValidatorCtx.scalars`. Поймал баг: репо-интернер не персистился → схема не переживала
  reopen; починил `interner_mgr.persist()` в хендлерах.
- **Phase C** (`f02c5f1` C1, `87f31a3` C2, `3817357` C3, `046d5bb` C4): реляционные.
  C0 feasibility (верифицировал: валидаторы бегут ДО staging/локов write_exec.rs:158 →
  нет ре-ентрант-дедлока). C1 `ValidatorDb` (read-only снапшот tx) + `ValidatorCtx.db`.
  C2 foreign_key (`fk_violation`/`fk_requires_index`). C3 unique + read-your-own-writes
  staged-probe через tx-layered интернер. C4 combined FK+unique + concurrency-no-deadlock
  (10 параллельных tx).

КАЖДУЮ фазу я верифицировал СВОИМИ руками (durable reopen + батч + полный gate), не по
envelope агентов. Финальный gate: fmt+clippy clean, 1797 lib + 415 e2e passed, serde_json
нигде. Реализацию вели агенты: сначала crush (умирал — agent-tool в Anthropic ловил 500;
обходили директивой «не использовать agent-tool»), потом по просьбе юзера переключились на
@o46l (agent) для всех будущих тасков (C2 умер на транзиентном socket уже после готовой
работы; C3/C4 чисто). Anthropic API днём имел устойчивый 500-outage — crush (zai/glm) его
обходил.

Ничего В ПОЛЁТЕ. Кронов НЕТ (babysit `ddaa2070` снят — цель /babygoal Phase C достигнута).
TaskList: только #195 (func-nav, pending).

## Active goal

None. /babygoal Phase C завершён. /goal не установлен.

## TaskList
### pending
- #195 Phase 0 follow-up: function record-navigation by-name (doc 08 item 6) — единственная
  оставшаяся; диффузная мелкая доработка (распространить by-name RecordFields на навигацию
  записи ВНУТРИ функций в where/set/keygen). blockedBy очищен (был за #207, тот удалён).
### deleted
- 15 выполненных задач удалены по просьбе юзера (Phase 0 review-notes + A + B + server-exec
  + C0-C4).

## Decisions
- **autocommit-caveat FK/unique** (открыто): реляционные проверки применяются ТОЛЬКО в
  tx-mode (ctx.db()=Some); single-op autocommit их ТИХО ПРОПУСКАЕТ (предел C1 — resolver
  нельзя занять в 'static implicit-tx замыкании). Для строгой целостности — дыра.
  Варианты: (a) обернуть autocommit в tx с resolver; (b) force-tx для FK/unique-таблиц;
  (c) документированный предел. НЕ решено.
- **C3 staged-probe**: read-your-own-writes покрыт e2e `unique_batch_duplicate_within_tx`
  (а не восстановлением удалённого юнит-теста) — e2e сильнее, принято.
- **within-batch unique** (single multi-row INSERT): НЕ ловится на валидации (бегут до
  staging); commit-time UniqueGuard страхует indexed-unique. Документировано.
- **Переход crush→o46l** по явной просьбе юзера для всех будущих тасков.
- **Phase D+** (referential actions cascade/restrict) — спроектировано, НЕ построено.

## Open questions
- **Пуш?** 4 коммита Phase C (`f02c5f1`/`87f31a3`/`3817357`/`046d5bb`) непушены; origin на
  `7506cc8`. Phase 0/A/B уже на origin.
- **autocommit-caveat** — какой вариант закрытия (a/b/c)?
- **#195 func-nav** — делать или отложить?
- **Phase D+** — нужны ли referential actions?

## Repo state
```
(working tree clean — only docs/dev-artifacts/checkpoints/*.md untracked)
```
```
046d5bb test(schema): Phase C4 — combined FK+unique e2e + concurrency no-deadlock
3817357 feat(schema): Phase C3 — declarative unique (index-backed, read-your-own-writes)
87f31a3 feat(schema): Phase C2 — declarative foreign_key (forward-only)
f02c5f1 feat(schema): Phase C1 — tx-scoped read-only validator db-handle (ValidatorDb)
7506cc8 docs(checkpoints): declarative schema design + libraries-update session checkpoints
```
(4 коммита ahead of origin/master=7506cc8, непушены.)
