# Checkpoint — 2026-07-12 18:45

## Session summary

Continuing the standing audit-remediation campaign (`/babygoal`, TaskList-driven; the Stop-hook's literal condition text "Используй агентов sx (agent). Реализуй все задачи" is superseded in *mechanism* by the user's earlier `/crush` invocation — implementation delegation now runs through `crush run` background sessions, not the `Agent`/sx tool). This window closed out four more tasks in the fxx-designed #548/#549 combined identity/privilege redesign chain — #555, #556, #557 — and is now mid-flight on #558, all via the established pipeline: investigate → write brief to `docs/dev-artifacts/prompts/audit/NN-*.md` → commit brief alone → launch `crush run` → independently verify (own diff read, own `cargo check`/`fmt`/`clippy`/`./scripts/test.sh` run, never trust the agent's self-report) → adversarial review via the `Agent` tool (sx) → fix any found defects directly → second review if a fix was made → commit.

**#555** (`Actor::Admin` + `principal64`, delete `principal_id`) — implemented (after one crush crash/restart on a transient provider error), independently verified, adversarial review found one real regression (the chown-to-`OWNER_SYSTEM` lockout guard in `admin_access.rs` checked `actor != Actor::System` only, so every real superuser wire session — now `Actor::Admin`, never bare `Actor::System` — was unconditionally locked out of legitimately chowning to System). Fixed directly by the orchestrator, added a regression test, second review confirmed the fix and found no sibling instances. Committed `7ba23698`.

**#556** (`FjallUserDirectory` v2 — principal64 keyspace, `remove()`, boot-time normalization) — implemented (one crash/restart), independently verified (1662/1662 → wait, this was the shamir-server-scoped suite, 350/350 passed), adversarial review found nothing exploitable. Committed `3d0775f9`.

**#557** (superuser as a first-class flag: reservation, `SetSuperuser` wire op, handshake wiring) — implemented cleanly (no crash this time). **Orchestrator caught and fixed a real scope violation before verifying further**: the crush agent's own `cargo fmt` run had accidentally reformatted 4 unrelated pre-existing-drift files outside its scope (`shamir-query-types/src/read/limit.rs`, `shamir-server/src/subscriptions/{bridge,registry}.rs`, `shamir-server/tests/hmac_gate.rs`) — reverted via `git checkout --` before proceeding. After that, independent verification (960/960 tests) and adversarial review both passed clean. The review's most significant finding was confirming (not newly discovering) that resumed sessions already silently lose superuser status post-#556 because `resume.rs` still builds `SessionPermissions` from the ticket's stale `roles` snapshot — explicitly tracked as task #558's job, not a #557 regression. Committed `63093cf7`. Filed a small non-blocking follow-up, **task #564**, for a pre-existing (unchanged by #557) silently-discarded error in `create_scram_user`.

**#558** (Ticket v2 — resume re-verifies against the directory) — this is the DIRECT fix for the gap #557's review surfaced. Investigated `ticket.rs`/`resume.rs` in depth: `TicketPlain.roles` must be dropped entirely (version bumped 1→2, both `ticket.rs`'s and `resume.rs`'s independent hardcoded version-check literals need to move together), `UserStateLookup`'s trait return type widens from bare `Option<u64>` to a new `ResumeUserState` struct (username/roles/superuser/tib) defined in `shamir-connect` itself (can't depend on shamir-server's directory type), the adapter in `shamir-server` maps `state_by_user_id`'s `UserDirectoryState` across the crate boundary, and `process_resume`'s session-construction steps switch from `plain.username_nfc`/`plain.roles` to `state.username`/`state.roles`+`state.superuser` — while the ticket's own opaque `username_nfc` field can keep round-tripping unchanged inside a refreshed ticket (not a security-relevant path). Found and flagged in the brief that two EXISTING tests in `crates/shamir-connect/tests/integration_resume.rs` (`resumed_admin_session_retains_roles_per_diagram_02`, `refresh_ticket_carries_roles_forward_per_diagram_02`) currently encode the exact bug being closed and need a real rewrite, not a mechanical patch. Brief written and committed (`160e036e`, `docs/dev-artifacts/prompts/audit/76-resume-549-authoritative-lookup.md`). **Currently IN FLIGHT** via crush session `task-558-resume-authoritative-lookup` — confirmed genuinely in progress (git status shows real diffs across `ticket.rs`/`resume.rs`/`handshake.rs`/`user_state_lookup.rs`/`user_state_lookup_tests.rs`, session `MSGS` count and token count both climbing between babysit-tick checks). No completion notification yet as of this checkpoint.

Remaining ready-but-blocked tasks in the chain: #559 (stores-549-shamir-db-retirement — PrincipalResolver/UserAdminPort ports, retire shamir-db's own ineffective users/roles tables) is blocked by #555+#556 (both done) so will be ready once #558 lands (dependency-wise #559 isn't formally blocked by #558, but the pipeline is strictly serial in this session so it'll run next regardless). #560 (TS/Rust client builder sync — explicitly where `SetSuperuser`'s client-side builder support belongs, deliberately deferred there from #557) is blocked by #559. #561 (chown/chgrp/addGroupMember target validation via PrincipalResolver) also blocked by #559. Task #519 (napi-rs bump) stays explicitly parked at the user's own prior request. Task #529 FINAL-GATE stays blocked until the whole #548/#549 chain (#555-561) completes. Tasks #562 (pre-existing clippy `type_complexity` in `shamir-engine`) and #563 (group-record TOCTOU race, found during #552's review) and #564 (CreateScramUser silently discards update_roles error, found during #557's review) are queued, low-priority follow-ups, not started.

## Active goal

Session-scoped Stop hook, condition verbatim: `Используй агентов @"sx (agent)". Реализуй все задачи`. As recorded in the prior checkpoint, this is superseded in *mechanism* (not intent) by the user's explicit `/crush` invocation earlier in the session — `crush run` is the standing implementation-delegation channel; the `Agent` tool (sx) is still used for the adversarial-review step of the pipeline, which has not been redirected to `crush`.

## TaskList

### in_progress
- #558 IMPLEMENT resume-549-authoritative-lookup (Ticket v2 drops roles, resume re-verifies against directory) — crush session `task-558-resume-authoritative-lookup` running, not yet verified/committed

### pending
- #519 CLIENT: node-binding typed error .code/.retryable (needs napi-rs 3.x bump — user explicitly declined this session; parked)
- #529 FINAL-GATE: full fmt + clippy + test --full across workspace (blocked by #548/#549's implementation chain, #555-561)
- #559 IMPLEMENT stores-549-shamir-db-retirement (PrincipalResolver/UserAdminPort ports, retire shamir-db users/roles tables)
- #560 IMPLEMENT clients-549-surface-sync (TS + Rust client builders: remove role-object ops, add setSuperuser) — blockedBy #559
- #561 IMPLEMENT validation-543-followup (chown/chgrp/addGroupMember targets resolve via PrincipalResolver) — blockedBy #559
- #562 CHORE(clippy): fix pre-existing type_complexity in read_planner.rs blocking every -p shamir-db clippy gate
- #563 FIX: unlocked owner/membership TOCTOU race on group records (found during #552 review)
- #564 FIX: CreateScramUser silently discards update_roles error on create (found during #557 review)

### recently completed
- #557 IMPLEMENT privilege-549-superuser-flag (PersistedUser.superuser bool + SetSuperuser op + reservation)
- #556 IMPLEMENT identity-548-directory-v2 (shamir-server: PersistedUser v2, principal64 keyspace, remove(), boot normalization)
- #555 IMPLEMENT identity-548-actor-principal64 (shamir-types: Actor::Admin, principal64, delete principal_id)
- #554 IMPLEMENT: CreateFunctionOp per-field wire gating for security/secret_grants/visibility
- #553 IMPLEMENT: wire-admin DAC gate explicit per-op allowlist
- #552 IMPLEMENT: Root/User/Group real permission model
- #551 SECURITY: extend destructive-op HMAC confirmation to group-mutating ops
- #550 DECISION: Root/User/Group always-open meta + wire-admin DAC posture + wire Definer/secret_grants gap
- #549 DECISION: unify parallel superuser/role privilege axis + reconcile desynced user/role stores
- #548 DECISION: unbind security-Actor from fxhash(username), bind to stable user_id

## Decisions

- **#555's chown-to-System lockout bug**: fixed directly by the orchestrator (`admin_access.rs`'s guard extended to `matches!(actor, Actor::System | Actor::Admin(_))`), confirmed by a second review with no sibling instances found.
- **#557's out-of-scope fmt drift**: the crush agent's `cargo fmt` run reformatted 4 unrelated files; reverted via `git checkout --` before those files were ever staged/committed — the campaign's "never run cargo fmt --all, scope to -p <crate>" rule was reinforced explicitly in #558's launch prompt to prevent recurrence.
- **#557/#558 scope boundary**: `SetSuperuser`'s client-builder support (Rust query-builder convenience, TS builder/HMAC helper) is explicitly deferred to task #560, not #557 — confirmed by #560's own task description already claiming this scope.
- **#558's `SetSuperuser`-is-not-a-BatchOp reasoning** (established when writing #557's brief, reaffirmed for #558): BatchOps route through shamir-db's engine, which has no bridge to shamir-server's real `FjallUserDirectory` until task #559's `UserAdminPort` exists — so any op needing the real directory must be a top-level `DbRequest`, not a `BatchOp`, until #559 lands.
- **#558's two existing integration_resume.rs tests need a real rewrite, not a mechanical patch**: `resumed_admin_session_retains_roles_per_diagram_02` and `refresh_ticket_carries_roles_forward_per_diagram_02` currently assert the ticket's role snapshot drives resumed-session permissions — exactly the bug being closed. The brief tells the crush implementer to rewrite (not just adjust field types in) both.

## Open questions

- None new this window — #558's crush run has not yet surfaced anything needing user input.
- The stray `D\357\200\272devrustshamir-db_audit_diff.patch` untracked file — still unexplained, still not investigated (carried over from prior checkpoints, unchanged).

## Repo state

```
 M CLAUDE.md
 M crates/shamir-connect/src/server/resume.rs
 M crates/shamir-connect/src/server/ticket.rs
 M crates/shamir-server/src/connection/handshake.rs
 M crates/shamir-server/src/connection/user_state_lookup.rs
 M crates/shamir-server/src/tests/user_state_lookup_tests.rs
?? "D\357\200\272devrustshamir-db_audit_diff.patch"
?? docs/dev-artifacts/audits/2026-07-11-security-permission-triage.md
?? docs/dev-artifacts/checkpoints/... (several prior-session checkpoint files, pre-existing clutter)
?? docs/dev-artifacts/design/identity-privilege-unification-548-549-decision.md
?? docs/dev-artifacts/design/principal-id-identity-548-decision.md
?? docs/dev-artifacts/design/root-user-group-dac-posture-550-decision.md
?? docs/dev-artifacts/prompts/audit/crush-launch-command.md
?? logs/
(plus a large number of stray root-level *.log/*.pdb files from earlier bench/test sessions — pre-existing clutter, not from this window's work)
```

The 5 modified files above (`resume.rs`, `ticket.rs`, `handshake.rs`, `user_state_lookup.rs`, `user_state_lookup_tests.rs`) are #558's in-flight, uncommitted, unverified work from the live crush session — not yet reviewed by the orchestrator.

```
160e036e docs(prompts): brief for #558 ticket v2 authoritative resume lookup
63093cf7 feat(server): superuser as a first-class flag (#557)
61797cf6 docs(prompts): brief for #557 superuser first-class flag
3d0775f9 feat(server): FjallUserDirectory v2 — principal64 keyspace, remove(), boot normalization (#556)
782738f3 docs(prompts): brief for #556 FjallUserDirectory v2
```
