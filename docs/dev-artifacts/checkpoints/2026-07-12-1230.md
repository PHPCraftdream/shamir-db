# Checkpoint — 2026-07-12 12:30

## Session summary

Continuing the standing audit-remediation campaign (`/babygoal`, goal condition: "Используй агентов @sx (agent). Реализуй все задачи"). This window's earlier portion resolved the #550 DAC-posture DECISION task (three sub-designs, each rejected by the user on first pass and re-vetted via `fl`/`fm` consultations before being written into `docs/dev-artifacts/design/root-user-group-dac-posture-550-decision.md`), plus got full user sign-off on both #550's revised designs and the combined `fxx`-authored #548+#549 identity/privilege redesign (`docs/dev-artifacts/design/identity-privilege-unification-548-549-decision.md`). With sign-off in hand, #550, #548, #549 were marked completed and 10 follow-up IMPLEMENT tasks were created (#552-561), each carrying a self-contained description referencing the relevant decision-doc section.

Since then, the established brief→commit→implement→verify→adversarial-review→commit pipeline has run three times, all via the `Agent` tool (sx):

- **#552** (Root/User/Group real permissions) — implemented, and the FIRST adversarial review caught two real bugs the orchestrator then fixed directly: (1) the wire dispatcher (`admin_access.rs`) still had its own older Root-Manage-only pre-check that made the new group-owner OR-gate (`Manage(Root) OR Manage(Group{name})`) unreachable from any real client; (2) the Root self-lockout guardrail checked the OLD owner instead of the NEW owner being written, letting the exact lockout it was meant to prevent through via two separate calls. A SECOND adversarial review confirmed both fixes correct (with one accepted, bounded, non-blocking tradeoff: a group-existence oracle from reordering resolve-before-authorize). Committed as `6bfeb334`. A third finding (unlocked TOCTOU race between group owner/membership read-modify-writes in `system_store.rs`) was filed separately as task #563.
- **#553** (wire-admin coarse gate explicit allowlist) — implemented cleanly on the first pass; both adversarial reviews found no defects (the second review went as far as temporarily re-adding `Batch` to the allowlist to prove the regression test `coarse_gate_nested_batch_stays_denied` actually fails without the fix, then reverted the experiment). Committed as `2571a2b7`.
- **#554** (CreateFunctionOp per-field wire gating: `visibility` ungated, `security: Definer` needs HMAC, non-empty `secret_grants` needs `Manage(Root)` + HMAC) — brief written and committed (`832f479b`, `docs/dev-artifacts/prompts/audit/72-create-function-per-field-wire-gating.md`). **Currently IN FLIGHT** via a NEWLY-ADOPTED delegation channel: the user explicitly invoked `/crush` mid-session ("переходим на агентов /crush" — let's switch to crush agents), so this task is running as a background `crush run` process (session id `task-554-create-function-gating`, hash `1db07381`), NOT the `Agent`/sx tool used for #552/#553. Launched via `crush run --role smart --session task-554-create-function-gating --timeout 60m --json`, prompt at `.crush/stdin/task-554.prompt`, output streaming to `.crush/stdin/task-554.out`/`.err`. As of the last two `babysit` ticks the session shows `running` status with growing message/token counts (68 msgs, ~77k tokens at last check) — genuine progress, not stalled. **This is the standing delegation mechanism going forward** — future tasks in this campaign should default to `crush run` per the `/crush` skill's conventions (stable session id, background launch, `crush sessions watch`/`locks` for liveness, zero-trust verification of the diff afterward) rather than the `Agent` tool, unless `/crush` explicitly hits a hard rate limit (in which case the skill's own fallback to `@o46l`/`@ash`-style local agents applies).

Remaining ready-but-not-started tasks in the #548/#549 combined redesign chain: #555 (shamir-types: `Actor::Admin`, `principal64`, delete `principal_id`) is the next unblocked one once #554 lands; #556-561 are chained behind it per the dependency graph recorded in each task's description (mirroring `fxx`'s 7-step plan, briefs numbered 73+ once #554's brief-72 slot is done). Task #519 (napi-rs bump) remains explicitly parked at the user's own request. Task #529 FINAL-GATE stays blocked until #548/#549's full implementation chain (#555-561) completes. Tasks #562 (pre-existing clippy `type_complexity` in `shamir-engine`, discovered blocking every `-p shamir-db` clippy gate) and #563 (group-record TOCTOU race) are queued, low-severity follow-ups not yet started.

## Active goal

Session-scoped Stop hook, condition verbatim: `Используй агентов @"sx (agent)". Реализуй все задачи`. Superseded in practice, mid-session, by the user's explicit `/crush` invocation for delegation going forward — the goal's *intent* (implement all tasks) stands, but the *mechanism* named in its text (`sx` agents) is stale as of this window; `crush` is now the actual delegation channel per the user's direct instruction.

## TaskList

### in_progress
- #554 IMPLEMENT: CreateFunctionOp per-field wire gating for security/secret_grants/visibility (revised per fm consultation, #550 P.3) — running via crush session `task-554-create-function-gating`, not yet verified/committed

### pending
- #519 CLIENT: node-binding typed error .code/.retryable (needs napi-rs 3.x bump — user explicitly declined this session; parked)
- #529 FINAL-GATE: full fmt + clippy + test --full across workspace (blocked by #548/#549's implementation chain, #555-561)
- #555 IMPLEMENT identity-548-actor-principal64 (shamir-types: Actor::Admin, principal64, delete principal_id) — next ready once #554 lands
- #556 IMPLEMENT identity-548-directory-v2 (blockedBy #555)
- #557 IMPLEMENT privilege-549-superuser-flag (blockedBy #555, #556)
- #558 IMPLEMENT resume-549-authoritative-lookup (blockedBy #555, #556)
- #559 IMPLEMENT stores-549-shamir-db-retirement (blockedBy #555, #556)
- #560 IMPLEMENT clients-549-surface-sync (blockedBy #559)
- #561 IMPLEMENT validation-543-followup (blockedBy #559)
- #562 CHORE(clippy): fix pre-existing type_complexity in read_planner.rs (blocks every -p shamir-db clippy gate)
- #563 FIX: unlocked owner/membership TOCTOU race on group records (found during #552 review)

### recently completed
- #553 IMPLEMENT: wire-admin DAC gate explicit per-op allowlist (#550 P.2)
- #552 IMPLEMENT: Root/User/Group real permission model (#550 P.1)
- #550 DECISION: Root/User/Group always-open meta + wire-admin DAC posture + wire Definer/secret_grants gap
- #549 DECISION: unify parallel superuser/role privilege axis + reconcile desynced user/role stores
- #548 DECISION: unbind security-Actor from fxhash(username), bind to stable user_id
- #551 SECURITY: extend destructive-op HMAC confirmation to group-mutating ops
- #547 FIX: wire changePassword flow into the live server request dispatcher
- #546 HARDENING: unified authz-mapping registry + doc-guards + create-TOCTOU + coverage matrix

## Decisions

- **#552's two review-found bugs**: fixed directly by the orchestrator (not re-delegated) — dispatcher OR-gate reachability and the Root-guardrail owner-check direction — both confirmed by a second independent review before commit.
- **#553's rejected `is_write()`-based gate relaxation**: replaced with an explicit 4-op allowlist (`List`/`AccessTree`/`DescribeTable`/`GetTableSchema`), never derived from a classifier, to avoid the `BatchOp::Batch` nested-authorization bypass a reviewer found in the earlier design.
- **Delegation channel switch**: mid-session, per the user's explicit `/crush` invocation, delegated implementation work now goes through `crush run` (background, session-id-tracked, `crush sessions watch`/`locks` monitored) instead of the `Agent` tool — applies from #554 onward.
- **#563 scoped out of #552's commit**: the group-record TOCTOU race was filed as its own follow-up task rather than fixed inline, since the first review rated it "worth a follow-up, not blocking" and fixing it would have expanded #552's diff beyond its brief's stated scope.

## Open questions

- None new this window — #554's crush run has not yet surfaced anything needing user input.
- The stray `D\357\200\272devrustshamir-db_audit_diff.patch` untracked file — still unexplained, still not investigated.

## Repo state

```
 M CLAUDE.md
?? "D\357\200\272devrustshamir-db_audit_diff.patch"
?? _check3.pdb
?? _check_escape.pdb
?? (a large number of stray root-level *.log/*.pdb files from earlier bench/test sessions — pre-existing clutter, not from this window's work)
?? docs/dev-artifacts/audits/2026-07-11-security-permission-triage.md
?? docs/dev-artifacts/checkpoints/2026-07-08-audit-remediation-a10-inflight.md
?? docs/dev-artifacts/checkpoints/... (several other pre-existing/this-session checkpoint files)
?? docs/dev-artifacts/design/identity-privilege-unification-548-549-decision.md
?? docs/dev-artifacts/design/principal-id-identity-548-decision.md
?? docs/dev-artifacts/design/root-user-group-dac-posture-550-decision.md
?? docs/dev-artifacts/prompts/audit/crush-launch-command.md
?? .crush/stdin/task-554.prompt / .out / .err (crush session I/O for the in-flight #554 run)
```

```
832f479b docs(prompts): brief for #554 CreateFunctionOp per-field wire gating
2571a2b7 security(admin): explicit 4-op allowlist for wire-admin coarse gate (#553)
15e403de docs(prompts): brief for #553 wire-admin gate explicit allowlist
6bfeb334 feat(access): Root/User/Group real permission model (#552)
493e55a6 docs(prompts): brief for #552 Root/User/Group real permission model
```
