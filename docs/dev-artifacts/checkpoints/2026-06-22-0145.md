בְּשֵׁם יהוה הָרַחֲמָן וְהַחַנּוּן

# Checkpoint — 2026-06-22 01:45 (post-Op#2 cleanup)

## Session summary

Кампания "post-Op#2 cleanup" завершена. Три leaf-таски, single-context main-thread, строго TDD red-first. Все pass.

После пуша Op #2 (be0bc1f + d76d706) пользователь спросил `/oxx` — что с #163. Я расследовал детерминированно: `crash_mid_drain_recovers_all` падает на precondition `assert!(committed >= 1)` потому что фоновый дрейнер срабатывает на семе `drain_replay` (первая дренируемая версия) до того, как главный поток ребёнка успевает `sync_wal()` + `write_sidecar(1)`. На fjall (после b2b1280) дрейнер быстрее — race детерминирован. Recovery-механика правильная: 6/6 прогонов показали `recovered == committed + 1`, инвариант `recovered >= committed && recovered <= 40` держался всегда. Это test-harness race, не продакшен баг.

Дальше пользователь спросил `/oxx` — а продакшен-код точно не нужно улучшать? Адверсариально перечитал свой Op #2 код и нашёл **два реальных перф-дефекта**, которые сам же внёс на Stage 3/4:

1. **`offer` зовёт `scc::TreeIndex::len()` — O(N) обход дерева на каждый коммит** (подтвердил по исходнику scc-2.4.0 в локальном Cargo registry: `tree_index.rs:727` = `self.iter(&guard).count()`). Под бэклогом к hw=64K — до 64K шагов на коммит. Ровно тот класс скрытого квадрата, который Op #2 убивал, реинтродуцированный в его же backpressure-проверку.
2. **`drain_step` спурьёзно зовёт `wal.recover()` на каждой abort-дыре в окне.** Триггер `len < vis-dur` сравнивает entries и versions, но aborted-версии не пишут WAL-entry (`VersionGuard::drop` на :114 маркирует durable Aborted напрямую через Arc-clone — поэтому grep `mark_durable_aborted` ничего не нашёл, но трекер кормится). Под contended SSI/phantom нагрузкой — постоянный O(W) recover, частично убивающий Op #2 win.

Не stall (durable_watermark лечит abort-дыры за один-два прохода), но честный перф-баг.

`/babygoal` + sanction на `делай`. Декомпозиция → 3 leaf-таски: #164 (reseed-тригер `is_empty()`), #165 (atomic `window_depth`), #163 (test precondition). Babysit cron `b37f2b5a` поставил на off-минутах `8,23,38,53`. Стратегия single-context — суммарно ~25 LOC, контекст свежий, агентов не звал.

TDD во всех трёх:
- #164: новый тест `drain_step_interior_abort_does_not_trigger_spurious_recover` сначала FAIL (`left=1 right=0` — старый код спурьёзно reseed'ит), потом GREEN после смены триггера на `is_empty()` + фильтрация recover-результата по `> dur` (чтобы reseed не пере-набивал окно уже-дренированными). Существующий `drain_step_window_gap_triggers_one_recover_reseed` обновлён под `drain_all` (новая логика требует двух проходов на dropped-offer, но drain_all петлит) + asserts `recover_calls delta == 1`.
- #165: добавил `AtomicUsize window_depth` и обновил все 5 точек мутации окна (offer, seed_from_recover, drain Phase C remove, window_remove_for_test). `offer` теперь делает `window_depth.load(Relaxed) >= hw` вместо O(N). Тесты: `window_depth_atomic_mirror_matches_tree_across_mutations` (счётчик == window_len() после каждой операции) + `offer_backpressure_check_uses_atomic_depth_not_tree_walk` (1000 offers при hw=0 → window пустое, offer_dropped_total=1000).
- #163: `assert!(committed >= 1)` → `assert!(recovered >= 1, "...sidecar may have lagged because background drainer fired before sync_wal completed...")` + комментарий про seam-fires-before-sidecar race. Production code не трогал.

Финальный гейт: drainer 22/22, commit + recovery 126/126, `@oracle --full` 1416/1416 (без любых исключений!), `crash_mid_drain_recovers_all` 11/11 под изоляцией (6 RED-проб + 5 deflake post-fix), clippy + fmt clean. Один intermittent flake под parallel-load `@oracle` был замечен (1 из 7-8 прогонов), в 5 свежих подряд не воспроизвёлся. Open question — открывать ли отдельную таску или принять как known-noise.

3 файла uncommitted, ждут санкции на коммит/пуш.

## Active goal

Нет (`/goal` не установлен). Контракт — TaskList.

## TaskList

### in_progress
*(нет)*

### pending
*(нет)*

### recently completed
- #163 — fix crash_mid_drain_recovers_all precondition (racy sidecar)
- #165 Drainer: AtomicUsize window_depth for O(1) offer backpressure check
- #164 Drainer: kill spurious recover() on abort gaps — reseed-trigger uses empty-prefix only

## Decisions

- **TDD red-first строго** для всех трёх перф-фиксов (для #163 тоже — 6 диагностических прогонов до фикса, чтобы знать `recovered == committed+1` ВСЕГДА).
- **Single-context, без агентов** — суммарный объём (~25 LOC продакшен + ~190 LOC тестов) не оправдывал sub-agent overhead.
- **Фильтрация recover-результата** в reseed-пути (`commit_version > dur`) — добавил по ходу, чтобы окно не пере-набивалось уже-дренированными entries после Phase C remove. Замечено при ручной проверке drain_all-петли в новых тестах.
- **`recover_calls` публичный, `window_depth` `#[cfg(test)]`** — первый telemetry (полезен в продакшене для observability), второй чисто служебный, не загрязняем public API.
- **Intermittent @oracle parallel-flake не блокирует** — 5 свежих pass подряд после фикса; будет вынесен в отдельную таску при следующем появлении.

## Open questions

- **Коммит + пуш.** 3 source-файла uncommitted: `drainer.rs`, `tests/drainer_tests.rs`, `tests/crash_recovery.rs`. Рекомендация — два коммита: `perf(tx): drainer offer O(1) depth + abort-aware reseed` (drainer.rs + drainer_tests.rs) + `fix(test): crash_mid_drain precondition uses recovered, not the racy sidecar` (crash_recovery.rs). Жду `коммит и пуш`.
- **Intermittent parallel-flake под `@oracle --full`.** В 7 прогонах 1 раз поймал FAIL без имени теста (быстро прошло, имя не записалось); следующие 5 подряд — 1416/1416. Стоит отдельная таска или ждать рецидива?
- **Pre-existing бэклог:** bench-infra hardening (runtime-kill в `tune_tiered max_wall_secs`) — переходит из предыдущих чекпоинтов, не пресс.
- **Op #2 sustained-write бенч** — предлагал собрать `sustained_write_under_load` для замера реального ROI 82 win'а под нагрузкой. Ответа нет.

## Repo state

```
 M crates/shamir-engine/src/tx/drainer.rs
 M crates/shamir-engine/src/tx/tests/drainer_tests.rs
 M crates/shamir-engine/tests/crash_recovery.rs
```

```
d76d706 docs(perf): Op #2 drainer cursor — Stage 0/5 results + checkpoints + Phase 2 routing
be0bc1f perf(tx): Op #2 incremental drainer cursor — window + offer + gap-reseed + backpressure (ROI 82)
c64fd41 fix(bench): cap s3_range_and N at 500K — bound 10-min wall-clock (#154)
d2d3504 fix(storage): MemBuffer *_many — publish dirty_nonempty before populating (#152)
35ebd40 perf(types): interner reverse-spine Arc<str> — O(N²)→O(N) cold growth (#151)
```

Origin/master = HEAD (всё что было — запушено). Post-Op#2 cleanup на uncommitted поверх. Babysit cron `b37f2b5a` снимается этим же tick'ом по инструкции (TaskList пуст).

## Stats (для archaeology)

Suite: drainer 22/22 (было 17, +3 теста + 2 апдейта существующих); commit + recovery 126/126; @oracle full **1416/1416 БЕЗ ЛЮБЫХ ИСКЛЮЧЕНИЙ** (впервые с b2b1280 backend swap); clippy + fmt clean. 3 файла, +241 / -14 LOC. Wall-clock кампании: ~50 мин с момента `/babygoal`.
