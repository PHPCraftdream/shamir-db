בְּשֵׁם יהוה הָרַחֲמָן וְהַחַנּוּן

# Checkpoint — 2026-06-21 22:06

## Session summary

Длинная сессия с тремя крупными этапами.

**Этап 1 (раньше):** bench-infra A→D (#132-#135), затем удаление 4 storage engines (redb/nebari/persy/canopy) и rewrite redb-инфраструктуры на fjall. Закоммичено + запушено как `af2ff2d`. Подробности в чекпоинте `2026-06-21-1840.md`.

**Этап 2 (deep research):** запустил workflow с 8 oxx-агентами параллельно — исследовали все hot-path dimensions (read planner, aggregations, WASM, tx/commit/WAL, wire/auth, subscriptions, vector/FTS, storage/cache/interner). Synthesis написал `docs/dev-artifacts/perf/perf-roadmap-2026-06-21.md` — 39 opportunities, ROI-ранжирование, рекомендована Phase 1 с 3 ops.

**Этап 3 (текущий, в полёте):** реализую план через `/workflows @o46l`. Структурировал в TaskList: Phase 0 (3 probes: fjall API + fast_path bail + baselines), Phase 1 (3 ops: A scan_prefix_stream / B interner Arc<str> / C MemBuffer sentinel), Phase 2 routing decision.

**Phase 0 закрыт:**
- 0a: fjall.range = lex-ordered (confirmed). insert НЕ возвращает prior value.
- 0b: try_plan_order_limit_fast_path/1M = 138µs (sub-ms) — уже починен, B1 blind spot снят.
- 0c: baselines пришлось снимать вручную (o46l agent падал дважды на bench livelock — busy-loop polling и duplicate background spawns).

**Phase 1 в работе:**
- **Op A scan_prefix_stream (fjall)** ✅ commit `98f256d` — speedup **164×** (38.5s → 241ms на N=50k). Заменили full-iter+linear-cursor на keyspace.range+take_while.
- **Op A.2 sled** (added on user request — я заметил что та же cliff в sled) ✅ commit `7140f82` — **50×** (21.7s → 430ms). Сделал вручную в main thread.
- **Op B interner Arc<str>** 🔄 в полёте — Sonnet high agent `a3cfec04`. Сейчас гоняет post-bench `interner_cold_growth`. Уже отредактировал 10 файлов (interner.rs, codecs/interned/*, layered_interner.rs, тесты, admin_list.rs).
- **Op C MemBuffer sentinel** 🔜 ждёт Op B.
- **Phase 2 routing** 🔜 ждёт Op C.

**Активные timers:** cron `417ee33f` (off-minute babysit, каждые 15 мин) — гоняет TaskList-driven tick.

**Workflow runs:** `wf_267d4b4f-f48` — originally launched via Workflow tool, был stop'нут и resume'нут 4 раза (livelock, prompt-discipline-failure, agentType switch). Текущий task id `wo420ct4k` после последнего resume с o46l→sh swap для Phase 1 ops.

**Working hypothesis:** Sonnet high (`sh`) лучше следует длинным prompt'ам чем Opus 4.6 low (`o46l`). Op A (sh) прошёл без проблем; o46l два раза провалил Phase 0c discipline. После Op B завершения станет видно подтверждается ли гипотеза для более крупного скоупа.

## Active goal

Нет активной цели (`/goal` Stop hook не установлен). Контракт — TaskList.

## TaskList

### in_progress
- #146 Perf Phase 0+1 — execute via o46l workflow (umbrella, blocked by #151, #152, #153, #154)
- #151 Phase 1B — Op B: interner reverse-spine Arc<str> (ROI 70)

### pending
- #152 Phase 1C — Op C: MemBuffer insert_many dirty-sentinel fix (correctness rider) [blocked by #151]
- #153 Phase 2 — routing decision (fast_path fix OR drainer cursor) [blocked by #152]
- #154 Bench-infra E — починить hard cells (s3_range_and/1M = 1019s/sample) в QUICK режиме

### recently completed
- #155 Op A.2 — sled scan_prefix_stream range-seek (зеркало Op A для sled) ✅ commit 7140f82, 50×
- #150 Phase 1A — Op A: scan_prefix_stream range-seek (ROI 98) ✅ commit 98f256d, 164×
- #149 Phase 0c — capture baselines (store_raw + interner_cold_growth + read_path_matrix)
- #148 Phase 0b — re-measure fast_path bail (denied)
- #147 Phase 0a — fjall API (confirmed)

## Decisions

- **Sonnet high (sh) over Opus 4.6 low (o46l)** для Phase 1+ ops. o46l два раза провалил prompt discipline (busy-loop polling, duplicate background spawns на одном target-dir). Sh point — следует длинным prompt'ам.
- **Phase 0c сделать вручную** вместо третьей попытки o46l. Заменил agent() call в workflow script на literal Promise.resolve со ссылкой на manually-written baselines doc.
- **Op A.2 sled добавить как отдельный коммит** (вне workflow) — пользователь заметил что fix симметричен fjall, попросил применить. ROI ниже (sled не sole durable) но cliff реальная.
- **`git add -A` оказался ошибкой** при коммите Op A.2 — затянул Op B agent's WIP. Откатил через `git reset --soft HEAD~1`, прицельно staged только sled-related файлы.
- **Bench-infra E (#154) отделена** в follow-up task. Не блокирует Phase 1. Корневая причина livelock агента — `read_path_matrix/s3_range_and/1M` = 1019s/sample в QUICK (Criterion расширяет measurement если первая iter не помещается).

## Open questions

- **Op B результат:** speedup Arc<str> ≥ 2× для commit или revert? Узнаем когда agent `a3cfec04` завершит post-bench.
- **Phase 2 next campaign:** при `fast_path bail = denied` (confirmed by 0b), routing должен рекомендовать **Op #2 incremental drainer cursor** (ROI 82, latent O(N²) на WAL recovery). Это xlarge campaign — большая отдельная работа, нужно решение пользователя запускать или отложить.
- **Bench-infra E (#154):** пользователь хочет фикс? Или это noted-for-later? Делать прямо сейчас или после Phase 2 routing?

## Repo state

```
 M crates/shamir-db/src/shamir_db/execute/admin_list.rs
 M crates/shamir-engine/benches/interner_cold_growth.rs
 M crates/shamir-engine/src/table/read_index_scan.rs
 M crates/shamir-engine/src/table/tests/interner_manager_tests.rs
 M crates/shamir-engine/src/table/tests/table_tests.rs
 M crates/shamir-tx/src/layered_interner.rs
 M crates/shamir-types/src/codecs/interned/codec.rs
 M crates/shamir-types/src/codecs/interned/validate_keys.rs
 M crates/shamir-types/src/core/interner/interner.rs
 M crates/shamir-types/src/core/interner/tests/interner_tests.rs
```
*(все 10 файлов — Op B agent's work-in-progress, не моё; ждёт коммита агента после успешного post-bench)*

```
7140f82 perf(storage): scan_prefix_stream range-seek on sled — O(M²/bs)→O(log N+bs) (#155)
98f256d perf(storage): scan_prefix_stream range-seek — O(N+M²)→O(log N+M) on fjall (#146)
af2ff2d arch(storage): drop nebari/persy/canopy/redb engines, fjall as sole durable backend
410af23 fix(bench): bench-utils — поднять SMOKE/QUICK samples до 10 (criterion floor)
472cfce docs(perf): bench-infra D — bench guidelines (#135)
```

3 коммита ahead of origin/master: af2ff2d, 98f256d, 7140f82.
