בְּשֵׁם יהוה הָרַחֲמָן וְהַחַנּוּן

# Checkpoint — 2026-06-24 07:10 [ts-e2e-campaign-complete-pushed]

## Session summary

Сессия **полностью завершена и запушена** — `master` синхронизирован с `origin/master`
(HEAD=`36b3ebf`). Стартовали с `/babygoal`: построить полное e2e-покрытие TS-клиента
S.H.A.M.I.R. против реального сервера (весь DDL включая сегодняшние декларативные
валидаторы + права/видимость БД + вся работа с данными), плюс клиентский query builder
с интернированием (id полей на проводе). Кампания сделана через o46l-агентов (по агенту
на фазу), каждую фазу я верифицировал СВОИМИ руками (zero-trust, не по envelope).

**Готово (9 коммитов на origin, 5 сегодняшних поверх 4 Phase C):**
- Ф1 #208 interner-packing (id-на-проводе, паритет с Rust execute_with_touch) + smart-write
  вшит в Db.run/Batch.run (#214).
- Ф3 #213 общий e2e-харнесс: эфемерный порт + **CARGO_TARGET_DIR-aware** резолв бинаря
  (корень мнимого «TLS»: харнесс смотрел на устаревший target/release вместо .cargo-target).
- #209 DDL lifecycle (15) · #210 валидаторы scalar/format/compare/FK/unique (27) ·
  #211 права+видимость БД (20) · #212 данные+round-trip интернера (33) · #216 e2e-principal.
- Полный TS-suite **577/577**; shamir-server lib **115/115**.

**5 реальных багов, пойманных моим zero-trust** (агенты не замечали): (1) BigInt-эпохи
слались в msgpack без конвертации — ломали interner-протокол; (2) харнесс/устаревший бинарь;
(3) модель прав #211 (superuser из SCRAM-ролей, форма accessTree) — довёл после смерти агента;
(4) **#215 серверная cross-db коллизия кэша подписок** — subscription decode/deliver-cache
глобальные, ключевались по db_id=указатель ЭКЗЕМПЛЯРА ShamirDb (одинаков для всех БД сервера),
repo "main" + пересекающиеся версии → фильтр подписки матчился против записи ДРУГОЙ БД →
событие терялось; нашёл runtime-трейсом (3 трейс-итерации + дискриминаторы), исправил
дискриминатором db_disc=hash(экземпляр, db_name); (5) **#216 useBigInt64-регрессия**: агент
включил глобальный useBigInt64 → большие Number-int (таймстемпы ≥2^32) кодировались как
float64 → сервер отвергал; поймал в полном прогоне, доказал node-тестом (порог ровно 2^32),
починил адаптером promoteWideInts.

**#216 GAP** (chown/addGroupMember из TS): principal_id=fxhash::hash64(username)&i64::MAX >2^53.
Решено клиент-only: TS-реплика fxhash 0.2.1 FxHasher64 (доказана кросс-языковой сверкой
principalId===accessTree-id) + BigInt-framing.

**#195** func-nav — закрыт доказательным аудитом: перф-интент (не де-интернировать всю запись
под функц-навигацию) уже реализован прошлой работой (C6 #80/E6 ретировали InnerValue-round-trips;
filter scalar-fn материализует только аргументы-поля; write computed-fn навигирует входящий
литерал; whole-record — только валидаторы, мигрированы в Phase 0; keygen-сайтов нет).

НИЧЕГО в полёте. Все задачи закрыты, babysit-крон снят (TaskList пуст). Дерево чистое.
Leftover убраны (.agent-briefs, .claude/worktrees/agent-*).

## Active goal
None. Цель «решить все задачи до конца» достигнута — все задачи completed, Stop-hook очистился.

## TaskList
Пуст (все задачи completed и/или очищены). Завершены за сессию: #208 #209 #210 #211 #212
#213 #214 #215 #216 #195 (+ #207/Phase-0/A/B/C-эпик удалены ранее).

## Decisions
- Коммиты — когезивно по подсистемам (Rust-фикс / TS-ядро / TS-билдеры / e2e / доки), а не
  буквально по фазам: файлы сильно пересекались между фазами, hunk-хирургия рискованнее.
- #215 db_disc = hash(Arc::as_ptr экземпляра, db_name) — изолирует и по экземпляру (тест-фикстуры),
  и по имени БД (прод/e2e: один сервер, много БД). Применено к decode И deliver кэшу.
- #216 — вариант (а) клиент-only fxhash вместо (б) string-principal на проводе: без Rust-изменений
  и без пересборок, риск «молчаливой порчи» снят проверяющим e2e (assert principalId===серверный id).
- #195 закрыт как «уже реализован», а не делаем cosmetic adoption трейта RecordFields
  (риск регрессии в ядре функц-исполнения > ~ноль перф-выгоды).
- Коммит на master напрямую (как весь поток проекта), не в ветку; пуш по явной просьбе.

## Open questions
Нет. Все решения приняты, всё запушено.

## Repo state
```
(working tree clean — synced with origin/master)
```
```
36b3ebf docs(client-ts): TS interning + e2e plan + session checkpoints
2b4231b test(client-ts): e2e harness + full DDL/validators/permissions/data/interner coverage
0a99c31 feat(client-ts): builder completeness — schema oneOf/unique + fxhash principal_id (chown/addGroupMember)
7a701b6 feat(client-ts): client-side interner id-on-wire (smart-write) + BigInt-safe framing
471658e fix(subscriptions): isolate decode/deliver cache by database (db_disc)
```
(origin/master = HEAD = 36b3ebf; 0 коммитов ahead.)
