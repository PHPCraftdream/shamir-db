# Checkpoint — 2026-07-13 09:12

## Session summary

Continuing the audit-remediation campaign (`/babygoal`, TaskList-driven). This window closed out the entire #548/#549 identity/privilege redesign chain (#559→#560→#561), plus two smaller follow-ups (#562, #564), and is now waiting on two crush-delegated tasks (#519, #563) that are both blocked on the crush provider `zai`'s peak-hours window (08:00–12:00 local). A one-shot cron wakeup (job `472e4aba`, fires 12:03 today, self-deletes) is armed to relaunch both stalled crush sessions once the window passes — set via `CronCreate` after `ScheduleWakeup` was tried first and refused (it's gated to `/loop` dynamic mode, not usable standalone).

**#559** (retire shamir-db's ineffective "Store B" users/roles tables via `PrincipalResolver`/`UserAdminPort`, the campaign's largest/riskiest step) — landed. Crashed twice on transient provider errors before completing; independently verified (cargo check/fmt/clippy/906 tests); adversarial review found and I fixed a real atomicity bug in `DirectoryPorts::create_user` (reserved-role rejection happened AFTER persisting the user, leaving an orphan account) — added a pre-check + regression test, plus a genuine end-to-end database-owner-delegation test (had to discover that a wire-level admin gate from #553 makes non-superuser delegation unreachable over the wire, so the positive test had to be written at the `execute_as` level instead). Committed `bcbec292`.

**#560** (TS client sync — remove role-object builders, add `setSuperuser`) — landed, but with a major complication: the crush agent's own `npm test` run (and my first verification pass) were BOTH silently running against a week-stale `shamir-server.exe` binary (dated 2026-07-05, predating #557-#559). Discovered this via the adversarial review, which caught that a test's assumptions ("grantRole('superuser', ...) must not error") contradicted the actual current backend. Rebuilt the release binary (confirmed real target dir is `D:\dev\rust\.cargo-target`, NOT the repo's own `target/` — a second, worse discovery), re-ran against fresh code, found the rewritten A8 test genuinely failed for real reasons (reserved-role misuse, then a discovered session-invalidation cascade from bumping a shared fixture's role). Fixed the test properly (separate ephemeral user for the grant/revoke round-trip, `setSuperuser` for the admin-capability proof), fixed a genuine duplicate-type issue (`SuperuserSet` defined twice) the review found. Committed `5dfc0284`. Filed **#566** (TS e2e harness must detect/refuse a stale server binary — the systemic gap this exposed) and **#567** (4 pre-existing TS e2e failures, unrelated to #560, surfaced only because this was the first fresh-binary run in weeks).

**#561** (re-enable chown/chgrp/addGroupMember target-existence validation, deferred from #543) — landed cleanly on first crush pass. Design: resolver-gated checks for user targets (chown owner, add_group_member's member — gated on `PrincipalResolver` being installed, permissive when absent) vs. unconditional check for group targets (chgrp, via the pre-existing `group_id_exists`, no port dependency). Fixed 3 pre-existing `hmac_gate.rs` test call sites that chgrp'd to a never-created group id. Adversarial review found nothing exploitable; confirmed the resolver-check's inherent TOCTOU (exists-at-check-time, could be dropped before write lands) is the SAME class already tracked as #563, not a new instance. Committed `757dd946`.

**#562** (clippy `type_complexity` in `read_planner.rs`, blocking every prior task's clean `-D warnings` gate) — small, done directly by me (not delegated): extracted `try_plan_keyset_seek`'s return tuple into a named `KeysetSeekPlan` type alias. Committed `ab86b4b7`.

**#564** (`CreateScramUser` silently discards `update_roles` errors) — small, done directly by me: same bug shape as the #559 fix (reserved-role rejection happening after persist), fixed with the identical pre-check pattern + a regression test. Adversarial review clean. Committed `0cf51cf7`.

Mid-session, disk cleanup was also done at the user's request: `D:\dev\rust\.cargo-target-bench` (35GB, stale bench artifacts) and several one-off A/B comparison target dirs (~13GB) deleted from `D:\dev\rust\`; ~88GB of orphaned e2e test temp directories (`.tmp*`, `shamir-e2e-*`, `shamir-ts-e2e-*`) deleted from `C:\Users\Computer\AppData\Local\Temp` (97GB→9.3GB). The user separately asked me to never again hit a stale binary silently — that's what became task #566.

The user explicitly authorized (mid-session) the previously-parked **#519** (napi-rs 2.x→3.x bump + typed `.code`/`.retryable` client-node errors) despite the standing "never bump versions without explicit request" rule — this is now in flight. Investigation surfaced a genuine, unresolved technical uncertainty: two independent research passes CONTRADICTED each other on whether napi-rs 3.x's `#[napi]` async functions can return a custom error type carrying structured fields, versus being hard-pinned to the default `Error<Status>`. The brief (`docs/dev-artifacts/prompts/audit/80-client-node-napi3-typed-errors.md`) explicitly instructs the implementing agent to resolve this via REAL COMPILATION, not more doc research, and provides a documented fallback (move structured-error construction to a thin JS-side wrapper) if the Rust-side approach doesn't compile for async fns. This is genuinely exploratory — not yet attempted by crush due to peak hours.

**#563** (unlocked group-record TOCTOU race, found during #552's review) — brief written (`docs/dev-artifacts/prompts/audit/81-group-toctou-563.md`), investigation found a FOURTH affected call site (`rename_group_as`) not named in the original finding, added to scope. Design: mirror the established `admin_user_locks`/`repo_create_locks` per-key-lock pattern (`DashMap<u64, Arc<Mutex<()>>, THasher>`) keyed by `group_id`, acquired across the full read-modify-write in all 4 call sites in `access_control.rs`. Not yet attempted by crush due to peak hours.

**#565** (test-coverage follow-ups from #559's review — boot-audit WARN branches untested, error-classification untested, no-resolver User-path degrade untested, stale doc comments, a redundant resolver read, plus a NEWLY ADDED item: a `clippy::type_complexity` warning in `delegation_e2e.rs:271`'s `RecordingPort` mock, found during #561's clippy run, pre-existing from #559's own commit) — just marked in_progress this tick; not yet started (no investigation/brief written yet).

## Active goal

No `/goal` Stop hook. Progress is TaskList-driven per the standing `/babygoal` methodology; a session-scoped babysit cron (~15 min ticks) is armed and has been driving progression through this whole window. A separate ONE-SHOT cron (`472e4aba`, fires 12:03 today, session-only, self-deletes after firing) is armed specifically to relaunch the two peak-hours-blocked crush sessions (`task-519-napi3-typed-errors`, `task-563-group-toctou`) once the provider's peak-hours window ends.

## TaskList

### in_progress
- #519 CLIENT: node-binding typed error .code/.retryable — napi-rs 2.x→3.x bump, user-authorized explicitly this session. Brief written (`docs/dev-artifacts/prompts/audit/80-client-node-napi3-typed-errors.md`), crush launch blocked by provider peak-hours (retries at 12:03 via cron `472e4aba`).
- #563 FIX: unlocked owner/membership TOCTOU race on group records. Brief written (`docs/dev-artifacts/prompts/audit/81-group-toctou-563.md`, found a 4th affected call site: `rename_group_as`), crush launch blocked by provider peak-hours (retries at 12:03 via cron `472e4aba`).
- #565 CHORE(test-coverage): #559 review follow-ups — just started this tick, no work done yet.

### pending
- #529 FINAL-GATE: full fmt+clippy+test --full across workspace + fix everything found — blockedBy list is now ALL completed/nonexistent (verified this session: #507/#516 don't exist, #512 completed, rest completed) — effectively unblocked, but should run LAST once #519/#563/#565/#566/#567 are also closed, per its own stated intent.
- #566 INFRA: TS e2e harness must detect/refuse a stale shamir-server binary — not started.
- #567 FIX: 4 pre-existing TS e2e test failures surfaced by first fresh-binary run (B1/B2/A11-group in `e2e-permissions.test.ts`, one in `e2e-principal.test.ts`) — not started.

### recently completed
- #564 FIX: CreateScramUser silently discards update_roles error on create
- #562 CHORE(clippy): fix pre-existing type_complexity in read_planner.rs
- #561 IMPLEMENT validation-543-followup (chown/chgrp/addGroupMember → PrincipalResolver)
- #560 IMPLEMENT clients-549-surface-sync (TS client sync + setSuperuser)
- #559 IMPLEMENT stores-549-shamir-db-retirement (Store B retirement, PrincipalResolver/UserAdminPort)

## Decisions

- **napi-rs 3.x design uncertainty resolved via mandated real compilation, not more research** — the brief for #519 explicitly forbids trusting documentation alone (two research passes contradicted each other) and prescribes a concrete fallback (JS-side error wrapper) if the Rust-side custom-error-type approach doesn't compile for async `#[napi]` fns.
- **#563's per-group_id lock design mirrors `admin_user_locks`/`repo_create_locks` exactly** — rejected the CAS/conditional-write and single-serialized-helper alternatives from the original finding as unnecessary novelty; this codebase already has 2 precedents for exactly this shape of fix.
- **#561's chown/add_group_member checks are resolver-gated (permissive when absent); chgrp's check is unconditional** — because group existence has never depended on the identity-model work (`PrincipalResolver`), only user existence did.
- **ScheduleWakeup is NOT usable outside `/loop` dynamic mode** (confirmed by a hard runtime refusal) — `CronCreate` with `recurring: false` is the correct one-shot-wakeup primitive going forward, not `ScheduleWakeup`.
- **Explicit user override of the "never bump versions" rule for #519 specifically** — scoped to napi-rs only, not a general license to bump other dependencies.

## Open questions

- None requiring user input right now — #519's design question is explicitly delegated to be resolved by compilation, not by asking the user.

## Repo state

```
 M CLAUDE.md
?? "D\357\200\272devrustshamir-db_audit_diff.patch"   (stray, unexplained, carried over unchanged for many checkpoints)
?? crates/shamir-client-ts/t537_new.log               (stray log, not mine)
?? docs/dev-artifacts/audits/2026-07-11-security-permission-triage.md
?? docs/dev-artifacts/checkpoints/... (prior-session checkpoint files, pre-existing clutter)
?? docs/dev-artifacts/design/identity-privilege-unification-548-549-decision.md
?? docs/dev-artifacts/design/principal-id-identity-548-decision.md
?? docs/dev-artifacts/design/root-user-group-dac-posture-550-decision.md
?? docs/dev-artifacts/prompts/audit/crush-launch-command.md
(plus a large number of stray root-level *.log/*.pdb files from earlier bench/test sessions — pre-existing clutter, not from this window's work)
```

```
0cf51cf7 fix(server): CreateScramUser no longer silently discards role-attach errors (#564)
25ce2584 docs(prompts): brief for #563 group-record TOCTOU race fix
ab86b4b7 chore(clippy): factor try_plan_keyset_seek's return tuple into KeysetSeekPlan (#562)
aaf0c759 docs(prompts): brief for #519 napi-rs 3.x upgrade + typed client-node errors
757dd946 fix(access): re-enable chown/chgrp/addGroupMember target-existence checks (#561)
```
