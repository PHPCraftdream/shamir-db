//! Test manifest for the [`record_view`](crate::record_view) module —
//! re-exports only (CLAUDE.md §test-organisation). Logic lives in siblings.

mod error_tests;
mod match_tests;
mod parity_tests;
mod path_tests;
mod record_ref_tests;
mod scalar_ref_cmp_tests;
